/******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ "../bpl-tools/Components/Mask/assets/shapes/blob.svg":
/*!***********************************************************!*\
  !*** ../bpl-tools/Components/Mask/assets/shapes/blob.svg ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ReactComponent: () => (/* binding */ SvgBlob),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
var _path;
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }

var SvgBlob = function SvgBlob(props) {
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", _extends({
    xmlns: "http://www.w3.org/2000/svg",
    width: 121,
    height: 89
  }, props), _path || (_path = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: "M100.016 7.397c-9.77-6.177-26.708-11.063-35.59-3.684-7.442 6.177-2.38 15.204-9.416 21.59-10.6 9.614-28.159-5.442-41.858 2.632C.676 35.255-4.28 59.149 4.278 73.798c13.137 22.524 53.13 13.854 61.194 12.106 4.5-.993 53.495-12.265 55.469-42.705.957-14.946-9.841-28.78-20.925-35.802"
  })));
};

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "../bpl-tools/Components/Mask/assets/shapes/circle.svg":
/*!*************************************************************!*\
  !*** ../bpl-tools/Components/Mask/assets/shapes/circle.svg ***!
  \*************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ReactComponent: () => (/* binding */ SvgCircle),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
var _circle;
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }

var SvgCircle = function SvgCircle(props) {
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", _extends({
    xmlns: "http://www.w3.org/2000/svg",
    width: 89,
    height: 89
  }, props), _circle || (_circle = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("circle", {
    cx: 44.5,
    cy: 44.5,
    r: 44.5
  })));
};

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHdpZHRoPSI4OSIgaGVpZ2h0PSI4OSIgdmlld0JveD0iMCAwIDg5IDg5Ij48Y2lyY2xlIGN4PSI0NC41IiBjeT0iNDQuNSIgcj0iNDQuNSIvPjwvc3ZnPg==");

/***/ }),

/***/ "../bpl-tools/Components/Mask/assets/shapes/flower.svg":
/*!*************************************************************!*\
  !*** ../bpl-tools/Components/Mask/assets/shapes/flower.svg ***!
  \*************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ReactComponent: () => (/* binding */ SvgFlower),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
var _path;
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }

var SvgFlower = function SvgFlower(props) {
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", _extends({
    xmlns: "http://www.w3.org/2000/svg",
    width: 89,
    height: 89
  }, props), _path || (_path = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: "m76.133 44.475 6.33-6.328a22.349 22.349 0 0 0-31.613-31.6l-6.318 6.318-6.372-6.318a22.352 22.352 0 1 0-31.612 31.61l6.318 6.318-6.318 6.372a22.352 22.352 0 0 0 31.612 31.61l6.318-6.318 6.372 6.253a22.349 22.349 0 0 0 31.612-31.6Z"
  })));
};

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "../bpl-tools/Components/Mask/assets/shapes/hexagon.svg":
/*!**************************************************************!*\
  !*** ../bpl-tools/Components/Mask/assets/shapes/hexagon.svg ***!
  \**************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ReactComponent: () => (/* binding */ SvgHexagon),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
var _path;
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }

var SvgHexagon = function SvgHexagon(props) {
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", _extends({
    xmlns: "http://www.w3.org/2000/svg",
    width: 103,
    height: 89
  }, props), _path || (_path = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: "M77.211 0H25.752L0 44.505 25.752 89h51.46L103 44.505ZM61.77 62.261H41.222L30.939 44.504 41.222 26.74h20.547l10.283 17.766Z"
  })));
};

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "../bpl-tools/Components/Mask/assets/shapes/sketch.svg":
/*!*************************************************************!*\
  !*** ../bpl-tools/Components/Mask/assets/shapes/sketch.svg ***!
  \*************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ReactComponent: () => (/* binding */ SvgSketch),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
var _path;
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }

var SvgSketch = function SvgSketch(props) {
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", _extends({
    xmlns: "http://www.w3.org/2000/svg",
    width: 91.114,
    height: 89
  }, props), _path || (_path = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: "m82.175 37.527 1.612-1.612c.948-.949-6.37-5.931-7.05-6.378a45 45 0 0 0-5.776-3.325c-.247-.115-.602-.3-.987-.478q1.781-1.897 3.586-3.771c.949-.98-6.363-5.923-7.05-6.37a44 44 0 0 0-5.76-3.332l-.293-.147 1.396-1.542c.94-1.05-6.332-5.9-7.05-6.37a44 44 0 0 0-5.76-3.34c-.54-.247-2.314-1.265-2.847-.687q-13.458 14.993-28.08 28.891c-7.844 7.45-16.567 15.58-18.11 26.87-.115.825 1.327 1.974 1.744 2.314a41 41 0 0 0 5.314 4.057 44 44 0 0 0 5.76 3.34c.64.293 2.176 1.203 2.847.686l2.074-1.558c1.45 1.859 5.97 4.72 6.671 5.175l1.489.956a257 257 0 0 0-4.435 5.622c-.91 1.188 6.263 5.862 7.05 6.37a44 44 0 0 0 5.76 3.332c.602.278 2.23 1.234 2.846.694l9.255-8.098c.825 2.568 14.569 11.63 15.672 9.972L91.06 45.362c.632-.948-4.335-5.036-8.885-7.835"
  })));
};

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "../bpl-tools/Components/Mask/assets/shapes/triangle.svg":
/*!***************************************************************!*\
  !*** ../bpl-tools/Components/Mask/assets/shapes/triangle.svg ***!
  \***************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ReactComponent: () => (/* binding */ SvgTriangle),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
var _path;
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }

var SvgTriangle = function SvgTriangle(props) {
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", _extends({
    xmlns: "http://www.w3.org/2000/svg",
    width: 103,
    height: 89
  }, props), _path || (_path = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: "M77.255 44.505 51.5 0 25.755 44.505 0 89h103z"
  })));
};

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHdpZHRoPSIxMDMiIGhlaWdodD0iODkiIHZpZXdCb3g9IjAgMCAxMDMgODkiPjxwb2x5Z29uIHBvaW50cz0iNzcuMjU1IDQ0LjUwNSA1MS41IDAgMjUuNzU1IDQ0LjUwNSAwIDg5IDUxLjUgODkgMTAzIDg5IDc3LjI1NSA0NC41MDUiLz48L3N2Zz4=");

/***/ }),

/***/ "./src/shared/Components/Common/AudioPlayer.js":
/*!*****************************************************!*\
  !*** ./src/shared/Components/Common/AudioPlayer.js ***!
  \*****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _BlockIcon__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./BlockIcon */ "./src/shared/Components/Common/BlockIcon.js");




/**
 * The player above each Audio Testimonials card.
 *
 * This was previously a picture of a player: a static play glyph and ten
 * fixed-height bars, with no `<audio>` element and no per-item audio field
 * anywhere in the block. Nothing could be uploaded and nothing could be played,
 * in the editor or on the front end.
 *
 * The bars double as the scrub track -- the layout has no room for a separate
 * progress bar, and a waveform that fills as the clip plays is what the design
 * was already imitating.
 *
 * @param {Object}   props
 * @param {string}   props.src         Audio file URL. Without one the player
 *                                     renders disabled rather than disappearing,
 *                                     so the card keeps its shape.
 * @param {Object}   props.playIcon  Icon slot from the Icon panel.
 * @param {boolean} props.isBackend Whether this is the editor render.
 */

// Deterministic bar heights: a waveform generated per render would jump about on
// every state change, and Math.random() in the editor would also make the block
// look dirty to the change detector.
const BARS = [35, 55, 25, 65, 45, 70, 30, 60, 40, 50];
const formatTime = seconds => {
  if (!Number.isFinite(seconds) || seconds < 0) {
    return "0:00";
  }
  const mins = Math.floor(seconds / 60);
  const secs = Math.floor(seconds % 60);
  return `${mins}:${secs < 10 ? "0" : ""}${secs}`;
};
const AudioPlayer = ({
  src = "",
  playIcon = {},
  isBackend
}) => {
  const audioRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const [isPlaying, setIsPlaying] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const [currentTime, setCurrentTime] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(0);
  const [duration, setDuration] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(0);

  // A card can be removed, or its file swapped, while the clip is playing.
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    setIsPlaying(false);
    setCurrentTime(0);
    setDuration(0);
    if (audioRef.current) {
      audioRef.current.pause();
      audioRef.current.currentTime = 0;
    }
  }, [src]);
  const toggle = () => {
    const audio = audioRef.current;
    if (!audio || !src) {
      return;
    }
    if (audio.paused) {
      // Pause every other card first: several testimonials playing at once is
      // never what a click on one of them meant.
      document.querySelectorAll("audio.btb-audio-el").forEach(el => {
        if (el !== audio) {
          el.pause();
        }
      });
      // A rejected play() (autoplay policy, missing file) must not leave the
      // button stuck showing "pause".
      audio.play().catch(() => setIsPlaying(false));
    } else {
      audio.pause();
    }
  };
  const progress = duration > 0 ? currentTime / duration : 0;
  const seek = e => {
    const audio = audioRef.current;
    if (!audio || !duration) {
      return;
    }
    const rect = e.currentTarget.getBoundingClientRect();
    if (!rect.width) {
      return;
    }
    const ratio = Math.max(0, Math.min(1, (e.clientX - rect.left) / rect.width));
    audio.currentTime = ratio * duration;
    setCurrentTime(audio.currentTime);
  };
  const nudge = delta => {
    const audio = audioRef.current;
    if (!audio || !duration) {
      return;
    }
    audio.currentTime = Math.max(0, Math.min(duration, audio.currentTime + delta));
    setCurrentTime(audio.currentTime);
  };
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: `btb-audio-player ${isPlaying ? "is-playing" : ""} ${src ? "" : "is-empty"}`
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("button", {
    type: "button",
    className: "btb-audio-toggle",
    onClick: toggle,
    disabled: !src,
    "aria-label": isPlaying ? "Pause audio" : "Play audio",
    style: {
      color: (0,_BlockIcon__WEBPACK_IMPORTED_MODULE_1__.resolveIconColor)(playIcon)
    }
  }, isPlaying ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
    viewBox: "0 0 24 24",
    width: "40",
    height: "40",
    "aria-hidden": "true"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
    cx: "12",
    cy: "12",
    r: "11",
    fill: "currentColor",
    opacity: "0.1"
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
    fill: "currentColor",
    d: "M9.5 7.5h1.8v9H9.5v-9zm3.2 0h1.8v9h-1.8v-9z"
  })) : (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_BlockIcon__WEBPACK_IMPORTED_MODULE_1__["default"], {
    icon: playIcon,
    size: 40,
    renderFallback: (color, box) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
      viewBox: "0 0 24 24",
      width: box,
      height: box,
      "aria-hidden": "true"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
      cx: "12",
      cy: "12",
      r: "11",
      fill: color,
      opacity: "0.1"
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
      fill: color,
      d: "M10 8.64L15.27 12 10 15.36V8.64M8 5v14l11-7L8 5z"
    }))
  })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "btb-audio-wave",
    onClick: src ? seek : undefined,
    role: "slider",
    tabIndex: src ? 0 : -1,
    "aria-label": "Seek audio",
    "aria-valuemin": 0,
    "aria-valuemax": Math.round(duration),
    "aria-valuenow": Math.round(currentTime),
    "aria-valuetext": `${formatTime(currentTime)} of ${formatTime(duration)}`,
    onKeyDown: e => {
      if ("ArrowRight" === e.key) {
        e.preventDefault();
        nudge(5);
      } else if ("ArrowLeft" === e.key) {
        e.preventDefault();
        nudge(-5);
      } else if ("Enter" === e.key || " " === e.key) {
        e.preventDefault();
        toggle();
      }
    }
  }, BARS.map((h, i) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    key: i,
    className: `btb-wave-bar ${progress > 0 && i / BARS.length < progress ? "is-played" : ""}`,
    style: {
      height: `${h}%`
    }
  }))), src && duration > 0 && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    className: "btb-audio-time"
  }, formatTime(currentTime), " / ", formatTime(duration)), src ?
  // No <track>: the block stores a single media URL per card and has no
  // field for a caption file, so an empty track element would advertise
  // captions that do not exist.
  // eslint-disable-next-line jsx-a11y/media-has-caption
  (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("audio", {
    ref: audioRef,
    className: "btb-audio-el",
    src: src,
    preload: "metadata",
    onPlay: () => setIsPlaying(true),
    onPause: () => setIsPlaying(false),
    onEnded: () => {
      setIsPlaying(false);
      setCurrentTime(0);
    },
    onTimeUpdate: e => setCurrentTime(e.currentTarget.currentTime),
    onLoadedMetadata: e => setDuration(e.currentTarget.duration || 0)
  }) : isBackend && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    className: "btb-audio-hint"
  }, "Add an audio file in the sidebar"));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (AudioPlayer);

/***/ }),

/***/ "./src/shared/Components/Common/BeforeAfterSlider.js":
/*!***********************************************************!*\
  !*** ./src/shared/Components/Common/BeforeAfterSlider.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);


const DEFAULT_IMG = "https://templates.bplugins.com/wp-content/uploads/2025/02/p-29.png";
const BeforeAfterSlider = ({
  attributes = {}
}) => {
  const {
    beforeImg = {},
    afterImg = {},
    beforeLabel = "Before",
    afterLabel = "After",
    startPosition = 50,
    accentColor = "#ffffff",
    orientation = "horizontal",
    interaction = "drag",
    aspectRatio = "auto",
    mediaRadius = "8px",
    dividerWidth = 3,
    gripSize = 38,
    showLabels = true,
    labelPosition = "bottom",
    labelFontSize = 13,
    items = [],
    badgeTitle = ""
  } = attributes;
  const isVertical = "vertical" === orientation;
  const isHover = "hover" === interaction;
  const beforeUrl = beforeImg?.url || items?.[0]?.img?.url || DEFAULT_IMG;
  const afterUrl = afterImg?.url || items?.[1]?.img?.url || DEFAULT_IMG;
  const [pos, setPos] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(startPosition);
  const [isDragging, setIsDragging] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const containerRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);

  // Without this the inspector's Start Position range moved nothing: the state
  // was seeded once at mount and the editor never remounts the block, so the
  // divider stayed wherever it already was.
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    setPos(startPosition);
  }, [startPosition]);
  const updatePos = (0,react__WEBPACK_IMPORTED_MODULE_0__.useCallback)((clientX, clientY) => {
    if (!containerRef.current) {
      return;
    }
    const rect = containerRef.current.getBoundingClientRect();
    const span = isVertical ? rect.height : rect.width;
    if (!span) {
      return;
    }
    const offset = isVertical ? clientY - rect.top : clientX - rect.left;
    setPos(Math.max(0, Math.min(100, offset / span * 100)));
  }, [isVertical]);

  // Drag is tracked with pointer capture on the container rather than listeners
  // on `window`. In the editor the block lives inside the canvas iframe, and
  // Gutenberg only re-dispatches `mousemove`/`dragover` up to the parent frame --
  // a `mouseup` in the canvas never reached a parent-window listener, so the
  // divider stayed glued to the pointer after the button was released. Capture
  // keeps every move and the release on this element, in this document, so the
  // coordinates also stay in the same space as `getBoundingClientRect()`.
  const isMouse = e => "mouse" === e.pointerType;
  const onPointerDown = e => {
    // Hover mode reveals on move, so a mouse press must not start a drag. Touch
    // and pen have no hover, so they still drag.
    if (isHover && isMouse(e)) {
      return;
    }
    e.currentTarget.setPointerCapture?.(e.pointerId);
    setIsDragging(true);
    updatePos(e.clientX, e.clientY);
  };
  const onPointerMove = e => {
    if (isDragging || isHover && isMouse(e)) {
      updatePos(e.clientX, e.clientY);
    }
  };

  // `lostpointercapture` covers the cases `pointerup` misses -- the browser
  // revoking capture, or the node unmounting mid-drag.
  const endDrag = e => {
    if (e?.currentTarget?.hasPointerCapture?.(e.pointerId)) {
      e.currentTarget.releasePointerCapture(e.pointerId);
    }
    setIsDragging(false);
  };
  const handleKeyDown = e => {
    const back = isVertical ? "ArrowUp" : "ArrowLeft";
    const forward = isVertical ? "ArrowDown" : "ArrowRight";
    if (back === e.key || "ArrowLeft" === e.key) {
      setPos(prev => Math.max(0, prev - 2));
    } else if (forward === e.key || "ArrowRight" === e.key) {
      setPos(prev => Math.min(100, prev + 2));
    }
  };
  const wrapClasses = ["ba-wrap", isVertical ? "is-vertical" : "", isHover ? "is-hover" : "", "top" === labelPosition ? "labels-top" : "", "auto" === aspectRatio ? "" : "has-ratio"].filter(Boolean).join(" ");
  const wrapStyle = {
    "--pos": `${pos}%`,
    "--ba-radius": mediaRadius || "0px",
    "--ba-divider-w": `${dividerWidth}px`,
    "--ba-grip": `${gripSize}px`,
    "--ba-label-size": `${labelFontSize}px`
  };
  if ("auto" !== aspectRatio) {
    wrapStyle.aspectRatio = aspectRatio.replace(":", " / ");
  }
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "bBeforeAfter"
  }, badgeTitle && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
    className: "btb-ba-title"
  }, badgeTitle), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    ref: containerRef,
    className: wrapClasses,
    role: "presentation",
    style: wrapStyle,
    onPointerDown: onPointerDown,
    onPointerMove: onPointerMove,
    onPointerUp: endDrag,
    onPointerCancel: endDrag,
    onLostPointerCapture: endDrag
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
    className: "ba-sizer",
    src: beforeUrl,
    alt: "",
    "aria-hidden": "true"
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "ba-before"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
    src: beforeUrl,
    alt: beforeImg?.alt || beforeLabel || "Before"
  }), showLabels && beforeLabel && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    className: "ba-label ba-label-before"
  }, beforeLabel)), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "ba-after"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
    src: afterUrl,
    alt: afterImg?.alt || afterLabel || "After"
  }), showLabels && afterLabel && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    className: "ba-label ba-label-after"
  }, afterLabel)), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "ba-handle",
    style: {
      borderColor: accentColor
    },
    tabIndex: 0,
    role: "slider",
    "aria-valuenow": Math.round(pos),
    "aria-valuemin": 0,
    "aria-valuemax": 100,
    "aria-orientation": isVertical ? "vertical" : "horizontal",
    "aria-label": "Before after slider handle",
    onKeyDown: handleKeyDown
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    className: "ba-handle-line",
    style: {
      background: accentColor
    }
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    className: "ba-handle-grip",
    style: {
      background: accentColor
    }
  }))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (BeforeAfterSlider);

/***/ }),

/***/ "./src/shared/Components/Common/BlockIcon.js":
/*!***************************************************!*\
  !*** ./src/shared/Components/Common/BlockIcon.js ***!
  \***************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__),
/* harmony export */   resolveIconColor: () => (/* binding */ resolveIconColor)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _utils_icons__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../utils/icons */ "./src/shared/utils/icons.js");



/**
 * Resolve the colour for an icon slot, used by the built-in SVG fallbacks too.
 *
 * @param {Object} icon     Icon slot value: { svg, color, size, strokeColor }.
 * @param {string} fallback Colour to use when nothing is set.
 * @return {string} A CSS colour.
 */
const resolveIconColor = (icon = {}, fallback = _utils_icons__WEBPACK_IMPORTED_MODULE_1__.BRAND_COLOR) => {
  const {
    color
  } = icon || {};
  return color && "inherit" !== color ? color : fallback;
};

/**
 * Renders a user-picked icon, or the block's original artwork.
 *
 * The picked icon is inline SVG markup stored by bpl-tools' IconLibrary, so it
 * needs no icon font on the front end. Colour is applied via `color` and the
 * `fill: currentColor` rule on `.btb-custom-icon svg`: fill inherits, so paths
 * carrying their own fill (Lucid's `fill="none"` outlines) keep it while the
 * Font Awesome and Bootstrap paths, which declare no fill, pick up the colour.
 *
 * When no icon has been picked the `renderFallback` render-prop is used and
 * handed the resolved colour and size, so both controls still work on the
 * built-in artwork without the user having to choose an icon first. The size
 * has to travel as an argument because the fallback is the caller's own markup:
 * the picked icon is sized by the wrapper this file writes, which the fallback
 * never gets, so every call site had its own hardcoded width and height and the
 * Icon Size control moved nothing until an icon was chosen.
 *
 * Stroke colour is deliberately opt-in rather than defaulted to the fill.
 * Font Awesome and Bootstrap glyphs are solid shapes carrying no stroke at all,
 * and SVG's initial `stroke-width` is 1 -- so painting a stroke unconditionally
 * would thicken every one of those icons the moment the control existed. The
 * class only goes on once the author picks a colour, which keeps an untouched
 * block rendering exactly as before.
 *
 * @param {Object}   props
 * @param {Object}   props.icon           Icon slot value.
 * @param {number}   props.size           Default size in px.
 * @param {boolean}  props.lockSize       Ignore the slot's own size.
 * @param {string}   props.className      Extra classes for the wrapper.
 * @param {string}   props.defaultColor   Colour the fallback uses when none is set.
 * @param {Function} props.renderFallback (color, box) => node, used when no icon
 *                                        is picked. `box` is the resolved size
 *                                        in px, as a number.
 */
const BlockIcon = ({
  icon = {},
  size = 36,
  lockSize = false,
  className = "",
  defaultColor = _utils_icons__WEBPACK_IMPORTED_MODULE_1__.BRAND_COLOR,
  renderFallback
}) => {
  const svg = icon?.svg;
  const strokeColor = icon?.strokeColor;
  const strokeClass = strokeColor ? " btb-icon-stroke" : "";
  const strokeVar = strokeColor ? {
    "--btb-icon-stroke": strokeColor
  } : {};

  // The slot's own size wins, since it is the more specific of the two -- but
  // `lockSize` turns that off for a block whose icon box is a block-level
  // control. There the slot has no size control at all, so a value left over
  // from before would be an override with no UI to clear it.
  const boxPx = !lockSize && icon.size || size;
  if (!svg) {
    const fallback = renderFallback ? renderFallback(resolveIconColor(icon, defaultColor), boxPx) : null;
    if (!fallback || !strokeColor) {
      return fallback;
    }

    // The built-in artwork is rendered by the caller, so the stroke reaches it
    // through a wrapper instead of a prop -- otherwise the control would look
    // dead until the author had picked an icon, and every call site would need
    // the same two lines. `display: contents` keeps the wrapper out of the
    // layout while custom properties and descendant selectors pass through it.
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-icon-stroke",
      style: strokeVar
    }, fallback);
  }
  const box = `${boxPx}px`;
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    className: `btb-custom-icon${strokeClass} ${className}`.trim(),
    style: {
      color: resolveIconColor(icon, defaultColor),
      width: box,
      height: box,
      ...strokeVar
    },
    "aria-hidden": "true",
    dangerouslySetInnerHTML: {
      __html: svg
    }
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (BlockIcon);

/***/ }),

/***/ "./src/shared/Components/Common/ExpandButton.js":
/*!******************************************************!*\
  !*** ./src/shared/Components/Common/ExpandButton.js ***!
  \******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);

const ExpandButton = ({
  attributes = {},
  expanded = false,
  onChange
}) => {
  const {
    elements = {}
  } = attributes || {};
  const {
    expandBtn = false,
    expandText = 'Expand',
    collapseText = 'Less'
  } = elements || {};
  return expandBtn && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("button", {
    className: "expandBtn btb-expand-btn",
    type: "button",
    onClick: onChange
  }, expanded ? collapseText : expandText);
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ExpandButton);

/***/ }),

/***/ "./src/shared/Components/Common/Image.js":
/*!***********************************************!*\
  !*** ./src/shared/Components/Common/Image.js ***!
  \***********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);

const Image = ({
  attributes = {},
  children,
  img = {}
}) => {
  var _elements$img;
  const {
    elements = {}
  } = attributes || {};
  return ((_elements$img = elements?.img) !== null && _elements$img !== void 0 ? _elements$img : true) && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "authorImg"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "img"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
    src: img?.url || '',
    alt: img?.title || img?.alt || ''
  }), children));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Image);

/***/ }),

/***/ "./src/shared/Components/Common/Layout/Layout.js":
/*!*******************************************************!*\
  !*** ./src/shared/Components/Common/Layout/Layout.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var react_responsive_masonry__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react-responsive-masonry */ "../plugin-slug/node_modules/react-responsive-masonry/es/index.js");
/* harmony import */ var _Themes_Default__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../Themes/Default */ "./src/shared/Components/Common/Themes/Default.js");
/* harmony import */ var _Themes_ThemeOne__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../Themes/ThemeOne */ "./src/shared/Components/Common/Themes/ThemeOne.js");
/* harmony import */ var _Themes_ThemeTwo__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../Themes/ThemeTwo */ "./src/shared/Components/Common/Themes/ThemeTwo.js");
/* harmony import */ var _Themes_ThemeThree__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../Themes/ThemeThree */ "./src/shared/Components/Common/Themes/ThemeThree.js");
/* harmony import */ var _Themes_ThemeFour__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../Themes/ThemeFour */ "./src/shared/Components/Common/Themes/ThemeFour.js");
/* harmony import */ var _Themes_ThemeFive__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../Themes/ThemeFive */ "./src/shared/Components/Common/Themes/ThemeFive.js");
/* harmony import */ var _Themes_ThemeSix__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../Themes/ThemeSix */ "./src/shared/Components/Common/Themes/ThemeSix.js");
/* harmony import */ var _Slider__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./Slider */ "./src/shared/Components/Common/Layout/Slider.js");
/* harmony import */ var _Marquee__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./Marquee */ "./src/shared/Components/Common/Layout/Marquee.js");
/* harmony import */ var _BeforeAfterSlider__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ../BeforeAfterSlider */ "./src/shared/Components/Common/BeforeAfterSlider.js");
/* harmony import */ var _TestimonialForm__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ../TestimonialForm */ "./src/shared/Components/Common/TestimonialForm.js");
/* harmony import */ var _utils_a11y__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ../../../utils/a11y */ "./src/shared/utils/a11y.js");
/* harmony import */ var _BlockIcon__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ../BlockIcon */ "./src/shared/Components/Common/BlockIcon.js");
/* harmony import */ var _VideoCard__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ../VideoCard */ "./src/shared/Components/Common/VideoCard.js");
/* harmony import */ var _AudioPlayer__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ../AudioPlayer */ "./src/shared/Components/Common/AudioPlayer.js");
/* harmony import */ var _utils_blockIcons__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ../../../utils/blockIcons */ "./src/shared/utils/blockIcons.js");
/* harmony import */ var _utils_trustBadgeArt__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ../../../utils/trustBadgeArt */ "./src/shared/utils/trustBadgeArt.js");
/* harmony import */ var _utils_layoutFeatures__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! ../../../utils/layoutFeatures */ "./src/shared/utils/layoutFeatures.js");






















/**
 * The Feedback & NPS Poll scale, shared by the editor and the front end.
 *
 * On a published page the block's view script owns this interaction: it binds a
 * delegated click, writes `.is-selected` onto the button it was given and posts
 * the vote to the REST route. That script is registered as `viewScript`, so it
 * never loads in the editor -- which left every number completely dead there.
 * Clicking one did nothing, and the selected state could not be seen at all
 * while styling it.
 *
 * So the editor gets local state and the front end deliberately gets none. If
 * React owned `.is-selected` on the page, its next render would wipe whatever
 * the view script had just written; and nothing in the editor may post a real
 * vote, which is why the click here only ever sets state.
 *
 * It lives in its own component because Layout returns early for each layout,
 * and a hook cannot be called inside one of those branches.
 */
const FeedbackPoll = ({
  attributes = {},
  isBackend,
  bt,
  bd
}) => {
  const [picked, setPicked] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(null);
  const minVal = attributes.minScore !== undefined ? Number(attributes.minScore) : 0;
  const maxVal = attributes.maxScore !== undefined ? Number(attributes.maxScore) : 10;
  const lowLbl = attributes.lowLabel !== undefined ? attributes.lowLabel : "Not likely";
  const highLbl = attributes.highLabel !== undefined ? attributes.highLabel : "Very likely";
  const pollNumbers = [];
  for (let i = minVal; i <= maxVal; i++) {
    pollNumbers.push(i);
  }
  const previewing = isBackend && picked !== null;
  return (
    // Endpoint and REST nonce come from render.php; the view script reads them
    // off this element rather than guessing the REST root.
    (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-poll-wrapper",
      "data-endpoint": attributes.pollEndpoint || "",
      "data-rest-nonce": attributes.pollRestNonce || ""
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
      className: "btb-poll-title"
    }, bt || "How likely are you to recommend us?"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
      className: "btb-poll-desc"
    }, bd || "Net Promoter Score Survey"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-poll-scale"
    }, lowLbl && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-poll-label-low"
    }, lowLbl), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-poll-buttons"
    }, pollNumbers.map(n => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("button", {
      key: n,
      type: "button",
      className: previewing && picked === n ? "btb-poll-num-btn is-selected" : "btb-poll-num-btn",
      "data-mark": n,
      onClick: isBackend ? () => setPicked(n) : undefined
    }, n))), highLbl && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-poll-label-high"
    }, highLbl)), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-poll-response-msg",
      style: {
        display: previewing ? "block" : "none"
      }
    }, previewing ? "Preview only — votes are recorded on the published page." : ""))
  );
};
const SocialProofToast = ({
  items = [],
  bt,
  bd,
  isBackend,
  activeIndex,
  toast = {},
  pauseInEditor = false
}) => {
  const [currentIndex, setCurrentIndex] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(0);
  const [isHovered, setIsHovered] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false);

  /*
   * The rotation runs in the editor too.
   *
   * It used to stop dead on `isBackend`, so the one thing this block is -- a
   * notification that cycles -- could only be seen by previewing the page. The
   * slider and the marquee both animate in the editor and both offer "Pause
   * while editing" for when they get in the way; this now follows that
   * convention rather than being the one animated layout that never moves.
   */
  const isPaused = isBackend && pauseInEditor;

  /*
   * Picking a card in the sidebar jumps the preview to it.
   *
   * Without this the author would select card 3, watch the rotation carry on
   * from wherever it was, and be styling something they cannot see. Syncing on
   * `activeIndex` keeps the selection meaningful while the cycle continues from
   * that card.
   */
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (isBackend && activeIndex < items.length) {
      setCurrentIndex(activeIndex);
    }
  }, [isBackend, activeIndex, items.length]);

  // How fast the notifications cycle, and whether hovering holds one still.
  //
  // The rotation was a bare 4000 in this effect -- the one setting a "live
  // social proof notification" is really made of, with nothing in the sidebar to
  // change it. Stored in seconds, because that is how an author thinks about it.
  const {
    speed = 4,
    pauseOnHover = false
  } = toast || {};
  const delay = Math.max(1, Number(speed) || 4) * 1000;
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (isPaused || items.length <= 1) return;
    // Hovering pauses by tearing the timer down and rebuilding it on leave,
    // rather than by tracking elapsed time: a notification the reader has
    // stopped on should stay up as long as they are on it, then start its full
    // interval again, not finish the remainder of an interval they interrupted.
    if (pauseOnHover && isHovered) return;
    const interval = setInterval(() => {
      setCurrentIndex(prev => (prev + 1) % items.length);
    }, delay);
    return () => clearInterval(interval);
  }, [isPaused, items.length, delay, pauseOnHover, isHovered]);

  // Paused in the editor, the card being edited is the one to show; otherwise
  // the cycle decides, in the editor as on the page.
  const activeItemIndex = isPaused ? activeIndex < items.length ? activeIndex : 0 : currentIndex < items.length ? currentIndex : 0;
  const currentItem = items[activeItemIndex] || {};
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "btb-toast-wrapper",
    ...(pauseOnHover ? {
      onMouseEnter: () => setIsHovered(true),
      onMouseLeave: () => setIsHovered(false)
    } : {})
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "btb-toast-card",
    key: activeItemIndex
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "btb-toast-avatar"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
    src: currentItem.img?.url || "https://templates.bplugins.com/wp-content/uploads/2025/02/p-29.png",
    alt: currentItem.name || ""
  })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "btb-toast-body"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
    className: "btb-toast-text"
  }, currentItem.reviewText || bt || "Someone just left a 5-star review!"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    className: "btb-toast-meta"
  }, currentItem.name || "John Doe", " \u2014 ", bd || "Just now"))));
};
const Layout = ({
  itemsEls = [],
  ToolbarButton,
  MediaUpload,
  MediaUploadCheck,
  attributes = {},
  setActiveIndex,
  activeIndex = 0,
  updateItem,
  isBackend = false,
  previewDevice = "Desktop",
  __,
  RichText,
  SandBox
}) => {
  const {
    items = [],
    columnGap = "30px",
    rowGap = "40px",
    layout = "default",
    theme = "default",
    columns = {
      desktop: 3,
      tablet: 2,
      mobile: 1
    }
  } = attributes || {};
  const {
    desktop = 3,
    tablet = 2,
    mobile = 1
  } = columns && typeof columns === "object" ? columns : {
    desktop: 3,
    tablet: 2,
    mobile: 1
  };

  // The column count to render at the "base" size.
  //
  // On the front end that is always the desktop value, with the media queries
  // taking over at narrower widths. In the editor it has to follow the device
  // buttons directly, because those only produce a real viewport when the
  // canvas is iframed -- and WordPress iframes it only when every registered
  // block is apiVersion 3. A single v2 block from any other active plugin
  // disables iframing for the whole editor, and then @media keeps measuring the
  // browser window, so the tablet and mobile rules never fire no matter which
  // device is selected. Resolving the count here works either way: when the
  // canvas *is* iframed the media queries agree with this value.
  const previewCols = isBackend && "Tablet" === previewDevice ? tablet : isBackend && "Mobile" === previewDevice ? mobile : desktop;
  const arrangement = (0,_utils_layoutFeatures__WEBPACK_IMPORTED_MODULE_19__.resolveArrangement)(attributes);
  const [selectedAvatarIdx, setSelectedAvatarIdx] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(0);
  const [cardStackIdx, setCardStackIdx] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(0);
  const [activeModalItem, setActiveModalItem] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(null);
  const [stackHovered, setStackHovered] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false);

  // The Stacked Review Cards deck advancing on its own.
  //
  // It is driven from here rather than from the block's `view.js`, which is where
  // a DOM-side autoplay would naturally have gone. That script never runs: it
  // hooks DOMContentLoaded and queries `.btb-card-stack-wrapper`, but
  // `shared/view.js` mounts React on the same event, so the wrapper does not
  // exist yet when it looks -- measured, the deck's `is-top` classes and its
  // next/prev buttons all come from React below, and an autoplay added to that
  // script sat dead. (Its drag-to-swipe is dead for the same reason; see the
  // note in that file.)
  //
  // Declared at the top level of the component because the card-stack branch is
  // an early return further down, and a hook cannot live behind one. The guard
  // is inside the effect instead, so every other layout runs it and exits.
  const stackAutoPlay = !!attributes?.stackAutoPlay;
  const stackDelay = Math.max(1, Number(attributes?.stackAutoPlayDelay) || 5) * 1000;
  const stackCount = items.length;
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if ("testimonials-card-stack" !== layout) return;
    // Not in the editor: the deck should hold still while it is being styled,
    // the same way the marquee's Pause while editing works.
    if (isBackend || !stackAutoPlay || stackCount <= 1) return;
    if (stackHovered) return;
    const timer = setInterval(() => {
      setCardStackIdx(prev => (prev + 1) % stackCount);
    }, stackDelay);
    return () => clearInterval(timer);
  }, [layout, isBackend, stackAutoPlay, stackDelay, stackCount, stackHovered]);
  const itemProps = {
    attributes,
    setActiveIndex,
    activeIndex,
    updateItem,
    isBackend,
    __,
    RichText,
    MediaUpload,
    MediaUploadCheck,
    ToolbarButton
  };

  // Dynamic badge attributes (from block.json / sidebar settings)
  const bt = attributes.badgeTitle || "";
  const bd = attributes.badgeDesc || "";
  const bs = attributes.badgeScore || "";
  const bc = attributes.badgeCount || "";

  // === Theme selector (shared by all testimonial-items layouts) ===
  const themeSelect = (item, index) => {
    const itemProp = {
      item: item || {},
      index,
      itemEls: itemsEls?.[index] || {},
      ...itemProps
    };
    switch (theme) {
      case "theme_1":
        return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_ThemeOne__WEBPACK_IMPORTED_MODULE_3__["default"], {
          ...itemProp
        });
      case "theme_2":
        return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_ThemeTwo__WEBPACK_IMPORTED_MODULE_4__["default"], {
          ...itemProp
        });
      case "theme_3":
        return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_ThemeThree__WEBPACK_IMPORTED_MODULE_5__["default"], {
          ...itemProp
        });
      case "theme_4":
        return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_ThemeFour__WEBPACK_IMPORTED_MODULE_6__["default"], {
          ...itemProp
        });
      case "theme_5":
        return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_ThemeFive__WEBPACK_IMPORTED_MODULE_7__["default"], {
          ...itemProp
        });
      case "theme_6":
        return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_ThemeSix__WEBPACK_IMPORTED_MODULE_8__["default"], {
          ...itemProp
        });
      default:
        return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_Default__WEBPACK_IMPORTED_MODULE_2__["default"], {
          ...itemProp
        });
    }
  };

  // ================================================================
  //  CATEGORY D: Badge / Score Widgets (fully custom JSX, dynamic)
  // ================================================================

  // Helper to calculate dynamic rating stats from items array
  const computedStats = (() => {
    const total = Array.isArray(items) ? items.length : 0;
    if (total === 0) {
      return {
        total: 0,
        avg: "5.0",
        countText: "",
        counts: {
          5: 0,
          4: 0,
          3: 0,
          2: 0,
          1: 0
        }
      };
    }
    const counts = {
      5: 0,
      4: 0,
      3: 0,
      2: 0,
      1: 0
    };
    let sum = 0;
    items.forEach(it => {
      var _it$rating;
      const rawVal = Number((_it$rating = it?.rating) !== null && _it$rating !== void 0 ? _it$rating : 5);
      const r = Math.min(5, Math.max(1, isNaN(rawVal) ? 5 : rawVal));
      const rounded = Math.round(r);
      counts[rounded] = (counts[rounded] || 0) + 1;
      sum += r;
    });
    const avg = (sum / total).toFixed(1);
    return {
      total,
      avg,
      countText: `Based on ${total} reviews`,
      counts
    };
  })();
  if (layout === "google-review-badge") {
    const score = bs || (computedStats.total > 0 ? computedStats.avg : "4.9");
    const count = bc || (computedStats.total > 0 ? `(${computedStats.total}+ Reviews)` : "(128+ Reviews)");
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-card btb-google-badge"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-header"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
      className: "btb-badge-brand-logo",
      viewBox: "0 0 24 24",
      width: "36",
      height: "36"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
      fill: "#4285F4",
      d: "M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
      fill: "#34A853",
      d: "M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
      fill: "#FBBC05",
      d: "M5.84 14.1c-.22-.66-.35-1.36-.35-2.1s.13-1.44.35-2.1V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.62z"
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
      fill: "#EA4335",
      d: "M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"
    })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-info"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
      className: "btb-badge-title"
    }, bt || "Google Reviews"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-rating"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "score"
    }, score), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "stars"
    }, "\u2605\u2605\u2605\u2605\u2605"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "count"
    }, count)))));
  }
  if (layout === "capterra-review-badge") {
    const score = bs || (computedStats.total > 0 ? computedStats.avg : "4.8");
    const count = bc || (computedStats.total > 0 ? `(${computedStats.total} Reviews)` : "Verified Software Reviews");
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-card btb-capterra-badge"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-header"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
      className: "btb-badge-brand-logo",
      viewBox: "0 0 24 24",
      width: "36",
      height: "36"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
      fill: "#FF9D28",
      d: "M2 2h9v9H2z"
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
      fill: "#68C5ED",
      d: "M13 2h9v9h-9z"
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
      fill: "#044D80",
      d: "M2 13h9v9H2z"
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
      fill: "#E54747",
      d: "M13 13h9v9h-9z"
    })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-info"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
      className: "btb-badge-title"
    }, bt || "Capterra Rating"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-rating"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "score"
    }, score), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "stars"
    }, "\u2605\u2605\u2605\u2605\u2605"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "count"
    }, count)))));
  }
  if (layout === "facebook-review-badge") {
    const score = bs || (computedStats.total > 0 ? computedStats.avg : "5.0");
    const count = bc || (computedStats.total > 0 ? `Recommended by ${computedStats.total} Customers` : "Recommended by 250+ Customers");
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-card btb-facebook-badge"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-header"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
      className: "btb-badge-brand-logo",
      viewBox: "0 0 24 24",
      width: "36",
      height: "36"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
      fill: "#1877F2",
      d: "M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"
    })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-info"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
      className: "btb-badge-title"
    }, bt || "Facebook Reviews"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-rating"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "score"
    }, score), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "stars"
    }, "\u2605\u2605\u2605\u2605\u2605"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "count"
    }, count)))));
  }
  if (layout === "trustpilot-review-badge") {
    const score = bs || (computedStats.total > 0 ? `${computedStats.avg} / 5` : "4.9 / 5");
    const count = bc || (computedStats.total > 0 ? `TrustScore | ${computedStats.total} Reviews` : "TrustScore | 500+ Reviews");
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-card btb-trustpilot-badge"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-header"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
      className: "btb-badge-brand-logo",
      viewBox: "0 0 24 24",
      width: "36",
      height: "36"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
      fill: "#00B67A",
      d: "M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z"
    })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-info"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
      className: "btb-badge-title"
    }, bt || "Trustpilot Score"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-rating"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "score"
    }, score), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "stars"
    }, "\u2605\u2605\u2605\u2605\u2605"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "count"
    }, count)))));
  }
  if (layout === "g2-review-badge") {
    const score = bs || (computedStats.total > 0 ? `${computedStats.avg} / 5` : "4.8 / 5");
    const count = bc || "Leader Category 2026";
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-card btb-g2-badge"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-header"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
      className: "btb-badge-brand-logo",
      viewBox: "0 0 24 24",
      width: "36",
      height: "36"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
      cx: "12",
      cy: "12",
      r: "11",
      fill: "#FF492C"
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("text", {
      x: "12",
      y: "16",
      textAnchor: "middle",
      fill: "#fff",
      fontSize: "12",
      fontWeight: "bold"
    }, "G2")), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-info"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
      className: "btb-badge-title"
    }, bt || "G2 High Performer"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-rating"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "score"
    }, score), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "stars"
    }, "\u2605\u2605\u2605\u2605\u2605"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "count"
    }, count)))));
  }
  if (layout === "verified-buyer-badge") {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-card btb-verified-badge"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-header"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_BlockIcon__WEBPACK_IMPORTED_MODULE_14__["default"], {
      icon: (0,_utils_blockIcons__WEBPACK_IMPORTED_MODULE_17__.getIcon)(attributes, "badge"),
      size: 36,
      className: "btb-badge-brand-logo",
      renderFallback: (color, box) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        className: "btb-badge-brand-logo",
        viewBox: "0 0 24 24",
        width: box,
        height: box
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        fill: color,
        d: "M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4zm-2 16l-4-4 1.41-1.41L10 14.17l6.59-6.59L18 9l-8 8z"
      }))
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-info"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
      className: "btb-badge-title"
    }, bt || "100% Verified Reviews"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
      className: "btb-badge-desc"
    }, bd || "All customer testimonials are authenticated & verified."))));
  }
  if (layout === "review-badge-widget") {
    const score = bs || (computedStats.total > 0 ? computedStats.avg : "4.9");
    const count = bc || (computedStats.total > 0 ? `Based on ${computedStats.total} reviews` : "Based on 320+ reviews");
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-card btb-review-widget"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-header"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_BlockIcon__WEBPACK_IMPORTED_MODULE_14__["default"], {
      icon: (0,_utils_blockIcons__WEBPACK_IMPORTED_MODULE_17__.getIcon)(attributes, "badge"),
      size: 36,
      className: "btb-badge-brand-logo",
      renderFallback: (color, box) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        className: "btb-badge-brand-logo",
        viewBox: "0 0 24 24",
        width: box,
        height: box
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        fill: color,
        d: "M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z"
      }))
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-info"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
      className: "btb-badge-title"
    }, bt || "Customer Reviews"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-badge-rating"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "score"
    }, score), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "stars"
    }, "\u2605\u2605\u2605\u2605\u2605"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "count"
    }, count)))));
  }
  if (layout === "trust-badges") {
    // Artwork from the shared list, so the editor preview draws the same badge
    // this does; only the labels below belong to this branch, since they are
    // read from the generic badge fields and used by the fallback alone.
    const TRUST_LABELS = [bt || "Secure & Verified", bd || "Money-Back Guarantee", bs || "5-Star Support", bc || "Trusted by 10K+ Users"];
    const trustItems = _utils_trustBadgeArt__WEBPACK_IMPORTED_MODULE_18__.TRUST_BADGE_ART.map((art, index) => ({
      ...art,
      label: TRUST_LABELS[index]
    }));

    // The Columns and Gap controls only reached this block's editor preview --
    // the front end grid was a fixed repeat(4, 1fr). Same custom properties the
    // video layout uses, so both honour the inspector.
    const gridVars = {
      "--cols-d": previewCols || 3,
      "--cols-t": tablet || 3,
      "--cols-m": mobile || 1,
      "--col-gap": columnGap || "16px",
      "--row-gap": rowGap || "16px"
    };

    // The block's editor is a repeater of badges -- icon image, title, subtitle
    // -- and none of it reached the page: this branch rendered four fixed
    // badges built out of the generic badge fields, so adding or editing a badge
    // moved the editor preview only. Where the repeater has content it now
    // drives the page, with the Icons panel supplying the artwork for any badge
    // whose image is empty.
    // Icon size reaches a picked icon through BlockIcon's `size` prop rather
    // than CSS -- BlockIcon writes width and height inline, which no selector
    // outranks. `lockSize` on both calls below is what makes this the one that
    // lands: the slot's own size normally wins, and this block's Icons panel
    // offers none, so an old saved value would override a control it can no
    // longer be cleared from.
    const iconBox = attributes.badgeIconSize || 32;
    const iconTop = "top" === attributes.badgeIconPosition;
    const badgeItems = Array.isArray(attributes.items) ? attributes.items : [];
    const hasRepeater = badgeItems.some(it => it && (it.img?.url || it.title || it.subtitle));
    if (hasRepeater) {
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
        className: "bTrustBadges"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
        className: "badges-grid",
        style: gridVars
      }, badgeItems.map((item, index) => {
        // Four drawings, no limit on badges: a fifth repeats the
        // first, which is what the helper does.
        const slot = (0,_utils_trustBadgeArt__WEBPACK_IMPORTED_MODULE_18__.getTrustBadgeArt)(index);
        return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
          className: `badge-item${iconTop ? " is-icon-top" : ""}`,
          key: index
        }, item?.img?.url ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
          className: "badge-icon",
          src: item.img.url,
          alt: item.img.alt || ""
        }) :
        /* `trust${index}` rather than the art's own slot: the
           drawings repeat past the fourth badge but the icon slots
           do not -- the Icon panel names one per badge. */
        (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_BlockIcon__WEBPACK_IMPORTED_MODULE_14__["default"], {
          icon: (0,_utils_blockIcons__WEBPACK_IMPORTED_MODULE_17__.getIcon)(attributes, `trust${index}`),
          size: iconBox,
          lockSize: true,
          defaultColor: slot.color,
          renderFallback: color => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
            className: "badge-icon",
            viewBox: "0 0 24 24",
            width: iconBox,
            height: iconBox
          }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
            fill: color,
            d: slot.d
          }))
        }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
          className: "badge-text"
        }, item?.title && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
          className: "badge-title"
        }, item.title), item?.subtitle && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
          className: "badge-subtitle"
        }, item.subtitle)));
      })));
    }
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-trust-badges-grid",
      style: gridVars
    }, trustItems.map(it => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-trust-item",
      key: it.slot
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_BlockIcon__WEBPACK_IMPORTED_MODULE_14__["default"], {
      icon: (0,_utils_blockIcons__WEBPACK_IMPORTED_MODULE_17__.getIcon)(attributes, it.slot),
      size: iconBox,
      lockSize: true,
      defaultColor: it.color,
      renderFallback: color => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        className: "badge-icon",
        viewBox: "0 0 24 24",
        width: iconBox,
        height: iconBox
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        fill: color,
        d: it.d
      }))
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", null, it.label))));
  }

  // ================================================================
  //  CATEGORY E: Interactive / Data Widgets (fully custom JSX)
  // ================================================================

  if (layout === "testimonial-form") {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_TestimonialForm__WEBPACK_IMPORTED_MODULE_12__["default"], {
      attributes: attributes,
      isBackend: isBackend
    });
  }
  if (layout === "user-feedback-poll") {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(FeedbackPoll, {
      attributes: attributes,
      isBackend: isBackend,
      bt: bt,
      bd: bd
    });
  }
  if (layout === "rating-summary") {
    // The Rating panel's own fields -- Rating, Out of, Show review count and
    // Count text -- reached the editor preview only: the score fell back to a
    // hard-coded 4.8 and the count line to "Based on 256 reviews", and there was
    // no way at all to turn the count off. Only Star color made it through,
    // through its palette alias.
    const {
      rating,
      outOf = 5,
      count,
      countText = "",
      showCount = true,
      stacked = false
    } = attributes;

    // Which of the two wins is decided by the data source, not by whether any
    // testimonials happen to exist. `items` ships with three demo reviews, so
    // "calculate when there are items" meant the Rating panel could never win --
    // the block would have kept ignoring it. Choosing the testimonials source is
    // the request to be calculated; anything else means the panel's own figures.
    const useComputed = "cpt" === attributes.dataSource && computedStats.total > 0;
    const manualScore = undefined === rating || null === rating || "" === rating ? "" : `${rating}`;
    const displayScore = bs || (useComputed ? computedStats.avg : manualScore || "4.8");
    const manualCountText = countText ? countText.replace("{count}", Number(count || 0).toLocaleString()) : "";
    const displayCountText = bc || (useComputed ? `Based on ${computedStats.total} reviews` : manualCountText || "Based on 256 reviews");

    // Out of is not always 5, so the star row is drawn rather than a literal
    // run of five glyphs, with the filled overlay clipped to the score.
    const starCount = Math.max(1, Math.min(10, Number(outOf) || 5));
    const scoreNum = parseFloat(displayScore) || 0;
    const fillPct = Math.max(0, Math.min(100, scoreNum / starCount * 100));
    const starRow = "★".repeat(starCount);
    const defaultPcts = {
      5: 78,
      4: 15,
      3: 4,
      2: 2,
      1: 1
    };

    // Same precedence as the score above, so the bars cannot disagree with it.
    const rows = [5, 4, 3, 2, 1].map(s => {
      var _attributes;
      const starTotal = computedStats.counts[s];
      const pct = useComputed ? Math.round(starTotal / computedStats.total * 100) : (_attributes = attributes[`star${s}Pct`]) !== null && _attributes !== void 0 ? _attributes : defaultPcts[s];
      return {
        star: s,
        pct,
        count: starTotal
      };
    });
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: `btb-rating-summary ${stacked ? "is-stacked" : ""}`
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-rs-left"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-rs-big-number"
    }, displayScore), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-rs-stars"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-rs-stars-base"
    }, starRow), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-rs-stars-fill",
      style: {
        width: `${fillPct}%`
      }
    }, starRow)), showCount && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-rs-count"
    }, displayCountText)), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-rs-bars"
    }, rows.map(r => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      key: r.star,
      className: "btb-rs-bar-row"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-rs-bar-label"
    }, r.star, " \u2605"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-rs-bar-track"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-rs-bar-fill",
      style: {
        width: `${r.pct}%`
      }
    })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-rs-bar-pct"
    }, r.pct, "%")))));
  }
  if (layout === "star-rating-bars") {
    const defaultCounts = {
      5: 184,
      4: 46,
      3: 15,
      2: 7,
      1: 3
    };
    const rows = [5, 4, 3, 2, 1].map(s => {
      const overrideVal = attributes[`star${s}Count`];
      let count;
      if (overrideVal !== undefined && overrideVal !== "") {
        count = Number(overrideVal) || 0;
      } else if (computedStats.total > 0 && computedStats.counts[s] > 0) {
        count = computedStats.counts[s];
      } else {
        count = defaultCounts[s];
      }
      return {
        star: s,
        count
      };
    });
    const totalCountSum = rows.reduce((sum, r) => sum + (Number(r.count) || 0), 0);
    const rowsWithPct = rows.map(r => {
      const rawPct = totalCountSum > 0 ? Math.round(r.count / totalCountSum * 100) : 0;
      const pct = isNaN(rawPct) ? 0 : rawPct;
      return {
        ...r,
        pct
      };
    });
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-star-rating-bars"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
      className: "btb-srb-title"
    }, bt || "Rating Breakdown"), rowsWithPct.map(r => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      key: r.star,
      className: "btb-srb-row"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-srb-label"
    }, r.star, " ", r.star === 1 ? "Star" : "Stars"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-srb-track"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-srb-fill",
      style: {
        width: `${r.pct}%`
      }
    })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-srb-count"
    }, r.count, " (", r.pct, "%)"))));
  }
  if (layout === "testimonial-stats") {
    const gridVars = {
      "--cols-d": previewCols || 3,
      "--cols-t": tablet || 3,
      "--cols-m": mobile || 1,
      "--col-gap": columnGap || "16px",
      "--row-gap": rowGap || "16px",
      "--accent": attributes.accentColor
    };

    // The block's editor is a repeater -- number, prefix, suffix, label per
    // stat -- and its view script animates `.stat-number[data-number]` inside
    // `.bTestimonialStats[data-animate="1"]`. Neither reached the page: this
    // branch used to render four fixed cards out of the generic badge fields,
    // so adding, removing or editing a stat changed only the editor, and Animate
    // count never ran. Rendering the repeater fixes all of it at once.
    const statItems = Array.isArray(attributes.items) ? attributes.items : [];
    const hasRepeater = statItems.some(it => it && (it.number !== undefined || it.label !== undefined || it.suffix || it.prefix));
    if (hasRepeater) {
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
        className: "bTestimonialStats",
        "data-animate": attributes.animate ? "1" : "0"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
        className: `stats-grid btb-ts-grid ${attributes.surfaceColor || attributes.borderColor ? "has-surface" : ""}`,
        style: gridVars
      }, statItems.map((item, index) => {
        var _item$number;
        const raw = String((_item$number = item?.number) !== null && _item$number !== void 0 ? _item$number : "");
        const decimals = raw.includes(".") ? raw.split(".")[1].length : 0;
        return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
          className: "stat-item btb-ts-item",
          key: index
        }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
          className: "stat-value btb-ts-value",
          style: {
            color: attributes.accentColor
          }
        }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
          className: "stat-prefix btb-ts-prefix"
        }, item?.prefix), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
          className: "stat-number btb-ts-number",
          "data-number": item?.number,
          "data-decimals": decimals
        }, Number(item?.number || 0).toLocaleString()), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
          className: "stat-suffix btb-ts-suffix"
        }, item?.suffix)), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
          className: "stat-label btb-ts-label"
        }, item?.label));
      })));
    }

    // Fallback for blocks saved before the repeater existed, which described
    // their stats through the generic badge fields.
    const fiveStarCount = computedStats.counts[5];
    const calcAvg = computedStats.total > 0 ? computedStats.avg : "4.9";
    const calc5Star = computedStats.total > 0 ? `${fiveStarCount}` : "500+";
    const legacyStats = [[bs || (computedStats.total > 0 ? `${computedStats.total}+` : "10K+"), bt || "Happy Customers"], [bc || "98%", bd || "Satisfaction Rate"], [attributes.stat3Number || calcAvg, attributes.stat3Label || "Average Rating"], [attributes.stat4Number || calc5Star, attributes.stat4Label || "5-Star Reviews"]];
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-stats-grid",
      style: gridVars
    }, legacyStats.map(([num, label], index) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-stat-card",
      key: index
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-stat-number"
    }, num), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-stat-label"
    }, label))));
  }
  if (layout === "social-proof-toast") {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(SocialProofToast, {
      items: items,
      bt: bt,
      bd: bd,
      isBackend: isBackend,
      activeIndex: activeIndex,
      toast: attributes?.toast,
      pauseInEditor: attributes?.pauseInEditor
    });
  }
  if (layout === "comparison-testimonial-table") {
    const title = bt || attributes.badgeTitle || "Customer Comparison";
    const col1 = attributes.col1Header || "Customer";
    const col2 = attributes.col2Header || "Rating";
    const col3 = attributes.col3Header || "Review";
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-comparison-table"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
      className: "btb-ct-title"
    }, title), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("table", {
      className: "btb-ct-table"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("thead", null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("tr", null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("th", null, col1), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("th", null, col2), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("th", null, col3))), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("tbody", null, items.map((item, i) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("tr", {
      key: i
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("td", {
      className: "btb-ct-name"
    }, item.name || ""), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("td", {
      className: "btb-ct-rating"
    }, "★".repeat(item.rating || 5)), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("td", {
      className: "btb-ct-text"
    }, itemsEls?.[i]?.reviewText))))));
  }
  if (layout === "faq-testimonial-accordion") {
    // The two settings every accordion has, and this one had neither: rows could
    // all be open at once and none opened on load.
    //
    // "One at a time" is the native `name` attribute on `<details>`, not a click
    // handler -- browsers make a named group exclusive themselves, so it keeps
    // working with JavaScript off and needs no state here. The name is scoped to
    // the block's own clientId, or two accordions on one page would close each
    // other's rows.
    const {
      faqExclusive = false,
      faqFirstOpen = false
    } = attributes || {};
    const groupName = faqExclusive ? `btb-faq-${attributes?.cId || "group"}` : undefined;
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-faq-accordion"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
      className: "btb-faq-title"
    }, bt || "Frequently Asked Questions"), items.map((item, i) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("details", {
      key: i,
      className: "btb-faq-item",
      name: groupName
      // `defaultOpen` is not a thing on <details>; `open` is, and React
      // treats it as a controlled-ish prop. Only the first row takes it,
      // and only as the initial state -- the reader can still close it.
      ,
      open: faqFirstOpen && 0 === i ? true : undefined
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("summary", {
      className: "btb-faq-question"
    }, item.name || `Question ${i + 1}`), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-faq-answer"
    }, itemsEls?.[i]?.reviewText, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-faq-author"
    }, "\u2014 ", item.deg || "Customer")))));
  }

  // ================================================================
  //  CATEGORY C: Custom testimonial layouts (unique JSX + items)
  // ================================================================

  if (layout === "testimonials-avatar-list") {
    const activeIdx = isBackend && typeof activeIndex === "number" ? activeIndex : selectedAvatarIdx;
    const currentActiveIdx = activeIdx < items.length ? activeIdx : 0;
    const activeItem = items[currentActiveIdx] || items[0] || {};
    const handleAvatarClick = idx => {
      setSelectedAvatarIdx(idx);
      if (typeof setActiveIndex === "function") {
        setActiveIndex(idx);
      }
    };
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-avatar-list-wrapper"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-avatar-row"
    }, items.map((it, idx) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      key: idx,
      ...(0,_utils_a11y__WEBPACK_IMPORTED_MODULE_13__.clickable)(() => handleAvatarClick(idx), it.name || ""),
      className: `btb-avatar-thumb ${currentActiveIdx === idx ? "active" : ""}`
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
      src: it.img?.url || "https://templates.bplugins.com/wp-content/uploads/2025/02/p-29.png",
      alt: it.name || ""
    }), itemsEls?.[idx]?.img))), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-avatar-detail"
    }, isBackend && RichText ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(RichText, {
      tagName: "p",
      className: "btb-avatar-review",
      value: activeItem.reviewText || "",
      onChange: val => updateItem("reviewText", val),
      placeholder: __("Enter review text", "b-testimonials-block"),
      inlineToolbar: true
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(RichText, {
      tagName: "h4",
      className: "btb-avatar-name",
      value: activeItem.name || "",
      onChange: val => updateItem("name", val),
      placeholder: __("Enter name", "b-testimonials-block"),
      inlineToolbar: true
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(RichText, {
      tagName: "span",
      className: "btb-avatar-deg",
      value: activeItem.deg || "",
      onChange: val => updateItem("deg", val),
      placeholder: __("Enter designation", "b-testimonials-block"),
      inlineToolbar: true
    })) : (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
      className: "btb-avatar-review"
    }, "\"", activeItem.reviewText || "", "\""), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
      className: "btb-avatar-name"
    }, activeItem.name || ""), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-avatar-deg"
    }, activeItem.deg || ""))));
  }
  if (layout === "testimonials-timeline") {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-timeline-layout"
    }, items.map((item, index) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      key: index,
      className: "btb-timeline-item"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-timeline-dot"
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-timeline-card"
    }, themeSelect(item, index)))));
  }
  if (layout === "audio-testimonials") {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: `layoutSection btb-audio-layout ${theme} btb-columns-${previewCols} btb-columns-tablet-${tablet} btb-columns-mobile-${mobile}`
    }, items.map((item, index) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      key: index,
      className: "btb-audio-card"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_AudioPlayer__WEBPACK_IMPORTED_MODULE_16__["default"], {
      src: item?.audio?.url || "",
      playIcon: (0,_utils_blockIcons__WEBPACK_IMPORTED_MODULE_17__.getIcon)(attributes, "play"),
      isBackend: isBackend
    }), themeSelect(item, index))));
  }
  if (layout === "video-testimonials") {
    // `grid-template-columns: repeat(N, 1fr)` always fills the grid's own box,
    // whatever N is -- so fewer videos than columns left the extra tracks
    // empty rather than absent, and the grid's own width still spanned all N
    // of them. Block Width narrowed and Alignment shifted that whole box
    // correctly, but the populated card sat wherever the grid placed it (the
    // first track, flush left) and never moved with it: measured, blockAlign
    // "right" on a single video in a 3-column grid put 50px between the box
    // and the page's right edge, and 437px between the card and that same
    // edge, so the box had moved but the card had not.
    //
    // Capping the effective column count at the item count -- desktop,
    // tablet and mobile can each run out at a different count -- shrinks the
    // grid's own box to only the tracks something is actually in, without
    // touching how wide any one card renders: 2 items in a 3-column desktop
    // grid still get 1/3 of the full width each, just inside a box that is
    // now 2/3 as wide instead of 3/3 with an invisible empty third. That
    // narrower box is then free to carry the block's own alignment, so a
    // partially-filled grid follows Left/Center/Right the same as the block
    // around it. A full grid keeps its box at 100% either way, so this is a
    // no-op whenever there are at least as many videos as columns -- which is
    // every grid that was already working correctly.
    const videoColCounts = {
      d: previewCols || 3,
      t: tablet || 2,
      m: mobile || 1
    };
    const videoItemCount = items.length || 1;
    const videoEffCols = {
      d: Math.max(1, Math.min(videoItemCount, videoColCounts.d)),
      t: Math.max(1, Math.min(videoItemCount, videoColCounts.t)),
      m: Math.max(1, Math.min(videoItemCount, videoColCounts.m))
    };
    // The same three options the block's own alignment offers, so a
    // partially-filled grid reads as an extension of it rather than a
    // separate setting -- "Default" falls back to today's flush-left.
    const VIDEO_GRID_ALIGN_MARGIN = {
      left: {
        l: "0",
        r: "auto"
      },
      center: {
        l: "auto",
        r: "auto"
      },
      right: {
        l: "auto",
        r: "0"
      }
    };
    const videoGridAlign = VIDEO_GRID_ALIGN_MARGIN[attributes.blockAlign] || VIDEO_GRID_ALIGN_MARGIN.left;
    const videoGridVars = {
      "--cols-d": videoEffCols.d,
      "--cols-t": videoEffCols.t,
      "--cols-m": videoEffCols.m,
      "--grid-width-d": `${videoEffCols.d / videoColCounts.d * 100}%`,
      "--grid-width-t": `${videoEffCols.t / videoColCounts.t * 100}%`,
      "--grid-width-m": `${videoEffCols.m / videoColCounts.m * 100}%`,
      "--grid-margin-left": videoGridAlign.l,
      "--grid-margin-right": videoGridAlign.r,
      "--col-gap": columnGap || "30px",
      "--row-gap": rowGap || "30px",
      "--accent": attributes.accentColor || "#0575e6"
    };
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "bVideoTestimonials"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "videos-grid",
      style: videoGridVars
    }, items.map((item, index) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_VideoCard__WEBPACK_IMPORTED_MODULE_15__["default"], {
      key: index,
      item: item,
      playIcon: (0,_utils_blockIcons__WEBPACK_IMPORTED_MODULE_17__.getIcon)(attributes, "play"),
      accentColor: attributes.accentColor,
      autoplay: attributes.videoAutoplay,
      loop: attributes.videoLoop,
      muted: attributes.videoMuted,
      controls: attributes.videoControls,
      SandBox: SandBox
    }))));
  }
  if (layout === "before-after") {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_BeforeAfterSlider__WEBPACK_IMPORTED_MODULE_11__["default"], {
      attributes: attributes
    });
  }
  if (layout === "case-study-card") {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: `btb-case-study-grid btb-columns-${previewCols} btb-columns-tablet-${tablet} btb-columns-mobile-${mobile}`
    }, items.map((item, index) => {
      var _ref, _item$challengeTitle, _item$challenge, _ref2, _item$solutionTitle, _ref3, _item$solution, _ref4, _item$resultTitle, _item$result;
      const sections = item.sections || [{
        title: (_ref = (_item$challengeTitle = item.challengeTitle) !== null && _item$challengeTitle !== void 0 ? _item$challengeTitle : item.challengeLabel) !== null && _ref !== void 0 ? _ref : "Challenge",
        content: (_item$challenge = item.challenge) !== null && _item$challenge !== void 0 ? _item$challenge : "The customer needed a reliable solution to improve their workflow."
      }, {
        title: (_ref2 = (_item$solutionTitle = item.solutionTitle) !== null && _item$solutionTitle !== void 0 ? _item$solutionTitle : item.solutionLabel) !== null && _ref2 !== void 0 ? _ref2 : "Solution",
        content: (_ref3 = (_item$solution = item.solution) !== null && _item$solution !== void 0 ? _item$solution : item.reviewText) !== null && _ref3 !== void 0 ? _ref3 : "It is a long-established fact that a reader will be distracted by the readable content of a page when looking at its layout"
      }, {
        title: (_ref4 = (_item$resultTitle = item.resultTitle) !== null && _item$resultTitle !== void 0 ? _item$resultTitle : item.resultLabel) !== null && _ref4 !== void 0 ? _ref4 : "Result",
        content: (_item$result = item.result) !== null && _item$result !== void 0 ? _item$result : "95% improvement in efficiency and customer satisfaction."
      }];
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
        key: index,
        className: "btb-case-study"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
        className: "btb-cs-header"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
        className: "btb-cs-avatar-wrap"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
        src: item.img?.url || "https://templates.bplugins.com/wp-content/uploads/2025/02/p-29.png",
        alt: item.name,
        className: "btb-cs-avatar"
      }), itemsEls?.[index]?.img), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h4", {
        className: "btb-cs-name"
      }, item.name || "John Doe"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
        className: "btb-cs-deg"
      }, item.deg || "Developer"))), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
        className: "btb-cs-body"
      }, sections.map((sec, secIdx) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
        key: secIdx,
        className: "btb-cs-section"
      }, sec.title && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
        className: "btb-cs-label"
      }, sec.title), sec.content && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", null, sec.content)))));
    }));
  }
  if (layout === "client-logos") {
    const logoItems = items.length > 0 ? items : attributes.logos || [];

    // Same markup and custom properties as the block's own editor.
    //
    // This used to render a `.btb-client-logos` grid of its own with the gap
    // and the logo height written into the stylesheet as literals, so four of
    // the block's controls -- Column Gap, Row Gap, Logo height and Grayscale --
    // moved the editor preview and nothing else. Matching the editor markup is
    // what makes them reach the page, and it is the markup logos.scss styles.
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "bClientLogos"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: `logos-grid ${attributes.grayscale ? "is-grayscale" : ""} ${attributes.trackColor || attributes.borderColor ? "has-surface" : ""}`,
      style: {
        "--cols-d": previewCols || 4,
        "--cols-t": tablet || 3,
        "--cols-m": mobile || 2,
        "--col-gap": columnGap || "30px",
        "--row-gap": rowGap || "30px",
        "--logo-h": `${attributes.logoHeight || 60}px`
      }
    }, logoItems.map((item, index) => {
      const imgEl = (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
        src: item.img?.url || "https://templates.bplugins.com/wp-content/uploads/2025/02/p-29.png",
        alt: item.name || item.img?.alt || ""
      });
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
        key: index,
        className: "logo-item"
      }, item.link ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("a", {
        href: item.link,
        target: "_blank",
        rel: "noopener noreferrer"
      }, imgEl) : imgEl);
    })));
  }

  // ================================================================
  //  CATEGORY B: Testimonial layouts with custom CSS wrappers
  //  (use themeSelect but wrapped in unique layout classes)
  // ================================================================

  if (layout === "testimonials-hero") {
    const heroItem = items[0] || {};

    // The spotlight takes the first testimonial, so the grid only ever holds
    // the ones after it -- which is why Columns looks broken here. A hero block
    // ships with a single testimonial and renders no grid at all, so the control
    // has nothing to act on until a second card is added; and with more columns
    // than follower cards the few that exist were squeezed into a fraction of
    // the row with the remaining tracks left empty. Measured at 4 columns and
    // one follower: a 310px card in a 1280px row.
    //
    // Clamping to the number of cards actually in the grid keeps the control
    // honest at every count -- it can still reduce the columns, it just cannot
    // ask for more tracks than there are cards to fill them.
    const followers = items.slice(1);
    const gridCols = value => Math.max(1, Math.min(Number(value) || 1, followers.length));
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-hero-layout"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-hero-card"
    }, themeSelect(heroItem, 0)), followers.length > 0 && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: `btb-hero-grid btb-columns-${gridCols(previewCols)} btb-columns-tablet-${gridCols(tablet)} btb-columns-mobile-${gridCols(mobile)}`
    }, followers.map((item, index) => themeSelect(item, index + 1))));
  }
  if (layout === "testimonials-popup-modal") {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-popup-modal-wrapper"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: `layoutSection btb-popup-modal-grid ${theme} btb-columns-${previewCols} btb-columns-tablet-${tablet} btb-columns-mobile-${mobile} ${isBackend ? "is-editing" : ""}`
    }, items.map((item, index) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      key: index,
      className: "btb-popup-modal-card-trigger",
      ...(0,_utils_a11y__WEBPACK_IMPORTED_MODULE_13__.clickable)(() => setActiveModalItem(item), item?.name || ""),
      style: {
        cursor: "pointer"
      }
    }, themeSelect(item, index)))), activeModalItem && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-modal-overlay",
      role: "presentation",
      onClick: e => e.target === e.currentTarget && setActiveModalItem(null)
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-modal-content-box",
      role: "dialog",
      "aria-modal": "true",
      "aria-label": activeModalItem?.name || "Testimonial"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("button", {
      type: "button",
      className: "btb-modal-close",
      onClick: () => setActiveModalItem(null)
    }, "\xD7"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-modal-head"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
      className: "btb-modal-avatar",
      src: activeModalItem.img?.url || "https://templates.bplugins.com/wp-content/uploads/2025/02/p-29.png",
      alt: activeModalItem.name || ""
    }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-modal-meta"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h3", {
      className: "btb-modal-name"
    }, activeModalItem.name || "John Doe"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-modal-deg"
    }, activeModalItem.deg || ""), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-modal-rating"
    }, "★".repeat(activeModalItem.rating || 5)))), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
      className: "btb-modal-review"
    }, "\"", activeModalItem.reviewText || "", "\""))));
  }
  if (layout === "testimonials-floating-bubble") {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-floating-bubble-layout"
    }, items.map((item, index) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      key: index,
      className: "btb-bubble-item",
      style: {
        animationDelay: `${index * 0.3}s`
      }
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-bubble-avatar"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("img", {
      src: item.img?.url || "https://templates.bplugins.com/wp-content/uploads/2025/02/p-29.png",
      alt: item.name
    }), itemsEls?.[index]?.img), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-bubble-content"
    }, itemsEls?.[index]?.reviewText, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btb-bubble-name"
    }, item.name)))));
  }
  if (layout === "testimonials-card-stack") {
    const totalItems = items.length;
    const currentIdx = typeof setActiveIndex === "function" ? activeIndex || 0 : cardStackIdx;
    const activeIdx = totalItems > 0 ? (currentIdx % totalItems + totalItems) % totalItems : 0;
    const updateStackActive = newIdx => {
      setCardStackIdx(newIdx);
      if (typeof setActiveIndex === "function") {
        setActiveIndex(newIdx);
      }
    };
    const nextCard = e => {
      if (e) e.preventDefault();
      if (totalItems > 1) {
        updateStackActive((activeIdx + 1) % totalItems);
      }
    };
    const prevCard = e => {
      if (e) e.preventDefault();
      if (totalItems > 1) {
        updateStackActive((activeIdx - 1 + totalItems) % totalItems);
      }
    };
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: `btb-card-stack-wrapper ${isBackend ? "is-editing" : ""}`,
      ...(stackAutoPlay && !isBackend ? {
        onMouseEnter: () => setStackHovered(true),
        onMouseLeave: () => setStackHovered(false)
      } : {})
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-card-stack-layout",
      "data-active-index": activeIdx,
      "data-total-items": totalItems
    }, items.map((item, index) => {
      const pos = totalItems > 0 ? (index - activeIdx + totalItems) % totalItems : 0;
      const isTop = pos === 0;

      // One state class, not two. This was `${isTop ? 'is-top' : ''}`
      // followed by a separate `is-behind-N` / `is-hidden` ternary, so the
      // top card came out as `is-top is-hidden`: both classes carry the
      // same specificity and `.is-hidden` is declared last, so it won with
      // `opacity: 0`. Every stack rendered its front card invisible, and a
      // stack of one -- which is the default -- showed nothing at all. The
      // front-end view script assigns these exclusively already, but it
      // returns early on a single card, so it never repaired that case.
      const stateClass = isTop ? "is-top" : pos <= 2 ? `is-behind-${pos}` : "is-hidden";
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
        key: index,
        className: `btb-stacked-card ${stateClass}`,
        "data-index": index,
        "data-stack-pos": pos,
        style: {
          zIndex: isTop ? 10 : 10 - pos
        },
        ...(0,_utils_a11y__WEBPACK_IMPORTED_MODULE_13__.editorClickable)(isBackend, () => updateStackActive(index))
      }, themeSelect(item, index));
    })), totalItems > 1 && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-stack-controls"
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("button", {
      type: "button",
      className: "btb-stack-btn btb-stack-prev",
      onClick: prevCard,
      "aria-label": "Previous card"
    }, "\u2039"), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: "btb-stack-dots"
    }, items.map((_, idx) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("button", {
      key: idx,
      type: "button",
      className: `btb-stack-dot ${idx === activeIdx ? "is-active" : ""}`,
      onClick: e => {
        e.preventDefault();
        updateStackActive(idx);
      },
      "aria-label": `Go to card ${idx + 1}`
    }))), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("button", {
      type: "button",
      className: "btb-stack-btn btb-stack-next",
      onClick: nextCard,
      "aria-label": "Next card"
    }, "\u203A")));
  }

  // ================================================================
  //  CATEGORY A: Standard layouts (existing switch logic, fixed)
  // ================================================================

  // The block keeps its own identity classes; the arrangement adds its own on
  // top. The one exception is a layout that only ever named an arrangement
  // (default/slider/masonry/list/marquee) -- once an explicit arrangement is
  // chosen, keeping the old class would let two arrangements style the same
  // element, e.g. marquee's overflow rules wrapping a Swiper.
  const identityClasses = _utils_layoutFeatures__WEBPACK_IMPORTED_MODULE_19__.ARRANGEMENTS.includes(layout) && arrangement !== layout ? [] : [`${layout}-layout`, `btb-${layout}-layout`];
  const sectionClasses = ["layoutSection", ...identityClasses,
  // Deduped: for a block whose layout already names its arrangement, the two
  // produce the same class.
  ...(identityClasses.includes(`${arrangement}-layout`) ? [] : [`${arrangement}-layout`]), theme, `btb-columns-${previewCols}`, `btb-columns-tablet-${tablet}`, `btb-columns-mobile-${mobile}`, isBackend ? "is-editing" : ""].filter(Boolean).join(" ");
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: sectionClasses
  }, (() => {
    switch (arrangement) {
      case "masonry":
        {
          const masonryItems = items.map((item, index) => themeSelect(item, index));

          // In the editor the count comes from the device buttons: this
          // measures the window, so inside a non-iframed canvas it would
          // report the desktop width whichever device is selected.
          if (isBackend) {
            return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react_responsive_masonry__WEBPACK_IMPORTED_MODULE_1__["default"], {
              columnsCount: previewCols,
              gutter: `${rowGap} ${columnGap}`
            }, masonryItems);
          }

          // Breakpoints are min-width here, so they are the CSS max-widths in
          // _devices.scss plus one, keeping this in step with the stylesheets.
          return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react_responsive_masonry__WEBPACK_IMPORTED_MODULE_1__.ResponsiveMasonry, {
            columnsCountBreakPoints: {
              0: mobile,
              641: tablet,
              1025: desktop
            }
          }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react_responsive_masonry__WEBPACK_IMPORTED_MODULE_1__["default"], {
            columnsCount: desktop,
            gutter: `${rowGap} ${columnGap}`
          }, masonryItems));
        }
      case "slider":
      case "slider-3d":
      case "coverflow":
        return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Slider__WEBPACK_IMPORTED_MODULE_9__["default"], {
          attributes: attributes,
          itemsEls: itemsEls,
          itemProps: itemProps,
          isBackend: isBackend,
          previewCols: previewCols,
          previewDevice: previewDevice,
          arrangement: arrangement
        });
      case "marquee":
        return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Marquee__WEBPACK_IMPORTED_MODULE_10__["default"], {
          items: items,
          themeSelect: themeSelect,
          columnGap: columnGap,
          marquee: attributes?.marquee,
          isBackend: isBackend,
          pauseInEditor: attributes?.pauseInEditor
        });
      // All other testimonial-items layouts: quote-box, speech-bubble, compact, list, etc.
      // The unique visual is produced by the CSS class on the wrapper div
      default:
        return items.map((item, index) => themeSelect(item, index));
    }
  })());
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Layout);

/***/ }),

/***/ "./src/shared/Components/Common/Layout/Marquee.js":
/*!********************************************************!*\
  !*** ./src/shared/Components/Common/Layout/Marquee.js ***!
  \********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);

const Marquee = ({
  items,
  themeSelect,
  columnGap,
  marquee = {},
  isBackend = false,
  pauseInEditor = false
}) => {
  const {
    speed = 30,
    direction = "left",
    pauseOnHover = true
  } = marquee && "object" === typeof marquee ? marquee : {};

  // `marquee-track`/`-group`/`-item` are plain English terms any other
  // carousel or logo-ticker plugin is just as likely to have picked -- on a
  // site running several of those, a same-named foreign rule landing on
  // `display: flex`/`width: max-content`/`flex: 0 0 auto` here is what turns
  // the seamless scroll into an overlapping pile. `btb-marquee-*` twins carry
  // the actual layout now; the bare classes stay for anyone already styling
  // against them.
  const group = (dupe = false) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: `marquee-group btb-marquee-group${dupe ? " marquee-group-dupe btb-marquee-group-dupe" : ""}`,
    "aria-hidden": dupe ? "true" : undefined
  }, items.map((item, index) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "marquee-item btb-marquee-item",
    style: {
      marginRight: columnGap
    },
    key: `${dupe ? "dup-" : ""}${index}`
  }, themeSelect(item, index))));

  // Paused rather than removed: `animation: none` snaps the track back to its
  // start, so toggling it would jump the cards around. `animation-play-state`
  // freezes them exactly where they are.
  const trackClasses = ["marquee-track", "btb-marquee-track", pauseOnHover ? "pause-on-hover" : "", isBackend && pauseInEditor ? "is-paused" : ""].filter(Boolean).join(" ");
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: trackClasses,
    style: {
      "--btb-marquee-duration": `${speed}s`,
      // The keyframes always translate the same way; reversing the animation
      // is what sends the cards the other way, and it keeps the seamless
      // wrap working in both directions.
      animationDirection: "right" === direction ? "reverse" : "normal"
    }
  }, group(), group(true));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Marquee);

/***/ }),

/***/ "./src/shared/Components/Common/Layout/Slider.js":
/*!*******************************************************!*\
  !*** ./src/shared/Components/Common/Layout/Slider.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var swiper_modules__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! swiper/modules */ "../plugin-slug/node_modules/swiper/modules/index.mjs");
/* harmony import */ var swiper_react__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! swiper/react */ "../plugin-slug/node_modules/swiper/swiper-react.mjs");
/* harmony import */ var swiper_css__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! swiper/css */ "../plugin-slug/node_modules/swiper/swiper.css");
/* harmony import */ var swiper_css_navigation__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! swiper/css/navigation */ "../plugin-slug/node_modules/swiper/modules/navigation.css");
/* harmony import */ var swiper_css_pagination__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! swiper/css/pagination */ "../plugin-slug/node_modules/swiper/modules/pagination.css");
/* harmony import */ var swiper_css_effect_coverflow__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! swiper/css/effect-coverflow */ "../plugin-slug/node_modules/swiper/modules/effect-coverflow.css");
/* harmony import */ var _Themes_Default__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../Themes/Default */ "./src/shared/Components/Common/Themes/Default.js");
/* harmony import */ var _Themes_ThemeOne__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../Themes/ThemeOne */ "./src/shared/Components/Common/Themes/ThemeOne.js");
/* harmony import */ var _Themes_ThemeTwo__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ../Themes/ThemeTwo */ "./src/shared/Components/Common/Themes/ThemeTwo.js");
/* harmony import */ var _Themes_ThemeThree__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ../Themes/ThemeThree */ "./src/shared/Components/Common/Themes/ThemeThree.js");
/* harmony import */ var _Themes_ThemeFour__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ../Themes/ThemeFour */ "./src/shared/Components/Common/Themes/ThemeFour.js");
/* harmony import */ var _Themes_ThemeFive__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ../Themes/ThemeFive */ "./src/shared/Components/Common/Themes/ThemeFive.js");
/* harmony import */ var _Themes_ThemeSix__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ../Themes/ThemeSix */ "./src/shared/Components/Common/Themes/ThemeSix.js");














const Slider = ({
  attributes = {},
  itemsEls = [],
  itemProps = {},
  isBackend = false,
  previewCols = 0,
  previewDevice = "Desktop",
  arrangement = ""
}) => {
  const {
    items = [],
    slider = {},
    columnGap = "30px",
    theme = "default",
    columns = {},
    layout,
    pauseInEditor = false
  } = attributes || {};
  const {
    desktop = 3,
    tablet = 2,
    mobile = 1
  } = columns && typeof columns === "object" ? columns : {
    desktop: 3,
    tablet: 2,
    mobile: 1
  };
  const {
    autoPlay = true,
    autoPlayDelay = 3,
    mouseWheel = true,
    navigation = true,
    loop = true,
    coverRotate,
    coverDepth,
    coverScale,
    coverStretch,
    slideShadows = false,
    visibleSides = 1,
    cardWidth = ""
  } = slider && typeof slider === "object" ? slider : {
    autoPlay: true,
    mouseWheel: true,
    navigation: true,
    loop: true
  };

  // The editor-only pause, so cards hold still while they are being styled.
  // Deliberately not folded into `autoPlay`: that one is a real setting and
  // would switch autoplay off on the published site too.
  const isPaused = isBackend && pauseInEditor;

  // Read from the arrangement, so a slider-3d block set to a plain slider gets
  // a plain slider instead of keeping the coverflow effect from its layout.
  const effective = arrangement || layout;
  const is3D = effective === "slider-3d";
  const isCoverflow = effective === "coverflow" || is3D;
  const spaceBetweenVal = parseInt(columnGap, 10) || 30;

  // Coverflow and 3D Slider are the same engine at two sets of numbers, so the
  // defaults live here per arrangement and a control only overrides the one it
  // touches. Left undefined until set, which is what lets switching arrangement
  // still move you between the two presets.
  const preset = is3D ? {
    rotate: 0,
    depth: 300,
    scale: 0.85,
    modifier: 2
  } : {
    rotate: 50,
    depth: 100,
    scale: 1,
    modifier: 1
  };
  const pick = (value, fallback) => undefined === value || null === value || "" === value ? fallback : Number(value);
  const effectRotate = pick(coverRotate, preset.rotate);
  const effectDepth = pick(coverDepth, preset.depth);
  // How far apart the cards are pushed along the track before the rotation is
  // applied. This is the coverflow effect's own spacing parameter, not
  // `spaceBetween` -- which is forced to 0 below, because a real gap holds the
  // side cards away from the centre one and flattens the effect into a plain
  // rotated slider. Stretch moves them without breaking the overlap, so it is
  // the knob a coverflow actually has for "further apart", and it was fixed at
  // 0 with nothing in the sidebar to change it. Negative values overlap them
  // harder, which is the tight fanned-deck look.
  const effectStretch = pick(coverStretch, 0);
  // Stored as a percentage, because a 0.85 slider is meaningless in the UI.
  const effectScale = undefined === coverScale || null === coverScale || "" === coverScale ? preset.scale : Number(coverScale) / 100;

  // How many cards flank the active one, as a plain count the user sets.
  //
  // This is the slides-per-view for coverflow rather than a separate culling
  // rule: laying out exactly 2n+1 slides means Swiper itself decides which are
  // on screen and tags the rest, instead of us trying to work it out. Earlier
  // attempts that guessed via sibling selectors, or added a class from JS that
  // React then wiped, both misfired -- Swiper maintains .swiper-slide-visible
  // on every update, so it cannot fall out of sync.
  //
  // 'all' is the old select's value; treated as 3 so those blocks keep working.
  const rawSides = "all" === visibleSides ? 3 : Number(visibleSides);
  const sideCards = Number.isFinite(rawSides) ? Math.max(0, rawSides) : 1;
  const coverPerView = sideCards * 2 + 1;

  // Card Width overrides that split.
  //
  // Slides-per-view sets the slide width as a share of the track, so raising
  // the side-card count necessarily narrows every card -- at 3 a side they are
  // a seventh of the container each. Giving the slides an explicit width and
  // switching to slidesPerView 'auto' unpicks the two: width is then whatever
  // was asked for, and how many fit follows from it.
  const hasCardWidth = !!cardWidth && "0" !== String(parseInt(cardWidth, 10));

  // Coverflow has to come down on a narrow screen.
  //
  // Visible Side Cards is deliberately not a per-device setting -- "how many
  // cards flank the active one" is the shape of the effect, not a column count.
  // But the count alone decides the slide width, so 1 card a side stayed three
  // slides wide at every size: measured on a 420px phone that is a 101px slide
  // holding 147px of content, spilling out of the card and unreadable.
  //
  // Fractional caps rather than a whole number, so the neighbours still peek in
  // from the edges and the effect survives at the smaller sizes instead of
  // collapsing to a plain one-up slider.
  const perViewCap = device => {
    if ("Mobile" === device) {
      return 1.4;
    }
    if ("Tablet" === device) {
      return 2.4;
    }
    return coverPerView;
  };

  // The editor follows the device buttons; the front end uses breakpoints
  // below, so it starts from the desktop count.
  const coverPerViewNow = isBackend ? Math.min(coverPerView, perViewCap(previewDevice)) : coverPerView;
  const maxPerView = isCoverflow ? coverPerView : Math.max(Number(previewCols) || 0, desktop, 1);
  // Loop needs enough real slides to fill BOTH sides of the centred one --
  // Swiper reorders the slides it has rather than cloning without limit, so
  // short of that it can only pad one side, which left several cards to the
  // left of centre and one to the right.
  //
  // Rather than switch looping off below the threshold (symmetric, but with
  // hard ends, so the first and last slides look nothing like the middle ones),
  // the list is repeated until there are enough. Swiper then has real slides to
  // work with in both directions and the carousel is endless at any number of
  // testimonials.
  // Enough real slides to fill the view plus a full screen of buffer on BOTH
  // sides. At `maxPerView * 2 + 1` Swiper ran out of slides at the wrap and had
  // to hard-reset the track instead of sliding across it, which is the jump back
  // to the first card you could see once a cycle. Three screens' worth means the
  // reset always happens somewhere off-screen.
  const minForLoop = maxPerView * 3 + 1;
  // Swiper cannot loop a single slide -- asking it to warns and leaves the
  // slider stuck -- so the item count still overrides the setting.
  const isLooping = false !== loop && items.length > 1;
  // Only when looping. The repeat exists purely to give Swiper real slides to
  // reorder around the centre; with looping off it has nothing to do and the
  // block simply rendered every testimonial three times over, with a pagination
  // dot for each copy.
  const repeats = isLooping && items.length > 0 ? Math.max(1, Math.ceil(minForLoop / items.length)) : 1;
  const isPadded = repeats > 1;

  // Each rendered slide keeps the index of the testimonial it came from, so a
  // repeat still reads and edits the original rather than a copy of it.
  const slideMap = Array.from({
    length: items.length * repeats
  }, (_, i) => i % items.length);
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(swiper_react__WEBPACK_IMPORTED_MODULE_2__.Swiper
  // Swiper resolves these once at init and caches them, so anything that
  // has to re-initialise the instance belongs in the key rather than
  // being left to a prop update:
  //
  // - `effective` because `effect` and `coverflowEffect` are init-time
  //   params. It used to be `layout`, which does not change when the
  //   arrangement does, so switching Slider -> Coverflow -> 3D Slider
  //   reused the running instance and the new effect never took. That is
  //   why an arrangement switch looked right sometimes and flat others:
  //   it depended on which one the block happened to mount with.
  // - previewCols, or a device switch keeps the previous slidesPerView.
  // - isPaused and the delay, or the old autoplay timer keeps running.
  // - the coverflow numbers, which are read once when the effect
  //   initialises, so dragging a slider would otherwise change nothing
  //   until the block remounted for some other reason.
  // - coverPerViewNow, so the editor's device buttons re-initialise the
  //   coverflow count. previewCols alone does not cover it: coverflow
  //   takes its count from Visible Side Cards, so switching to Mobile
  //   with the same column settings would not change the key at all.
  // - isLooping, because `loop` is an init-time param like the rest of
  //   these. Without it in the key the toggle did nothing at all: Swiper
  //   kept the instance it had already built and went on looping.
  // - autoPlay. The delay was already here but the on/off switch was not,
  //   so turning Autoplay back on in the editor changed nothing on screen
  //   -- Swiper had been built with `autoplay: false` and keeps that. The
  //   toggle looked broken, and a block could end up saved with autoplay
  //   off after flipping it twice to try to make it take.
  , {
    key: `${effective}-${theme}-${items.length}-${spaceBetweenVal}-${previewCols}-${isPaused}-${autoPlay}-${autoPlayDelay}-${effectRotate}-${effectDepth}-${effectScale}-${effectStretch}-${slideShadows}-${repeats}-${sideCards}-${cardWidth}-${coverPerViewNow}-${isLooping}`,
    modules: [swiper_modules__WEBPACK_IMPORTED_MODULE_1__.Navigation, swiper_modules__WEBPACK_IMPORTED_MODULE_1__.A11y, swiper_modules__WEBPACK_IMPORTED_MODULE_1__.Autoplay, swiper_modules__WEBPACK_IMPORTED_MODULE_1__.Mousewheel, swiper_modules__WEBPACK_IMPORTED_MODULE_1__.Pagination, swiper_modules__WEBPACK_IMPORTED_MODULE_1__.EffectCoverflow],
    effect: isCoverflow ? "coverflow" : undefined
    // slideShadows is off by default and opt-in through the Slider panel.
    //
    // Swiper paints them as an absolutely positioned overlay filling the
    // whole SLIDE box, which works for a slide that is one flat image but
    // not for these cards: on Theme 1 and Theme 4 the avatar overhangs the
    // top of the coloured card body, so the slide is 50px taller than the
    // card and the shadow showed in that strip as a grey rectangle floating
    // over the layout. That is why it was hardcoded off. The stylesheet now
    // insets the overlay by the same 50px on those two themes, so the
    // control can be offered without bringing the rectangle back.
    //
    // Still off unless asked for: the cards carry their own box-shadow, and
    // the rotation and scale below already read as depth.
    ,
    coverflowEffect: isCoverflow ? {
      rotate: effectRotate,
      stretch: effectStretch,
      depth: effectDepth,
      scale: effectScale,
      modifier: preset.modifier,
      slideShadows: !!slideShadows
    } : undefined
    // Drives the fade-out of off-screen cards in the stylesheet. Only set
    // for the 3D arrangements; the flat slider shows every column it has.
    ,
    className: isCoverflow ? `btb-cull${hasCardWidth ? " btb-fixed-w" : ""}` : undefined
    // Read by the stylesheet as the slide width. Swiper measures the slides
    // themselves under slidesPerView 'auto', so this has to be real CSS on
    // the element rather than a Swiper parameter.
    ,
    style: hasCardWidth ? {
      "--btb-slide-w": cardWidth
    } : undefined
    // Required for Swiper to maintain .swiper-slide-visible, which is what
    // the stylesheet fades the off-screen cards with.
    ,
    watchSlidesProgress: isCoverflow,
    centeredSlides: isCoverflow,
    grabCursor: true
    // Looping was hardcoded on, so a slider could only ever run in a
    // circle -- there was no way to make one stop at the last review, and
    // with loop on the arrows never reach their disabled state either.
    // `loop` defaults to true, so a block saved before the control
    // existed keeps running exactly as it did.
    //
    // The item count still has the final say: Swiper cannot loop a single
    // slide, and asking it to warns in the console and leaves the slider
    // stuck.
    ,
    loop: isLooping
    // Loop mode has to be told how many slides to keep in hand, because
    // `slidesPerView` above starts at the MOBILE count on the front end and
    // only reaches the desktop count once a breakpoint applies. Swiper works
    // its loop bookkeeping out at init, so it prepared a one-per-view loop and
    // then ran three per view: measured over 48s, `realIndex` kept counting
    // 6,7,8,0,1,2 exactly on schedule while `activeIndex` stuck at 6 with
    // `isEnd: true` -- the timer running normally against a track that could
    // no longer wrap. That is the "slides a few times then freezes".
    //
    // `maxPerView` is the largest count any breakpoint will ask for, so
    // reserving that many covers every size without depending on which one
    // happens to be active when Swiper initialises.
    // A full screen either side, not just one. With `maxPerView` the buffer
    // covered the slides coming into view but not the ones leaving it, so the
    // wrap still landed on the edge of what Swiper had prepared and snapped.
    ,
    loopAdditionalSlides: isLooping ? maxPerView * 2 : 0
    // Coverflow overlaps its slides on purpose -- that overlap is the
    // effect. A gap between them holds the side slides away from the
    // centre one and flattens it into a plain slider that happens to be
    // rotated, so the column gap is not applied here.
    ,
    spaceBetween: isCoverflow ? 0 : spaceBetweenVal
    // In the editor follow the device buttons directly: Swiper's
    // breakpoints measure the window by default, so inside a non-iframed
    // canvas they report the desktop width whichever device is picked.
    // Coverflow takes its count from Visible Side Cards, not from Columns:
    // on a 3D carousel "how many cards flank the active one" is the real
    // question, and it should not change with the device the way a grid's
    // column count does.
    ,
    slidesPerView: isCoverflow ? hasCardWidth ? "auto" : coverPerViewNow : isBackend ? previewCols || desktop : mobile
    // min-width breakpoints, so these are the CSS max-widths in
    // _devices.scss plus one -- keeps the slider in step with the grids.
    //
    // Coverflow gets them too now. It used to opt out entirely, which is
    // what left its slides at the desktop count on a phone; the caps below
    // only ever lower that count, so a block asking for fewer side cards
    // than the cap keeps exactly what it asked for.
    ,
    breakpoints: isBackend ? undefined : isCoverflow ? hasCardWidth ? undefined : {
      0: {
        slidesPerView: Math.min(coverPerView, perViewCap("Mobile"))
      },
      641: {
        slidesPerView: Math.min(coverPerView, perViewCap("Tablet"))
      },
      1025: {
        slidesPerView: coverPerView
      }
    } : {
      641: {
        slidesPerView: tablet
      },
      1025: {
        slidesPerView: desktop
      }
    },
    autoplay: autoPlay && !isPaused ? {
      delay: (Number(autoPlayDelay) || 3) * 1000,
      disableOnInteraction: false
    } : false,
    mousewheel: mouseWheel,
    navigation: navigation
    // A repeated list means one bullet per rendered slide, which would show
    // more dots than there are testimonials. dynamicBullets keeps the
    // strip short rather than advertising the padding.
    ,
    pagination: {
      clickable: true,
      dynamicBullets: isPadded
    }
  }, slideMap.map((index, slideIndex) => {
    const item = items[index];
    const itemProp = {
      item,
      index,
      itemEls: itemsEls?.[index] || {},
      ...itemProps
    };
    let content;
    switch (theme) {
      case "theme_1":
        content = (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_ThemeOne__WEBPACK_IMPORTED_MODULE_8__["default"], {
          ...itemProp
        });
        break;
      case "theme_2":
        content = (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_ThemeTwo__WEBPACK_IMPORTED_MODULE_9__["default"], {
          ...itemProp
        });
        break;
      case "theme_3":
        content = (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_ThemeThree__WEBPACK_IMPORTED_MODULE_10__["default"], {
          ...itemProp
        });
        break;
      case "theme_4":
        content = (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_ThemeFour__WEBPACK_IMPORTED_MODULE_11__["default"], {
          ...itemProp
        });
        break;
      case "theme_5":
        content = (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_ThemeFive__WEBPACK_IMPORTED_MODULE_12__["default"], {
          ...itemProp
        });
        break;
      case "theme_6":
        content = (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_ThemeSix__WEBPACK_IMPORTED_MODULE_13__["default"], {
          ...itemProp
        });
        break;
      default:
        content = (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Themes_Default__WEBPACK_IMPORTED_MODULE_7__["default"], {
          ...itemProp
        });
        break;
    }

    // Keyed by position, not by testimonial: once the list is repeated
    // the same index appears several times, and reusing it would give
    // React duplicate keys.
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(swiper_react__WEBPACK_IMPORTED_MODULE_2__.SwiperSlide, {
      key: slideIndex
    }, content);
  }));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Slider);

/***/ }),

/***/ "./src/shared/Components/Common/RatingIcon.js":
/*!****************************************************!*\
  !*** ./src/shared/Components/Common/RatingIcon.js ***!
  \****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);

const RatingIcon = ({
  attributes = {},
  getStar,
  rating = 5,
  starIconColor = '#FF8C02'
}) => {
  var _elements$icon;
  const {
    elements = {}
  } = attributes || {};
  const showIcon = (_elements$icon = elements?.icon) !== null && _elements$icon !== void 0 ? _elements$icon : true;
  return showIcon ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "rating"
  }, typeof getStar === 'function' ? getStar(rating, starIconColor) : null) : null;
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (RatingIcon);

/***/ }),

/***/ "./src/shared/Components/Common/ReviewText.js":
/*!****************************************************!*\
  !*** ./src/shared/Components/Common/ReviewText.js ***!
  \****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
const ReviewText = ({
  attributes = {},
  itemEls = {}
}) => {
  var _elements$reviewText;
  const {
    elements = {}
  } = attributes || {};
  return ((_elements$reviewText = elements?.reviewText) !== null && _elements$reviewText !== void 0 ? _elements$reviewText : true) ? itemEls?.reviewText || null : null;
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ReviewText);

/***/ }),

/***/ "./src/shared/Components/Common/Style.js":
/*!***********************************************!*\
  !*** ./src/shared/Components/Common/Style.js ***!
  \***********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/functions */ "../bpl-tools/utils/functions.js");
/* harmony import */ var _bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/getCSS */ "../bpl-tools/utils/getCSS.js");
/* harmony import */ var _bpl_tools_utils_data__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../../../../bpl-tools/utils/data */ "../bpl-tools/utils/data.js");
/* harmony import */ var _utils_visualControls__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../utils/visualControls */ "./src/shared/utils/visualControls.js");
/* harmony import */ var _utils_responsiveBox__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../../utils/responsiveBox */ "./src/shared/utils/responsiveBox.js");
/* harmony import */ var _utils_layoutFeatures__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../../utils/layoutFeatures */ "./src/shared/utils/layoutFeatures.js");
/* harmony import */ var _utils_layoutControls__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../../utils/layoutControls */ "./src/shared/utils/layoutControls.js");








const Style = ({
  attributes = {},
  clientId
}) => {
  const {
    layout = "default",
    ratingColor = "",
    columnGap = "30px",
    rowGap = "40px",
    background = "#0000",
    padding = {},
    border = {},
    shadow = {},
    image = {
      width: 50,
      height: 50
    },
    imgBorder = {},
    nameTypo = {},
    nameColor = "#000",
    degTypo = {},
    degColor = "#7B7B7B",
    textTypo = {},
    textColor = "#000",
    expandedTypo = {},
    expandColor = "",
    expandHoverColor = "",
    grid2Bg = "#f9f8f8",
    grid2Padding = {},
    grid2Border = {},
    starIconColor = "#FF8C02",
    slider = {},
    blockWidth = {},
    cardHeight = {},
    cardMargin = {},
    blockMargin = {},
    blockAlign = "",
    degDivider = {},
    labelTypo = {},
    labelColor = "",
    inputTypo = {},
    inputColor = "",
    placeholderColor = "",
    inputFocusColor = "",
    inputRadius = "",
    inputPadding = {},
    textareaHeight = "",
    btnBg = "",
    btnHoverBg = "",
    btnColor = "",
    btnHoverColor = "",
    btnTypo = {},
    btnPadding = {},
    btnRadius = "",
    btnWidth = "auto",
    btnAlign = "start",
    pollTitleTypo = {},
    pollDescTypo = {},
    pollLabelTypo = {},
    pollBtnTypo = {},
    pollBtnColor = "",
    pollBtnActiveColor = "",
    pollBtnSize = "",
    pollBtnRadius = "",
    pollBtnGap = "",
    pollPadding = {},
    pollShadow = {},
    pollBtnShadow = {},
    pollRadius = "",
    gradientBorder = false,
    gradientFrom = "#0575e6",
    gradientTo = "#7b2ff7",
    gradientAngle = 135,
    gradientWidth = 3,
    showStarBadge = false,
    starBadgeBg = "",
    starBadgeColor = "",
    starBadgePosition = "top-right",
    starBadgeSize = "",
    starBadgeRadius = "",
    headerWash = true,
    headerWashStrength = 100,
    bubbleTail = true,
    bubbleTailSize = 12,
    bubbleTailOffset = 30,
    bubbleTailAlign = "left",
    bubbleTailFill = "",
    bubbleTailLine = "",
    badgeScoreTypo = {},
    badgeStarsSize = "",
    badgeLogoSize = "",
    starSize = "",
    modalOverlayColor = "",
    modalOverlayBlur = "",
    modalBg = "",
    modalWidth = "",
    modalPadding = {},
    modalRadius = "",
    modalCloseSize = "",
    modalCloseColor = "",
    modalAvatarSize = "",
    modalNameSize = "",
    modalDegSize = "",
    modalStarsSize = "",
    cardWash = true,
    cardWashStrength = 100,
    cardWashColor = "",
    avatarRing = true,
    avatarRingWidth = 3,
    avatarRingColor = "",
    faqRadius = "",
    faqRowGap = "",
    faqBg = "",
    faqShadow = {},
    faqQuestionPadding = {},
    faqAnswerPadding = {},
    cardHoverBg = "",
    cardHoverBorderColor = "",
    cardHoverShadow = {},
    cardHoverLift = "",
    badgeTitleTypo = {},
    badgeSubtitleTypo = {},
    badgeIconSize = "",
    badgeIconGap = "",
    badgePadding = {},
    badgeRadius = "",
    badgeShadow = {},
    modalReviewSize = ""
  } = attributes || {};

  // Declared here rather than beside the first thing that used it.
  //
  // It is a `const`, so every use above its old declaration sat in the temporal
  // dead zone -- and `isSet` is the natural helper to reach for when adding a
  // rule, which made that an easy trap to fall into. One that took the whole
  // editor down rather than the block being worked on: Style renders inside every
  // block, so a throw here put all of them behind "This block has encountered an
  // error and cannot be previewed". It depends on nothing, so the top is where it
  // belongs.
  //
  // 0, and an empty box side, are real values -- only absent ones are skipped.
  const isSet = value => undefined !== value && null !== value && "" !== value;
  const cId = clientId || attributes?.cId || "";
  const mainEl = cId ? `#btbTestimonialsDir-${cId}` : ".bTestimonials";

  // Block Width has to outrank the theme's own layout rule.
  //
  // A block-theme emits `.is-layout-constrained > .alignwide { max-width: ... }`,
  // and every block here is aligned wide by default -- two classes beat the one
  // in the `.bTestimonials` fallback, so on a block with no `cId` the width was
  // set and then overruled, measured in the browser rather than guessed. The ID
  // selector already wins where a `cId` exists; doubling the class evens the
  // score for the fallback, and being emitted from the body wins the tie.
  const widthEl = cId ? mainEl : ".bTestimonials.bTestimonials";

  // Slider arrow navigation.
  //
  // Colour and side offset go through Swiper's own custom properties. Size does
  // too, but the variable alone is not enough: Swiper makes the arrow a
  // `size/44 * 27` by `size` rectangle with a glyph as tall as the whole box, so
  // raising it grows a tall sliver rather than a bigger button, and any
  // background painted on it reads as a pill. So whenever the arrow is styled at
  // all, the box is squared off here and the glyph scaled to fit inside it.
  //
  // The vertical centring still comes from Swiper's own
  // `margin-top: calc(0px - size/2)`, which is why the height stays tied to the
  // variable instead of being written as a plain pixel value.
  const {
    navSize,
    navOffset,
    navColor,
    navHoverColor,
    navBg,
    navHoverBg,
    navBorder,
    navShadow,
    sideOpacity
  } = slider && "object" === typeof slider ? slider : {};
  const arrowEl = `${mainEl} .swiper-button-prev, ${mainEl} .swiper-button-next`;
  const arrowAfterEl = `${mainEl} .swiper-button-prev::after, ${mainEl} .swiper-button-next::after`;
  const navBox = "var(--swiper-navigation-size, 44px)";

  // Any one of these means the user has taken over the arrow's appearance.
  const isArrowStyled = !!(navSize || navBg || navBorder || navShadow);
  const navVars = [navColor ? `--swiper-navigation-color: ${navColor};` : "", navSize ? `--swiper-navigation-size: ${navSize}px;` : "",
  // A 0 offset is a real choice -- it pins the arrows to the edge.
  undefined === navOffset || null === navOffset || "" === navOffset ? "" : `--swiper-navigation-sides-offset: ${navOffset}px;`].filter(Boolean).join("\n\t\t\t");
  const arrowBox = isArrowStyled ? [`width: ${navBox};`, `height: ${navBox};`,
  // Centres the glyph in the squared box; Swiper leaves it top-left.
  "display: flex;", "align-items: center;", "justify-content: center;", "box-sizing: border-box;", navBg ? `background: ${navBg};` : "", navBorder ? (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getBorderCSS)(navBorder) : "",
  // The stylesheet gives the arrow a raised drop shadow by default; this is
  // how an author flattens it or deepens it. Emitted only when set, so an
  // untouched block keeps the shipped shadow.
  // getShadowCSS returns the value only, not the property -- unlike
  // getBorderCSS above, which returns whole declarations.
  navShadow ? `box-shadow: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getShadowCSS)(navShadow)};` : ""].filter(Boolean).join("\n\t\t\t") : "";

  // Swiper sizes the glyph at 100% of the box, which overflows a square button.
  const arrowGlyph = isArrowStyled ? `font-size: calc(${navBox} * 0.45);\n\t\t\tline-height: 1;` : "";
  const arrowHover = [navHoverColor ? `color: ${navHoverColor};` : "", navHoverBg ? `background: ${navHoverBg};` : "",
  // The default arrow darkens on hover with an inset wash, so that it works
  // over the accent, a theme's own accent, or an Arrow Background, none of
  // which CSS can darken through a custom property. An explicit Arrow Hover
  // Background is a chosen colour rather than a starting point, so the wash is
  // dropped here and only the lift shadow is kept -- otherwise the colour
  // picked in the sidebar would render 14% darker than it was picked.
  navHoverBg ? "box-shadow: 0 8px 18px rgba(15, 23, 42, 0.26);" : ""].filter(Boolean).join("\n\t\t\t");

  // How far back the cards either side of the active one fade.
  //
  // The flat slider dims its neighbours to 0.75 in the stylesheet; coverflow and
  // the 3D slider deliberately do not, because washing out the side cards is
  // most of what made them read as grey slivers. That left the two 3D
  // arrangements with no way to dim at all, which is the opposite problem on a
  // pale card over a pale page -- there the rotation alone does not separate the
  // active card from its neighbours.
  //
  // Restricted to `.swiper-slide-visible` on purpose. The cull rule that hides
  // the far slides is `opacity: 0` at class specificity, and this rule is
  // ID-scoped, so without that qualifier it would outrank the cull and bring the
  // whole track back into view. Swiper maintains the class on every update under
  // `watchSlidesProgress`, which the 3D arrangements set.
  //
  // 100 is the shipped look, so it emits nothing: no rule at all is one fewer
  // thing to outrank, and it keeps a block saved before this control rendering
  // exactly as it did.
  const arrangement = (0,_utils_layoutFeatures__WEBPACK_IMPORTED_MODULE_6__.resolveArrangement)(attributes);
  const is3DArrangement = ["slider-3d", "coverflow"].includes(arrangement);

  // Written out rather than through the `isSet` helper below, which is declared
  // further down the body and so is not in scope yet.
  const hasSideOpacity = undefined !== sideOpacity && null !== sideOpacity && "" !== sideOpacity;
  const sideOpacityCSS = is3DArrangement && hasSideOpacity && 100 !== Number(sideOpacity) ? `${mainEl} .swiper-slide.swiper-slide-visible:not(.swiper-slide-active) {
			opacity: ${Number(sideOpacity) / 100};
		}` : "";

  // Block width and card height, per device.
  //
  // `max-width` rather than `width`, so a value wider than its container still
  // fits on a narrow screen, and `min-height` rather than `height`, so a card
  // with more review text than the rest grows instead of clipping it. Both are
  // empty until set, which is what keeps an untouched block deferring to the
  // theme's layout.
  // getBoxCSS rather than getBoxValue: it orders the sides, fills a side left
  // blank with 0, and returns nothing at all when every side is empty. Passing
  // an untouched `{ top: '', right: '', ... }` through getBoxValue would emit a
  // bare `margin: ;` instead.
  //
  // `ownBoxForDevice` rather than the value itself: Padding, Card Margin and
  // Block Margin are per device now, and the desktop entry is what the base rule
  // wants. A block saved before that shape existed reads as desktop-only, so it
  // renders unchanged. See utils/responsiveBox.js.
  const cardMarginCSS = (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getBoxCSS)((0,_utils_responsiveBox__WEBPACK_IMPORTED_MODULE_5__.ownBoxForDevice)(cardMargin, "desktop"));

  // Every per-card element across the layouts.
  //
  // The background/padding/border/shadow rule further down only names `.single`
  // and a few widgets, so on the twelve layouts that build their card out of
  // something else -- timeline, hero, audio, logos, videos, badges, stats, FAQ
  // and friends -- a margin set here reached nothing at all. Margin gets its own
  // rule rather than widening that one, because widening it would retroactively
  // drop the default background, padding, border and shadow onto layouts that
  // have never carried them.
  const cardSelectors = [".layoutSection .single", ".btb-audio-card", ".btb-timeline-card", ".btb-hero-card", ".btb-popup-modal-card-trigger", ".btb-bubble-item", ".btb-faq-item", ".btb-star-rating-bars", ".btb-avatar-list-wrapper", ".btb-badge-card", ".btb-stat-card", ".btb-toast-card",
  // Layout.js and the bespoke editors disagree on the prefix for these four,
  // so both spellings are listed.
  ".btb-logo-item", ".logo-item", ".btb-trust-item", ".badge-item", ".video-item", ".stat-item",
  // The seven blocks that build a card out of none of the above. Each was
  // checked in the browser rather than read off the source: with the list as
  // it stood, setting Margin on these changed the computed style of nothing
  // inside the block.
  //
  // For the single-widget blocks the whole widget is the card, so the margin
  // goes on its outer box; for case-study it is the repeated tile inside the
  // grid, and for card-stack the stacked card itself -- every card in the
  // stack shifts by the same amount, so the stack stays square.
  ".ba-wrap", ".btb-case-study", ".btb-comparison-table", ".btb-rating-summary",
  // `.btb-form-wrapper` was the form's old front-end-only markup; the form is
  // one component now and renders `.btb-tform` in both places. The old name
  // stays listed so a page served from cache before the change still moves.
  ".btb-form-wrapper", ".btb-tform", ".btb-stacked-card", ".btb-poll-wrapper"];
  const cardMarginEl = cardSelectors.map(selector => `${mainEl} ${selector}`).join(",\n\t\t");

  // Card Height needs the card itself, which is not always what Margin wants.
  //
  // Several layouts wrap their card in a positioning div and put the visible
  // card inside it -- the hero spotlight is the clearest: `.btb-hero-card` only
  // carries a bottom margin, and the box a reader sees is the `.single` within
  // it. A margin on the wrapper still moves the card, so the list above is right
  // for margin; a min-height on the wrapper just grows an invisible box while
  // the card keeps its own height, which is why Card Height appeared to do
  // nothing there. Hero's secondary cards sit in `.btb-hero-grid`, which is not
  // a `.layoutSection` either, so they matched no height selector at all.
  //
  // Naming `.single` on its own reaches the card in every one of those layouts;
  // the widget classes stay because those blocks have no `.single`.
  const cardHeightEl = cardSelectors.map(selector => ".layoutSection .single" === selector ? ".single" : selector).map(selector => `${mainEl} ${selector}`).join(",\n\t\t");

  // Which element the Card panel's background, padding, border and shadow land
  // on.
  //
  // For nearly every layout that is `.single`, the themed card itself. Two build
  // a painted box *around* the themed card and put the card inside it -- the
  // audio player's `.btb-audio-card` and the swipeable `.btb-stacked-card`, both
  // of which carry their own surface, border, radius, padding and shadow in the
  // stylesheet. On those two the panel was painting the inner box while the card
  // a reader sees ignored every control: measured on the front end, setting the
  // Card background to red left `.btb-audio-card` computing `rgb(255,255,255)`
  // and reddened only the quote inside it.
  //
  // So on those two the outer box is the target, and their block.json defaults
  // now carry the values the stylesheet used to hardcode, which is what keeps an
  // untouched block rendering as it always did. Every other layout is unchanged.
  // `shadow: false` on the stack is deliberate. Its depth is the layered
  // shadows -- `.is-top` carries a stronger one than the cards behind it, which
  // is what makes it read as a stack rather than a stack of flat rectangles --
  // and an ID-scoped `box-shadow` here outranks the `.is-top` rule and flattens
  // all of them to one value. The audio card's shadow is a plain drop shadow
  // with no such state, so it comes across as a default instead.
  const CARD_BOX_OVERRIDE = {
    "audio-testimonials": {
      selector: ".btb-audio-card",
      shadow: true
    },
    "testimonials-card-stack": {
      selector: ".btb-stacked-card",
      shadow: false
    }
  };
  const cardBox = CARD_BOX_OVERRIDE[layout];
  const cardBoxEl = `${mainEl} ${cardBox?.selector || ".single"}`;
  const cardBoxShadowCSS = cardBox && !cardBox.shadow ? "" : `box-shadow: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getShadowCSS)(shadow)};`;

  // The widgets that build their card out of neither `.single` nor a wrapper
  // around one. The Card panel's surface rule has always named them; the corner
  // wash and the hover state did not, so both were dead on every one of them --
  // toast, the seven review badges, the star bars, the avatar panel, the case
  // study tile and the comparison table. The wash in particular is a
  // background-image layered over that rule's background-color, so any element
  // in one list and not the other gets a control that moves half its box.
  const CARD_WIDGETS = [".btb-star-rating-bars", ".btb-badge-card", ".btb-stat-card", ".btb-toast-card", ".btb-avatar-detail", ".btb-trust-badges-grid", ".btb-case-study", ".btb-comparison-table"];
  const scopeAll = (selectors, suffix = "") => selectors.map(sel => `${mainEl} ${sel}${suffix}`).join(",\n\t\t");

  // Surface, padding, border and shadow, and the hover state of the same box.
  const cardPaintEl = scopeAll([cardBox?.selector || ".single", ...CARD_WIDGETS]);
  const cardPaintHoverEl = scopeAll([cardBox?.selector || ".single", ...CARD_WIDGETS], ":hover");

  // The card's hover state.
  //
  // frontend.scss already lifts the card and deepens its wash on hover, and its
  // own comment records why it stops there: "the Card panel owns both
  // `box-shadow` and `border-color` at ID specificity and a hover rule here
  // would lose to them". True of the stylesheet, not of this file -- emitted
  // here the hover rule carries the same ID and, coming later, wins. So the
  // three the stylesheet had to leave alone get controls.
  //
  // Every part is conditional and the whole rule is dropped when nothing is set,
  // so a card nobody has touched keeps the lift-and-wash it has today.
  //
  // `background-color` rather than the `background` shorthand, for the reason the
  // resting rule gives: the shorthand also resets `background-image`, which is
  // the wash -- so a hover colour would flatten the very gradient the hover state
  // is built on.
  //
  // A lift of 0 is a real choice: it holds the card still, which is the only way
  // to turn the movement off, so only an absent value is skipped.
  const cardHoverShadowCSS = (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getShadowCSS)(cardHoverShadow);
  const cardHoverCSS = [cardHoverBg ? `background-color: ${cardHoverBg};` : "", cardHoverBorderColor ? `border-color: ${cardHoverBorderColor};` : "", cardHoverShadowCSS ? `box-shadow: ${cardHoverShadowCSS};` : "", isSet(cardHoverLift) ? `transform: translateY(-${cardHoverLift}px);` : ""].filter(Boolean).join(" ");

  // The Top panel (Style tab) paints the card's header strip -- avatar, name and
  // designation -- and is shown for Theme 2 or the masonry arrangement.
  //
  // The masonry half used to target a bare `.masonry`, which Layout.js has never
  // emitted: the arrangement adds `masonry-layout` and, when the block's own
  // layout names it too, `btb-masonry-layout`. So the panel opened, the controls
  // moved and the header stayed transparent -- measured, not inferred. Both
  // spellings are listed because the two classes arrive by different routes.
  //
  // A companion rule tinting `.masonry .single .top::after` went with it. There
  // is no notch on a masonry card -- the only `.top::after` in the stylesheet
  // belongs to theme_6, and it is drawn with `border-bottom`, not the
  // `border-right` that rule set -- so it could never have coloured anything.

  // The short rule under the designation on Theme 1.
  //
  // These were written as `border-width` / `border-color` because the stylesheet
  // drew the divider as a hairline: a 1px #cccccc border, 30px wide. It is not
  // drawn that way any more -- frontend.scss paints a short accent bar instead,
  // `width: 34px; height: 3px; border: 0; background: var(--btb-accent)`, in the
  // `.theme_1 .single .footer .deg::after` rule. Against that markup
  // `border-color` coloured an edge no `border-style` ever showed and
  // `border-width` widened the same absent edge, so Color and Thickness moved
  // nothing on any Theme 1 layout -- the 3D Flip Perspective Carousel included.
  // Length kept working, because `width` is still `width`.
  //
  // Each property now names what the bar actually uses: its fill, its height and
  // its width. Colour is only emitted once one is picked, so an untouched divider
  // still follows the Accent role the stylesheet defers to.
  //
  // 0 is a real choice for both thickness and length -- either one removes the
  // rule -- so only an absent value is skipped.

  /*
   * An untouched `degDivider` arrives as `[]`, not `{}`.
   *
   * block.json declares `"default": {}`, but that default round-trips through
   * PHP on its way to the client: `json_decode` turns an empty JSON object into
   * an empty PHP array, and `wp_json_encode` sends an empty PHP array back out as
   * `[]`. Measured on a fresh block -- `Array.isArray( attributes.degDivider )`
   * is true.
   *
   * An array has a real `length`, and it is 0. So `isSet( degDivider.length )`
   * was true on every untouched block and emitted `width: 0px`, which collapsed
   * the bar the stylesheet draws at 34px -- the divider was invisible from the
   * start, on every Theme 1 layout. Setting Color or Thickness then changed the
   * colour and height of something with no width, which is why the panel looked
   * like it did nothing at all.
   *
   * `color` and `width` never collided with an array property, so only `length`
   * was affected; normalising the whole value keeps the next reader out of the
   * same trap.
   */
  const divider = Array.isArray(degDivider) ? {} : degDivider || {};
  const degDividerCSS = [isSet(divider.width) ? `height: ${divider.width}px;` : "", divider.color ? `background: ${divider.color};` : "", isSet(divider.length) ? `width: ${divider.length}px;` : ""].filter(Boolean).join("\n\t\t\t");

  // Space around the whole block, which is a different thing from Card Margin.
  //
  // Card Margin moves the card *inside* the block -- each testimonial in a grid,
  // the slider inside the before/after block -- so it can never separate one
  // block from the next. Nothing targeted the block's own box, which is why
  // there was no way to space the child blocks of a B Testimonials parent apart.
  //
  // Only the sides actually filled in are emitted: the Block Width rule centres
  // the block with `margin-left/right: auto`, and writing a blanket `margin`
  // here -- 0 on the sides left empty, as getBoxCSS would -- would silently undo
  // that centring for anyone who only wanted room above and below.
  // The side margins need `!important`, which nothing else here does.
  //
  // A block theme's constrained layout emits
  // `margin-left: auto !important; margin-right: auto !important` for its
  // children, and no selector -- ID or otherwise -- outranks an `!important`
  // declaration. Measured on this site: a block asking for 43px on all four
  // sides rendered 43px top and bottom and 281.5px either side, which is the
  // theme's auto centring, not the value asked for.
  //
  // Top and bottom face no such rule, so they stay plain. Only a side the author
  // actually filled in is emitted, so the centring survives untouched for
  // everyone who only wanted room above and below.
  const blockMarginSides = box => ["top", "right", "bottom", "left"].map(side => {
    if (!isSet(box?.[side])) {
      return "";
    }
    const beatsTheme = "left" === side || "right" === side;
    return `margin-${side}: ${box[side]}${beatsTheme ? " !important" : ""};`;
  }).filter(Boolean).join("\n\t\t\t");
  const blockMarginCSS = blockMarginSides((0,_utils_responsiveBox__WEBPACK_IMPORTED_MODULE_5__.ownBoxForDevice)(blockMargin, "desktop"));

  // Where a shrink-to-fit widget sits in its column.
  //
  // Only the badges and the toast are offered this -- their card is narrower
  // than the column and so had nowhere to go but the left edge, while every
  // other layout fills its column and would be unmoved by it. The panel gates
  // it; see SHRINK_TO_FIT_LAYOUTS.
  //
  // A flex container on the block's own box, rather than `text-align` or auto
  // margins. `text-align: center` would centre the badge's own lines of text
  // along with it, and auto margins do nothing to `.btb-badge-card`, which is
  // `display: inline-flex`. Making the box a flex row centres whatever width
  // the widget turns out to have, which is the point: the right value differs
  // per badge and none of them should have to be measured.
  //
  // Nothing is emitted while the value is empty, so a block nobody has aligned
  // keeps the block layout it has today.
  // Gated to the shrink-to-fit layouts, which is where it has always been able
  // to apply -- the Alignment control was offered on those alone until Block
  // Width started offering it too, so this rule had never met another layout.
  //
  // It must not: making the block box a flex row leaves the grid inside it
  // sized to its content instead of to the box. Measured on Video Testimonials
  // at Block Width 600px -- the video went from 600px wide to 93px the moment
  // Alignment was set to Center or Right, while Left and Default (which emit no
  // justify-content) stayed at 600px. The margin rule below is what moves a
  // block that fills its own box, and it needs no flex container.
  const BLOCK_ALIGN_JUSTIFY = {
    center: "center",
    right: "flex-end"
  };
  const blockAlignCSS = _utils_layoutControls__WEBPACK_IMPORTED_MODULE_7__.SHRINK_TO_FIT_LAYOUTS.includes(layout) && BLOCK_ALIGN_JUSTIFY[blockAlign] ? `${widthEl} {\n\t\t\tdisplay: flex;\n\t\t\tjustify-content: ${BLOCK_ALIGN_JUSTIFY[blockAlign]};\n\t\t}` : "";

  // The other half of Alignment: which side of the column a *narrowed* block
  // sits on.
  //
  // justify-content above moves a widget inside the block box, which is all a
  // badge needs -- its box is the full column and the badge is narrower than
  // it. A block given a Block Width is the opposite case: the box itself is the
  // narrow thing, and the width rule centres it with `margin-left: auto;
  // margin-right: auto` because centring was the only thing it could do. Those
  // auto margins are what a left or right alignment has to overrule, and
  // justify-content cannot -- there is nothing to move inside a box the grid
  // already fills.
  //
  // Emitted after the per-device width rules so a tablet or mobile one cannot
  // win on source order at equal specificity, and before the base Block Margin
  // rule so an explicit side still beats an alignment.
  //
  // Nothing is written for an empty value, so a block nobody has aligned keeps
  // the centring it has today.
  //
  // !important, because source order stopped being enough to win this once a
  // "wide"-aligned block is inside core's own constrained-layout CSS: every
  // block theme built on `theme.json` ships
  // `.is-layout-constrained > :where(:not(.alignleft):not(.alignright):not(.alignfull))
  // { margin-left: auto !important; margin-right: auto !important; }`, and
  // `.alignwide` is not in that exclusion list -- only left/right/full are.
  // Measured: Left and Right silently did nothing on a wide block on Twenty
  // Twenty-Five, while Center looked fine only because it happened to agree
  // with core's own forced centring. `:where()` zeroes the specificity of
  // core's own selector, so the ID selector these rules already render at
  // only has to match core's !important with its own to win outright.
  const BLOCK_ALIGN_MARGIN = {
    left: "margin-left: 0 !important;\n\t\t\tmargin-right: auto !important;",
    center: "margin-left: auto !important;\n\t\t\tmargin-right: auto !important;",
    right: "margin-left: auto !important;\n\t\t\tmargin-right: 0 !important;"
  };
  const blockAlignMarginCSS = BLOCK_ALIGN_MARGIN[blockAlign] ? `${widthEl} {\n\t\t\t${BLOCK_ALIGN_MARGIN[blockAlign]}\n\t\t}` : "";

  // Block Margin's per-device rules cannot ride along in sizeCSS the way Padding
  // and Card Margin do.
  //
  // The base Block Margin rule is emitted last on purpose, so an explicit side
  // beats the auto-centring the Block Width rules apply -- including the ones
  // inside the media queries. That also puts it after anything sizeCSS writes,
  // at equal specificity, so a tablet margin written there simply lost: measured
  // at 900px with tablet set to 25px, the block still rendered the desktop 50px.
  // Emitting these after the base rule instead is what makes the override stick.
  const blockMarginBreakpoints = [[_bpl_tools_utils_data__WEBPACK_IMPORTED_MODULE_3__.tabBreakpoint, "tablet"], [_bpl_tools_utils_data__WEBPACK_IMPORTED_MODULE_3__.mobileBreakpoint, "mobile"]].map(([query, device]) => {
    const sides = blockMarginSides((0,_utils_responsiveBox__WEBPACK_IMPORTED_MODULE_5__.ownBoxForDevice)(blockMargin, device));
    return sides ? `${query} {\n\t\t\t${widthEl} {\n\t\t\t\t${sides}\n\t\t\t}\n\t\t}` : "";
  }).filter(Boolean).join("\n\t\t");

  // Let the Colors panel outrank the classic Style panels.
  //
  // The Colors panel writes CSS custom properties that the stylesheets read as
  // `var(--btb-surface, #fff)` and friends. These rules are emitted at ID
  // specificity with values that always exist -- `background` defaults to
  // `#0000`, `nameColor` to `#000`, the border to whatever block.json says -- so
  // they beat the stylesheet every time and 62 of the Colors controls changed
  // nothing at all. Measured across all 31 layouts, not inferred: a badge card
  // asked for a surface colour still computed `rgba(0, 0, 0, 0)`.
  //
  // Wrapping the classic value as the var's fallback settles it in the right
  // order without touching anything else. A block whose author has not opened
  // the Colors panel has no custom property declared, so the fallback applies
  // and it renders exactly as it did before; once a role is set, it wins.
  const withRole = (cssVar, value) => `var(${cssVar}, ${value})`;

  // The layouts whose stylesheet spends one border side on a brand stripe.
  //
  // The badges draw `border-left: 4px solid var(--btb-accent)` and the quote box
  // `border-left: 5px solid var(--btb-accent)`, on the same element the Card
  // panel's border lands on. The Card border is emitted per side at ID
  // specificity from a block.json default that is never empty, so it claimed the
  // left side on every one of them and the Accent control painted nothing --
  // measured on the front end, a Capterra badge with Accent set computed
  // `border-left: 9px solid rgb(7, 8, 9)`, the Border Color, while `--btb-accent`
  // sat declared and unused.
  //
  // Leaving that side to the stylesheet gives both controls something to do: the
  // Card border still draws the other three sides, and the stripe follows
  // Accent. It also matches what the side is for -- the brand bar is what makes
  // a Google badge look like a Google badge, so the Card panel taking it over
  // was never the intent.
  const ACCENT_STRIPE_SIDE = {
    "google-review-badge": "left",
    "capterra-review-badge": "left",
    "facebook-review-badge": "left",
    "trustpilot-review-badge": "left",
    "g2-review-badge": "left",
    "verified-buyer-badge": "left",
    "review-badge-widget": "left",
    "testimonials-quote-box": "left"
  };

  // getBorderCSS builds the shorthand from a plain width and colour, so the
  // palette has to be woven in rather than wrapped around the result. Emitting
  // nothing when the width is absent is deliberate and matches getBorderCSS:
  // with no rule here the stylesheet's own `var()` border applies untouched,
  // which is why the layouts with no border default already worked.
  const paletteBorderCSS = value => {
    const {
      width = "",
      style = "solid",
      color = "",
      side = "all",
      radius = ""
    } = value || {};
    const wanted = s => {
      const bSide = side?.toLowerCase();
      if (ACCENT_STRIPE_SIDE[layout] === s) {
        return false;
      }
      return bSide?.includes("all") || bSide?.includes(s);
    };
    const sides = !width || 0 === parseInt(width, 10) ? "" : ["top", "right", "bottom", "left"].map(s => wanted(s) ? `border-${s}: ${withRole("--btb-border-width", width)} ${style} ${withRole("--btb-border", color)};` : "").join("");
    return [sides, radius ? `border-radius: ${radius};` : ""].filter(Boolean).join("\n\t\t\t");
  };

  // Every class each card part is rendered with, across all the layouts.
  //
  // The bespoke layouts name their parts themselves -- `.btb-cs-name` for a case
  // study's customer, `.btb-faq-question` for an accordion's headline -- so a
  // rule that only knew `.single .name` never reached them and the stylesheet's
  // own font size was the only value that ever applied. That is why those
  // controls read as dead: not specificity (these rules are ID-scoped and win
  // every time) but a selector that names nothing on the page.
  //
  // Grouped by role rather than by block, since the roles line up with what the
  // Colors panel already paints: name/title, designation/muted, review/body.
  const NAME_PARTS = [".single .name", ".btb-avatar-name", ".btb-cs-name", ".btb-toast-text", ".btb-faq-question", ".btb-ct-title", ".btb-srb-title",
  // The video card's caption. Its editor is bespoke, so nothing ever named
  // these two and the stylesheet's 17px/14px were the only values in play.
  ".video-item .name",
  // The seven review badges' heading -- "Google Reviews", "Trustpilot Score",
  // "100% Verified Reviews" and so on.
  //
  // `layoutControls.js` gave these blocks `cardBox` alone, so the Name,
  // Designation and Review Text panels were all hidden and none of their text
  // was named here either: the stylesheet's 17px/700 was the only value in
  // play. Their `nameTypo` defaults now carry that value, so an untouched
  // badge renders exactly as it did and the control owns it from there.
  ".btb-badge-title"];
  const DEG_PARTS = [".single .deg", ".btb-avatar-deg", ".btb-cs-deg", ".btb-toast-meta", ".btb-faq-author", ".btb-srb-count", ".video-item .deg",
  // The badges' secondary line, which is one of two things depending on the
  // block: the Verified Buyer seal renders a sentence of description, the
  // other six a review count beside the score. Both sit at the stylesheet's
  // 13px in the muted colour, which is the Designation role exactly, so they
  // share one control rather than growing two that would always be set alike.
  ".btb-badge-desc", ".btb-badge-rating .count"];
  const TEXT_PARTS = [".single .reviewText", ".btb-avatar-review", ".btb-cs-body p", ".btb-bubble-content p", ".btb-faq-answer", ".btb-ct-table td", ".btb-srb-label"];

  // A floating bubble's name is painted with the accent colour rather than the
  // title colour, so it takes the Name typography without joining the Name
  // colour rule -- adding it there would repaint it and change how the layout
  // looks for anyone who has never opened the panel.
  const NAME_TYPO_ONLY = [".btb-bubble-name"];

  // The avatar box. Same story: four layouts draw their own and pinned the size
  // in the stylesheet, so the Image width and height moved nothing. Both the
  // case-study wrapper and the image inside it are named -- the wrapper anchors
  // the editor's upload overlay, the image is what a reader sees.
  const IMAGE_PARTS = [".single .img", ".btb-avatar-thumb", ".btb-cs-avatar-wrap", ".btb-cs-avatar", ".btb-bubble-avatar", ".btb-toast-avatar"];
  const selectorList = parts => parts.map(part => `${mainEl} ${part}`).join(",\n\t\t");

  // The star-rating-bars parts keep their own colour rules further down: each
  // carries a different fallback from the shared one, and folding them in would
  // repaint the widget for anyone who has not set a colour. Typography they do
  // take from the shared groups.
  const COLOR_EXCLUDE = [".btb-srb-title", ".btb-srb-label", ".btb-srb-count"];
  const colorParts = parts => parts.filter(part => !COLOR_EXCLUDE.includes(part));

  // Avatar size, per device.
  //
  // `image.width` and `image.height` were single numbers, so one avatar size had
  // to serve every screen -- a 300px portrait that suits a desktop hero card
  // fills a phone. Each is now either a plain number, as every block saved so
  // far holds it, or a `{ desktop, tablet, mobile }` object.
  //
  // A device left empty emits nothing and simply inherits the wider one, which
  // is what keeps the tablet and mobile fields optional rather than something
  // that must be filled in before the block looks right.
  const imageFor = (value, device) => {
    if (value && "object" === typeof value) {
      return value[device];
    }
    return "desktop" === device ? value : undefined;
  };
  const imageCSS = device => {
    const width = imageFor(image?.width, device);
    const height = imageFor(image?.height, device);
    const fallback = "desktop" === device ? 50 : undefined;
    const declarations = [isSet(width) ? `width: ${width}px;` : isSet(fallback) ? `width: ${fallback}px;` : "", isSet(height) ? `height: ${height}px;` : isSet(fallback) ? `height: ${fallback}px;` : ""].filter(Boolean).join("\n\t\t\t");
    return declarations ? `${selectorList(IMAGE_PARTS)} {\n\t\t\t${declarations}\n\t\t}` : "";
  };
  const sizeCSS = device => {
    const width = blockWidth?.[device];
    const height = cardHeight?.[device];
    // The desktop boxes are already written by the base rules further down, so
    // only the two breakpoints add anything here.
    const isBase = "desktop" === device;
    const devicePadding = isBase ? "" : (0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_1__.getBoxValue)((0,_utils_responsiveBox__WEBPACK_IMPORTED_MODULE_5__.ownBoxForDevice)(padding, device) || {});
    const deviceCardMargin = isBase ? "" : (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getBoxCSS)((0,_utils_responsiveBox__WEBPACK_IMPORTED_MODULE_5__.ownBoxForDevice)(cardMargin, device));
    return [imageCSS(device),
    // Centred once narrowed, otherwise it would sit against the left edge.
    width ? `${widthEl} {\n\t\t\tmax-width: ${width};\n\t\t\tmargin-left: auto;\n\t\t\tmargin-right: auto;\n\t\t}` : "",
    // `.layoutSection .single` alone reached none of the layouts that build
    // their card out of something else, so Card Height silently did nothing on
    // timeline, hero, audio, logos, badges, stats and the rest.
    height ? `${cardHeightEl} {\n\t\t\tmin-height: ${height};\n\t\t}` : "",
    // Padding, Card Margin and Block Margin, for the devices that set one of
    // their own. `ownBoxForDevice` returns nothing when a device inherits, so
    // a media query is only written where there is something to override --
    // repeating the desktop value here would freeze it in place instead.
    //
    // Each targets the same element its desktop rule does, so a value set for
    // one device behaves exactly like the value set for another.
    devicePadding ? `${cardBoxEl} {\n\t\t\tpadding: ${devicePadding};\n\t\t}` : "", deviceCardMargin ? `${cardMarginEl} {\n\t\t\tmargin: ${deviceCardMargin};\n\t\t}` : ""
    // Block Margin is deliberately not here -- see blockMarginBreakpoints.
    ].filter(Boolean).join("\n\t\t");
  };
  const tabletSize = sizeCSS("tablet");
  const mobileSize = sizeCSS("mobile");

  // The Testimonial Form's labels and inputs.
  //
  // Neither had a control of any kind. form.scss pinned a label at
  // `font-weight: 600; font-size: 14px` with an inherited colour, and gave the
  // inputs a background and a border read from the palette and nothing else --
  // no text colour, no typography, no placeholder, no radius, no padding, no
  // focus state. So the one layout built almost entirely out of form fields had
  // the least say over how those fields look.
  //
  // Scoped to `.bTestimonialForm` rather than hung off `mainEl` alone: that is
  // what the form component wraps itself in, and naming it keeps these rules
  // ahead of form.scss even on a block saved without a `cId`, where `mainEl`
  // falls back to a single class and would otherwise lose to the stylesheet's
  // own two-class selectors.
  //
  // Every declaration below is conditional, so a form nobody has styled emits an
  // empty string here and renders exactly as it always has.
  const formEl = `${mainEl} .bTestimonialForm`;
  const labelEl = `${formEl} .btb-tform-field label`;
  const inputParts = ["input", "textarea", "select"];
  const inputEl = inputParts.map(part => `${formEl} .btb-tform-field ${part}`).join(",\n\t\t");
  // A rule of its own rather than joining the one above: a pseudo-element in a
  // selector list would drop the whole list on a browser that cannot parse it.
  const placeholderEl = inputParts.map(part => `${formEl} .btb-tform-field ${part}::placeholder`).join(",\n\t\t");
  const focusEl = inputParts.map(part => `${formEl} .btb-tform-field ${part}:focus`).join(",\n\t\t");
  const inputPaddingCSS = (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getBoxCSS)(inputPadding);
  const inputBoxCSS = [inputColor ? `color: ${inputColor};` : "", inputRadius ? `border-radius: ${inputRadius};` : "", inputPaddingCSS ? `padding: ${inputPaddingCSS};` : ""].filter(Boolean).join("\n\t\t\t");

  // getTypoCSS always returns a ruleset, empty braces and two empty media
  // queries included, so an untouched typography object would add dead rules to
  // all 35 blocks rather than only to a form somebody has styled.
  const typoCSS = (selector, typo) => Object.keys(typo || {}).length ? (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getTypoCSS)(selector, typo)?.styles || "" : "";
  const formCSS = [labelColor ? `${labelEl} {\n\t\t\tcolor: ${labelColor};\n\t\t}` : "", typoCSS(labelEl, labelTypo), inputBoxCSS ? `${inputEl} {\n\t\t\t${inputBoxCSS}\n\t\t}` : "", typoCSS(inputEl, inputTypo), placeholderColor ? `${placeholderEl} {\n\t\t\tcolor: ${placeholderColor};\n\t\t}` : "",
  // `outline-color` alongside the border, so the browser's own focus ring
  // follows the chosen colour instead of contradicting it.
  inputFocusColor ? `${focusEl} {\n\t\t\tborder-color: ${inputFocusColor};\n\t\t\toutline-color: ${inputFocusColor};\n\t\t}` : "",
  // min-height, so a longer review grows the box rather than scrolling inside
  // a fixed one.
  textareaHeight ? `${formEl} .btb-tform-field textarea {\n\t\t\tmin-height: ${textareaHeight};\n\t\t}` : "",
  // The submit button.
  //
  // It used to carry exactly one control -- its label -- with the colour,
  // padding, radius, weight and size all pinned in form.scss, so the one
  // element on the form a visitor is meant to click was the only one that
  // could not be styled at all.
  //
  // `background-color` rather than the `background` shorthand, matching the
  // card rule: the shorthand resets a background image, and that is exactly
  // what silently killed the masonry header wash earlier.
  //
  // An empty Background falls through to the accent, so a form whose Accent is
  // set gets a matching button without anyone picking the same colour twice.
  `${formEl} .btb-tform-submit {
			background-color: ${btnBg || "var(--btb-accent, #146ef5)"};
			color: ${btnColor || "#ffffff"};
			padding: ${(0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_1__.getBoxValue)(btnPadding)};
			border-radius: ${btnRadius || "6px"};
			justify-self: ${"full" === btnWidth ? "stretch" : btnAlign || "start"};
			width: ${"full" === btnWidth ? "100%" : "auto"};
			transition: background-color 0.2s ease, color 0.2s ease;
		}`, (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getTypoCSS)(`${formEl} .btb-tform-submit`, btnTypo)?.styles || "",
  // Only emitted when asked for. With no hover colours chosen the button keeps
  // its base ones rather than being handed an invented darker shade, which is
  // the same rule the slider arrows follow.
  btnHoverBg || btnHoverColor ? `${formEl} .btb-tform-submit:hover {
			${btnHoverBg ?
  // `!important` is unavoidable here, and only here. The button's base
  // background is an inline style written by TestimonialForm.js so that
  // Accent applies in the editor preview too, and an inline style beats
  // every stylesheet rule regardless of specificity -- measured, the
  // hover colour computed as the base green with the rule in place.
  // Matching specificity cannot win against inline; this can.
  `background-color: ${btnHoverBg} !important;` : ""}
			${btnHoverColor ? `color: ${btnHoverColor};` : ""}
		}` : ""].filter(Boolean).join("\n\t\t");

  // The Feedback & NPS Poll's text, scale buttons and box.
  //
  // This is the layout layoutControls.js gives an empty entry -- no shared panel
  // reaches it -- so every size it renders was pinned in frontend.scss: an
  // 18px/700 question, a 13px description, 12px scale labels, a 38x38 button at
  // radius 8, a 32px box at radius 16. The button's two text colours were worse
  // than fixed sizes: idle it inherited, and once picked it was a literal `#fff`
  // over the accent, so an author who set a pale accent got white on near-white
  // with nothing to fix it.
  //
  // The colours a `--btb-*` role already paints stay with the Colors panel, so
  // only what no role covers is emitted here.
  const pollEl = `${mainEl} .btb-poll-wrapper`;
  const pollBtnEl = `${pollEl} .btb-poll-num-btn`;
  // Hover and selected are one state as far as the stylesheet is concerned --
  // both paint the accent behind a white number -- so they take one control.
  const pollBtnActiveEl = `${pollBtnEl}:hover,\n\t\t${pollBtnEl}.is-selected`;
  const pollPaddingCSS = (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getBoxCSS)(pollPadding);
  const pollBoxCSS = [pollPaddingCSS ? `padding: ${pollPaddingCSS};` : "", pollRadius ? `border-radius: ${pollRadius};` : "",
  // The box is a flat bordered panel in the stylesheet with no shadow of its
  // own, so this adds one rather than overriding one, and an untouched poll is
  // unchanged. getShadowCSS returns the value only, not the whole declaration.
  (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getShadowCSS)(pollShadow) ? `box-shadow: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getShadowCSS)(pollShadow)};` : ""].filter(Boolean).join("\n\t\t\t");

  // Square: `width` and `height` from one value, so the button cannot end up a
  // rectangle with a radius meant for a square.
  const pollBtnBoxCSS = [isSet(pollBtnSize) ? `width: ${pollBtnSize}px;` : "", isSet(pollBtnSize) ? `height: ${pollBtnSize}px;` : "", pollBtnRadius ? `border-radius: ${pollBtnRadius};` : "", pollBtnColor ? `color: ${pollBtnColor};` : ""].filter(Boolean).join("\n\t\t\t");
  const pollCSS = [pollBoxCSS ? `${pollEl} {\n\t\t\t${pollBoxCSS}\n\t\t}` : "", typoCSS(`${pollEl} .btb-poll-title`, pollTitleTypo), typoCSS(`${pollEl} .btb-poll-desc`, pollDescTypo), typoCSS(`${pollEl} .btb-poll-label-low,\n\t\t${pollEl} .btb-poll-label-high`, pollLabelTypo),
  // 0 is a real gap -- it butts the buttons together into one strip.
  isSet(pollBtnGap) ? `${pollEl} .btb-poll-buttons {\n\t\t\tgap: ${pollBtnGap}px;\n\t\t}` : "", pollBtnBoxCSS ? `${pollBtnEl} {\n\t\t\t${pollBtnBoxCSS}\n\t\t}` : "",
  // Idle buttons only, and its own rule rather than joining pollBtnBoxCSS
  // above.
  //
  // The stylesheet gives the picked button a lift of its own --
  // `box-shadow: 0 4px 12px rgba(#146ef5, 0.3)` on `.is-selected`, alongside
  // the accent fill and the 1.1 scale -- and that rule is class specificity,
  // so a shadow emitted flat on `.btb-poll-num-btn` from an ID selector would
  // outrank it and quietly delete the one thing marking which number the
  // reader picked. Excluding the selected button leaves that lift standing, so
  // the control styles the buttons it is actually about.
  (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getShadowCSS)(pollBtnShadow) ? `${pollBtnEl}:not(.is-selected) {\n\t\t\tbox-shadow: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getShadowCSS)(pollBtnShadow)};\n\t\t}` : "", typoCSS(pollBtnEl, pollBtnTypo),
  // After the idle rule above, so the picked state wins the colour.
  pollBtnActiveColor ? `${pollBtnActiveEl} {\n\t\t\tcolor: ${pollBtnActiveColor};\n\t\t}` : ""].filter(Boolean).join("\n\t\t");

  // The Gradient Border Grid's ring and score pill.
  //
  // `gradientBorder` defaults to false here and is declared only by that block,
  // so every other layout emits nothing at all -- including the coverflow
  // carousel, which renders the same theme_2 card and must keep its flat border.
  //
  // The ring is a masked pseudo-element. `inset` pulls it out past the card by
  // its own width so it reads as a border rather than eating into the padding,
  // and the two masks cancel over the content box, leaving only the ring itself
  // painted. `border-radius: inherit` keeps it following whatever radius the
  // Card panel sets.
  //
  // The @supports guard matters: without mask compositing the same rule paints a
  // solid gradient slab over the whole card, so a browser that cannot do this
  // gets the card's ordinary border instead of a broken one.
  const gradientRingCSS = gradientBorder ? `@supports ((-webkit-mask-composite: xor) or (mask-composite: exclude)) {
			${mainEl} .single::before {
				content: "";
				position: absolute;
				inset: -${gradientWidth}px;
				padding: ${gradientWidth}px;
				border-radius: inherit;
				background: linear-gradient(${gradientAngle}deg, ${gradientFrom}, ${gradientTo});
				-webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
				-webkit-mask-composite: xor;
				mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
				mask-composite: exclude;
				pointer-events: none;
			}
		}` : "";
  const starBadgeVars = [starBadgeBg ? `--btb-star-badge-bg: ${starBadgeBg};` : "", starBadgeColor ? `--btb-star-badge-color: ${starBadgeColor};` : "",
  // The pill's star tracks the card's own star row rather than growing a third
  // control for one glyph. Only emitted where the badge exists, so no other
  // block picks up the variable.
  showStarBadge && starIconColor ? `--btb-star-badge-icon: ${starIconColor};` : ""].filter(Boolean).join("\n\t\t\t");

  // Which corner the score pill sits in, and how big it is.
  //
  // The stylesheet pins it `top: 12px; right: 12px` at a 12px font on a fully
  // rounded pill, and there was nothing in the sidebar to move it -- so on a
  // theme_2 card whose header already carries something in that corner the badge
  // landed on top of it with no way out.
  //
  // All four insets are written on every branch, `auto` included: the shipped
  // rule sets top and right, so a bottom-left badge that only added its own two
  // would keep the old pair as well and stretch the pill across the card.
  const BADGE_INSETS = {
    "top-right": ["12px", "12px", "auto", "auto"],
    "top-left": ["12px", "auto", "auto", "12px"],
    "bottom-right": ["auto", "12px", "12px", "auto"],
    "bottom-left": ["auto", "auto", "12px", "12px"]
  };
  const [badgeTop, badgeRight, badgeBottom, badgeLeft] = BADGE_INSETS[starBadgePosition] || BADGE_INSETS["top-right"];
  const starBadgeBox = showStarBadge ? [`top: ${badgeTop};`, `right: ${badgeRight};`, `bottom: ${badgeBottom};`, `left: ${badgeLeft};`, isSet(starBadgeSize) ? `font-size: ${starBadgeSize}px;` : "",
  // 999px is the shipped value and means "fully rounded"; 0 squares the
  // pill off, which is a real choice, so only an absent value is skipped.
  isSet(starBadgeRadius) ? `border-radius: ${starBadgeRadius}px;` : ""].filter(Boolean).join("\n\t\t\t") : "";

  // The star glyph ships one pixel under the score text (11px against 12px).
  // Scaling it with the pill rather than leaving it at 11px keeps that
  // relationship at any size, so a 20px badge does not end up with a tiny star.
  const starBadgeIconCSS = showStarBadge && isSet(starBadgeSize) ? `${mainEl} .single > .btb-star-badge .btb-star-badge-icon {
			font-size: ${Math.max(1, Number(starBadgeSize) - 1)}px;
		}` : "";
  const gradientCSS = [gradientRingCSS, starBadgeVars ? `${mainEl} .single {\n\t\t\t${starBadgeVars}\n\t\t}` : "", starBadgeBox ? `${mainEl} .single > .btb-star-badge {\n\t\t\t${starBadgeBox}\n\t\t}` : "", starBadgeIconCSS].filter(Boolean).join("\n\t\t");

  // The theme_2 header wash.
  //
  // The strip above the review is painted twice: the Top panel's Background
  // Color underneath, and a translucent gradient of the accent over it. Only the
  // colour had a control, so the wash itself could be neither softened nor taken
  // off -- an author who set a header colour got it tinted whatever they chose.
  //
  // `color-mix` with no fallback on purpose. A browser without it drops the
  // declaration and keeps the stylesheet's own wash, which is the right thing to
  // fall back to; the alternative is resolving the accent to a literal here,
  // where it may be a palette variable rather than a colour.
  // Gated on the block declaring `headerWash`, which is now every block that can
  // paint the strip -- that is, every one that declares `grid2Bg`.
  //
  // It used to be gated on `gradientBorder`, so the Gradient Border Grid was the
  // only layout whose wash could be softened or switched off. Everywhere else --
  // the coverflow carousel, the masonry arrangement, every block a reader can
  // switch to Theme 2 -- the wash was fixed, which is what made the strip look
  // like it had no colour control at all: the Background Color underneath moved,
  // and the accent tint over it did not.
  //
  // All three selectors the stylesheet's own wash uses, or turning it off would
  // leave the masonry arrangement still washed.
  const paintsHeaderStrip = undefined !== attributes?.headerWash;
  const washPct = base => Math.round(base * (Number(headerWashStrength) || 0)) / 100;
  const headerWashCSS = paintsHeaderStrip ? `${mainEl} .theme_2 .single .top,
		${mainEl} .masonry-layout .single .top,
		${mainEl} .btb-masonry-layout .single .top {
			background-image: ${headerWash ? `linear-gradient(
				180deg,
				color-mix(in srgb, var(--btb-accent, #0575e6) ${washPct(16)}%, transparent) 0%,
				color-mix(in srgb, var(--btb-accent, #0575e6) ${washPct(8)}%, transparent) 100%
			)` : "none"};
		}` : "";

  // The popup modal's box and text sizes.
  //
  // Written as custom properties on the overlay rather than as declarations on
  // each part, so one rule feeds the stylesheet's nine `var()` fallbacks and
  // anything left untouched keeps the value it shipped with. Emitted only where
  // something is actually set, which is what keeps every other block -- none of
  // which declares these -- from carrying a dead ruleset.
  //
  // Scoped by ID like everything else here: the overlay is rendered inside the
  // block wrapper (it is `position: fixed`, not reparented), so two popup blocks
  // on one page cannot style each other.
  const modalPaddingCSS = (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getBoxCSS)(modalPadding);
  const modalVars = [modalOverlayColor ? `--btb-modal-overlay: ${modalOverlayColor};` : "",
  // 0 is a real choice -- it turns the frosting off and leaves a flat wash.
  isSet(modalOverlayBlur) ? `--btb-modal-blur: ${modalOverlayBlur}px;` : "", modalBg ? `--btb-modal-bg: ${modalBg};` : "", isSet(modalWidth) ? `--btb-modal-width: ${modalWidth}px;` : "",
  // getBoxCSS orders the sides and fills a blank one with 0, and returns
  // nothing at all when every side is empty -- unlike getBoxValue, which would
  // join an untouched `{ top: '', ... }` into a bare set of spaces.
  modalPaddingCSS ? `--btb-modal-padding: ${modalPaddingCSS};` : "", isSet(modalRadius) ? `--btb-modal-radius: ${modalRadius}px;` : "", isSet(modalCloseSize) ? `--btb-modal-close-size: ${modalCloseSize}px;` : "", modalCloseColor ? `--btb-modal-close-color: ${modalCloseColor};` : "", isSet(modalAvatarSize) ? `--btb-modal-avatar: ${modalAvatarSize}px;` : "", isSet(modalNameSize) ? `--btb-modal-name-size: ${modalNameSize}px;` : "", isSet(modalDegSize) ? `--btb-modal-deg-size: ${modalDegSize}px;` : "", isSet(modalStarsSize) ? `--btb-modal-stars-size: ${modalStarsSize}px;` : "", isSet(modalReviewSize) ? `--btb-modal-review-size: ${modalReviewSize}px;` : ""].filter(Boolean).join("\n\t\t\t");
  const modalCSS = modalVars ? `${mainEl} .btb-modal-overlay {\n\t\t\t${modalVars}\n\t\t}` : "";

  // The rating star's size.
  //
  // `getStar` renders an inline SVG with `width="15px" height="15px"` on the
  // element, so the one dimension of a rating that an author is most likely to
  // want was fixed in the icon markup -- the Review Text panel offered a colour
  // and nothing else. Presentation attributes lose to CSS, so a rule here takes
  // it over without touching the icon.
  //
  // Left unset by default and emitting nothing at all: the layouts do not agree
  // on a star size to begin with (the compact card sizes its own at 14px), so a
  // default here would quietly resize several of them. Untouched blocks keep
  // whatever they render today.
  // The compact card's star colour.
  //
  // Every layout draws its stars through getStar, which writes the colour as
  // a fill ATTRIBUTE on the <svg>. The compact card is the only one whose
  // stylesheet also declares `fill` on `.rating svg` -- and a declaration
  // beats an attribute, so on that layout alone the Review Text panel's Rating
  // Icon Color moved nothing at all and only the palette role did. Measured:
  // the star computed #f59e0b with the control set to red.
  //
  // Restating it here at ID specificity, after that rule, hands the star back
  // -- the same fix the comparison table's rating cell needed further down,
  // and for the same reason. Scoped to the one layout that has the problem so
  // no other card's stars are repainted by a rule they never had.
  const compactStarCSS = "testimonials-compact" === layout ? `${mainEl} .btb-testimonials-compact-layout .rating svg {
			fill: ${withRole("--btb-star", starIconColor)};
		}` : "";
  const starSizeCSS = isSet(starSize) ? `${mainEl} .btbStar svg,
		${mainEl} .rating svg {
			width: ${starSize}px;
			height: ${starSize}px;
		}` : "";

  // The Speech Bubble card's tail.
  //
  // The tail is two CSS triangles under the card, and its colours could not come
  // from the stylesheet alone. It painted `var(--btb-surface, #ffffff)`, but the
  // card itself paints `var(--btb-surface, <Card panel Background>)` from the
  // rule further up -- so a card coloured through the Card panel rather than the
  // palette's Surface role got a white tail hanging off a coloured bubble. The
  // fill is resolved here instead, where both values are in scope.
  //
  // `#0000` is this block's shipped Background default, which means "not set"
  // rather than a transparent bubble, so it falls through to white the same way
  // the stylesheet always did.
  //
  // The outline follows the Card border. A border confined to sides that exclude
  // the bottom leaves the tail unoutlined rather than drawing an edge the card
  // above it does not have.
  const bubbleSide = (border?.side || "all").toLowerCase();
  const bubbleLineWidth = bubbleSide.includes("all") || bubbleSide.includes("bottom") ? border?.width || "0px" : "0px";

  // Where the tail sits along the bottom edge, as the fill triangle's `left`.
  //
  // Size and offset were fixed in the stylesheet at 12px and 30px, so the one
  // part that makes this layout a speech bubble rather than a plain card was the
  // one part with nothing in the sidebar to change. All three are attributes
  // now, and `--btb-bubble-x` is the single value the stylesheet positions both
  // triangles from -- see the note beside its declaration there for why the
  // outline takes the same value less one border width.
  //
  // The triangle is `2 * size` wide, which is why centre subtracts one size and
  // right subtracts two: `left` alone can express every position, so no rule
  // here has to reach for `right` and unset the stylesheet's `left`.
  const bubbleX = side => {
    if ("center" === side) {
      return `calc(50% - ${bubbleTailSize}px)`;
    }
    if ("right" === side) {
      return `calc(100% - ${bubbleTailOffset}px - ${bubbleTailSize * 2}px)`;
    }
    return `${bubbleTailOffset}px`;
  };

  // "Alternate" is the chat-transcript look: the tail hangs off the left of one
  // card and the right of the next, so a column of bubbles reads as a
  // back-and-forth rather than as one speaker. Every other value is a single
  // position for all of them.
  const isAlternating = "alternate" === bubbleTailAlign;
  const bubbleVars = [`--btb-bubble-size: ${bubbleTailSize}px;`, `--btb-bubble-offset: ${bubbleTailOffset}px;`, `--btb-bubble-x: ${bubbleX(isAlternating ? "left" : bubbleTailAlign)};`,
  // The fill still follows the Card panel's Background and the palette's
  // Surface role unless the Tail panel names a colour of its own, so a tail
  // left alone stays part of the bubble it hangs from.
  `--btb-bubble-fill: ${bubbleTailFill ? bubbleTailFill : withRole("--btb-surface", !isSet(background) || "#0000" === background ? "#ffffff" : background)};`, `--btb-bubble-line: ${bubbleTailLine ? bubbleTailLine : withRole("--btb-border", border?.color || "transparent")};`, `--btb-bubble-line-w: ${withRole("--btb-border-width", bubbleLineWidth)};`].join("\n\t\t\t");
  const bubbleEl = `${mainEl} .btb-testimonials-speech-bubble-layout .single`;
  const bubbleCSS = "testimonials-speech-bubble" === layout ? [`${bubbleEl} {\n\t\t\t${bubbleVars}\n\t\t}`, isAlternating ? `${bubbleEl}:nth-child(even) {\n\t\t\t--btb-bubble-x: ${bubbleX("right")};\n\t\t}` : "",
  // Both pseudo-elements, or the outline is left hanging on its own.
  // `content` rather than `display` so nothing else on the card shifts:
  // the tail is absolutely positioned and out of flow either way.
  //
  // A size of 0 counts as off as well. The fill triangle collapses to
  // nothing at 0 but the outline behind it is a border-width bigger, so
  // it would survive as a couple of stray pixels in the border colour.
  !bubbleTail || 0 === Number(bubbleTailSize) ? `${bubbleEl}::before,\n\t\t${bubbleEl}::after {\n\t\t\tcontent: none;\n\t\t}` : ""].filter(Boolean).join("\n\n\t\t") : "";

  // The card's corner wash.
  //
  // frontend.scss paints `.layoutSection .single` with
  // `radial-gradient(115% 90% at 0% 0%, var(--btb-wash) 0%, transparent 58%)` --
  // a tint of the Accent strongest at the top-left corner, deepening to
  // `--btb-wash-strong` on hover. Neither state had a control, so the corner
  // could be neither recoloured nor removed, and because it only deepens under
  // the pointer it reads as a hover effect stuck on.
  //
  // Written as `background-image` on the card rather than by redefining
  // `--btb-wash`: those two variables also feed the header strip's own gradient,
  // and moving them here would tie two separate controls back together.
  //
  // `.single` rather than `cardBoxEl`. The audio and card-stack overrides point
  // that at their outer box, which is not the element the stylesheet washes --
  // washing the wrapper would leave the themed card inside it carrying the
  // stylesheet's own wash as well, so the card would end up with two.
  //
  // The widgets are named alongside it. Without them the toggle opened on a
  // review badge, the toast, the star bars, the avatar panel, the case study and
  // the comparison table and moved nothing at all, which is what made the
  // control look broken on some cards and not others. Those blocks default the
  // toggle off, so none of them gains a tint it never had -- see their
  // block.json.
  const cardWashEl = scopeAll([".single", ...CARD_WIDGETS]);
  const cardWashHoverEl = scopeAll([".single", ...CARD_WIDGETS], ":hover");
  const cardWashPct = base => Math.round(base * (Number(cardWashStrength) || 0)) / 100;
  const cardWashTint = pct => `color-mix(in srgb, ${cardWashColor || "var(--btb-accent, #0575e6)"} ${pct}%, transparent)`;

  // Both states, or turning it off would leave the hover wash arriving on a card
  // that has none at rest.
  const cardWashCSS = undefined !== attributes?.cardWash ? `${cardWashEl} {
			background-image: ${cardWash ? `radial-gradient(115% 90% at 0% 0%, ${cardWashTint(cardWashPct(8))} 0%, transparent 58%)` : "none"};
		}

		${cardWashHoverEl} {
			background-image: ${cardWash ? `radial-gradient(115% 90% at 0% 0%, ${cardWashTint(cardWashPct(16))} 0%, transparent 62%)` : "none"};
		}` : "";

  // The avatar's tinted ring.
  //
  // frontend.scss draws it as `box-shadow: 0 0 0 3px var(--btb-ring)` on
  // `.single .authorImg .img`, and its own comment explains why it is a shadow
  // and not a border: "the Image Border control owns the border on this element,
  // and the ring has to sit outside whatever that is set to". Sound -- but it
  // left the ring itself with no control of any kind. Not its colour, which is
  // the Accent at a fixed 18% alpha, not its width, and no way to remove it. It
  // reads as a soft halo, which is why it looks like a hover effect that cannot
  // be turned off; there is no hover rule on the avatar anywhere.
  //
  // Emitted only where the block declares `avatarRing`, so the three layouts that
  // draw their own avatar -- case study, toast, avatar list -- are untouched, the
  // same set the Image Border control already skips.
  //
  // An empty colour keeps `--btb-ring`, so a ring nobody has recoloured still
  // follows the Accent role the way it always has.
  const avatarRingCSS = undefined !== attributes?.avatarRing ? `${mainEl} .single .authorImg .img {
			box-shadow: ${avatarRing ? `0 0 0 ${isSet(avatarRingWidth) ? avatarRingWidth : 3}px ${avatarRingColor || "var(--btb-ring)"}` : "none"};
		}` : "";

  // The FAQ Review Accordion's row box.
  //
  // layoutControls.js withholds the Card panel from this layout so the sidebar
  // cannot outrank `.btb-faq-item[open] { border-color: var(--btb-accent) }`,
  // which is what marks the expanded row. That is right for the border and only
  // for the border -- radius, background, shadow, row gap and the two paddings
  // were pinned in frontend.scss with nothing to reach them, so withholding one
  // control had withheld six.
  //
  // Nothing here writes `border-color` or `border-width`, so the open row keeps
  // its accent and the Colors panel keeps both.
  const faqItemEl = `${mainEl} .btb-faq-item`;
  const faqBoxCSS = [faqRadius ? `border-radius: ${faqRadius};` : "",
  // 0 is a real gap: it butts the rows into one block.
  isSet(faqRowGap) ? `margin-bottom: ${faqRowGap}px;` : "", faqBg ? `background-color: ${faqBg};` : "", (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getShadowCSS)(faqShadow) ? `box-shadow: ${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getShadowCSS)(faqShadow)};` : ""].filter(Boolean).join(" ");
  const faqQuestionPaddingCSS = (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getBoxCSS)(faqQuestionPadding);
  const faqAnswerPaddingCSS = (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getBoxCSS)(faqAnswerPadding);
  const faqCSS = "faq-testimonial-accordion" === layout ? [faqBoxCSS ? `${faqItemEl} {
			${faqBoxCSS}
		}` : "", faqQuestionPaddingCSS ? `${faqItemEl} .btb-faq-question {
			padding: ${faqQuestionPaddingCSS};
		}` : "", faqAnswerPaddingCSS ? `${faqItemEl} .btb-faq-answer {
			padding: ${faqAnswerPaddingCSS};
		}` : ""].filter(Boolean).join(`

		`) : "";

  // The Trust Badges grid.
  //
  // This block registers its own editor, and that editor renders the Colors
  // panel and Width & Height and nothing else -- no Card panel, no typography
  // panels. So every size in trust-badges.scss was final: a 16px/700 title, a
  // 14px subtitle, a 44px icon at a 14px gap, an 18px by 20px box at radius 10
  // with no shadow, and the icon always beside the text.
  //
  // The Card panel would not have helped even if it were offered. Its box rule
  // names `.btb-trust-badges-grid`, which is the wrapper of the four-badge
  // fallback shown before any badge is added, and the grid rather than a badge
  // in it -- so nothing in it reaches `.badge-item`, which is what both the
  // editor and the published page render once the repeater has content.
  //
  // The fallback is styled alongside it, so it keeps up with the repeater.
  const badgeItemEl = `${mainEl} .badge-item`;
  const badgeFallbackEl = `${mainEl} .btb-trust-item`;
  const badgePaddingCSS = (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getBoxCSS)(badgePadding);

  // Joined with spaces rather than newlines: these are declarations inside one
  // rule, and CSS does not care which whitespace separates them.
  const badgeVars = [isSet(badgeIconSize) ? `--btb-badge-icon: ${badgeIconSize}px;` : "", isSet(badgeIconGap) ? `--btb-badge-gap: ${badgeIconGap}px;` : "", badgePaddingCSS ? `--btb-badge-padding: ${badgePaddingCSS};` : "", badgeRadius ? `--btb-badge-radius: ${badgeRadius};` : ""].filter(Boolean).join(" ");
  const badgeShadowCSS = (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getShadowCSS)(badgeShadow);
  const trustBadgeCSS = "trust-badges" === layout ? [badgeVars ? `${mainEl} {
			${badgeVars}
		}` : "",
  // The badge ships flat, so this adds a shadow rather than replacing
  // one, and an untouched block stays flat.
  badgeShadowCSS ? `${badgeItemEl},
		${badgeFallbackEl} {
			box-shadow: ${badgeShadowCSS};
		}` : "",
  // On the fallback the label is the item's own text, so the title
  // typography lands on the item there rather than on a child of it.
  typoCSS(`${badgeItemEl} .badge-title,
		${badgeFallbackEl}`, badgeTitleTypo), typoCSS(`${badgeItemEl} .badge-subtitle`, badgeSubtitleTypo)].filter(Boolean).join(`

		`) : "";

  // These are `@import url(...)` statements, and CSS drops any @import that
  // follows a style rule -- an empty ruleset counts as a rule. So they have to
  // be emitted before everything else below, or every block silently loses its
  // font family and falls back to the generic category. Collected here so the
  // constraint survives reformatting instead of depending on line order.
  const fontImports = [nameTypo, degTypo, textTypo, expandedTypo, labelTypo, inputTypo, pollTitleTypo, pollDescTypo, pollLabelTypo, pollBtnTypo, badgeScoreTypo, badgeTitleTypo, badgeSubtitleTypo].map(typo => (0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getTypoCSS)("", typo)?.googleFontLink || "").filter(Boolean).join("\n");
  // Every rule below names `.single` on its own rather than
  // `.layoutSection .single`.
  //
  // The hero spotlight renders its card straight inside `.btb-hero-card`, with
  // no `.layoutSection` anywhere, so the narrower selector missed it and the
  // whole Style tab slid off that block: the avatar ignored the Image width and
  // height and rendered at its natural 589px -- inflating the card to 860px, at
  // which point Card Height had nothing left to do -- and the card took none of
  // the background, padding, border, shadow, colours or typography set for it.
  // Measured against a grid block on the same page, not inferred.
  //
  // The hero card therefore now carries the same Card defaults as every other
  // layout, in place of the flat 28px padding its stylesheet used to give it.
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("style", {
    dangerouslySetInnerHTML: {
      __html: `
		${fontImports}

	
		
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getTypoCSS)(selectorList([...NAME_PARTS, ...NAME_TYPO_ONLY]), nameTypo)?.styles || ""}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getTypoCSS)(selectorList(DEG_PARTS), degTypo)?.styles || ""}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getTypoCSS)(selectorList(TEXT_PARTS), textTypo)?.styles || ""}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getTypoCSS)(`${mainEl} .btb-expand-btn`, expandedTypo)?.styles || ""}

		${
      /* The badge's score, and the star row beside it.
         Neither maps onto a shared role: the score is a bold number the
         stylesheet sets at 18px/800, and the stars are five glyphs whose only
         real question is how big they are. Both were literals with nothing in
         the sidebar to change them.
          Their colours are not touched here -- the score already reads
         `--btb-title` and the stars `--btb-star`, so the Colors panel's Title
         and Rating Stars roles reach both, and painting them again from a
         second control would only give an author two ways to set one
         pixel. */
      ""}
		${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getTypoCSS)(`${mainEl} .btb-badge-rating .score`, badgeScoreTypo)?.styles || ""}
		${isSet(badgeStarsSize) ? `${mainEl} .btb-badge-rating .stars {\n\t\t\tfont-size: ${badgeStarsSize}px;\n\t\t}` : ""}

		${
      /* The brand mark's box. Google, Capterra, Facebook, Trustpilot and G2
         each draw their own logo at a hardcoded 36px in Layout.js, and the
         icon controls skip them on purpose -- the marks are other companies'
         trademarks -- which left the size with no control either.
          A rule rather than a prop: the SVG's own `width` and `height` are
         presentation attributes, and any selector naming the element beats
         them, so nothing in Layout.js has to change.
          The other two badges carry the same class but are not offered the
         control: their logo is a real icon slot, so its size is the Icon
         panel's Icon Size. With no control the attribute stays unset and no
         rule is written, which is what keeps the two from having two ways to
         set one pixel. */
      ""}
		${isSet(badgeLogoSize) ? `${mainEl} .btb-badge-brand-logo {\n\t\t\twidth: ${badgeLogoSize}px;\n\t\t\theight: ${badgeLogoSize}px;\n\t\t}` : ""}
 
		
		${mainEl} .slider-layout .swiper-slide {
			
		}

		${mainEl} {
			${(0,_utils_visualControls__WEBPACK_IMPORTED_MODULE_4__.getPaletteCSS)(attributes, layout)}
		}

		${mainEl} .layoutSection {
			grid-gap: ${rowGap} ${columnGap};
		}

		${
      /* The bubble layout is the one bespoke wrapper where a gap is a real
         question: it is a wrapping flex row of several bubbles, spaced by a
         `gap` of its own. Every other layout without `.layoutSection` either
         holds a single widget -- where there is nothing to space it from --
         or stacks its items with a margin. Its own rule rather than joining
         the selector above, because `grid-gap` is the legacy grid alias and
         this one is flex. */
      ""}
		${mainEl} .btb-floating-bubble-layout {
			gap: ${rowGap} ${columnGap};
		}

		${mainEl} .theme_2 .single .top,
		${mainEl} .masonry-layout .single .top,
		${mainEl} .btb-masonry-layout .single .top {
			background-color:${grid2Bg};
			padding:${(0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_1__.getBoxValue)(grid2Padding)};
			${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getBorderCSS)(grid2Border)};
		}

		${mainEl} .theme_4 .single .bottom{
			border-top:${border?.width || "0px"} ${border?.style || "solid"} ${border?.color || "transparent"};
		}

		${mainEl} .theme_4 .single .info {
			border-left: ${border?.width || "0px"} ${border?.style || "solid"} ${border?.color || "transparent"};
		}

		${
      /* The card box. `.btb-avatar-detail` in place of
         `.btb-avatar-list-wrapper`: the wrapper is a transparent container
         and the box a reader sees is the panel inside it, which carried its
         own background, padding, border and shadow in the stylesheet -- so
         the Card panel painted something invisible while the visible box
         ignored it. The wrapper stays in the margin list, which is the one
         thing it is the right element for.
          The last two build their card out of neither `.single` nor any of
         the widget classes, so the Card panel reached nothing on them at
         all. Their stylesheet values now live in each block's defaults.
          `.btb-faq-item` is deliberately absent: the stylesheet recolours its
         border on `[open]` to mark the expanded row, and an ID-scoped border
         from here would outrank that. */
      ""}
		${cardPaintEl} {
			${
      /* `background-color`, not the `background` shorthand. The shorthand
         also resets `background-image`, which erased the quote box's
         `linear-gradient(135deg, var(--btb-surface), var(--btb-track))`
         -- so its Card Gradient End control painted nothing and the card
         rendered flat. Measured: `background-image` computed `none` with
         the gradient's own rule matching. Every other layout paints its
         card with a plain colour, so dropping the shorthand changes
         nothing for them. */
      ""}
			background-color:${withRole("--btb-surface", background)};
			padding:${(0,_bpl_tools_utils_functions__WEBPACK_IMPORTED_MODULE_1__.getBoxValue)((0,_utils_responsiveBox__WEBPACK_IMPORTED_MODULE_5__.ownBoxForDevice)(padding, "desktop"))};
			${paletteBorderCSS(border)};
			${cardBoxShadowCSS}
		}

		${
      /* Hover, straight after the resting rule it has to beat. Same ID, same
         element, later in the sheet -- which is exactly what the stylesheet's
         own hover rule could not manage. */
      ""}
		${cardHoverCSS ? `${cardPaintHoverEl} {
			${cardHoverCSS}
		}` : ""}

		${cardMarginCSS ? `${cardMarginEl} {\n\t\t\tmargin: ${cardMarginCSS};\n\t\t}` : ""}
 
		${mainEl} .single .img{
			${
      /* Width and height are emitted per device by sizeCSS below. A fixed
         px avatar is wider than the card on a narrow screen, so it is
         capped here rather than allowed to push out of the card. */
      ""}
			max-width: 100%;
			${(0,_bpl_tools_utils_getCSS__WEBPACK_IMPORTED_MODULE_2__.getBorderCSS)(imgBorder)};
		}

		${selectorList(colorParts(NAME_PARTS))} {
			color:${withRole("--btb-title", nameColor)};
		}

		${selectorList(colorParts(DEG_PARTS))} {
			color:${withRole("--btb-muted", degColor)};
		}

		${degDividerCSS ? `${mainEl} .theme_1 .single .footer .deg::after {\n\t\t\t${degDividerCSS}\n\t\t}` : ""}

		${selectorList(colorParts(TEXT_PARTS))} {
			color:${withRole("--btb-body", textColor)};
		}

		${
      /* The rating cell is inside `.btb-ct-table td`, so the Body Text rule
         above -- ID-scoped, and therefore stronger than the stylesheet's
         `.btb-ct-rating { color: var(--btb-star) }` -- was repainting the
         stars with the body colour and leaving Rating Stars dead. Restating
         it here at the same specificity, after that rule, hands the cell
         back. Measured: the stars computed the body colour while
         `--btb-star` sat declared and unused. */
      ""}
		${mainEl} .btb-ct-table .btb-ct-rating {
			color:${withRole("--btb-star", starIconColor)};
		}

		${mainEl} .btb-srb-title {
			color: ${withRole("--btb-title", nameColor || "#1e293b")};
		}

		${
      /* Split from the label: the stylesheet paints the count with
         --btb-muted and the label with --btb-body, so grouping them meant
         Secondary Text had nothing left to colour on this layout. */
      ""}
		${mainEl} .btb-srb-label {
			color: ${withRole("--btb-body", textColor || "#334155")};
		}

		${
      /* The per-star count is the Designation role -- that is where its
         typography comes from, and `--btb-muted` is the palette role beside
         it. It read `textColor` here, so the Designation panel's colour
         control had no target on this layout while its typography worked. */
      ""}
		${mainEl} .btb-srb-count {
			color: ${withRole("--btb-muted", degColor || "#334155")};
		}

		${expandColor ? `${mainEl} .btb-expand-btn { color: ${expandColor}; }` : ""}

		${expandHoverColor ? `${mainEl} .btb-expand-btn:hover, ${mainEl} .btb-expand-btn:focus-visible { color: ${expandHoverColor}; }` : ""}

		${mainEl} .btb-srb-fill {
			background: ${ratingColor || starIconColor || "#f59e0b"};
		}

		${navVars ? `${mainEl} .swiper {\n\t\t\t${navVars}\n\t\t}` : ""}

		${arrowBox ? `${arrowEl} {\n\t\t\t${arrowBox}\n\t\t}` : ""}

		${arrowGlyph ? `${arrowAfterEl} {\n\t\t\t${arrowGlyph}\n\t\t}` : ""}

		${arrowHover ? `${mainEl} .swiper-button-prev:hover, ${mainEl} .swiper-button-next:hover {\n\t\t\t${arrowHover}\n\t\t}` : ""}

		${
      /* The Testimonial Form's labels and inputs. After the shared colour and
         typography rules above, which name none of these elements, and before
         the size rules, which carry their own media queries. */
      ""}
		${formCSS}

		${/* The poll's text, scale buttons and box, on the same terms. */""}
		${pollCSS}

		${
      /* The Gradient Border Grid's ring and score pill. After the card box rule
         above, whose `background-color` the ring has to sit on top of. */
      ""}
		${gradientCSS}

		${
      /* Its header wash, after the `.theme_2 .single .top` rule above that
         paints the colour underneath it. */
      ""}
		${headerWashCSS}

		${/* The Speech Bubble tail: geometry, position, fill and outline. */""}
		${bubbleCSS}

		${/* The Trust Badges box, icon and text. */""}
		${trustBadgeCSS}

		${/* The FAQ accordion's row box. */""}
		${faqCSS}

		${/* The avatar ring. */""}
		${avatarRingCSS}

		${/* The card's corner wash, at rest and on hover. */""}
		${cardWashCSS}

		${/* The 3D arrangements' side-card fade. */""}
		${sideOpacityCSS}

		${/* The popup modal's box and text sizes, and the rating star's size. */""}
		${modalCSS}

		${starSizeCSS}

		${compactStarCSS}

		${sizeCSS("desktop")}

		${tabletSize ? `${_bpl_tools_utils_data__WEBPACK_IMPORTED_MODULE_3__.tabBreakpoint} {\n\t\t${tabletSize}\n\t\t}` : ""}

		${mobileSize ? `${_bpl_tools_utils_data__WEBPACK_IMPORTED_MODULE_3__.mobileBreakpoint} {\n\t\t${mobileSize}\n\t\t}` : ""}

		${
      /* After the width rules above, whose auto margins this overrules; before
         Block Margin below, which overrules it in turn. */
      ""}
		${blockAlignMarginCSS}

		${
      /* Last, so an explicit side beats the auto centring the per-device Block
         Width rules above apply -- including the ones inside the media queries. */
      ""}
		${blockMarginCSS ? `${widthEl} {\n\t\t\t${blockMarginCSS}\n\t\t}` : ""}

		${blockAlignCSS}

		${blockMarginBreakpoints}
	`
    }
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Style);

/***/ }),

/***/ "./src/shared/Components/Common/TestimonialForm.js":
/*!*********************************************************!*\
  !*** ./src/shared/Components/Common/TestimonialForm.js ***!
  \*********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__);




/**
 * The Testimonial Form block's markup, shared by the editor and the front end.
 *
 * It used to exist twice over, and neither copy worked: the editor rendered a
 * disabled `.btb-tform` preview, while the page got an entirely different
 * `.btb-form-wrapper` of read-only inputs with a `type="button"` submit. So the
 * block's own view script -- which binds to `.btb-tform[data-endpoint]` and posts
 * to the REST route in includes/form.php -- never found a form to bind, and the
 * Title, Button text, Success message and Fields controls changed nothing that
 * a visitor could see. One component means the controls drive both.
 *
 * `formEndpoint`, `formNonce` and `formRestNonce` are injected by render.php
 * rather than stored as block attributes: a nonce is per-visitor and must not be
 * baked into post content.
 *
 * @param {Object}  props.attributes Block attributes.
 * @param {boolean} props.isBackend  True in the editor, where the fields can be
 *                                   filled in to see the form take shape but
 *                                   nothing may be submitted.
 */
const TestimonialForm = ({
  attributes = {},
  isBackend = false
}) => {
  // Editor-only. On a page the block's view script writes into
  // `.btb-tform-msg` itself, so React must leave that node empty there.
  const [previewNote, setPreviewNote] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)('');
  const {
    formTitle = '',
    buttonText = '',
    successMessage = '',
    fields = {},
    fieldLabels = {},
    fieldPlaceholders = {},
    accentColor,
    btnBg = '',
    badgeTitle = '',
    badgeCount = ''
  } = attributes;

  // Blocks saved before this block had its own editor described themselves
  // through the generic badge fields, so those stay as the fallback.
  const title = formTitle || badgeTitle || (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)('Leave a Customer Review', 'b-testimonials-block');
  const button = buttonText || badgeCount || (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)('Submit Testimonial', 'b-testimonials-block');
  const field = (label, input) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "btb-tform-field"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("label", null, label), input);

  // Every visible label and placeholder was a hardcoded `__()` string, so the
  // form could only ever ask for "Name", "Email", "Designation" -- there was no
  // way to word a question for the site, or to translate one on a site that is
  // not running in the plugin's own locale. Placeholders did not exist at all.
  //
  // Empty means "use the shipped wording", not "blank": that keeps the default
  // form identical to what it renders today and leaves the strings translatable
  // through the usual .po route for anyone who has not overridden them.
  const labelFor = (key, fallback) => fieldLabels?.[key] || fallback;

  // `undefined` rather than `''` so React omits the attribute entirely instead
  // of writing `placeholder=""`.
  const placeholderFor = key => fieldPlaceholders?.[key] || undefined;

  // The two required fields carry a marker after whatever they are called.
  const required = label => `${label} *`;
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "bTestimonialForm"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("form", {
    className: "btb-tform",
    method: "post"
    // Posting to the endpoint rather than to the page is the safe
    // default if a submit ever escapes the view script: the page URL
    // would hand WordPress the fields as query vars.
    ,
    action: isBackend ? undefined : attributes.formEndpoint || '',
    "data-endpoint": attributes.formEndpoint || '',
    "data-nonce": attributes.formNonce || '',
    "data-rest-nonce": attributes.formRestNonce || '',
    "data-success": successMessage || ''
    // The fields themselves are live in the editor so the form can
    // be tried out while it is being styled -- they used to be
    // `disabled`, which left the whole block unusable there. Only
    // the submit is stopped, and `noValidate` keeps the browser's
    // own required-field bubble from pre-empting the note below.
    ,
    noValidate: isBackend,
    onSubmit: isBackend ? e => {
      e.preventDefault();
      setPreviewNote((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)('Preview only. The form accepts submissions on the published page.', 'b-testimonials-block'));
    } : undefined
  }, title && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h3", {
    className: "btb-tform-title"
  }, title), !isBackend && attributes.formNonce && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("input", {
    type: "hidden",
    name: "nonce",
    value: attributes.formNonce
  }), field(required(labelFor('name', (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)('Name', 'b-testimonials-block'))),
  // Not `name`: that is one of WordPress's public query vars, and
  // WP::parse_request() reads those out of $_POST as well as $_GET.
  // A submit that reached the page URL therefore asked WordPress for
  // a post whose slug was whatever the visitor typed, and got the
  // 404 template. The endpoint accepts the old name too, for pages
  // cached with the previous markup.
  (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("input", {
    type: "text",
    name: "btb_name",
    required: true,
    placeholder: placeholderFor('name')
  })), fields?.email && field(labelFor('email', (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)('Email', 'b-testimonials-block')), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("input", {
    type: "email",
    name: "email",
    placeholder: placeholderFor('email')
  })), fields?.designation && field(labelFor('designation', (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)('Designation', 'b-testimonials-block')), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("input", {
    type: "text",
    name: "designation",
    placeholder: placeholderFor('designation')
  })), fields?.company && field(labelFor('company', (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)('Company', 'b-testimonials-block')), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("input", {
    type: "text",
    name: "company",
    placeholder: placeholderFor('company')
  })), fields?.rating && field(labelFor('rating', (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)('Rating', 'b-testimonials-block')), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("select", {
    name: "rating",
    defaultValue: "5"
  }, [5, 4, 3, 2, 1].map(n => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("option", {
    key: n,
    value: n
  }, n)))), fields?.image && field(labelFor('image', (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)('Photo', 'b-testimonials-block')), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("input", {
    type: "file",
    name: "image",
    accept: "image/*"
  })), field(required(labelFor('review', (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)('Review', 'b-testimonials-block'))), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("textarea", {
    name: "review",
    required: true,
    placeholder: placeholderFor('review')
  })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("button", {
    type: "submit",
    className: "btb-tform-submit"
    // Inline, so it outranks every stylesheet -- which is why the new
    // Background control painted nothing until it was named here.
    // `btnBg` first: the button's own colour is the more specific
    // choice, and Accent stays the default when it is empty.
    ,
    style: {
      backgroundColor: btnBg || accentColor
    }
  }, button), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
    className: `btb-tform-msg${isBackend && previewNote ? ' is-notice' : ''}`
  }, isBackend ? previewNote : null)));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (TestimonialForm);

/***/ }),

/***/ "./src/shared/Components/Common/TestimonialsView.js":
/*!**********************************************************!*\
  !*** ./src/shared/Components/Common/TestimonialsView.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _Style__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./Style */ "./src/shared/Components/Common/Style.js");
/* harmony import */ var _Layout_Layout__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./Layout/Layout */ "./src/shared/Components/Common/Layout/Layout.js");
/* harmony import */ var _ExpandButton__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./ExpandButton */ "./src/shared/Components/Common/ExpandButton.js");






/**
 * Read-only rendering of a testimonials block, shared by the front end
 * (view.js) and the editor's CPT-source preview.
 */
/**
 * Truncated review text with its Expand/Less toggle.
 *
 * Declared at module scope on purpose. It used to be defined inside the
 * items.map() below, which gave it a fresh function identity on every render of
 * TestimonialsView -- React then treated it as a different component type,
 * unmounted it and remounted a fresh one, throwing away `expanded`. Any parent
 * re-render (slider autoplay, marquee, popup, active-item changes) silently
 * collapsed text the visitor had just expanded.
 */
const ViewReviewText = ({
  attributes,
  elements,
  expandBtn,
  textLength,
  reviewText
}) => {
  const [expanded, setExpanded] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const contentLength = (reviewText || "").length;
  const showText = expanded ? reviewText : (reviewText || "").slice(0, textLength);
  const text = expandBtn ? showText : reviewText;
  return elements?.reviewText && reviewText && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
    className: "reviewText",
    dangerouslySetInnerHTML: {
      __html: text
    }
  }), expandBtn && contentLength > textLength && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_ExpandButton__WEBPACK_IMPORTED_MODULE_3__["default"], {
    attributes: attributes,
    reviewText: reviewText,
    expanded: expanded,
    onChange: () => setExpanded(!expanded)
  }));
};
const TestimonialsView = ({
  attributes,
  clientId,
  isBackend = false,
  previewDevice = "Desktop"
}) => {
  const cId = clientId !== null && clientId !== void 0 ? clientId : attributes.cId;
  // textLength needs a default here: without one, slice(0, undefined) returns
  // the whole string and `contentLength > undefined` is false, so the text
  // never truncates and the toggle never appears.
  const {
    items = [],
    elements = {},
    textLength = 120
  } = attributes;
  const {
    expandBtn
  } = elements || {};
  const itemsEls = items.map(item => {
    const {
      name,
      deg,
      reviewText
    } = item;
    return {
      img: (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null),
      name: elements?.name && name && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h3", {
        className: "name",
        dangerouslySetInnerHTML: {
          __html: name
        }
      }),
      deg: elements?.deg && deg && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h5", {
        className: "deg",
        dangerouslySetInnerHTML: {
          __html: deg
        }
      }),
      reviewText: (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(ViewReviewText, {
        attributes: attributes,
        elements: elements,
        expandBtn: expandBtn,
        textLength: textLength,
        reviewText: reviewText
      })
    };
  });
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Style__WEBPACK_IMPORTED_MODULE_1__["default"], {
    attributes: attributes,
    clientId: cId
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "btbTestimonialsDir"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Layout_Layout__WEBPACK_IMPORTED_MODULE_2__["default"], {
    attributes: attributes,
    itemsEls: itemsEls,
    isBackend: isBackend,
    previewDevice: previewDevice
  })));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (TestimonialsView);

/***/ }),

/***/ "./src/shared/Components/Common/Themes/Default.js":
/*!********************************************************!*\
  !*** ./src/shared/Components/Common/Themes/Default.js ***!
  \********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../utils/functions */ "./src/shared/utils/functions.js");
/* harmony import */ var _utils_a11y__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../utils/a11y */ "./src/shared/utils/a11y.js");
/* harmony import */ var _Image__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../Image */ "./src/shared/Components/Common/Image.js");
/* harmony import */ var _ReviewText__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../ReviewText */ "./src/shared/Components/Common/ReviewText.js");
/* harmony import */ var _RatingIcon__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../RatingIcon */ "./src/shared/Components/Common/RatingIcon.js");






const Default = ({
  item = {},
  index,
  itemEls = {},
  attributes = {},
  activeIndex,
  setActiveIndex,
  isBackend
}) => {
  const {
    starIconColor
  } = attributes || {};
  const {
    img = {},
    reviewText = '',
    rating = 5
  } = item || {};
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: `single ${isBackend && index === activeIndex ? "btbNowEditing" : ""}`,
    ...(0,_utils_a11y__WEBPACK_IMPORTED_MODULE_2__.editorClickable)(isBackend, () => setActiveIndex(index))
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "top"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Image__WEBPACK_IMPORTED_MODULE_3__["default"], {
    attributes: attributes,
    img: img
  }, itemEls?.img), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "info"
  }, itemEls?.name, itemEls?.deg)), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_RatingIcon__WEBPACK_IMPORTED_MODULE_5__["default"], {
    attributes: attributes,
    getStar: _utils_functions__WEBPACK_IMPORTED_MODULE_1__.getStar,
    rating: rating,
    starIconColor: starIconColor
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "body"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_ReviewText__WEBPACK_IMPORTED_MODULE_4__["default"], {
    attributes: attributes,
    itemEls: itemEls,
    isBackend: isBackend,
    reviewText: reviewText
  })));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Default);

/***/ }),

/***/ "./src/shared/Components/Common/Themes/ThemeFive.js":
/*!**********************************************************!*\
  !*** ./src/shared/Components/Common/Themes/ThemeFive.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _Image__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../Image */ "./src/shared/Components/Common/Image.js");
/* harmony import */ var _ReviewText__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../ReviewText */ "./src/shared/Components/Common/ReviewText.js");
/* harmony import */ var _RatingIcon__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../RatingIcon */ "./src/shared/Components/Common/RatingIcon.js");
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../../utils/functions */ "./src/shared/utils/functions.js");
/* harmony import */ var _utils_a11y__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../../../utils/a11y */ "./src/shared/utils/a11y.js");






const ThemeFive = ({
  itemEls = {},
  item = {},
  index,
  attributes = {},
  activeIndex,
  setActiveIndex,
  isBackend
}) => {
  const {
    starIconColor
  } = attributes || {};
  const {
    img = {},
    reviewText = '',
    rating = 5
  } = item || {};
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    key: index,
    className: `single ${isBackend && index === activeIndex ? "btbNowEditing" : ""}`,
    ...(0,_utils_a11y__WEBPACK_IMPORTED_MODULE_5__.editorClickable)(isBackend, () => setActiveIndex(index))
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "top"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Image__WEBPACK_IMPORTED_MODULE_1__["default"], {
    attributes: attributes,
    img: img
  }, itemEls?.img), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "right"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_ReviewText__WEBPACK_IMPORTED_MODULE_2__["default"], {
    attributes: attributes,
    itemEls: itemEls,
    isBackend: isBackend,
    reviewText: reviewText
  }))), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "bottom"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "info"
  }, itemEls?.name, itemEls?.deg), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_RatingIcon__WEBPACK_IMPORTED_MODULE_3__["default"], {
    attributes: attributes,
    getStar: _utils_functions__WEBPACK_IMPORTED_MODULE_4__.getStar,
    rating: rating,
    starIconColor: starIconColor
  })));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ThemeFive);

/***/ }),

/***/ "./src/shared/Components/Common/Themes/ThemeFour.js":
/*!**********************************************************!*\
  !*** ./src/shared/Components/Common/Themes/ThemeFour.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _Image__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../Image */ "./src/shared/Components/Common/Image.js");
/* harmony import */ var _ReviewText__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../ReviewText */ "./src/shared/Components/Common/ReviewText.js");
/* harmony import */ var _utils_icons__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../../utils/icons */ "./src/shared/utils/icons.js");
/* harmony import */ var _utils_a11y__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../../utils/a11y */ "./src/shared/utils/a11y.js");





const ThemeFour = ({
  itemEls = {},
  item = {},
  index,
  attributes = {},
  activeIndex,
  setActiveIndex,
  isBackend
}) => {
  const {
    nameColor,
    quoteIcon
  } = attributes || {};
  const {
    img = {},
    reviewText = ''
  } = item || {};
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    key: index,
    className: `single ${isBackend && index === activeIndex ? "btbNowEditing" : ""}`,
    ...(0,_utils_a11y__WEBPACK_IMPORTED_MODULE_4__.editorClickable)(isBackend, () => setActiveIndex(index))
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "top"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Image__WEBPACK_IMPORTED_MODULE_1__["default"], {
    attributes: attributes,
    img: img
  }, itemEls?.img)), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "body"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_ReviewText__WEBPACK_IMPORTED_MODULE_2__["default"], {
    attributes: attributes,
    itemEls: itemEls,
    isBackend: isBackend,
    reviewText: reviewText
  })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "bottom"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "rightQuote"
  }, (0,_utils_icons__WEBPACK_IMPORTED_MODULE_3__.rightQuote)(nameColor, quoteIcon?.size)), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "info"
  }, itemEls?.name, itemEls?.deg)));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ThemeFour);

/***/ }),

/***/ "./src/shared/Components/Common/Themes/ThemeOne.js":
/*!*********************************************************!*\
  !*** ./src/shared/Components/Common/Themes/ThemeOne.js ***!
  \*********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../utils/functions */ "./src/shared/utils/functions.js");
/* harmony import */ var _utils_a11y__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../utils/a11y */ "./src/shared/utils/a11y.js");
/* harmony import */ var _Image__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../Image */ "./src/shared/Components/Common/Image.js");
/* harmony import */ var _ReviewText__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../ReviewText */ "./src/shared/Components/Common/ReviewText.js");
/* harmony import */ var _RatingIcon__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../RatingIcon */ "./src/shared/Components/Common/RatingIcon.js");






const ThemeOne = ({
  attributes = {},
  itemEls = {},
  item = {},
  index,
  activeIndex,
  setActiveIndex,
  isBackend
}) => {
  const {
    starIconColor
  } = attributes || {};
  const {
    img = {},
    reviewText = '',
    rating = 5
  } = item || {};
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    key: index,
    className: `single ${isBackend && index === activeIndex ? "btbNowEditing" : ""}`,
    ...(0,_utils_a11y__WEBPACK_IMPORTED_MODULE_2__.editorClickable)(isBackend, () => setActiveIndex(index))
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "top"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Image__WEBPACK_IMPORTED_MODULE_3__["default"], {
    attributes: attributes,
    img: img
  }, itemEls?.img)), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "body"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_ReviewText__WEBPACK_IMPORTED_MODULE_4__["default"], {
    itemEls: itemEls,
    attributes: attributes,
    isBackend: isBackend,
    reviewText: reviewText
  })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "footer"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "info"
  }, itemEls?.name, itemEls?.deg), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_RatingIcon__WEBPACK_IMPORTED_MODULE_5__["default"], {
    attributes: attributes,
    getStar: _utils_functions__WEBPACK_IMPORTED_MODULE_1__.getStar,
    rating: rating,
    starIconColor: starIconColor
  })));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ThemeOne);

/***/ }),

/***/ "./src/shared/Components/Common/Themes/ThemeSix.js":
/*!*********************************************************!*\
  !*** ./src/shared/Components/Common/Themes/ThemeSix.js ***!
  \*********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../utils/functions */ "./src/shared/utils/functions.js");
/* harmony import */ var _utils_a11y__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../utils/a11y */ "./src/shared/utils/a11y.js");
/* harmony import */ var _Image__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../Image */ "./src/shared/Components/Common/Image.js");
/* harmony import */ var _ReviewText__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../ReviewText */ "./src/shared/Components/Common/ReviewText.js");
/* harmony import */ var _RatingIcon__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../RatingIcon */ "./src/shared/Components/Common/RatingIcon.js");






const ThemeSix = ({
  itemEls = {},
  item = {},
  index,
  attributes = {},
  activeIndex,
  setActiveIndex,
  isBackend
}) => {
  const {
    starIconColor
  } = attributes || {};
  const {
    img = {},
    reviewText = '',
    rating = 5
  } = item || {};
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    key: index,
    className: `single ${isBackend && index === activeIndex ? "btbNowEditing" : ""}`,
    ...(0,_utils_a11y__WEBPACK_IMPORTED_MODULE_2__.editorClickable)(isBackend, () => setActiveIndex(index))
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "top"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_ReviewText__WEBPACK_IMPORTED_MODULE_4__["default"], {
    attributes: attributes,
    itemEls: itemEls,
    isBackend: isBackend,
    reviewText: reviewText
  })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "bottom"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Image__WEBPACK_IMPORTED_MODULE_3__["default"], {
    attributes: attributes,
    img: img
  }, itemEls?.img), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "info"
  }, itemEls?.name, itemEls?.deg, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_RatingIcon__WEBPACK_IMPORTED_MODULE_5__["default"], {
    attributes: attributes,
    getStar: _utils_functions__WEBPACK_IMPORTED_MODULE_1__.getStar,
    rating: rating,
    starIconColor: starIconColor
  }))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ThemeSix);

/***/ }),

/***/ "./src/shared/Components/Common/Themes/ThemeThree.js":
/*!***********************************************************!*\
  !*** ./src/shared/Components/Common/Themes/ThemeThree.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../utils/functions */ "./src/shared/utils/functions.js");
/* harmony import */ var _utils_a11y__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../utils/a11y */ "./src/shared/utils/a11y.js");
/* harmony import */ var _Image__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../Image */ "./src/shared/Components/Common/Image.js");
/* harmony import */ var _ReviewText__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../ReviewText */ "./src/shared/Components/Common/ReviewText.js");
/* harmony import */ var _RatingIcon__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../RatingIcon */ "./src/shared/Components/Common/RatingIcon.js");






const ThemeThree = ({
  attributes = {},
  itemEls = {},
  item = {},
  index,
  activeIndex,
  setActiveIndex,
  isBackend
}) => {
  const {
    starIconColor
  } = attributes || {};
  const {
    img = {},
    reviewText = '',
    rating = 5
  } = item || {};
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    key: index,
    className: `single ${isBackend && index === activeIndex ? "btbNowEditing" : ""}`,
    ...(0,_utils_a11y__WEBPACK_IMPORTED_MODULE_2__.editorClickable)(isBackend, () => setActiveIndex(index))
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "top"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_ReviewText__WEBPACK_IMPORTED_MODULE_4__["default"], {
    attributes: attributes,
    itemEls: itemEls,
    isBackend: isBackend,
    reviewText: reviewText
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_RatingIcon__WEBPACK_IMPORTED_MODULE_5__["default"], {
    attributes: attributes,
    getStar: _utils_functions__WEBPACK_IMPORTED_MODULE_1__.getStar,
    rating: rating,
    starIconColor: starIconColor
  })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "bottom"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "info"
  }, itemEls?.name, itemEls?.deg), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Image__WEBPACK_IMPORTED_MODULE_3__["default"], {
    attributes: attributes,
    img: img
  }, itemEls?.img)));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ThemeThree);

/***/ }),

/***/ "./src/shared/Components/Common/Themes/ThemeTwo.js":
/*!*********************************************************!*\
  !*** ./src/shared/Components/Common/Themes/ThemeTwo.js ***!
  \*********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../utils/functions */ "./src/shared/utils/functions.js");
/* harmony import */ var _utils_a11y__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../utils/a11y */ "./src/shared/utils/a11y.js");
/* harmony import */ var _utils_icons__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../../utils/icons */ "./src/shared/utils/icons.js");
/* harmony import */ var _Image__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../Image */ "./src/shared/Components/Common/Image.js");
/* harmony import */ var _ReviewText__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../ReviewText */ "./src/shared/Components/Common/ReviewText.js");
/* harmony import */ var _RatingIcon__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../RatingIcon */ "./src/shared/Components/Common/RatingIcon.js");







const ThemeTwo = ({
  attributes = {},
  itemEls = {},
  item = {},
  index,
  activeIndex,
  setActiveIndex,
  isBackend
}) => {
  const {
    starIconColor,
    grid2Bg,
    showStarBadge
  } = attributes || {};
  const {
    img = {},
    reviewText = "",
    rating = 5
  } = item || {};

  // The corner score pill. Only the Gradient Border Grid declares
  // `showStarBadge`, so every other block rendering this theme reads `undefined`
  // here and is unchanged -- the attribute is both the control and the gate.
  //
  // The numeric score rather than a second row of stars: the star row above
  // already says how many, and repeating it in a pill would say it twice.
  const badgeScore = Math.min(5, Math.max(0, Number(rating) || 0));
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    key: index,
    className: `single ${isBackend && index === activeIndex ? "btbNowEditing" : ""}`,
    ...(0,_utils_a11y__WEBPACK_IMPORTED_MODULE_2__.editorClickable)(isBackend, () => setActiveIndex(index))
  }, showStarBadge && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    className: "btb-star-badge"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    className: "btb-star-badge-icon",
    "aria-hidden": "true"
  }, "\u2605"), badgeScore.toFixed(1)), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "top"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_RatingIcon__WEBPACK_IMPORTED_MODULE_6__["default"], {
    attributes: attributes,
    getStar: _utils_functions__WEBPACK_IMPORTED_MODULE_1__.getStar,
    rating: rating,
    starIconColor: starIconColor
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_ReviewText__WEBPACK_IMPORTED_MODULE_5__["default"], {
    attributes: attributes,
    itemEls: itemEls,
    isBackend: isBackend,
    reviewText: reviewText
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "icon"
  }, (0,_utils_icons__WEBPACK_IMPORTED_MODULE_3__.symbol)(grid2Bg))), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "bottom"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Image__WEBPACK_IMPORTED_MODULE_4__["default"], {
    attributes: attributes,
    img: img
  }, itemEls?.img), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "info"
  }, itemEls?.name, itemEls?.deg)));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ThemeTwo);

/***/ }),

/***/ "./src/shared/Components/Common/VideoCard.js":
/*!***************************************************!*\
  !*** ./src/shared/Components/Common/VideoCard.js ***!
  \***************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _BlockIcon__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./BlockIcon */ "./src/shared/Components/Common/BlockIcon.js");
/* harmony import */ var _utils_functions__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../utils/functions */ "./src/shared/utils/functions.js");





/**
 * One video testimonial: poster, play button, and the embed once it is clicked.
 *
 * Lifted out of Layout.js so the Video Testimonials block's own editor can use
 * it too. That editor had hand-written its own copy of this markup with no state
 * and no click handler at all, so a video could never be played while editing --
 * the play button was a picture of a play button. Sharing the component is what
 * stops the two drifting apart again.
 *
 * The embed is injected on click rather than up front: a grid of YouTube iframes
 * would load a player per card before anyone asked for one.
 *
 * @param {Object}   props
 * @param {Object}   props.item        Video item: { videoUrl, poster, name, deg, company }.
 * @param {string}   props.accentColor Colour for the play glyph.
 * @param {Object}   props.playIcon    Icon slot from the Icon panel.
 * @param {boolean}  props.autoplay    Start the embed as soon as the click swaps it in.
 * @param {boolean}  props.loop        Repeat the video once it ends.
 * @param {boolean}  props.muted       Start muted.
 * @param {boolean}  props.controls    Show the player's native controls.
 * @param {Function} props.SandBox     `@wordpress/components`' SandBox, passed in
 *                                     by the editor only -- see below. The front
 *                                     end must not pull wp-components in, so it
 *                                     is injected the way Layout.js already takes
 *                                     RichText and MediaUpload.
 */
const VideoCard = ({
  item,
  accentColor,
  playIcon = {},
  autoplay = true,
  loop = false,
  muted = false,
  controls = true,
  SandBox
}) => {
  const [isPlaying, setIsPlaying] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const embedHtml = item?.videoUrl ? (0,_utils_functions__WEBPACK_IMPORTED_MODULE_2__.getVideoEmbed)(item.videoUrl, {
    autoplay,
    loop,
    muted,
    controls
  }) : "";

  /*
   * SandBox is only for the iframe embeds. It is here to solve one problem --
   * a YouTube or Vimeo player in the canvas gets no Referer and answers with
   * Error 153 (see the note further down) -- and a self-hosted file has no
   * such problem: getVideoEmbed returns a plain <video> for anything that is
   * not YouTube or Vimeo, and it is served from this site.
   *
   * Sending it through SandBox actively broke it. The sandbox document is a
   * document of its own, so .video-frame's `video { position: absolute; inset:
   * 0; width: 100%; height: 100% }` cannot reach inside it, and the only
   * sizing left is SandBox's own `wp-has-aspect-ratio` rule -- which names
   * `html`, `body`, `body > div` and `body > div iframe`, and no `video`. The
   * file rendered at its intrinsic size in the corner of the card while the
   * published page, which never had SandBox, looked right.
   */
  const isIframeEmbed = /^s*<iframe/i.test(embedHtml);
  const useSandbox = Boolean(SandBox) && isIframeEmbed;
  const fillStyle = {
    width: "100%",
    height: "100%",
    position: "absolute",
    inset: 0
  };
  const play = () => {
    if (embedHtml) {
      setIsPlaying(true);
    }
  };
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "video-item"
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: `video-frame ${isPlaying ? "is-playing" : ""}`,
    style: !isPlaying && item?.poster?.url ? {
      backgroundImage: `url(${item.poster.url})`
    } : undefined,
    "data-embed": embedHtml,
    onClick: play,
    tabIndex: 0,
    role: "button",
    onKeyDown: e => {
      if ((e.key === "Enter" || e.key === " ") && embedHtml) {
        e.preventDefault();
        play();
      }
    }
  }, isPlaying ? (
  /*
   * Two separate elements rather than one with a spread, because a React
   * element may carry `dangerouslySetInnerHTML` or children, never both --
   * and `{useSandbox && <SandBox/>}` is a child even when it is `false`.
   * (`{SandBox && ...}` got away with it only because on the front end
   * SandBox is `undefined`, which React drops, while `false` it counts.
   * Caught in the editor as "Minified React error #60".)
   */
  useSandbox ? (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "video-embed-container",
    style: fillStyle
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(SandBox, {
    html: embedHtml,
    allowSameOrigin: true,
    type: "wp-has-aspect-ratio",
    title: item?.name ? `${item.name} video` : "Video testimonial"
  })) : (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "video-embed-container",
    style: fillStyle,
    dangerouslySetInnerHTML: {
      __html: embedHtml
    }
  })) : (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
    className: "video-play",
    style: accentColor ? {
      color: accentColor
    } : undefined
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_BlockIcon__WEBPACK_IMPORTED_MODULE_1__["default"], {
    icon: playIcon,
    size: 26,
    defaultColor: "currentColor",
    renderFallback: (color, box) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
      viewBox: "0 0 24 24",
      width: box,
      height: box,
      fill: color
    }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
      d: "M8 5v14l11-7z"
    }))
  }))), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: "video-meta"
  }, item?.name && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("h3", {
    className: "name"
  }, item.name), (item?.deg || item?.company) && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("p", {
    className: "deg"
  }, [item?.deg, item?.company].filter(Boolean).join(", "))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (VideoCard);

/***/ }),

/***/ "./src/shared/utils/a11y.js":
/*!**********************************!*\
  !*** ./src/shared/utils/a11y.js ***!
  \**********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   clickable: () => (/* binding */ clickable),
/* harmony export */   editorClickable: () => (/* binding */ editorClickable)
/* harmony export */ });
/**
 * Accessibility helpers for non-native interactive elements.
 *
 * Several layouts need a clickable wrapper that can't be a real <button>
 * (they contain headings, paragraphs and images, which a button may not).
 * These helpers spread the full button contract onto such elements so they
 * stay keyboard reachable and are announced correctly.
 */

/**
 * Props that turn a plain element into a keyboard-operable button.
 *
 * @param {Function} onActivate Called on click, Enter or Space.
 * @param {string}   label      Optional accessible name.
 * @return {Object} Props to spread onto the element.
 */
const clickable = (onActivate, label) => ({
  role: 'button',
  tabIndex: 0,
  onClick: onActivate,
  onKeyDown: e => {
    if ('Enter' === e.key || ' ' === e.key) {
      e.preventDefault();
      onActivate(e);
    }
  },
  ...(label ? {
    'aria-label': label
  } : {})
});

/**
 * Same as clickable(), but only while the block is rendered in the editor.
 *
 * The theme cards are selectable only in the backend, so on the frontend they
 * must stay non-interactive rather than advertising a button that does nothing.
 *
 * @param {boolean}  isBackend  Whether the block is rendering inside the editor.
 * @param {Function} onActivate Called on click, Enter or Space.
 * @param {string}   label      Optional accessible name.
 * @return {Object} Props to spread onto the element.
 */
const editorClickable = (isBackend, onActivate, label) => isBackend ? clickable(onActivate, label) : {};

/***/ }),

/***/ "./src/shared/utils/blockIcons.js":
/*!****************************************!*\
  !*** ./src/shared/utils/blockIcons.js ***!
  \****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ICON_LAYOUTS: () => (/* binding */ ICON_LAYOUTS),
/* harmony export */   getIcon: () => (/* binding */ getIcon),
/* harmony export */   hasIconControls: () => (/* binding */ hasIconControls),
/* harmony export */   resolveIconSlots: () => (/* binding */ resolveIconSlots)
/* harmony export */ });
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__);


/**
 * Which layouts expose icon controls, and what each icon slot is called.
 *
 * Third-party review-badge logos (Google, Capterra, Facebook, Trustpilot, G2)
 * are deliberately absent: they are other companies' trademarks and a swapped
 * or recoloured logo stops working as a recognisable review badge.
 *
 * Rating stars are absent too — they already have their own `starIconColor`.
 *
 * `size` is the layout's default icon box and the reset value behind the Icon
 * Size control. It is absent on trust-badges alone: that block grew a
 * block-level Icon Size of its own, and with both controls live the per-slot
 * one silently won, so there the box belongs to the block and not the slot.
 *
 * `slots` is either an array or a function of the attributes, for the layouts
 * whose icons come from a repeater -- see resolveIconSlots().
 */
const ICON_LAYOUTS = {
  "verified-buyer-badge": {
    size: 36,
    slots: [{
      key: "badge",
      label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Badge Icon", "b-testimonials-block")
    }]
  },
  "review-badge-widget": {
    size: 36,
    slots: [{
      key: "badge",
      label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Badge Icon", "b-testimonials-block")
    }]
  },
  "trust-badges": {
    // The only layout whose icons are a repeater rather than a fixed set, so
    // its slots are derived instead of listed: the Badges panel adds badges
    // without limit, and four hardcoded slots both ran out past the fourth
    // badge and offered pickers for badges that did not exist. With no badges
    // at all the block still renders its four-badge fallback, so four is the
    // right count then.
    slots: ({
      items = []
    } = {}) => (items.length ? items : Array.from({
      length: 4
    })).map((item, i) => ({
      key: `trust${i}`,
      // Names the badge, not the control: the panel shows one slot at a time
      // behind a row of these, so this is the chip and the heading above the
      // icon controls rather than a label beside one.
      label: item?.title ||
      // translators: %d is the badge's position, starting at 1.
      (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Badge %d", "b-testimonials-block"), i + 1),
      // A badge with an uploaded image never reaches its icon slot, so say so
      // rather than leaving a picker that quietly does nothing.
      note: item?.img?.url ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("This badge shows its uploaded image, so the icon below is unused until that image is removed.", "b-testimonials-block") : ""
    })),
    // Adding a badge is what creates the next slot, so the Badges panel's Add
    // button is repeated here -- otherwise picking an icon for a fifth badge
    // means leaving this panel to make the badge first.
    add: {
      attribute: "items",
      newItem: {
        img: {
          url: ""
        },
        title: "",
        subtitle: ""
      },
      label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Add New Badge", "b-testimonials-block")
    },
    // Singular noun for the chips. Same word the Badges panel numbers its own
    // cards with, so the two panels agree on what "Badge 2" means.
    itemLabel: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Badge", "b-testimonials-block")
  },
  "audio-testimonials": {
    size: 40,
    slots: [{
      key: "play",
      label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Play Icon", "b-testimonials-block")
    }]
  },
  "video-testimonials": {
    size: 26,
    slots: [{
      key: "play",
      label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Play Icon", "b-testimonials-block")
    }]
  }
};

/**
 * The icon slots a block currently has.
 *
 * @param {Object} config     An ICON_LAYOUTS entry.
 * @param {Object} attributes Block attributes.
 * @return {Array} Slots: { key, label, note }.
 */
const resolveIconSlots = (config, attributes = {}) => "function" === typeof config?.slots ? config.slots(attributes) : config?.slots || [];

/** Whether a layout has any icon to configure. */
const hasIconControls = layout => Boolean(ICON_LAYOUTS[layout]);

/** Read one icon slot off the `customIcons` attribute. */
const getIcon = (attributes = {}, slot) => (attributes?.customIcons || {})[slot] || {};

/***/ }),

/***/ "./src/shared/utils/functions.js":
/*!***************************************!*\
  !*** ./src/shared/utils/functions.js ***!
  \***************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   checkTheme: () => (/* binding */ checkTheme),
/* harmony export */   getStar: () => (/* binding */ getStar),
/* harmony export */   getVideoEmbed: () => (/* binding */ getVideoEmbed),
/* harmony export */   htmlTagsStrip: () => (/* binding */ htmlTagsStrip)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _icons__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./icons */ "./src/shared/utils/icons.js");



/**
 * Render a 0-5 star rating, supporting fractions.
 *
 * Each star is a grey base with the coloured star layered on top and clipped to
 * the portion that should be filled, so any fraction works -- 4.5, 4.3, 3.75 --
 * not just halves. Previously this compared `i <= value`, which snapped every
 * rating down to a whole star and made fractional values invisible.
 *
 * @param {number} value Rating from 0 to 5.
 * @param {string} color Fill colour for the earned portion.
 * @return {Array} Star elements.
 */
const getStar = (value, color) => {
  const score = Math.max(0, Math.min(5, parseFloat(value) || 0));
  const rating = [];
  for (let i = 1; i <= 5; i++) {
    // How much of this particular star is earned, as a percentage.
    const filled = Math.max(0, Math.min(1, score - (i - 1))) * 100;
    rating.push((0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      key: i,
      className: "btbStar"
    }, (0,_icons__WEBPACK_IMPORTED_MODULE_1__.star)('#ccc'), filled > 0 && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("span", {
      className: "btbStarFill",
      style: {
        width: `${filled}%`
      }
    }, (0,_icons__WEBPACK_IMPORTED_MODULE_1__.star)(color))));
  }
  return rating;
};
const defaultTheme = {
  padding: {
    top: "10px",
    right: "15px",
    bottom: "10px",
    left: "15px"
  },
  image: {
    width: "70",
    height: "70"
  },
  imgBorder: {
    width: "1px",
    style: "solid",
    color: "#C6C6C652",
    radius: '50%'
  }
};
const checkTheme = (val, border) => {
  if (val === 'default') {
    return defaultTheme;
  }
  if (val === 'theme_1') {
    return {
      ...defaultTheme,
      image: {
        width: "70",
        height: "70"
      },
      imgBorder: {
        width: border?.width,
        style: border?.style,
        color: border?.color,
        side: "all",
        radius: "50%"
      }
    };
  }
  if (val === 'theme_2') {
    return {
      ...defaultTheme,
      expandedColors: {
        bg: "#fbfbfb00"
      }
    };
  }
  if (val === 'theme_3') {
    return defaultTheme;
  }
  if (val === 'theme_4') {
    return {
      ...defaultTheme,
      padding: {
        top: "0px",
        right: "0px",
        bottom: "0px",
        left: "0px"
      },
      image: {
        width: "70",
        height: "70"
      },
      quoteIcon: {
        size: 23
      }
    };
  }
  if (val === 'theme_5') {
    return {
      ...defaultTheme,
      imgBorder: {
        width: "4px",
        style: "solid",
        color: "#C6C6C652",
        radius: '1px'
      },
      quoteIcon: {
        size: 15
      }
    };
  }
};
const htmlTagsStrip = str => {
  if (!str || typeof str !== 'string') return '';
  return str.replace(/(<([^>]+)>)/ig, '');
};
const getVideoEmbed = (url, options = {}) => {
  if (!url) return '';

  // Embed is only ever injected after the poster is clicked (see VideoCard),
  // so `autoplay` here means "start as soon as the click swaps it in", not
  // "play on page load" -- that click is the user gesture browsers require
  // before they'll allow audible autoplay at all.
  const {
    autoplay = true,
    loop = false,
    muted = false,
    controls = true
  } = options;
  const ytMatch = url.match(/(?:youtu\.be\/|youtube\.com\/(?:embed\/|v\/|watch\?v=|watch\?.+&v=))([\w-]{11})/);
  if (ytMatch && ytMatch[1]) {
    const videoId = ytMatch[1];
    const params = new URLSearchParams({
      autoplay: autoplay ? '1' : '0',
      controls: controls ? '1' : '0',
      mute: muted ? '1' : '0'
    });
    if (loop) {
      // YouTube only loops a single video when `playlist` repeats its own id.
      params.set('loop', '1');
      params.set('playlist', videoId);
    }
    return `<iframe src="https://www.youtube.com/embed/${videoId}?${params.toString()}" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>`;
  }
  const vimeoMatch = url.match(/vimeo\.com\/(?:video\/)?(\d+)/);
  if (vimeoMatch && vimeoMatch[1]) {
    const params = new URLSearchParams({
      autoplay: autoplay ? '1' : '0',
      controls: controls ? '1' : '0',
      muted: muted ? '1' : '0',
      loop: loop ? '1' : '0'
    });
    return `<iframe src="https://player.vimeo.com/video/${vimeoMatch[1]}?${params.toString()}" allow="autoplay; fullscreen; picture-in-picture" allowfullscreen></iframe>`;
  }
  const videoAttrs = [controls && 'controls', autoplay && 'autoplay', loop && 'loop', muted && 'muted'].filter(Boolean).join(' ');
  return `<video src="${url}" ${videoAttrs}></video>`;
};

/***/ }),

/***/ "./src/shared/utils/icons.js":
/*!***********************************!*\
  !*** ./src/shared/utils/icons.js ***!
  \***********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   BRAND_COLOR: () => (/* binding */ BRAND_COLOR),
/* harmony export */   blockIcon: () => (/* binding */ blockIcon),
/* harmony export */   getLayoutSvgIcon: () => (/* binding */ getLayoutSvgIcon),
/* harmony export */   horizontalLineIcon: () => (/* binding */ horizontalLineIcon),
/* harmony export */   leftQuote: () => (/* binding */ leftQuote),
/* harmony export */   rightQuote: () => (/* binding */ rightQuote),
/* harmony export */   star: () => (/* binding */ star),
/* harmony export */   symbol: () => (/* binding */ symbol),
/* harmony export */   upload: () => (/* binding */ upload),
/* harmony export */   verticalLineIcon: () => (/* binding */ verticalLineIcon)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);

const leftQuote = (color, size) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  fill: color,
  width: size,
  height: size,
  viewBox: "0 0 512 512"
}, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
  d: "M464 256h-80v-64c0-35.3 28.7-64 64-64h8c13.3 0 24-10.7 24-24V56c0-13.3-10.7-24-24-24h-8c-88.4 0-160 71.6-160 160v240c0 26.5 21.5 48 48 48h128c26.5 0 48-21.5 48-48V304c0-26.5-21.5-48-48-48zm-288 0H96v-64c0-35.3 28.7-64 64-64h8c13.3 0 24-10.7 24-24V56c0-13.3-10.7-24-24-24h-8C71.6 32 0 103.6 0 192v240c0 26.5 21.5 48 48 48h128c26.5 0 48-21.5 48-48V304c0-26.5-21.5-48-48-48z"
}));
const rightQuote = (color, size) => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  fill: color,
  width: size,
  height: size,
  viewBox: "0 0 512 512"
}, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
  d: "M464 32H336c-26.5 0-48 21.5-48 48v128c0 26.5 21.5 48 48 48h80v64c0 35.3-28.7 64-64 64h-8c-13.3 0-24 10.7-24 24v48c0 13.3 10.7 24 24 24h8c88.4 0 160-71.6 160-160V80c0-26.5-21.5-48-48-48zm-288 0H48C21.5 32 0 53.5 0 80v128c0 26.5 21.5 48 48 48h80v64c0 35.3-28.7 64-64 64h-8c-13.3 0-24 10.7-24 24v48c0 13.3 10.7 24 24 24h8c88.4 0 160-71.6 160-160V80c0-26.5-21.5-48-48-48z"
}));
const symbol = color => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: "20px",
  height: "20px",
  viewBox: "0 0 19 13",
  fill: color,
  style: {
    left: "24px"
  }
}, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
  d: "M0.965704 0.000125914H10.3736L19 5.15272e-05C19 5.15272e-05 16.2331 5.15665 10.3736 8.99489C6.68171 11.4132 3.12703 12.3741 1.00222 12.7541C0.488597 12.8459 0.227225 12.1436 0.617463 11.7973C2.03909 10.5355 3.88298 8.3072 3.88294 5.23718C3.88287 9.44134e-05 0.965704 0.000125914 0.965704 0.000125914Z"
}));
const upload = (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  fill: "#000000",
  width: "20px",
  height: "20px",
  viewBox: "0 0 512 512"
}, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
  d: "M232 280L64 280 64 232 232 232 232 64 280 64 280 232 448 232 448 280 280 280 280 448 232 448 232 280Z"
}));
const star = color => (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  fill: color,
  width: "15px",
  height: "15px",
  viewBox: "0 -32 576 576"
}, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
  d: "M259.3 17.8L194 150.2 47.9 171.5c-26.2 3.8-36.7 36.1-17.7 54.6l105.7 103-25 145.5c-4.5 26.3 23.2 46 46.4 33.7L288 439.6l130.7 68.7c23.2 12.2 50.9-7.4 46.4-33.7l-25-145.5 105.7-103c19-18.5 8.5-50.8-17.7-54.6L382 150.2 316.7 17.8c-11.7-23.6-45.6-23.9-57.4 0z"
}));

// bPlugins brand colour, used for the block icons and editor UI accents.
const BRAND_COLOR = '#146EF5';
const blockIconSvg = (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: "800px",
  height: "800px",
  viewBox: "0 0 20 20"
}, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
  x: "0",
  fill: "none",
  width: "20",
  height: "20"
}), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("g", null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
  d: "M4 3h12c.55 0 1.02.2 1.41.59S18 4.45 18 5v7c0 .55-.2 1.02-.59 1.41S16.55 14 16 14h-1l-5 5v-5H4c-.55 0-1.02-.2-1.41-.59S2 12.55 2 12V5c0-.55.2-1.02.59-1.41S3.45 3 4 3zm11 2H4v1h11V5zm1 3H4v1h12V8zm-3 3H4v1h9v-1z"
})));

// Generic fallback icon, and the icon for the parent container block.
// `foreground` is the block API's own way to tint an icon.
const blockIcon = {
  src: blockIconSvg,
  foreground: BRAND_COLOR
};
const verticalLineIcon = (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: 24,
  height: 24,
  viewBox: "0 0 14.707 14.707"
}, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
  x: "6.275",
  y: "0",
  width: "2.158",
  height: "14.707"
}));
const horizontalLineIcon = (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: 24,
  height: 24,
  viewBox: "0 0 357 357"
}, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
  d: "M357,204H0v-51h357V204z"
}));
const getLayoutSvgIcon = (iconName, size = 22, color = 'currentColor') => {
  switch (iconName) {
    case 'slides':
    case 'carousel':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "2",
        y: "5",
        width: "20",
        height: "14",
        rx: "2"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M8 12l4-4 4 4"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M12 8v8"
      }));
    case 'editor-ul':
    case 'list':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "8",
        y1: "6",
        x2: "21",
        y2: "6"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "8",
        y1: "12",
        x2: "21",
        y2: "12"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "8",
        y1: "18",
        x2: "21",
        y2: "18"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "3",
        y1: "6",
        x2: "3.01",
        y2: "6"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "3",
        y1: "12",
        x2: "3.01",
        y2: "12"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "3",
        y1: "18",
        x2: "3.01",
        y2: "18"
      }));
    case 'dashboard':
    case 'masonry':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "3",
        y: "3",
        width: "7",
        height: "9",
        rx: "1"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "14",
        y: "3",
        width: "7",
        height: "5",
        rx: "1"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "14",
        y: "12",
        width: "7",
        height: "9",
        rx: "1"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "3",
        y: "16",
        width: "7",
        height: "5",
        rx: "1"
      }));
    case 'update-alt':
    case 'update':
    case 'marquee':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M21.5 2v6h-6"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M21.34 15.57a10 10 0 1 1-.57-8.38l5.67-5.67"
      }));
    case 'star-filled':
    case 'star-half':
    case 'rating':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: color === 'currentColor' ? '#ff9800' : color,
        stroke: color === 'currentColor' ? '#ff9800' : color,
        strokeWidth: "1.5"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polygon", {
        points: "12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"
      }));
    case 'chart-bar':
    case 'stats':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "12",
        y1: "20",
        x2: "12",
        y2: "10"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "18",
        y1: "20",
        x2: "18",
        y2: "4"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "6",
        y1: "20",
        x2: "6",
        y2: "16"
      }));
    case 'shield':
    case 'yes-alt':
    case 'verified':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M9 12l2 2 4-4"
      }));
    case 'groups':
    case 'admin-users':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
        cx: "9",
        cy: "7",
        r: "4"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M23 21v-2a4 4 0 0 0-3-3.87"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M16 3.13a4 4 0 0 1 0 7.75"
      }));
    case 'video-alt3':
    case 'video':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polygon", {
        points: "23 7 16 12 23 17 23 7"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "1",
        y: "5",
        width: "15",
        height: "14",
        rx: "2",
        ry: "2"
      }));
    case 'image-flip-horizontal':
    case 'before-after':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "3",
        y: "3",
        width: "18",
        height: "18",
        rx: "2"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "12",
        y1: "3",
        x2: "12",
        y2: "21"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M8 10l-3 3 3 3"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M16 10l3 3-3 3"
      }));
    case 'feedback':
    case 'form':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"
      }));
    case 'align-center':
    case 'grid-view':
    case 'grid':
    case 'layout':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "3",
        y: "3",
        width: "7",
        height: "7",
        rx: "1"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "14",
        y: "3",
        width: "7",
        height: "7",
        rx: "1"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "14",
        y: "14",
        width: "7",
        height: "7",
        rx: "1"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "3",
        y: "14",
        width: "7",
        height: "7",
        rx: "1"
      }));
    case 'columns':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "3",
        y: "3",
        width: "18",
        height: "18",
        rx: "2"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "9",
        y1: "3",
        x2: "9",
        y2: "21"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "15",
        y1: "3",
        x2: "15",
        y2: "21"
      }));
    case 'excerpt-view':
    case 'compact':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "3",
        y: "4",
        width: "18",
        height: "4",
        rx: "1"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "3",
        y: "10",
        width: "18",
        height: "4",
        rx: "1"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "3",
        y: "16",
        width: "18",
        height: "4",
        rx: "1"
      }));
    case 'format-quote':
    case 'quote':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: color === 'currentColor' ? 'currentColor' : color
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M6 17h3l2-4V7H5v6h3zm8 0h3l2-4V7h-6v6h3z"
      }));
    case 'format-chat':
    case 'speech':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"
      }));
    case 'list-view':
    case 'timeline':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "12",
        y1: "2",
        x2: "12",
        y2: "22"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
        cx: "12",
        cy: "6",
        r: "2"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
        cx: "12",
        cy: "12",
        r: "2"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
        cx: "12",
        cy: "18",
        r: "2"
      }));
    case 'index-card':
    case 'card-stack':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "2",
        y: "7",
        width: "16",
        height: "14",
        rx: "2"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M6 3h14a2 2 0 0 1 2 2v12"
      }));
    case 'welcome-learn-more':
    case 'case-study':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polyline", {
        points: "14 2 14 8 20 8"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "16",
        y1: "13",
        x2: "8",
        y2: "13"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "16",
        y1: "17",
        x2: "8",
        y2: "17"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polyline", {
        points: "10 9 9 9 8 9"
      }));
    case 'google':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        fill: "#4285F4",
        d: "M23.745 12.27c0-.7-.06-1.4-.19-2.07H12v4.51h6.6c-.29 1.52-1.14 2.82-2.4 3.68v3.05h3.88c2.27-2.09 3.665-5.17 3.665-9.17z"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        fill: "#34A853",
        d: "M12 24c3.24 0 5.95-1.08 7.93-2.91l-3.88-3.05c-1.08.72-2.45 1.16-4.05 1.16-3.12 0-5.77-2.1-6.72-4.93H1.26v3.15C3.25 21.35 7.33 24 12 24z"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        fill: "#FBBC05",
        d: "M5.28 14.27c-.25-.72-.38-1.49-.38-2.27s.13-1.55.38-2.27V6.58H1.26C.46 8.18 0 9.98 0 12s.46 3.82 1.26 5.42l4.02-3.15z"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        fill: "#EA4335",
        d: "M12 4.75c1.77 0 3.35.61 4.6 1.8l3.42-3.42C17.95 1.19 15.24 0 12 0 7.33 0 3.25 2.65 1.26 6.58l4.02 3.15c.95-2.83 3.6-4.98 6.72-4.98z"
      }));
    case 'trustpilot':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "#00b67a"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polygon", {
        points: "12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"
      }));
    case 'facebook':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "#1877F2"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"
      }));
    case 'awards':
    case 'g2':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
        cx: "12",
        cy: "8",
        r: "7"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polyline", {
        points: "8.21 13.89 7 23 12 20 17 23 15.79 13.88"
      }));
    // Capterra's own mark is a set of quadrants -- distinct from the G2 medal.
    case 'capterra':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M3 3h8v8H3z"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M13 3h8v8h-8z"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M3 13h8v8H3z"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M13 13l8 8h-8z"
      }));
    // Horizontal ticker tape, so the marquee reads differently from a refresh.
    case 'marquee-scroll':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "2",
        y: "7",
        width: "9",
        height: "10",
        rx: "2"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M14 9h7"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M14 12h5"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M14 15h7"
      }));
    // Ranked bars of differing length -- a rating distribution, not a bar chart.
    case 'progress-bars':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M3 6h18"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M3 12h12"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M3 18h7"
      }));
    // Rosette seal with a tick, distinct from the plain trust shield.
    case 'verified-seal':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
        cx: "12",
        cy: "9",
        r: "6"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polyline", {
        points: "9.5 9 11.5 11 14.5 7.5"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polyline", {
        points: "8 14.5 6.5 21 12 18.5 17.5 21 16 14.5"
      }));
    // Overlapping avatars, distinct from the client-logos group icon.
    case 'avatar-list':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
        cx: "8",
        cy: "12",
        r: "4"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M14 8.5a4 4 0 0 1 0 7"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M18 9.5a3 3 0 0 1 0 5"
      }));
    // A single centred card, for the centred-cards grid.
    case 'grid-centered':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "7",
        y: "4",
        width: "10",
        height: "16",
        rx: "2"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M3 8v8"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M21 8v8"
      }));
    // Bordered card with an accent edge, for the gradient-border grid.
    case 'grid-gradient':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "3",
        y: "4",
        width: "18",
        height: "16",
        rx: "3"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M3 9h18"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M7 14h6"
      }));
    case 'sticky':
    case 'widget':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M16 3H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V8z"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polyline", {
        points: "15 3 15 9 21 9"
      }));
    case 'testimonial':
    case 'toast':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "2",
        y: "4",
        width: "20",
        height: "16",
        rx: "3"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
        cx: "8",
        cy: "12",
        r: "2"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M14 10h4"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M14 14h3"
      }));
    case 'controls-play':
    case 'audio':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polygon", {
        points: "11 5 6 9 2 9 2 15 6 15 11 19 11 5"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M15.54 8.46a5 5 0 0 1 0 7.07"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M19.07 4.93a10 10 0 0 1 0 14.14"
      }));
    case 'chart-pie':
    case 'poll':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M21.21 15.89A10 10 0 1 1 8 2.83"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M22 12A10 10 0 0 0 12 2v10z"
      }));
    case 'table-col-after':
    case 'table':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "3",
        y: "3",
        width: "18",
        height: "18",
        rx: "2"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "12",
        y1: "3",
        x2: "12",
        y2: "21"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "3",
        y1: "9",
        x2: "21",
        y2: "9"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "3",
        y1: "15",
        x2: "21",
        y2: "15"
      }));
    case 'arrow-down-alt2':
    case 'accordion':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polyline", {
        points: "6 9 12 15 18 9"
      }));
    case 'superhero':
    case 'hero':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polygon", {
        points: "12 2 2 7 12 12 22 7 12 2"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polyline", {
        points: "2 17 12 22 22 17"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polyline", {
        points: "2 12 12 17 22 12"
      }));
    case 'bubbles':
    case 'floating-bubble':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
        cx: "7.5",
        cy: "7.5",
        r: "4.5"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
        cx: "16.5",
        cy: "16.5",
        r: "4.5"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
        cx: "17.5",
        cy: "6.5",
        r: "2.5"
      }));
    case 'external':
    case 'popup-modal':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("polyline", {
        points: "15 3 21 3 21 9"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "10",
        y1: "14",
        x2: "21",
        y2: "3"
      }));
    case 'search':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
        cx: "11",
        cy: "11",
        r: "8"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "21",
        y1: "21",
        x2: "16.65",
        y2: "16.65"
      }));
    case 'close':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "18",
        y1: "6",
        x2: "6",
        y2: "18"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("line", {
        x1: "6",
        y1: "6",
        x2: "18",
        y2: "18"
      }));
    case 'settings':
    case 'admin-settings':
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("circle", {
        cx: "12",
        cy: "12",
        r: "3"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("path", {
        d: "M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"
      }));
    default:
      return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("svg", {
        width: size,
        height: size,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: color,
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "3",
        y: "3",
        width: "7",
        height: "7",
        rx: "1"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "14",
        y: "3",
        width: "7",
        height: "7",
        rx: "1"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "14",
        y: "14",
        width: "7",
        height: "7",
        rx: "1"
      }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("rect", {
        x: "3",
        y: "14",
        width: "7",
        height: "7",
        rx: "1"
      }));
  }
};

/***/ }),

/***/ "./src/shared/utils/layoutControls.js":
/*!********************************************!*\
  !*** ./src/shared/utils/layoutControls.js ***!
  \********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   BRAND_LOGO_LAYOUTS: () => (/* binding */ BRAND_LOGO_LAYOUTS),
/* harmony export */   CONTROL_CONDITIONS: () => (/* binding */ CONTROL_CONDITIONS),
/* harmony export */   LAYOUT_CONTROLS: () => (/* binding */ LAYOUT_CONTROLS),
/* harmony export */   SCORED_BADGE_LAYOUTS: () => (/* binding */ SCORED_BADGE_LAYOUTS),
/* harmony export */   SHRINK_TO_FIT_LAYOUTS: () => (/* binding */ SHRINK_TO_FIT_LAYOUTS),
/* harmony export */   TYPO_PANEL_LABELS: () => (/* binding */ TYPO_PANEL_LABELS),
/* harmony export */   getLayoutControls: () => (/* binding */ getLayoutControls)
/* harmony export */ });
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__);


/**
 * Which inspector controls each layout can actually act on.
 *
 * All 40 child blocks share one Settings component and one Style component, and
 * the sidebar was gated almost entirely on `isSingleItemBlock` -- "does this
 * layout show more than one testimonial". That is a different question from
 * "does this layout paint the thing this control sets", so the two answers
 * diverged in both directions: a timeline offered a Columns range it renders as
 * a single file, a social proof toast offered a Theme select whose attribute it
 * does not even register, every layout offered the Card panel even where its CSS
 * reaches nothing, and the card stack -- which does render themed cards -- was
 * denied the Elements and Theme controls that work on it.
 *
 * Each entry below is read off two places rather than guessed:
 *
 *   Layout.js  -- what the layout renders, and through which component. Anything
 *                 built from a Themes/* card honours the Elements toggles, the
 *                 Theme select and the inline rating colour; bespoke markup does
 *                 not.
 *   Style.js   -- which selector the control's CSS lands on. The card, image,
 *                 name, designation and review-text rules are all scoped to
 *                 `.layoutSection .single`, and the grid-gap rule to
 *                 `.layoutSection`, so a layout that builds its own wrapper gets
 *                 none of them however it renders its cards.
 *
 * A control is offered only where both agree it moves a pixel.
 */

/**
 * The card parts a Themes/* component renders, each behind its Elements toggle.
 */
const CARD_PARTS = ["img", "name", "deg", "reviewText", "icon"];

/**
 * What a layout gets for handing its items to a Themes/* component.
 *
 * These three are component-level rather than CSS-level -- the toggles drop the
 * markup, the theme picks the component, and the rating colour is written inline
 * by getStar -- so they work regardless of which wrapper the layout builds.
 */
const THEMED = {
  elements: CARD_PARTS,
  theme: true,
  ratingIcon: true
};

/**
 * What a layout gets for wrapping its cards in `.layoutSection`.
 *
 * Everything here is a Style.js rule scoped to that wrapper or to
 * `.layoutSection .single`, plus the column classes the wrapper's grid reads.
 */
const SECTIONED = {
  columns: true,
  gaps: true,
  cardBox: true,
  image: true,
  imageBorder: true,
  nameStyle: true,
  degStyle: true,
  textStyle: true,
  topStrip: true
};
const FULL = {
  ...THEMED,
  ...SECTIONED
};

/**
 * The card-level half of SECTIONED: everything Style.js writes against `.single`
 * itself, with none of the rules that need the `.layoutSection` wrapper (the
 * column tracks and the grid gap).
 */
const CARD_STYLE = {
  cardBox: true,
  image: true,
  imageBorder: true,
  nameStyle: true,
  degStyle: true,
  textStyle: true
};

/**
 * The single-widget blocks whose whole widget is `.btb-badge-card`, which is one
 * of the seven selectors the Card panel's background/padding/border/shadow rule
 * names.
 *
 * The Card panel used to be all they had, which left every font size in them a
 * stylesheet literal: the heading at 17px/700, the secondary line at 13px, the
 * score at 18px/800 and the star row at 16px, none of them reachable from the
 * sidebar. Two of those four map onto shared roles -- Style.js names
 * `.btb-badge-title` alongside the other Name parts and `.btb-badge-desc` /
 * `.btb-badge-rating .count` alongside the Designation ones -- so those two
 * panels are offered here. The score and the stars have no shared role and get
 * their own panel instead.
 *
 * No `textStyle`: nothing in a badge is a review paragraph, so the Review Text
 * panel would open onto a selector list that names none of this markup.
 */
const BADGE = {
  cardBox: true,
  nameStyle: true,
  degStyle: true
};
const LAYOUT_CONTROLS = {
  // -- Themed cards in `.layoutSection` -------------------------------------
  // The five interchangeable arrangements, the slider variants, and the three
  // CSS-only variations of the default card list.
  default: FULL,
  list: FULL,
  masonry: FULL,
  marquee: FULL,
  slider: FULL,
  "slider-3d": FULL,
  coverflow: FULL,
  "testimonials-quote-box": FULL,
  "testimonials-speech-bubble": FULL,
  "testimonials-compact": FULL,
  // Both build their own wrapper but keep `layoutSection` and the theme class
  // on it, so they read every rule the arrangements do.
  "audio-testimonials": FULL,
  "testimonials-popup-modal": FULL,
  // -- Themed cards in a bespoke wrapper ------------------------------------
  // `.btb-timeline-card`, `.btb-hero-card` and `.btb-stacked-card` hold a real
  // `.single` with no `.layoutSection` above it. That used to cost them the
  // Card, Image, Name, Designation and Review Text panels, because every rule
  // behind those panels was scoped to `.layoutSection .single` and so reached
  // nothing here -- the hero card's avatar in particular ignored the Image
  // width and height entirely and rendered at its natural size.
  //
  // Style.js names `.single` on its own now, so all five reach these three. The
  // grid-gap rule is still scoped to `.layoutSection`, which is why `gaps` stays
  // off.
  "testimonials-timeline": {
    ...THEMED,
    ...CARD_STYLE
  },
  // The secondary grid below the hero card carries the `columns-*` classes,
  // which are global rather than scoped.
  "testimonials-hero": {
    ...THEMED,
    ...CARD_STYLE,
    columns: true
  },
  "testimonials-card-stack": {
    ...THEMED,
    ...CARD_STYLE
  },
  // -- Bespoke markup ------------------------------------------------------
  // Renders its own `.btb-avatar-*` parts, which Style.js names alongside the
  // `.single` ones for exactly this reason, and `.btb-avatar-list-wrapper` is
  // in the Card rule. Its three parts are always rendered, so no toggle for
  // them would do anything.
  // Every entry below gained controls when Style.js stopped naming only
  // `.single .name` and friends and started naming each layout's own classes as
  // well. Before that the stylesheet's hardcoded font sizes and avatar sizes
  // were the only values in play, which is what made the panels look dead; those
  // values now live in each block's attribute defaults, so an untouched block
  // renders as it always did and the control owns the property from there.
  //
  // None of the four below sets `imageBorder`. Avatar *size* reaches their own
  // classes, but the border rule is still scoped to `.single .img`, so the Image
  // panel's Border control moved nothing on them -- measured on each. Widening
  // that rule would repaint rings these layouts define themselves (the avatar
  // list's `--btb-border` ring, the bubble's accent ring), so the control is
  // hidden rather than left half-working.
  "testimonials-avatar-list": {
    cardBox: true,
    image: true,
    nameStyle: true,
    degStyle: true,
    textStyle: true
  },
  // `.btb-toast-text` and `.btb-toast-meta` are the message and its byline, and
  // `.btb-toast-avatar` the photo beside them.
  "social-proof-toast": {
    cardBox: true,
    image: true,
    nameStyle: true,
    degStyle: true
  },
  // Title, per-star label and count map onto Name, Review Text and Designation
  // -- the same three roles the Colors panel already paints them with.
  "star-rating-bars": {
    cardBox: true,
    nameStyle: true,
    degStyle: true,
    textStyle: true
  },
  // `.btb-case-study-grid` takes its track count from the global `columns-*`
  // classes, but its gap is the stylesheet's own.
  "case-study-card": {
    columns: true,
    cardBox: true,
    image: true,
    nameStyle: true,
    degStyle: true,
    textStyle: true
  },
  // These three print the review text through `itemsEls`, so that one toggle
  // -- and the excerpt controls with it -- is honoured. Everything else in
  // their markup is unconditional.
  //
  // The bubble's name is accent-coloured by design, so it takes the Name
  // typography without the Name colour; no Designation part is rendered.
  //
  // `gaps` is on despite the missing `.layoutSection`: the bubbles sit in a
  // wrapping flex row spaced by a gap, so both controls have something real to
  // move, and Style.js names that wrapper directly. The block's own gap
  // defaults were lowered to the 20px the stylesheet had been using, so an
  // untouched block renders exactly as it did before the control existed.
  "testimonials-floating-bubble": {
    elements: ["reviewText"],
    gaps: true,
    image: true,
    nameStyle: true,
    textStyle: true
  },
  // Table title is the Name role, the review cells the Review Text role. The
  // column headers keep the stylesheet's own styling -- they are chrome rather
  // than a card part.
  "comparison-testimonial-table": {
    elements: ["reviewText"],
    cardBox: true,
    nameStyle: true,
    textStyle: true
  },
  // Question / answer / author map onto Name, Review Text and Designation.
  //
  // No Card panel: `.btb-faq-item` is where it would land, and the stylesheet
  // recolours that border on `[open]` to mark the expanded row. An ID-scoped
  // border from the Card panel outranks the `[open]` rule, so offering it would
  // trade the open-state indicator for a box control.
  "faq-testimonial-accordion": {
    elements: ["reviewText"],
    nameStyle: true,
    degStyle: true,
    textStyle: true
  },
  // Nothing in the sidebar's shared panels reaches the poll.
  "user-feedback-poll": {},
  "google-review-badge": BADGE,
  "capterra-review-badge": BADGE,
  "facebook-review-badge": BADGE,
  "trustpilot-review-badge": BADGE,
  "g2-review-badge": BADGE,
  "verified-buyer-badge": BADGE,
  "review-badge-widget": BADGE,
  // -- The blocks that ship their own editor --------------------------------
  // These do not render the shared Settings tab, so nothing reads the entries
  // below today. They are listed so the registry stays a complete answer to
  // "what can this layout be styled with", and so a block moved onto the
  // shared panels later starts from a checked answer rather than the FULL
  // fallback.
  "before-after": {},
  "rating-summary": {},
  "testimonial-form": {},
  "client-logos": {
    columns: true,
    gaps: true
  },
  "video-testimonials": {
    columns: true,
    gaps: true
  },
  "testimonial-stats": {
    columns: true,
    gaps: true,
    cardBox: true
  },
  "trust-badges": {
    columns: true,
    gaps: true,
    cardBox: true
  }
};

/**
 * Controls whose target only exists at certain item counts.
 *
 * now. The hero spotlight is the case: it spends the first testimonial on the
 * A layout can support a control and still have nothing for it to act on right
 * big card, so Columns only lays out the row beneath, and that row is clamped to
 * the number of cards in it so a four-column setting cannot leave three tracks
 * empty. Measured across item counts:
 *
 *   1 testimonial  -> no follower row is rendered at all; Columns is inert
 *   2              -> one follower, clamped to one track; Columns is inert
 *   3              -> two followers; Columns moves between 1 and 2
 *   5              -> four followers; Columns moves between 1 and 4
 *
 * So the control is offered from three testimonials up, which is the first count
 * at which changing it changes the page.
 *
 * Each predicate takes the block's attributes and returns whether the control
 * has something to act on.
 */
const CONTROL_CONDITIONS = {
  "testimonials-hero": {
    columns: attributes => (attributes?.items?.length || 0) > 2
  }
};

/**
 * Per-layout titles for the shared typography panels.
 *
 * The Name, Designation and Review Text panels are three roles shared by forty
 * blocks, so their default titles have to describe a testimonial card. On the
 * review badges they do not: the Name panel styles a heading like "Google
 * Reviews", and the Designation panel a review count or a line of description.
 * Offering the control under the card's name would leave an author guessing
 * which of the two lines in front of them it moves.
 *
 * Only the badges are listed. Every other layout keeps the default titles, so
 * this renames nothing that reads correctly today.
 *
 * @see ROLE_LABELS in visualControls.js, which does the same for the Colors
 *      panel's per-role labels.
 */
const BADGE_TITLE_LABEL = (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Badge Title", "b-testimonials-block");
const REVIEW_COUNT_LABEL = (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Review Count", "b-testimonials-block");
const TYPO_PANEL_LABELS = {
  "google-review-badge": {
    name: BADGE_TITLE_LABEL,
    deg: REVIEW_COUNT_LABEL
  },
  "capterra-review-badge": {
    name: BADGE_TITLE_LABEL,
    deg: REVIEW_COUNT_LABEL
  },
  "facebook-review-badge": {
    name: BADGE_TITLE_LABEL,
    deg: REVIEW_COUNT_LABEL
  },
  "trustpilot-review-badge": {
    name: BADGE_TITLE_LABEL,
    deg: REVIEW_COUNT_LABEL
  },
  "g2-review-badge": {
    name: BADGE_TITLE_LABEL,
    deg: REVIEW_COUNT_LABEL
  },
  "review-badge-widget": {
    name: BADGE_TITLE_LABEL,
    deg: REVIEW_COUNT_LABEL
  },
  // The only badge that renders a sentence rather than a count.
  "verified-buyer-badge": {
    name: BADGE_TITLE_LABEL,
    deg: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Description", "b-testimonials-block")
  }
};

/**
 * The badges that render a score and a star row, and so have something for the
 * Badge Score panel to act on.
 *
 * The Verified Buyer seal is the one that does not -- it renders a heading and a
 * line of description and nothing else -- so it is absent rather than shown a
 * panel that moves nothing.
 */
const SCORED_BADGE_LAYOUTS = ["google-review-badge", "capterra-review-badge", "facebook-review-badge", "trustpilot-review-badge", "g2-review-badge", "review-badge-widget"];

/**
 * The badges whose logo is a fixed third-party mark.
 *
 * Google, Capterra, Facebook, Trustpilot and G2 draw their own wordmark or
 * glyph, and blockIcons.js deliberately keeps them out of the icon controls:
 * they are other companies' trademarks, and a swapped or recoloured logo stops
 * working as a recognisable review badge.
 *
 * Size is not that. Making the mark bigger or smaller leaves it the same mark,
 * and it was the one thing about these five with no control at all -- a 36px
 * literal in Layout.js, whatever the badge sat next to. The other two badges are
 * absent because their logo is a real icon slot, so its size belongs to the Icon
 * panel and a second control here would set the same pixel twice.
 */
/**
 * The layouts whose widget is narrower than the column it sits in.
 *
 * A review badge paints around 280px and the toast 380px, both shrink-to-fit --
 * `.btb-badge-card` is `display: inline-flex` and `.btb-toast-card` carries a
 * 380px max-width -- so in a full-width column they sit against the left edge
 * with empty space beside them, and nothing in the sidebar moved them. Every
 * other layout is a grid, a slider or a card that fills its column, where an
 * alignment control would be a control that changes nothing.
 *
 * The same eight the demo previews centre inside their stage, measured there
 * across all forty blocks -- see `bpbtb_demo_compact_blocks()` in
 * includes/demo-preview.php. Kept as two lists because one is PHP and one is
 * JS, not because they answer different questions.
 */
const SHRINK_TO_FIT_LAYOUTS = ["google-review-badge", "capterra-review-badge", "g2-review-badge", "trustpilot-review-badge", "facebook-review-badge", "verified-buyer-badge", "review-badge-widget", "social-proof-toast"];
const BRAND_LOGO_LAYOUTS = ["google-review-badge", "capterra-review-badge", "facebook-review-badge", "trustpilot-review-badge", "g2-review-badge"];
const NONE = {
  elements: [],
  theme: false,
  ratingIcon: false,
  columns: false,
  gaps: false,
  cardBox: false,
  image: false,
  imageBorder: false,
  nameStyle: false,
  degStyle: false,
  textStyle: false,
  topStrip: false
};

/**
 * The controls a layout supports.
 *
 * An unlisted layout falls back to the full set: that is what the classic parent
 * block and the `single` / `testimonials-single` layouts are, and it keeps a
 * newly added layout visible rather than silently stripped.
 *
 * @param {string} layout     Current layout name.
 * @param {Object} attributes Block attributes, for the item-count conditions in
 *                            CONTROL_CONDITIONS. Omit to skip those checks.
 * @return {Object} One flag per control group, plus `elements` as the list of
 *                  card parts whose toggle is honoured.
 */
const getLayoutControls = (layout, attributes) => {
  const controls = {
    ...NONE,
    ...(LAYOUT_CONTROLS[layout] || FULL)
  };
  if (!attributes) {
    return controls;
  }
  for (const [name, condition] of Object.entries(CONTROL_CONDITIONS[layout] || {})) {
    if (controls[name] && !condition(attributes)) {
      controls[name] = false;
    }
  }
  return controls;
};

/***/ }),

/***/ "./src/shared/utils/layoutFeatures.js":
/*!********************************************!*\
  !*** ./src/shared/utils/layoutFeatures.js ***!
  \********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ARRANGEABLE_LAYOUTS: () => (/* binding */ ARRANGEABLE_LAYOUTS),
/* harmony export */   ARRANGEMENTS: () => (/* binding */ ARRANGEMENTS),
/* harmony export */   NO_REVIEW_TEXT_LAYOUTS: () => (/* binding */ NO_REVIEW_TEXT_LAYOUTS),
/* harmony export */   rendersReviewText: () => (/* binding */ rendersReviewText),
/* harmony export */   resolveArrangement: () => (/* binding */ resolveArrangement),
/* harmony export */   supportsArrangement: () => (/* binding */ supportsArrangement)
/* harmony export */ });
/**
 * Which layouts render which parts, for gating the inspector.
 *
 * The excerpt and Expand/Less controls used to hang off `isSingleItemBlock`,
 * which is a different question -- it asks how many testimonials a layout
 * shows, not whether it prints their review text. The two sets nearly match,
 * and the gap went both ways: three layouts that do print review text hid the
 * controls, and three that print none offered them.
 */

/**
 * Layouts an excerpt cut and an Expand toggle cannot act on.
 *
 * Most render no review text at all. The last two do render it, but not as a
 * review paragraph: the avatar list styles its own `.btb-avatar-review` with a
 * separate editor path, and the case study only falls back to `reviewText` as
 * the body of its "Solution" section. Truncating either would be wrong, so they
 * stay out rather than showing a control that does nothing.
 */
const NO_REVIEW_TEXT_LAYOUTS = ["google-review-badge", "capterra-review-badge", "facebook-review-badge", "trustpilot-review-badge", "g2-review-badge", "verified-buyer-badge", "review-badge-widget", "trust-badges", "testimonial-form", "user-feedback-poll", "rating-summary", "star-rating-bars", "testimonial-stats", "social-proof-toast", "video-testimonials", "before-after", "client-logos", "testimonials-avatar-list", "case-study-card"];

/**
 * Whether a layout prints the review text.
 *
 * Everything not listed above either renders it directly or hands the item to
 * a theme component that does, including the classic layouts that fall through
 * Layout.js without a branch of their own.
 *
 * @param {string} layout Current layout name.
 * @return {boolean} True when the layout renders review text.
 */
const rendersReviewText = layout => !NO_REVIEW_TEXT_LAYOUTS.includes(layout);

/**
 * The five interchangeable arrangements of a card list.
 */
const ARRANGEMENTS = ["default", "slider", "masonry", "list", "marquee"];

/**
 * Layouts that can be arranged.
 *
 * `layout` used to carry two different things: which branch renders, and the
 * wrapper class that produces the visual style. That is why the old Layout
 * select was destructive -- choosing "Marquee" on a quote box swapped its
 * identity class away along with the arrangement. `arrangement` now carries the
 * second job on its own.
 *
 * Only layouts that reach the arrangement switch belong here. The rest (audio,
 * timeline, hero, popup, avatar list, case study, floating bubble, client logos,
 * video, toast and the single-item badges) return their own markup first, so an
 * arrangement could not apply to them without rewriting each renderer.
 */
const ARRANGEABLE_LAYOUTS = [...ARRANGEMENTS,
// Slider variants: an identity and an arrangement at once.
"slider-3d", "coverflow",
// CSS-only variations of the default card list.
"testimonials-quote-box", "testimonials-speech-bubble", "testimonials-compact"];
const supportsArrangement = layout => ARRANGEABLE_LAYOUTS.includes(layout);

/**
 * The arrangement to render.
 *
 * Falls back to `layout` when that already names an arrangement, so posts saved
 * before this attribute existed keep rendering exactly as they did.
 *
 * @param {Object} attributes Block attributes.
 * @return {string} One of ARRANGEMENTS, or a slider variant.
 */
const resolveArrangement = (attributes = {}) => {
  const {
    arrangement,
    layout = "default"
  } = attributes;
  if (arrangement) {
    return arrangement;
  }
  return ARRANGEMENTS.includes(layout) || "slider-3d" === layout || "coverflow" === layout ? layout : "default";
};

/***/ }),

/***/ "./src/shared/utils/responsiveBox.js":
/*!*******************************************!*\
  !*** ./src/shared/utils/responsiveBox.js ***!
  \*******************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   boxForDevice: () => (/* binding */ boxForDevice),
/* harmony export */   ownBoxForDevice: () => (/* binding */ ownBoxForDevice),
/* harmony export */   setBoxForDevice: () => (/* binding */ setBoxForDevice),
/* harmony export */   toPerDevice: () => (/* binding */ toPerDevice)
/* harmony export */ });
/**
 * Per-device box values, for the spacing controls.
 *
 * Block Width and Card Height have always been `{ desktop, tablet, mobile }`,
 * but Padding, Block Margin and Card Margin were one flat
 * `{ top, right, bottom, left }` for every screen -- so the padding that suits a
 * three-column desktop grid was the same padding a phone got, with no way to
 * trim it. These helpers give the three box controls the same per-device shape
 * without breaking the blocks already saved with the flat one.
 *
 * A stored value is one of:
 *   { top, right, bottom, left }                     -- every device (old)
 *   { desktop: {...}, tablet: {...}, mobile: {...} } -- per device (new)
 *
 * Old values are read as the desktop entry, so an existing block keeps rendering
 * exactly as it did and only gains tablet and mobile once someone sets them.
 */

const DEVICES = ["desktop", "tablet", "mobile"];

/**
 * Whether a stored value is already in the per-device shape.
 *
 * Checked by key rather than by depth: a flat box carries only side names, so
 * the two shapes can never be confused.
 *
 * @param {Object} value Stored attribute value.
 * @return {boolean} True when the value is keyed by device.
 */
const isPerDevice = value => !!value && "object" === typeof value && DEVICES.some(device => device in value);

/**
 * The stored value as a `{ desktop, tablet, mobile }` map.
 *
 * @param {Object} value Stored attribute value.
 * @return {Object} Map of device to box.
 */
const toPerDevice = value => {
  if (!value || "object" !== typeof value) {
    return {};
  }
  return isPerDevice(value) ? value : {
    desktop: value
  };
};

/**
 * The box a device renders with, following the inheritance the media queries
 * apply: mobile falls back to tablet, tablet to desktop.
 *
 * Use for the control's displayed value, so a device with nothing of its own
 * shows what it actually inherits rather than an empty field.
 *
 * @param {Object} value  Stored attribute value.
 * @param {string} device 'desktop', 'tablet' or 'mobile'.
 * @return {Object} The box for that device.
 */
const boxForDevice = (value, device) => {
  var _perDevice$desktop;
  const perDevice = toPerDevice(value);
  if ("mobile" === device) {
    var _ref, _ref2, _perDevice$mobile;
    return (_ref = (_ref2 = (_perDevice$mobile = perDevice.mobile) !== null && _perDevice$mobile !== void 0 ? _perDevice$mobile : perDevice.tablet) !== null && _ref2 !== void 0 ? _ref2 : perDevice.desktop) !== null && _ref !== void 0 ? _ref : {};
  }
  if ("tablet" === device) {
    var _ref3, _perDevice$tablet;
    return (_ref3 = (_perDevice$tablet = perDevice.tablet) !== null && _perDevice$tablet !== void 0 ? _perDevice$tablet : perDevice.desktop) !== null && _ref3 !== void 0 ? _ref3 : {};
  }
  return (_perDevice$desktop = perDevice.desktop) !== null && _perDevice$desktop !== void 0 ? _perDevice$desktop : {};
};

/**
 * The box a device has set *of its own*, with no inheritance.
 *
 * Use when emitting CSS: a tablet value that merely repeats the desktop one
 * would fill the media query with declarations that change nothing, and worse,
 * would freeze the desktop value in place for anyone who later edits it.
 *
 * @param {Object} value  Stored attribute value.
 * @param {string} device 'desktop', 'tablet' or 'mobile'.
 * @return {Object|undefined} The box, or undefined when that device sets none.
 */
const ownBoxForDevice = (value, device) => toPerDevice(value)[device];

/**
 * The stored value with one device's box replaced.
 *
 * Upgrades a flat value on the way, carrying it over as the desktop entry so
 * editing tablet on an existing block does not drop what desktop already had.
 *
 * @param {Object} value  Stored attribute value.
 * @param {string} device 'desktop', 'tablet' or 'mobile'.
 * @param {Object} box    The new box for that device.
 * @return {Object} The value to store.
 */
const setBoxForDevice = (value, device, box) => ({
  ...toPerDevice(value),
  [device]: box
});

/***/ }),

/***/ "./src/shared/utils/trustBadgeArt.js":
/*!*******************************************!*\
  !*** ./src/shared/utils/trustBadgeArt.js ***!
  \*******************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   TRUST_BADGE_ART: () => (/* binding */ TRUST_BADGE_ART),
/* harmony export */   getTrustBadgeArt: () => (/* binding */ getTrustBadgeArt)
/* harmony export */ });
/* harmony import */ var _icons__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./icons */ "./src/shared/utils/icons.js");


/**
 * The built-in artwork behind each Trust Badges slot.
 *
 * A badge with no uploaded image and no icon picked falls back to one of these,
 * chosen by its position: a shield, a tick, a star and a verified circle.
 *
 * Shared because the block draws itself twice. Layout.js renders the published
 * page, `src/blocks/trust-badges/edit.js` renders the editor preview, and only
 * the first had this list -- the editor drew the shield for every badge, in the
 * brand colour, whatever position it sat in. So a three-badge block showed three
 * identical blue shields while editing and a shield, a tick and an amber star
 * once published.
 *
 * The star keeps its own amber; the rest use the brand colour.
 */
const TRUST_BADGE_ART = [{
  slot: "trust0",
  color: _icons__WEBPACK_IMPORTED_MODULE_0__.BRAND_COLOR,
  d: "M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z"
}, {
  slot: "trust1",
  color: _icons__WEBPACK_IMPORTED_MODULE_0__.BRAND_COLOR,
  d: "M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"
}, {
  slot: "trust2",
  color: "#FF9D28",
  d: "M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z"
}, {
  slot: "trust3",
  color: _icons__WEBPACK_IMPORTED_MODULE_0__.BRAND_COLOR,
  d: "M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"
}];

/**
 * The artwork for one badge position.
 *
 * There are four drawings and no limit on badges, so a fifth badge and beyond
 * repeats the first rather than rendering nothing -- which is what the page has
 * always done.
 *
 * @param {number} index Badge position, from 0.
 * @return {{slot: string, color: string, d: string}} Slot key, colour and path.
 */
const getTrustBadgeArt = index => TRUST_BADGE_ART[index] || TRUST_BADGE_ART[0];

/***/ }),

/***/ "./src/shared/utils/visualControls.js":
/*!********************************************!*\
  !*** ./src/shared/utils/visualControls.js ***!
  \********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   LAYOUT_ROLES: () => (/* binding */ LAYOUT_ROLES),
/* harmony export */   PALETTE_ALIASES: () => (/* binding */ PALETTE_ALIASES),
/* harmony export */   ROLES: () => (/* binding */ ROLES),
/* harmony export */   ROLE_CONDITIONS: () => (/* binding */ ROLE_CONDITIONS),
/* harmony export */   ROLE_LABELS: () => (/* binding */ ROLE_LABELS),
/* harmony export */   getPaletteCSS: () => (/* binding */ getPaletteCSS),
/* harmony export */   getVisualControls: () => (/* binding */ getVisualControls)
/* harmony export */ });
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__);


/**
 * Registry of the visual roles each layout paints with.
 *
 * The bespoke layouts (badges, stats, timeline, card stack, ...) were styled
 * with fixed hex values in SCSS, so nothing they render could be recoloured
 * from the inspector -- the Style tab only ever reached the classic card, name,
 * designation and review text. Rather than one control per declaration, the
 * stylesheets reuse the same handful of roles, so each role becomes a single
 * CSS custom property that both the inspector and the stylesheets agree on.
 *
 * Every value stays optional. Style.js only emits a custom property once the
 * author actually picks one, and each SCSS declaration keeps its original
 * literal as the var() fallback, so an untouched block renders exactly as
 * before.
 */

const ROLES = {
  brandColor: {
    cssVar: '--btb-accent',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Accent', 'b-testimonials-block')
  },
  surfaceColor: {
    cssVar: '--btb-surface',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Card Surface', 'b-testimonials-block')
  },
  // Outlines and separators were one role, so restyling a card's outline also
  // restyled every table rule and section divider. They are independent now,
  // each with its own width.
  borderColor: {
    cssVar: '--btb-border',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Border Color', 'b-testimonials-block')
  },
  borderWidth: {
    cssVar: '--btb-border-width',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Border Width', 'b-testimonials-block'),
    type: 'width'
  },
  dividerColor: {
    cssVar: '--btb-divider',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Divider Color', 'b-testimonials-block')
  },
  dividerWidth: {
    cssVar: '--btb-divider-width',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Divider Width', 'b-testimonials-block'),
    type: 'width'
  },
  titleColor: {
    cssVar: '--btb-title',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Headings', 'b-testimonials-block')
  },
  bodyColor: {
    cssVar: '--btb-body',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Body Text', 'b-testimonials-block')
  },
  mutedColor: {
    cssVar: '--btb-muted',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Secondary Text', 'b-testimonials-block')
  },
  // Separate from the classic `starIconColor`, which the Review Text panel
  // owns and which these single-item layouts never show a control for.
  ratingColor: {
    cssVar: '--btb-star',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Rating Stars', 'b-testimonials-block')
  },
  trackColor: {
    cssVar: '--btb-track',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Bars & Tracks', 'b-testimonials-block')
  },
  successColor: {
    cssVar: '--btb-success',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Thank-you Message', 'b-testimonials-block')
  },
  // The testimonial form's own result banners. form.scss has always read these
  // four custom properties, and the block has always declared the attributes,
  // but no layout claimed the roles -- so the banners were stuck on their
  // literals and three of the attributes were unreachable dead weight.
  successBg: {
    cssVar: '--btb-success-bg',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Thank-you Background', 'b-testimonials-block')
  },
  errorColor: {
    cssVar: '--btb-error',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Error Message', 'b-testimonials-block')
  },
  errorBg: {
    cssVar: '--btb-error-bg',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Error Background', 'b-testimonials-block')
  },
  // The before/after slider's drag handle already follows `accentColor`; only
  // its two corner labels were stuck on a fixed translucent black.
  labelBgColor: {
    cssVar: '--btb-ba-label-bg',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Label Background', 'b-testimonials-block')
  },
  labelTextColor: {
    cssVar: '--btb-ba-label-color',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Label Text', 'b-testimonials-block')
  },
  gripIconColor: {
    cssVar: '--btb-ba-grip',
    label: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Handle Arrow', 'b-testimonials-block')
  }
};

/**
 * The roles each layout's stylesheet actually reads.
 *
 * Derived from the compiled selectors rather than guessed: a layout is offered
 * a control only where it paints something. Showing all of them everywhere
 * would recreate the exact problem this registry exists to fix -- a video block
 * has no headings, and a client-logo strip has no rating stars.
 *
 * The last four have no branch in Layout.js (they are CSS-only variations of
 * the default rendering), so their roles come from their own stylesheet block.
 */
const LAYOUT_ROLES = {
  // Each role below was checked in the browser rather than read off the source:
  // with the block on a page, every CSS rule matching anything inside it was
  // scanned for the `var(--btb-*)` it references. A role no rule asks for is a
  // control that cannot move a pixel, so it is not offered -- see the removals
  // noted through this list, and ROLE_CONDITIONS for the ones that depend on
  // what the block is currently rendering.
  'google-review-badge': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'mutedColor', 'ratingColor'],
  'capterra-review-badge': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'mutedColor', 'ratingColor'],
  'facebook-review-badge': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'mutedColor', 'ratingColor'],
  'trustpilot-review-badge': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'mutedColor', 'ratingColor'],
  'g2-review-badge': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'mutedColor', 'ratingColor'],
  // No Rating Stars: this badge is a tick and a line of text, with no stars for
  // the role to colour.
  'verified-buyer-badge': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'mutedColor'],
  'review-badge-widget': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'mutedColor', 'ratingColor'],
  // Secondary Text was missing while trust-badges.scss has always painted the
  // badge subtitle `var(--btb-muted, #7b7b7b)` -- so the variable was declared
  // by nothing and the subtitle colour could not be changed at all. The
  // attribute was already in block.json; only the role was absent.
  'trust-badges': ['surfaceColor', 'borderColor', 'borderWidth', 'bodyColor', 'mutedColor'],
  // No stars here -- the rating is a select -- so Rating Stars is not offered.
  'testimonial-form': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'successColor', 'successBg', 'errorColor', 'errorBg'],
  'user-feedback-poll': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'mutedColor', 'trackColor', 'successColor'],
  'rating-summary': ['surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'bodyColor', 'mutedColor', 'ratingColor', 'trackColor'],
  'star-rating-bars': ['surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'bodyColor', 'mutedColor', 'ratingColor', 'trackColor'],
  'testimonial-stats': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'mutedColor'],
  'social-proof-toast': ['surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'mutedColor'],
  // No Accent: nothing in the table's stylesheet reads `--btb-accent`. Its
  // section colour comes from Headings, its rules from Divider.
  'comparison-testimonial-table': ['surfaceColor', 'borderColor', 'borderWidth', 'dividerColor', 'dividerWidth', 'titleColor', 'bodyColor', 'ratingColor', 'trackColor'],
  'faq-testimonial-accordion': ['brandColor', 'borderColor', 'borderWidth', 'titleColor', 'bodyColor', 'mutedColor'],
  'testimonials-avatar-list': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'bodyColor', 'mutedColor'],
  'testimonials-timeline': ['brandColor', 'borderColor', 'borderWidth', 'trackColor'],
  'audio-testimonials': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'dividerColor', 'dividerWidth'],
  'video-testimonials': ['surfaceColor', 'mutedColor'],
  'before-after': ['labelBgColor', 'labelTextColor', 'gripIconColor'],
  'case-study-card': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'dividerColor', 'dividerWidth', 'titleColor', 'bodyColor', 'mutedColor'],
  'client-logos': ['borderColor', 'borderWidth', 'trackColor'],
  // No Accent: the hero is a themed card in a plain wrapper and its stylesheet
  // reads no `--btb-accent`.
  'testimonials-hero': ['borderColor', 'borderWidth'],
  // The popup modal used to sit here with the hero, on the grounds that "the
  // popup's modal is styled inline rather than through the palette". That was
  // true and is the reason the block shipped with an unstyleable popup: every
  // value was an inline style, which outranks the palette variables entirely.
  //
  // The modal is CSS now and its five parts read `--btb-surface`, `--btb-title`,
  // `--btb-body`, `--btb-muted` and `--btb-star`, so those roles reach inside
  // the popup -- which is what stops a dark palette from opening a white popup
  // out of a dark trigger card. Each one was checked against the declaration
  // that consumes it in frontend.scss, not assumed.
  'testimonials-popup-modal': ['surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'bodyColor', 'mutedColor', 'ratingColor'],
  // No Border Color or Width: the bubble's avatar ring is a fixed 2px in the
  // accent colour, and the bubble itself has no border at all.
  'testimonials-floating-bubble': ['brandColor', 'bodyColor', 'trackColor'],
  // Accent and Bars & Tracks paint the prev/next buttons and the dots, which
  // only exist with more than one review -- see ROLE_CONDITIONS.
  'testimonials-card-stack': ['brandColor', 'surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'mutedColor', 'trackColor'],
  'testimonials-quote-box': ['brandColor', 'surfaceColor', 'trackColor'],
  'testimonials-speech-bubble': ['surfaceColor', 'borderColor', 'borderWidth'],
  'testimonials-compact': ['surfaceColor', 'borderColor', 'borderWidth', 'titleColor', 'bodyColor', 'mutedColor', 'ratingColor'],
  'slider-3d': ['brandColor']
};

/**
 * Roles a block already has its own control for, mapped to that control's
 * attribute.
 *
 * These three blocks ship a bespoke editor with, for example, a "Star color"
 * picker, but nothing carried that value through to the front end -- the
 * attribute only ever styled the editor preview, so the published page kept the
 * stylesheet's literal. Pointing the role at the existing attribute fixes that
 * without adding a second control for the same pixel.
 */
/**
 * Per-layout label overrides.
 *
 * A role is one CSS custom property shared by every stylesheet, so its default
 * label has to be generic -- but what it paints is not. On the video block
 * `--btb-surface` is the round play button and nothing else; calling that
 * control "Card Surface" describes a card the layout never draws. The same
 * happens to `--btb-track`, which is a bar track on the rating widgets but a
 * logo tile, a table header row, a chat bubble or a row of nav dots elsewhere.
 *
 * Each override below was read off the declaration that consumes the variable,
 * quoted beside it, so the label and the pixel cannot drift apart.
 */
const ROLE_LABELS = {
  // video.scss: `.video-play { background: var(--btb-surface, #fff) }`
  'video-testimonials': {
    surfaceColor: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Play Button', 'b-testimonials-block')
  },
  // logos.scss: `.logo-item { background: var(--btb-track, transparent) }`
  'client-logos': {
    trackColor: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Logo Tile Background', 'b-testimonials-block')
  },
  // frontend.scss: `.btb-ct-table th { background: var(--btb-track, #f8fafc) }`
  'comparison-testimonial-table': {
    trackColor: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Header Row', 'b-testimonials-block')
  },
  // frontend.scss: `.btb-bubble-content { background: var(--btb-track, #f1f5f9) }`
  'testimonials-floating-bubble': {
    trackColor: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Bubble Background', 'b-testimonials-block')
  },
  // frontend.scss: `.btb-stack-dot { background: var(--btb-track) }`, and the
  // accent covers both the active dot and the prev/next buttons on hover.
  'testimonials-card-stack': {
    trackColor: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Nav Dots', 'b-testimonials-block'),
    brandColor: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Nav Buttons & Active Dot', 'b-testimonials-block')
  },
  // frontend.scss: the card is
  // `linear-gradient(135deg, var(--btb-surface) 0%, var(--btb-track) 100%)`
  // with `border-left: 5px solid var(--btb-accent)`.
  'testimonials-quote-box': {
    surfaceColor: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Card Gradient Start', 'b-testimonials-block'),
    trackColor: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Card Gradient End', 'b-testimonials-block'),
    brandColor: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Left Bar', 'b-testimonials-block')
  },
  // frontend.scss: the rail is
  // `linear-gradient(180deg, var(--btb-accent) 0%, var(--btb-track) 100%)`,
  // and the accent also fills the dots along it.
  'testimonials-timeline': {
    trackColor: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Timeline Rail End', 'b-testimonials-block'),
    brandColor: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Timeline Rail Start & Dots', 'b-testimonials-block')
  },
  // frontend.scss:
  // `.btb-poll-wrapper { background: var(--btb-track, #f8fafc) }` and
  // `.btb-poll-num-btn { background: var(--btb-surface, #fff) }`.
  //
  // The poll draws no bar, no track and no card, so both generic names
  // described something that is not on the page -- "Bars & Tracks" is the box
  // a reader sees behind the whole poll, and "Card Surface" is the scale
  // buttons. Neither variable is read anywhere else in this layout, so each
  // override names the one element it paints.
  'user-feedback-poll': {
    trackColor: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Poll Box Background', 'b-testimonials-block'),
    surfaceColor: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)('Number Button Background', 'b-testimonials-block')
  }
};

/**
 * Roles whose element only exists in some states of the block.
 *
 * A layout can read a role and still have nothing to paint right now: the card
 * stack's prev/next buttons and dots are only rendered when there is more than
 * one review to move between, and the before/after slider is a one-line notice
 * until an image is picked. Offering those controls in that state is the same
 * dead-control problem as offering a role the stylesheet never reads, so they
 * are gated on what is actually on the page.
 *
 * Not applied to getPaletteCSS: a value set while the control was showing stays
 * declared, so adding the second review brings the colour back rather than
 * silently dropping it.
 *
 * Each predicate takes the block's attributes and returns whether the element
 * the role paints is currently rendered.
 */
const hasStackNav = attributes => (attributes?.items?.length || 0) > 1;

// Matches the editor's own condition for rendering the slider rather than the
// "pick an image" notice.
const hasSliderImages = attributes => !!(attributes?.beforeImg?.url || attributes?.afterImg?.url);
const ROLE_CONDITIONS = {
  'testimonials-card-stack': {
    brandColor: hasStackNav,
    trackColor: hasStackNav
  },
  'before-after': {
    labelBgColor: hasSliderImages,
    labelTextColor: hasSliderImages,
    gripIconColor: hasSliderImages
  }
};
const PALETTE_ALIASES = {
  'rating-summary': {
    ratingColor: 'starColor'
  },
  // The form's Accent no longer has a control of its own. Its "Form Colors"
  // panel held one "Accent (button)" picker, and the Submit Button panel beside
  // it now owns that pixel through `btnBg` -- TestimonialForm reads
  // `btnBg || accentColor`, so the two were a second dial on one value, which
  // is what the Card/Colors consolidation elsewhere in this plugin exists to
  // avoid.
  //
  // The alias stays regardless. `accentColor` is still the button's fallback,
  // so a form saved with an accent and no button background keeps its colour,
  // and `--btb-accent` still reaches the `.btb-form-wrapper` rules that a page
  // cached before the markup changed is still using. Keeping the role aliased
  // is also what keeps the Colors panel from offering an Accent control that
  // paints nothing in the current markup.
  'testimonial-form': {
    brandColor: 'accentColor'
  },
  'testimonial-stats': {
    brandColor: 'accentColor'
  }
};
const aliasFor = (layout, attr) => PALETTE_ALIASES[layout]?.[attr];

/**
 * The controls a given layout should show.
 *
 * Aliased roles are dropped: the block's own inspector already offers them, and
 * two controls writing one attribute is its own kind of broken. So are roles
 * whose element the block is not rendering in its current state.
 *
 * @param {string} layout     Current layout name.
 * @param {Object} attributes Block attributes, for the state-dependent roles in
 *                            ROLE_CONDITIONS. Omit to offer them unconditionally.
 * @return {Array} Control descriptors, empty when the layout paints nothing the
 *                 registry reaches.
 */
const getVisualControls = (layout, attributes) => (LAYOUT_ROLES[layout] || []).filter(attr => !aliasFor(layout, attr)).filter(attr => {
  const condition = ROLE_CONDITIONS[layout]?.[attr];
  if (!condition || !attributes) {
    return true;
  }

  // A role that already carries a value keeps its control even while the
  // element is absent, so a colour set earlier can still be changed or
  // cleared rather than being stranded behind a hidden control.
  const value = attributes[aliasFor(layout, attr) || attr];
  const isSet = undefined !== value && null !== value && '' !== value;
  return condition(attributes) || isSet;
}).map(attr => ({
  attr,
  ...ROLES[attr],
  // Named for what it paints on this layout, where that differs from the
  // role's generic name.
  ...(ROLE_LABELS[layout]?.[attr] ? {
    label: ROLE_LABELS[layout][attr]
  } : {})
}));

/**
 * CSS custom property declarations for the values the author has set.
 *
 * @param {Object} attributes Block attributes.
 * @param {string} layout     Current layout name.
 * @return {string} Declarations for a style block, empty when nothing is set.
 */
const getPaletteCSS = (attributes = {}, layout = '') => (LAYOUT_ROLES[layout] || []).map(attr => {
  const value = attributes[aliasFor(layout, attr) || attr];

  // A width of 0 is a real choice -- it removes the border -- so only
  // an absent value falls back to the stylesheet's literal.
  if (undefined === value || null === value || '' === value) {
    return '';
  }
  const {
    cssVar,
    type
  } = ROLES[attr];
  return `${cssVar}: ${'width' === type ? `${value}px` : value};`;
}).filter(Boolean).join('\n\t\t');

/***/ }),

/***/ "./src/shared/view.js":
/*!****************************!*\
  !*** ./src/shared/view.js ***!
  \****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   initTestimonials: () => (/* binding */ initTestimonials)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_wordpress_element__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _Components_Common_TestimonialsView__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./Components/Common/TestimonialsView */ "./src/shared/Components/Common/TestimonialsView.js");




/**
 * Shared frontend renderer for every testimonials block.
 *
 * All blocks output a root element with the `.bTestimonials` class plus a
 * `data-attributes` JSON payload (with CPT items already resolved server-side).
 * The attribute is removed after mount, so if more than one block's view script
 * runs on the same page the second pass safely skips initialised elements.
 */
const initTestimonials = (selector = ".bTestimonials") => {
  document.querySelectorAll(selector).forEach(el => {
    if (!el.dataset.attributes) {
      return;
    }
    const attributes = JSON.parse(el.dataset.attributes);
    (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.createRoot)(el).render((0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(_Components_Common_TestimonialsView__WEBPACK_IMPORTED_MODULE_2__["default"], {
      attributes: attributes
    }));
    el.removeAttribute("data-attributes");
  });
};

// Auto-initialise once the DOM is ready.
if (document.readyState === "loading") {
  document.addEventListener("DOMContentLoaded", () => initTestimonials());
} else {
  initTestimonials();
}

/***/ }),

/***/ "../bpl-tools/utils/common.js":
/*!************************************!*\
  !*** ../bpl-tools/utils/common.js ***!
  \************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   SVGSanitizer: () => (/* binding */ SVGSanitizer),
/* harmony export */   escapeHTML: () => (/* binding */ escapeHTML),
/* harmony export */   isExist: () => (/* binding */ isExist),
/* harmony export */   sanitizeHTML: () => (/* binding */ sanitizeHTML),
/* harmony export */   sanitizeInput: () => (/* binding */ sanitizeInput),
/* harmony export */   sanitizeURL: () => (/* binding */ sanitizeURL)
/* harmony export */ });
const isExist = value => {
  if (value === undefined || value === null || value === '') {
    return false;
  }
  if (Array.isArray(value) && value.length === 0) {
    return false;
  }
  if (typeof value === 'object' && Object.keys(value).length === 0) {
    return false;
  }
  if (typeof value === 'string' && value.trim() === '') {
    return false;
  }
  if (typeof value === 'number' && value === 0) {
    return false;
  }
  return true;
};
const escapeHTML = (input = '') => {
  if (!input) {
    return '';
  }

  // Regular expression to match all HTML tags and their attributes
  return input?.replace(/<([a-z][a-z0-9]*)\b([^>]*)>/gi, (match, tagName, attrs) => {
    // List of allowed tags and their attributes
    const allowedTags = ['b', 'strong', 'i', 'em', 'span', 'a', 'br'];
    const allowedAttrs = ['style', 'href', 'target', 'rel', 'class'];

    // If the tag is allowed, keep it, but sanitize its attributes
    if (allowedTags.includes(tagName.toLowerCase())) {
      // Process the tag's attributes
      const sanitizedAttrs = attrs.replace(/([a-z0-9-]+)=["'][^"']*["']/gi, (attrMatch, attrName) => {
        // Only keep allowed attributes
        if (allowedAttrs.includes(attrName.toLowerCase())) {
          return attrMatch; // Keep allowed attributes as they are
        }
        return ''; // Remove any other attributes
      });
      return `<${tagName}${sanitizedAttrs}>`;
    }
    return match?.replace(/</g, '&lt;').replace(/>/g, '&gt;');
  });
};
const sanitizeURL = inputUrl => {
  try {
    const url = new URL(inputUrl);

    // 1. Check for safe protocols
    if (!['http:', 'https:'].includes(url.protocol)) {
      return null;
    } else {
      // 2. Strip query and fragment for safety
      // url.search = '';
      // url.hash = '';

      return url.toString();
    }
  } catch (err) {
    if (typeof inputUrl === 'string' && inputUrl.startsWith('/') && !inputUrl.startsWith('//')) {
      return inputUrl;
    } else {
      return null;
    }
  }
};
const sanitizeHTML = input => {
  const parser = new DOMParser();
  const doc = parser.parseFromString(input, 'text/html');
  const allowedTags = ['b', 'strong', 'i', 'em', 'span', 'a', 'br'];
  const allowedAttrs = ['style', 'href', 'target', 'rel', 'class'];
  doc.body.querySelectorAll('*').forEach(node => {
    // Remove disallowed tags
    if (!allowedTags.includes(node.tagName.toLowerCase())) {
      node.remove();
      return;
    }

    // Loop through attributes and sanitize
    [...node.attributes].forEach(attr => {
      if (!allowedAttrs.includes(attr.name)) {
        node.removeAttribute(attr.name);
      }

      // if (attr.name === 'href' && attr.value.trim().toLowerCase().startsWith('javascript:')) {
      // 	node.removeAttribute('href');
      // }

      if (attr.name === 'href') {
        const sanitizeHref = sanitizeURL(attr.value);
        if (sanitizeHref) {
          node.setAttribute('href', sanitizeHref);
        } else {
          node.removeAttribute('href');
        }
      }
    });
  });
  return doc.body.innerHTML;
};
const sanitizeInput = input => {
  return input.replace(/[<>]/g, '').replace(/javascript:/gi, '').replace(/on\w+=/gi, '').trim();
};

//-------- Sanitize SVG ----------//
class SVGSanitizer {
  constructor(options = {}) {
    this.defaultOptions = {
      allowedTags: ['svg', 'g', 'path', 'rect', 'circle', 'ellipse', 'line', 'polyline', 'polygon', 'text', 'tspan', 'defs', 'clipPath', 'mask', 'linearGradient', 'radialGradient', 'stop', 'style', 'title', 'desc'],
      allowedAttributes: ['id', 'class', 'style', 'transform', 'd', 'x', 'y', 'width', 'height', 'cx', 'cy', 'r', 'rx', 'ry', 'points', 'x1', 'y1', 'x2', 'y2', 'fill', 'stroke', 'stroke-width', 'stroke-linecap', 'stroke-linejoin', 'opacity', 'fill-opacity', 'stroke-opacity', 'viewBox', 'preserveAspectRatio', 'xmlns', 'xmlns:xlink'],
      allowedProtocols: ['http', 'https', 'data'],
      removeScripts: true,
      removeEvents: true,
      removeExternalResources: true,
      sanitizeStyle: true
    };
    this.options = {
      ...this.defaultOptions,
      ...options
    };
  }
  sanitize(svgString) {
    const parser = new DOMParser();
    const doc = parser.parseFromString(svgString, 'image/svg+xml');
    this.removeScripts(doc);
    this.sanitizeElements(doc);
    const serializer = new XMLSerializer();
    return serializer.serializeToString(doc.documentElement);
  }

  // ✅ New method: sanitize from File object
  async sanitizeFile(file) {
    const response = await fetch(file);
    const svgText = await response.text();
    // console.log(svgText)
    return this.sanitize(`${svgText}`);
    // return "";
  }
  removeScripts(doc) {
    if (this.options.removeScripts) {
      const scripts = doc.querySelectorAll('script');
      scripts.forEach(script => script.remove());
      const allElements = doc.querySelectorAll('*');
      allElements.forEach(el => {
        if (el.tagName.toLowerCase().includes('script')) {
          el.remove();
        }
      });
    }
  }
  sanitizeElements(doc) {
    const allElements = doc.querySelectorAll('*');
    allElements.forEach(element => {
      const tagName = element.tagName.toLowerCase();
      if (!this.options.allowedTags.includes(tagName)) {
        element.remove();
        return;
      }
      this.sanitizeAttributes(element);
    });
  }
  sanitizeAttributes(element) {
    const attributes = Array.from(element.attributes);
    attributes.forEach(attr => {
      const attrName = attr.name.toLowerCase();
      const attrValue = attr.value;
      if (this.options.removeEvents && attrName.startsWith('on')) {
        element.removeAttribute(attrName);
        return;
      }
      if (this.options.removeExternalResources) {
        if ((attrName === 'href' || attrName === 'xlink:href') && !this.isAllowedUrl(attrValue)) {
          element.removeAttribute(attrName);
          return;
        }
      }
      const baseAttrName = attrName.replace('xlink:', '');
      if (!this.options.allowedAttributes.includes(baseAttrName)) {
        element.removeAttribute(attrName);
        return;
      }
      if (attrName === 'style' && this.options.sanitizeStyle) {
        this.sanitizeStyleAttribute(element, attrValue);
      }
    });
  }
  isAllowedUrl(url) {
    if (url.startsWith('data:') || url.startsWith('#')) {
      return true;
    }
    try {
      const parsedUrl = new URL(url);
      return this.options.allowedProtocols.includes(parsedUrl.protocol.replace(':', ''));
    } catch {
      return false;
    }
  }
  sanitizeStyleAttribute(element, styleValue) {
    const safeStyle = styleValue.replace(/expression\(|javascript:|url\(javascript:/gi, '').replace(/behavior\s*:/gi, '').replace(/binding\s*:/gi, '');
    element.setAttribute('style', safeStyle);
  }
}

/***/ }),

/***/ "../bpl-tools/utils/data.js":
/*!**********************************!*\
  !*** ../bpl-tools/utils/data.js ***!
  \**********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   contentColor: () => (/* binding */ contentColor),
/* harmony export */   deskBreakpoint: () => (/* binding */ deskBreakpoint),
/* harmony export */   gradient: () => (/* binding */ gradient),
/* harmony export */   mobileBreakpoint: () => (/* binding */ mobileBreakpoint),
/* harmony export */   primaryColor: () => (/* binding */ primaryColor),
/* harmony export */   primaryColor100: () => (/* binding */ primaryColor100),
/* harmony export */   primaryColor1000: () => (/* binding */ primaryColor1000),
/* harmony export */   primaryColor200: () => (/* binding */ primaryColor200),
/* harmony export */   primaryColor300: () => (/* binding */ primaryColor300),
/* harmony export */   primaryColor400: () => (/* binding */ primaryColor400),
/* harmony export */   primaryColor500: () => (/* binding */ primaryColor500),
/* harmony export */   primaryColor600: () => (/* binding */ primaryColor600),
/* harmony export */   primaryColor700: () => (/* binding */ primaryColor700),
/* harmony export */   primaryColor800: () => (/* binding */ primaryColor800),
/* harmony export */   primaryColor900: () => (/* binding */ primaryColor900),
/* harmony export */   secondaryColor: () => (/* binding */ secondaryColor),
/* harmony export */   tabBreakpoint: () => (/* binding */ tabBreakpoint),
/* harmony export */   titleColor: () => (/* binding */ titleColor)
/* harmony export */ });
const deskBreakpoint = '@media only screen and (min-width: 1025px)';
const tabBreakpoint = '@media only screen and (max-width: 1024px)';
const mobileBreakpoint = '@media only screen and (max-width: 640px)';
const primaryColor = '#146EF5';
const primaryColor100 = '#e7f0fe';
const primaryColor200 = '#b6d2fc';
const primaryColor300 = '#85b4fa';
const primaryColor400 = '#5495f8';
const primaryColor500 = '#2377f6';
const primaryColor600 = '#095edc';
const primaryColor700 = '#0749ab';
const primaryColor800 = '#05347a';
const primaryColor900 = '#031f49';
const primaryColor1000 = '#010a18';
const secondaryColor = '#FF7A00';
const titleColor = '#070127';
const contentColor = '#485781';
const gradient = 'linear-gradient(135deg, #0040E3, #18D4FD)';

/***/ }),

/***/ "../bpl-tools/utils/functions.js":
/*!***************************************!*\
  !*** ../bpl-tools/utils/functions.js ***!
  \***************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   MimeTypeChecker: () => (/* binding */ MimeTypeChecker),
/* harmony export */   debounce: () => (/* binding */ debounce),
/* harmony export */   getBoxValue: () => (/* binding */ getBoxValue),
/* harmony export */   getImageSizes: () => (/* binding */ getImageSizes),
/* harmony export */   getRandomColor: () => (/* binding */ getRandomColor),
/* harmony export */   randomNumber: () => (/* binding */ randomNumber),
/* harmony export */   tabController: () => (/* binding */ tabController),
/* harmony export */   updateAttributes: () => (/* binding */ updateAttributes),
/* harmony export */   updateData: () => (/* binding */ updateData)
/* harmony export */ });
/* harmony import */ var immer__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! immer */ "../bpl-tools/node_modules/immer/dist/immer.mjs");

const getBoxValue = object => Object.values(object).join(' ');
const getImageSizes = (image, imageSizes) => {
  if (!image) return [];
  let options = [];
  const sizes = image.media_details.sizes;
  for (const key in sizes) {
    const imageSize = imageSizes.find(s => s.slug === key);
    if (imageSize) {
      options.push({
        label: imageSize.name,
        value: sizes[key].source_url
      });
    }
  }
  return options;
};
const tabController = () => {
  setTimeout(() => {
    const panelBodies = document.querySelectorAll('.bPlPanelBody:not(.itemPanelBody) > h2 > button');
    panelBodies.forEach(item => {
      item.addEventListener('click', clickEveryItem);
    });
    function clickEveryItem() {
      this.removeEventListener('click', clickEveryItem);
      panelBodies.forEach(item => {
        if (item.getAttribute('aria-expanded') === 'true' && !item.isEqualNode(this)) {
          item.click();
        }
      });
      setTimeout(() => {
        this.addEventListener('click', clickEveryItem);
      }, 500);
    }
  }, 500);
};
const updateData = (attr, value, ...props) => {
  if (props.length === 0) {
    return value;
  }
  const [currentProp, ...remainingProps] = props;
  if (remainingProps.length === 0) {
    return (0,immer__WEBPACK_IMPORTED_MODULE_0__.produce)(attr, draft => {
      if (Array.isArray(draft[currentProp]) && (draft === null || draft === undefined)) {
        draft = {};
      }
      draft[currentProp] = value;
    });
  }
  return (0,immer__WEBPACK_IMPORTED_MODULE_0__.produce)(attr, draft => {
    if (draft === null || draft === undefined) {
      draft = {};
    }
    if (!Object.prototype.hasOwnProperty.call(draft, currentProp)) {
      draft[currentProp] = {};
    }
    draft[currentProp] = updateData(draft[currentProp], value, ...remainingProps);
  });
};
const updateAttributes = (attributes, setAttributes) => {
  return (object, val, ...props) => {
    setAttributes({
      [object]: updateData(attributes[object], val, ...props)
    });
  };
};
const debounce = (fn, delay) => {
  let timeoutId;
  return (...args) => {
    clearTimeout(timeoutId);
    timeoutId = setTimeout(() => {
      fn(...args);
    }, delay);
  };
};
const getRandomColor = () => {
  return '#' + Math.floor(Math.random() * 16777215).toString(16).padStart(6, '0');
};
const randomNumber = number => parseInt(Math.random() * number);
class MimeTypeChecker {
  // Common MIME types mapping

  static get mimeTypes() {
    return {
      // Images
      'jpg': 'image/jpeg',
      'jpeg': 'image/jpeg',
      'png': 'image/png',
      'gif': 'image/gif',
      'webp': 'image/webp',
      'svg': 'image/svg+xml',
      'bmp': 'image/bmp',
      'ico': 'image/x-icon',
      // Documents
      'pdf': 'application/pdf',
      'doc': 'application/msword',
      'docx': 'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
      'xls': 'application/vnd.ms-excel',
      'xlsx': 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
      'ppt': 'application/vnd.ms-powerpoint',
      'pptx': 'application/vnd.openxmlformats-officedocument.presentationml.presentation',
      'txt': 'text/plain',
      'csv': 'text/csv',
      // Audio/Video
      'mp3': 'audio/mpeg',
      'wav': 'audio/wav',
      'ogg': 'audio/ogg',
      'mp4': 'video/mp4',
      'webm': 'video/webm',
      'avi': 'video/x-msvideo',
      'mov': 'video/quicktime',
      // Archives
      'zip': 'application/zip',
      'rar': 'application/x-rar-compressed',
      'tar': 'application/x-tar',
      'gz': 'application/gzip',
      // Code
      'js': 'application/javascript',
      'css': 'text/css',
      'html': 'text/html',
      'json': 'application/json',
      'xml': 'application/xml'
    };
  }

  // Check if a file has a valid MIME type (now handles both string and array)
  static isValidMimeType(file, allowedTypes = []) {
    if (!file) return false;
    const fileType = this.getMimeTypeFromFile(file);
    if (!fileType) return false;

    // Convert single string to array for consistent processing
    const typesArray = Array.isArray(allowedTypes) ? allowedTypes : [allowedTypes];
    if (typesArray.length === 0) return true;
    return typesArray.some(allowedType => {
      if (allowedType.includes('*')) {
        // Handle wildcard types like 'image/*'
        const baseType = allowedType.split('/*')[0];
        return fileType.startsWith(baseType);
      }
      return fileType === allowedType;
    });
  }

  // Get MIME type from file extension
  static getMimeTypeFromExtension(filename) {
    if (!filename) return null;
    const extension = filename.split('.').pop().toLowerCase();
    return this.mimeTypes[extension] || null;
  }

  // Get MIME type from File object
  static getMimeTypeFromFile(file) {
    if (file instanceof File || file instanceof Blob) {
      return file.type || this.getMimeTypeFromExtension(file.name);
    }
    return this.getMimeTypeFromExtension(file);
  }

  // Check if file is an image
  static isImage(file) {
    const mimeType = this.getMimeTypeFromFile(file);
    return mimeType ? mimeType.startsWith('image/') : false;
  }

  // Check if file is a video
  static isVideo(file) {
    const mimeType = this.getMimeTypeFromFile(file);
    return mimeType ? mimeType.startsWith('video/') : false;
  }

  // Check if file is an audio file
  static isAudio(file) {
    const mimeType = this.getMimeTypeFromFile(file);
    return mimeType ? mimeType.startsWith('audio/') : false;
  }

  // Check if file is a document
  static isDocument(file) {
    const mimeType = this.getMimeTypeFromFile(file);
    return mimeType ? mimeType.startsWith('application/') || mimeType.startsWith('text/') || mimeType.includes('word') || mimeType.includes('excel') || mimeType.includes('powerpoint') || mimeType.includes('pdf') : false;
  }

  // Validate file against specific MIME types
  // Validate file against specific MIME types (updated to handle string/array)
  static validateFile(file, options = {}) {
    const {
      allowedTypes = [],
      maxSize = Infinity,
      minSize = 0
    } = options;
    const errors = [];

    // Convert allowedTypes to array if it's a string
    const typesArray = Array.isArray(allowedTypes) ? allowedTypes : [allowedTypes];

    // Check MIME type
    if (typesArray.length > 0 && !this.isValidMimeType(file, typesArray)) {
      const allowedTypesString = typesArray.join(', ');
      errors.push(`File type not allowed. Allowed types: ${allowedTypesString}`);
    }

    // Check file size (only if it's a File/Blob object)
    if (file instanceof File || file instanceof Blob) {
      if (file.size > maxSize) {
        errors.push(`File size too large. Maximum size: ${this.formatBytes(maxSize)}`);
      }
      if (file.size < minSize) {
        errors.push(`File size too small. Minimum size: ${this.formatBytes(minSize)}`);
      }
    }
    return {
      isValid: errors.length === 0,
      errors,
      mimeType: this.getMimeTypeFromFile(file),
      size: file instanceof File || file instanceof Blob ? file.size : null
    };
  }

  // Helper to format bytes
  static formatBytes(bytes, decimals = 2) {
    if (bytes === 0) return '0 Bytes';
    const k = 1024;
    const dm = decimals < 0 ? 0 : decimals;
    const sizes = ['Bytes', 'KB', 'MB', 'GB', 'TB'];
    const i = Math.floor(Math.log(bytes) / Math.log(k));
    return parseFloat((bytes / Math.pow(k, i)).toFixed(dm)) + ' ' + sizes[i];
  }
  static addMimeType(extension, mimeType) {
    const types = this.mimeTypes;
    types[extension.toLowerCase()] = mimeType;
  }

  // Add an instance property that references the static methods
  static get helpers() {
    return {
      validateFile: this.validateFile.bind(this),
      isValidMimeType: this.isValidMimeType.bind(this),
      isImage: this.isImage.bind(this),
      isVideo: this.isVideo.bind(this),
      isAudio: this.isAudio.bind(this),
      isDocument: this.isDocument.bind(this),
      getMimeTypeFromFile: this.getMimeTypeFromFile.bind(this),
      getMimeTypeFromExtension: this.getMimeTypeFromExtension.bind(this),
      addMimeType: this.addMimeType.bind(this)
    };
  }
}

/***/ }),

/***/ "../bpl-tools/utils/getCSS.js":
/*!************************************!*\
  !*** ../bpl-tools/utils/getCSS.js ***!
  \************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   getAdvBGCSS: () => (/* binding */ getAdvBGCSS),
/* harmony export */   getBackgroundCSS: () => (/* binding */ getBackgroundCSS),
/* harmony export */   getBorderBoxCSS: () => (/* binding */ getBorderBoxCSS),
/* harmony export */   getBorderCSS: () => (/* binding */ getBorderCSS),
/* harmony export */   getBoxCSS: () => (/* binding */ getBoxCSS),
/* harmony export */   getColorsCSS: () => (/* binding */ getColorsCSS),
/* harmony export */   getGradientCSS: () => (/* binding */ getGradientCSS),
/* harmony export */   getIconCSS: () => (/* binding */ getIconCSS),
/* harmony export */   getMaskCSS: () => (/* binding */ getMaskCSS),
/* harmony export */   getMultiShadowCSS: () => (/* binding */ getMultiShadowCSS),
/* harmony export */   getOverlayCSS: () => (/* binding */ getOverlayCSS),
/* harmony export */   getSeparatorCSS: () => (/* binding */ getSeparatorCSS),
/* harmony export */   getShadowCSS: () => (/* binding */ getShadowCSS),
/* harmony export */   getSpaceCSS: () => (/* binding */ getSpaceCSS),
/* harmony export */   getTransformCSS: () => (/* binding */ getTransformCSS),
/* harmony export */   getTypoCSS: () => (/* binding */ getTypoCSS),
/* harmony export */   isValidCSS: () => (/* binding */ isValidCSS)
/* harmony export */ });
/* harmony import */ var _Components_Mask_assets_shapes_blob_svg__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../Components/Mask/assets/shapes/blob.svg */ "../bpl-tools/Components/Mask/assets/shapes/blob.svg");
/* harmony import */ var _Components_Mask_assets_shapes_circle_svg__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../Components/Mask/assets/shapes/circle.svg */ "../bpl-tools/Components/Mask/assets/shapes/circle.svg");
/* harmony import */ var _Components_Mask_assets_shapes_flower_svg__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../Components/Mask/assets/shapes/flower.svg */ "../bpl-tools/Components/Mask/assets/shapes/flower.svg");
/* harmony import */ var _Components_Mask_assets_shapes_hexagon_svg__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../Components/Mask/assets/shapes/hexagon.svg */ "../bpl-tools/Components/Mask/assets/shapes/hexagon.svg");
/* harmony import */ var _Components_Mask_assets_shapes_sketch_svg__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../Components/Mask/assets/shapes/sketch.svg */ "../bpl-tools/Components/Mask/assets/shapes/sketch.svg");
/* harmony import */ var _Components_Mask_assets_shapes_triangle_svg__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../Components/Mask/assets/shapes/triangle.svg */ "../bpl-tools/Components/Mask/assets/shapes/triangle.svg");
/* harmony import */ var _common__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./common */ "../bpl-tools/utils/common.js");
/* harmony import */ var _data__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./data */ "../bpl-tools/utils/data.js");







const safeCSSUrl = url => {
  const sanitized = (0,_common__WEBPACK_IMPORTED_MODULE_6__.sanitizeURL)(url);
  if (!sanitized) {
    return 'none';
  }
  const cleanUrl = sanitized.replace(/['"()]/g, '');
  return `url('${cleanUrl}')`;
};

const isValidCSS = (p, v) => (0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(v) ? `${p}: ${v};` : '';
const getBackgroundCSS = (bg, isSolid = true, isGradient = true, isImage = true) => {
  const {
    type = 'solid',
    color = '',
    gradient = _data__WEBPACK_IMPORTED_MODULE_7__.gradient,
    image = {},
    position = 'center center',
    attachment = '',
    repeat = 'no-repeat',
    size = '',
    overlayColor = ''
  } = bg || {};
  const styles = 'gradient' === type && isGradient ? isValidCSS('background', gradient) : 'image' === type && isImage ? `background: ${safeCSSUrl(image?.url)};
				${isValidCSS('background-color', overlayColor)}
				${isValidCSS('background-position', position)}
				${isValidCSS('background-size', size)}
				${isValidCSS('background-repeat', repeat)}
				${isValidCSS('background-attachment', attachment)}
				${isValidCSS('background-repeat', repeat)}
				background-blend-mode: overlay;` : isSolid && isValidCSS('background', color);
  return styles;
};
const getBorderCSS = border => {
  const {
    width = '',
    style = 'solid',
    color = '',
    side = 'all',
    radius = ''
  } = border || {};
  const borderSideCheck = s => {
    const bSide = side?.toLowerCase();
    return bSide?.includes('all') || bSide?.includes(s);
  };
  const borderCSS = `${width} ${style} ${color}`;
  const styles = `
		${!width || parseInt(width) === 0 ? '' : ['top', 'right', 'bottom', 'left'].map(side => borderSideCheck(side) ? `border-${side}: ${borderCSS};` : '').join('')}
		${isValidCSS('border-radius', radius)}
	`;
  return styles;
};
const getBorderBoxCSS = border => {
  if (!border) return '';
  const generateBorderCSS = borderObj => {
    const {
      color = '#000000',
      style = 'solid',
      width = '0px'
    } = borderObj;
    return `${width} ${style} ${color}`;
  };
  if ('object' === typeof border && !Array.isArray(border)) {
    if (border.hasOwnProperty('top') || border.hasOwnProperty('right') || border.hasOwnProperty('bottom') || border.hasOwnProperty('left')) {
      const sides = ['top', 'right', 'bottom', 'left'];
      return sides.map(side => border[side] ? `border-${side}: ${generateBorderCSS(border[side])};` : '').join(' ').trim();
    } else {
      return isValidCSS('border', generateBorderCSS(border));
    }
  }
  return '';
};
const getColorsCSS = colors => {
  const {
    color = '',
    bgType = 'solid',
    bg = '',
    gradient = _data__WEBPACK_IMPORTED_MODULE_7__.gradient
  } = colors || {};
  const styles = `
		${isValidCSS('color', color)}
		${gradient || bg ? isValidCSS('background', 'gradient' === bgType ? gradient : bg) : ''}
	`;
  return styles;
};
const getIconCSS = (icon, isSize = true, isColor = true) => {
  const {
    fontSize = 16,
    colorType = 'solid',
    color = 'inherit',
    gradient = _data__WEBPACK_IMPORTED_MODULE_7__.gradient
  } = icon || {};
  const colorCSS = 'gradient' === colorType ? `color: transparent; background-image: ${gradient}; -webkit-background-clip: text; background-clip: text;` : isValidCSS('color', color);
  const styles = `
		${!fontSize || !isSize ? '' : isValidCSS('font-size', fontSize ? `${fontSize}px` : '')}
		${isColor ? colorCSS : ''}
	`;
  return styles;
};
const getMultiShadowCSS = (value, type = 'box') => {
  let styles = '';
  value?.map((item, index) => {
    const {
      hOffset = '0px',
      vOffset = '0px',
      blur = '0px',
      spreed = '0px',
      color = '#e7f0fe',
      isInset = false
    } = item || {};
    const inset = isInset ? 'inset' : '';
    const offsetBlur = `${hOffset} ${vOffset} ${blur}`;
    const isComa = index + 1 >= value.length ? '' : ', ';
    styles += 'text' === type ? `${offsetBlur} ${color}${isComa}` : `${offsetBlur} ${spreed} ${color} ${inset}${isComa}`;
  });
  return styles || '';
};
const getSeparatorCSS = separator => {
  const {
    width = '50%',
    height = '2px',
    style = 'solid',
    color = _data__WEBPACK_IMPORTED_MODULE_7__.primaryColor300
  } = separator || {};
  const styles = `
		${isValidCSS('width', width)}
		${0 === parseInt(height) ? '' : `border-top: ${height} ${style} ${color};`}
	`;
  return styles;
};
const getShadowCSS = (shadow, type = 'box') => {
  const {
    hOffset = '0px',
    vOffset = '0px',
    blur = '0px',
    spreed = '0px',
    color = '#7090b0',
    isInset = false
  } = shadow || {};
  const inset = isInset ? 'inset' : '';
  const offsetBlur = `${hOffset} ${vOffset} ${blur}`;
  const styles = 'text' === type ? `${offsetBlur} ${color}` : `${offsetBlur} ${spreed} ${color} ${inset}`;
  return styles || 'none';
};
const getSpaceCSS = space => {
  const {
    side = 2,
    vertical = '0px',
    horizontal = '0px',
    top = '0px',
    right = '0px',
    bottom = '0px',
    left = '0px'
  } = space || {};
  const styles = 2 === side ? `${vertical} ${horizontal}` : `${top} ${right} ${bottom} ${left}`;
  return styles;
};
const getTypoCSS = (selector, typo, isFamily = true) => {
  const {
    fontFamily = 'Default',
    fontCategory = 'sans-serif',
    fontVariant = 400,
    fontWeight,
    isUploadFont = true,
    fontSize = {
      desktop: null,
      tablet: null,
      mobile: null
    },
    fontStyle,
    textTransform,
    textDecoration,
    lineHeight,
    letterSpace
  } = typo || {};
  const isEmptyFamily = !isFamily || !fontFamily || 'Default' === fontFamily;
  const desktopFontSize = fontSize?.desktop || fontSize;
  const tabletFontSize = fontSize?.tablet || desktopFontSize;
  const mobileFontSize = fontSize?.mobile || tabletFontSize;
  const checkUnit = size => {
    const value = String(size);
    const units = ['px', 'em', 'rem', '%', 'vh', 'vw'];
    if (units.some(unit => value.endsWith(unit))) {
      return value;
    } else if (typeof size === 'number') {
      return `${value}px`;
    }
    return '';
  };
  const styles = `
		${isEmptyFamily ? '' : `font-family: '${fontFamily}', ${fontCategory};`}
		${isValidCSS('font-weight', fontWeight)}
		${isValidCSS('font-size', checkUnit(desktopFontSize))}
		${isValidCSS('font-style', fontStyle)}
		${isValidCSS('text-transform', textTransform)}
		${isValidCSS('text-decoration', textDecoration)}
		${isValidCSS('line-height', lineHeight)}
		${isValidCSS('letter-spacing', letterSpace)}
	`;

  // Google font link
  const linkQuery = !fontVariant || 400 === fontVariant ? '' : '400i' === fontVariant ? ':ital@1' : fontVariant?.includes('00i') ? `: ital, wght@1, ${fontVariant?.replace('00i', '00')} ` : `: wght@${fontVariant} `;
  const link = isEmptyFamily ? '' : `https://fonts.googleapis.com/css2?family=${fontFamily?.split(' ').join('+')}${linkQuery.replace(/ /g, '')}&display=swap`;
  return {
    googleFontLink: !isUploadFont || isEmptyFamily ? '' : `@import url(${link});`,
    styles: `${selector}{
			${styles}
		}
		${_data__WEBPACK_IMPORTED_MODULE_7__.tabBreakpoint} {
			${selector}{
				${isValidCSS('font-size', checkUnit(tabletFontSize))}
			}
		}
		${_data__WEBPACK_IMPORTED_MODULE_7__.mobileBreakpoint} {
			${selector}{
				${isValidCSS('font-size', checkUnit(mobileFontSize))}
			}
		}`.replace(/\s+/g, ' ').trim()
  };
};
const getBoxCSS = val => {
  if (!val) return '';
  if (typeof val === 'string') return val;
  if (typeof val === 'object' && !Array.isArray(val)) {
    const order = ['top', 'right', 'bottom', 'left'];
    const values = order.map(side => val[side] || '');
    const allEmpty = values.every(value => !value);
    return allEmpty ? '' : order.map(side => val[side] || '0').join(' ');
  }
  return '';
};

// Murad Wahid
const getGradientCSS = gradient => {
  const {
    type = 'linear',
    radialType = 'ellipse',
    colors = [],
    centerPositions = {
      x: 0,
      y: 0
    },
    angel = 0
  } = gradient || {};
  if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(gradient)) {
    const gradientColors = colors?.map(({
      color,
      position
    }) => `${color} ${position}%`);
    const liner = `linear-gradient(${angel}deg, ${gradientColors})`;
    const radial = `radial-gradient(${radialType} at ${centerPositions?.x}% ${centerPositions?.y}%,${gradientColors})`;
    return isValidCSS('background', type === 'radial' ? radial : liner);
  }
  return '';
};
const getImagePosition = img => {
  const {
    position = 'center center',
    xPosition = 0,
    yPosition = 0,
    attachment = '',
    repeat = 'no-repeat',
    size = 'cover',
    customSize = '0px'
  } = img || {};
  const cd = v => 'initial' !== v || (0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(v);
  return `
		${cd(position) ? `background-position: ${'custom' === position ? `${xPosition} ${yPosition}` : position};` : ''}
		${attachment && cd(attachment) ? `background-attachment: ${attachment};` : ''}
		${cd(repeat) ? `background-repeat: ${repeat};` : ''}
		${cd(size) ? `background-size: ${'custom' === size ? `${customSize} auto` : size};` : ''}
	`;
};
const getImageCSS = (img = {}) => {
  if (img) {
    return {
      desktop: (0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(img.url) ? `background-image: ${safeCSSUrl(img.url)}; ${getImagePosition(img?.desktop)}` : '',
      tablet: (0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(img.url) ? getImagePosition(img?.tablet) : '',
      mobile: (0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(img.url) ? getImagePosition(img?.mobile) : ''
    };
  }
  return '';
};
const getVideoCSS = (video, selector) => {
  const {
    url,
    loop
  } = video || {};
  const parentEl = document.querySelector(selector);
  const el = parentEl?.querySelector('.bPlVideo');
  const videoEl = document.createElement('video');
  videoEl.muted = true;
  videoEl.autoplay = true;
  videoEl.classList.add('bPlVideo');
  if (!el) {
    if (parentEl && url) {
      const sanitizedUrl = (0,_common__WEBPACK_IMPORTED_MODULE_6__.sanitizeURL)(url);
      if (sanitizedUrl) {
        const sourceEl = document.createElement('source');
        sourceEl.src = sanitizedUrl;
        videoEl.appendChild(sourceEl);
        parentEl.appendChild(videoEl);
      }
    }
  }
  videoEl.loop = loop;
  videoEl.play();
  return `${selector} .bPlVideo{
		left: 0;
		min-height: 100%;
		min-width: 100%;
		position: absolute;
		width: -webkit-fill-available;
		top: 0;
		z-index: 0;
	}`;
};
const getAdvBGCSS = (background, selector, isHover = false) => {
  const {
    type = 'color',
    color,
    gradient,
    img,
    video
  } = background || {};
  const bgCSS = type === 'color' ? isValidCSS('background', color) : type === 'gradient' ? getGradientCSS(gradient) : type === 'image' ? getImageCSS(img).desktop : '';
  const tablet = type === 'image' ? getImageCSS(img).tablet : '';
  const mobile = type === 'image' ? getImageCSS(img).mobile : '';
  const sl = isHover ? `${selector}:hover` : selector;
  return `
		${type === 'video' ? getVideoCSS(video, selector) : ''}

		${bgCSS ? `${sl}{
			${bgCSS}
		}` : ''}

		${tablet ? `${_data__WEBPACK_IMPORTED_MODULE_7__.tabBreakpoint} {
			${sl}{
				${tablet}
			}
		}` : ''}

		${mobile ? `${_data__WEBPACK_IMPORTED_MODULE_7__.mobileBreakpoint} {
			${sl}{
				${mobile}
			}
		}` : ''}
	`.replace(/\s+/g, ' ').trim();
};
const getOverlayCSS = (overlay, selector, isHover = false) => {
  const {
    isEnabled,
    colors,
    opacity = 1,
    blend,
    filter = '',
    blur = 0,
    brightness = 100,
    contrast = 100,
    saturation = 100,
    hue = 0,
    position = {
      top: 0,
      right: 0,
      bottom: 0,
      left: 0
    },
    zIndex = -1
  } = overlay || {};
  const filterCSSValue = `${100 !== brightness ? `brightness(${brightness}%)` : ''} ${100 !== contrast ? `contrast(${contrast}%)` : ''} ${100 !== saturation ? `saturate(${saturation}%)` : ''} ${0 !== blur ? `blur(${blur}px)` : ''} ${0 !== hue ? `hue-rotate(${hue}deg)` : ''}`;
  const filterCSS = `${filter}: ${filter ? filterCSSValue : ''}; -webkit-${filter}: ${filter ? filterCSSValue : ''};`;
  const sl = isHover ? `${selector}:hover::after` : `${selector}::after`;
  return isEnabled ? `
		${selector}{
			position:relative;
			z-index:1;
		}
		${selector}::after{
			content: '';
			position: absolute;
			top:${(0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(position.top) ? position.top : 0};
			right:${(0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(position.right) ? position.right : 0};
			bottom:${(0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(position.bottom) ? position.bottom : 0};
			left:${(0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(position.left) ? position.left : 0};
			z-index:${zIndex};
		}
		${getAdvBGCSS(colors, sl, false)}
		${sl}{
			${isValidCSS('opacity', opacity)}
			${isValidCSS('mix-blend-mode', blend)}
			${filterCSS}
		}
	`.replace(/\s+/g, ' ').trim() : '';
};
const getTransformCSS = (transform, selector, isHover = false) => {
  const generateTransformCSS = (value, device = 'desktop') => {
    if (!(0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(value)) return '';
    const {
      skew,
      scale,
      rotate,
      offset,
      flipX,
      flipY
    } = value;
    const {
      threeDRotate
    } = rotate || {};
    const {
      isProportion
    } = scale || {};
    const transforms = [];
    //skew
    if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(skew)) {
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(skew?.[device]?.x)) transforms.push(`skewX(${skew[device].x}deg)`);
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(skew?.[device]?.y)) transforms.push(`skewY(${skew[device].y}deg)`);
    }
    //scale
    if (isProportion) {
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(scale?.[device]?.scale)) transforms.push(`scale(${scale[device].scale})`);
    } else {
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(scale?.[device]?.x)) transforms.push(`scaleX(${scale[device].x})`);
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(scale?.[device]?.y)) transforms.push(`scaleY(${scale[device].y})`);
    }

    //rotate
    if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(rotate)) {
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(rotate?.[device]?.z)) transforms.push(`rotateZ(${rotate[device].z}deg)`);
      if (threeDRotate) {
        if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(rotate?.[device]?.x)) transforms.push(`rotateX(${rotate[device].x}deg)`);
        if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(rotate?.[device]?.y)) transforms.push(`rotateY(${rotate[device].y}deg)`);
      }
    }

    //offset
    if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(offset)) {
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(offset?.[device]?.x)) transforms.push(`translateX(${offset[device].x})`);
      if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(offset?.[device]?.y)) transforms.push(`translateY(${offset[device].y})`);
    }

    //flip
    if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(flipX)) transforms.push(flipX ? 'scaleX(-1)' : '');
    if ((0,_common__WEBPACK_IMPORTED_MODULE_6__.isExist)(flipY)) transforms.push(flipY ? 'scaleY(-1)' : '');
    if (transforms.length === 0) return '';
    return isValidCSS('transform', transforms.join(' '));
  };
  const sl = isHover ? `${selector}:hover` : selector;
  // ${isExist(hover?.transition)?`transition:transform ${hover.transition}ms ease-in-out`:'' }

  return `
		${sl} {
			${generateTransformCSS(transform, 'desktop')}
		}
		${_data__WEBPACK_IMPORTED_MODULE_7__.tabBreakpoint}{
			${sl} {
				${generateTransformCSS(transform, 'tablet')}
			}
		}
		${_data__WEBPACK_IMPORTED_MODULE_7__.mobileBreakpoint}{
			${sl} {
				${generateTransformCSS(transform, 'mobile')}
			}
		}
	`;
};
const getMaskCSS = mask => {
  const {
    isMask = false,
    shape = {
      type: 'circle'
    },
    size = {
      type: 'contain',
      scale: '100%'
    },
    position = {
      type: 'center center',
      x: 50,
      y: 50
    },
    repeat = 'no-repeat'
  } = mask || {};
  const svgShape = [{
    svg: _Components_Mask_assets_shapes_circle_svg__WEBPACK_IMPORTED_MODULE_1__["default"],
    type: 'circle'
  }, {
    svg: _Components_Mask_assets_shapes_flower_svg__WEBPACK_IMPORTED_MODULE_2__["default"],
    type: 'flower'
  }, {
    svg: _Components_Mask_assets_shapes_sketch_svg__WEBPACK_IMPORTED_MODULE_4__["default"],
    type: 'sketch'
  }, {
    svg: _Components_Mask_assets_shapes_triangle_svg__WEBPACK_IMPORTED_MODULE_5__["default"],
    type: 'triangle'
  }, {
    svg: _Components_Mask_assets_shapes_blob_svg__WEBPACK_IMPORTED_MODULE_0__["default"],
    type: 'blob'
  }, {
    svg: _Components_Mask_assets_shapes_hexagon_svg__WEBPACK_IMPORTED_MODULE_3__["default"],
    type: 'hexagon'
  }];
  const getShape = type => svgShape.find(e => e.type === type);
  return isMask ? `-webkit-mask-image: ${shape.type === 'custom' ? safeCSSUrl(shape.url) : `url(${getShape(shape.type).svg})`};
		-webkit-mask-size: ${size.type === 'custom' ? size.scale : size.type};
		${position.type === 'custom' ? `${isValidCSS('-webkit-mask-position-x', position.x)}
			${isValidCSS('-webkit-mask-position-y', position.y)}` : `${isValidCSS('-webkit-mask-position', position.type)}`}
		${size.type !== 'cover' ? `-webkit-mask-repeat: ${repeat};` : ''}` : '';
};

/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/effect-coverflow.css":
/*!***********************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/effect-coverflow.css ***!
  \***********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
// extracted by mini-css-extract-plugin


/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/navigation.css":
/*!*****************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/navigation.css ***!
  \*****************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
// extracted by mini-css-extract-plugin


/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/pagination.css":
/*!*****************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/pagination.css ***!
  \*****************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
// extracted by mini-css-extract-plugin


/***/ }),

/***/ "../plugin-slug/node_modules/swiper/swiper.css":
/*!*****************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/swiper.css ***!
  \*****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
// extracted by mini-css-extract-plugin


/***/ }),

/***/ "./src/shared/styles/form.scss":
/*!*************************************!*\
  !*** ./src/shared/styles/form.scss ***!
  \*************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
// extracted by mini-css-extract-plugin


/***/ }),

/***/ "./src/shared/styles/frontend.scss":
/*!*****************************************!*\
  !*** ./src/shared/styles/frontend.scss ***!
  \*****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
// extracted by mini-css-extract-plugin


/***/ }),

/***/ "../plugin-slug/node_modules/object-assign/index.js":
/*!**********************************************************!*\
  !*** ../plugin-slug/node_modules/object-assign/index.js ***!
  \**********************************************************/
/***/ ((module) => {

"use strict";
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/


/* eslint-disable no-unused-vars */
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var hasOwnProperty = Object.prototype.hasOwnProperty;
var propIsEnumerable = Object.prototype.propertyIsEnumerable;

function toObject(val) {
	if (val === null || val === undefined) {
		throw new TypeError('Object.assign cannot be called with null or undefined');
	}

	return Object(val);
}

function shouldUseNative() {
	try {
		if (!Object.assign) {
			return false;
		}

		// Detect buggy property enumeration order in older V8 versions.

		// https://bugs.chromium.org/p/v8/issues/detail?id=4118
		var test1 = new String('abc');  // eslint-disable-line no-new-wrappers
		test1[5] = 'de';
		if (Object.getOwnPropertyNames(test1)[0] === '5') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test2 = {};
		for (var i = 0; i < 10; i++) {
			test2['_' + String.fromCharCode(i)] = i;
		}
		var order2 = Object.getOwnPropertyNames(test2).map(function (n) {
			return test2[n];
		});
		if (order2.join('') !== '0123456789') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test3 = {};
		'abcdefghijklmnopqrst'.split('').forEach(function (letter) {
			test3[letter] = letter;
		});
		if (Object.keys(Object.assign({}, test3)).join('') !==
				'abcdefghijklmnopqrst') {
			return false;
		}

		return true;
	} catch (err) {
		// We don't expect any of the above to throw, but better to be safe.
		return false;
	}
}

module.exports = shouldUseNative() ? Object.assign : function (target, source) {
	var from;
	var to = toObject(target);
	var symbols;

	for (var s = 1; s < arguments.length; s++) {
		from = Object(arguments[s]);

		for (var key in from) {
			if (hasOwnProperty.call(from, key)) {
				to[key] = from[key];
			}
		}

		if (getOwnPropertySymbols) {
			symbols = getOwnPropertySymbols(from);
			for (var i = 0; i < symbols.length; i++) {
				if (propIsEnumerable.call(from, symbols[i])) {
					to[symbols[i]] = from[symbols[i]];
				}
			}
		}
	}

	return to;
};


/***/ }),

/***/ "../plugin-slug/node_modules/prop-types/checkPropTypes.js":
/*!****************************************************************!*\
  !*** ../plugin-slug/node_modules/prop-types/checkPropTypes.js ***!
  \****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var printWarning = function() {};

if (true) {
  var ReactPropTypesSecret = __webpack_require__(/*! ./lib/ReactPropTypesSecret */ "../plugin-slug/node_modules/prop-types/lib/ReactPropTypesSecret.js");
  var loggedTypeFailures = {};
  var has = __webpack_require__(/*! ./lib/has */ "../plugin-slug/node_modules/prop-types/lib/has.js");

  printWarning = function(text) {
    var message = 'Warning: ' + text;
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) { /**/ }
  };
}

/**
 * Assert that the values match with the type specs.
 * Error messages are memorized and will only be shown once.
 *
 * @param {object} typeSpecs Map of name to a ReactPropType
 * @param {object} values Runtime values that need to be type-checked
 * @param {string} location e.g. "prop", "context", "child context"
 * @param {string} componentName Name of the component for error messages.
 * @param {?Function} getStack Returns the component stack.
 * @private
 */
function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
  if (true) {
    for (var typeSpecName in typeSpecs) {
      if (has(typeSpecs, typeSpecName)) {
        var error;
        // Prop type validation may throw. In case they do, we don't want to
        // fail the render phase where it didn't fail before. So we log it.
        // After these have been cleaned up, we'll let them throw.
        try {
          // This is intentionally an invariant that gets caught. It's the same
          // behavior as without this statement except with a better message.
          if (typeof typeSpecs[typeSpecName] !== 'function') {
            var err = Error(
              (componentName || 'React class') + ': ' + location + ' type `' + typeSpecName + '` is invalid; ' +
              'it must be a function, usually from the `prop-types` package, but received `' + typeof typeSpecs[typeSpecName] + '`.' +
              'This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.'
            );
            err.name = 'Invariant Violation';
            throw err;
          }
          error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
        } catch (ex) {
          error = ex;
        }
        if (error && !(error instanceof Error)) {
          printWarning(
            (componentName || 'React class') + ': type specification of ' +
            location + ' `' + typeSpecName + '` is invalid; the type checker ' +
            'function must return `null` or an `Error` but returned a ' + typeof error + '. ' +
            'You may have forgotten to pass an argument to the type checker ' +
            'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' +
            'shape all require an argument).'
          );
        }
        if (error instanceof Error && !(error.message in loggedTypeFailures)) {
          // Only monitor this failure once because there tends to be a lot of the
          // same error.
          loggedTypeFailures[error.message] = true;

          var stack = getStack ? getStack() : '';

          printWarning(
            'Failed ' + location + ' type: ' + error.message + (stack != null ? stack : '')
          );
        }
      }
    }
  }
}

/**
 * Resets warning cache when testing.
 *
 * @private
 */
checkPropTypes.resetWarningCache = function() {
  if (true) {
    loggedTypeFailures = {};
  }
}

module.exports = checkPropTypes;


/***/ }),

/***/ "../plugin-slug/node_modules/prop-types/factoryWithTypeCheckers.js":
/*!*************************************************************************!*\
  !*** ../plugin-slug/node_modules/prop-types/factoryWithTypeCheckers.js ***!
  \*************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var ReactIs = __webpack_require__(/*! react-is */ "../plugin-slug/node_modules/prop-types/node_modules/react-is/index.js");
var assign = __webpack_require__(/*! object-assign */ "../plugin-slug/node_modules/object-assign/index.js");

var ReactPropTypesSecret = __webpack_require__(/*! ./lib/ReactPropTypesSecret */ "../plugin-slug/node_modules/prop-types/lib/ReactPropTypesSecret.js");
var has = __webpack_require__(/*! ./lib/has */ "../plugin-slug/node_modules/prop-types/lib/has.js");
var checkPropTypes = __webpack_require__(/*! ./checkPropTypes */ "../plugin-slug/node_modules/prop-types/checkPropTypes.js");

var printWarning = function() {};

if (true) {
  printWarning = function(text) {
    var message = 'Warning: ' + text;
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };
}

function emptyFunctionThatReturnsNull() {
  return null;
}

module.exports = function(isValidElement, throwOnDirectAccess) {
  /* global Symbol */
  var ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
  var FAUX_ITERATOR_SYMBOL = '@@iterator'; // Before Symbol spec.

  /**
   * Returns the iterator method function contained on the iterable object.
   *
   * Be sure to invoke the function with the iterable as context:
   *
   *     var iteratorFn = getIteratorFn(myIterable);
   *     if (iteratorFn) {
   *       var iterator = iteratorFn.call(myIterable);
   *       ...
   *     }
   *
   * @param {?object} maybeIterable
   * @return {?function}
   */
  function getIteratorFn(maybeIterable) {
    var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
    if (typeof iteratorFn === 'function') {
      return iteratorFn;
    }
  }

  /**
   * Collection of methods that allow declaration and validation of props that are
   * supplied to React components. Example usage:
   *
   *   var Props = require('ReactPropTypes');
   *   var MyArticle = React.createClass({
   *     propTypes: {
   *       // An optional string prop named "description".
   *       description: Props.string,
   *
   *       // A required enum prop named "category".
   *       category: Props.oneOf(['News','Photos']).isRequired,
   *
   *       // A prop named "dialog" that requires an instance of Dialog.
   *       dialog: Props.instanceOf(Dialog).isRequired
   *     },
   *     render: function() { ... }
   *   });
   *
   * A more formal specification of how these methods are used:
   *
   *   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
   *   decl := ReactPropTypes.{type}(.isRequired)?
   *
   * Each and every declaration produces a function with the same signature. This
   * allows the creation of custom validation functions. For example:
   *
   *  var MyLink = React.createClass({
   *    propTypes: {
   *      // An optional string or URI prop named "href".
   *      href: function(props, propName, componentName) {
   *        var propValue = props[propName];
   *        if (propValue != null && typeof propValue !== 'string' &&
   *            !(propValue instanceof URI)) {
   *          return new Error(
   *            'Expected a string or an URI for ' + propName + ' in ' +
   *            componentName
   *          );
   *        }
   *      }
   *    },
   *    render: function() {...}
   *  });
   *
   * @internal
   */

  var ANONYMOUS = '<<anonymous>>';

  // Important!
  // Keep this list in sync with production version in `./factoryWithThrowingShims.js`.
  var ReactPropTypes = {
    array: createPrimitiveTypeChecker('array'),
    bigint: createPrimitiveTypeChecker('bigint'),
    bool: createPrimitiveTypeChecker('boolean'),
    func: createPrimitiveTypeChecker('function'),
    number: createPrimitiveTypeChecker('number'),
    object: createPrimitiveTypeChecker('object'),
    string: createPrimitiveTypeChecker('string'),
    symbol: createPrimitiveTypeChecker('symbol'),

    any: createAnyTypeChecker(),
    arrayOf: createArrayOfTypeChecker,
    element: createElementTypeChecker(),
    elementType: createElementTypeTypeChecker(),
    instanceOf: createInstanceTypeChecker,
    node: createNodeChecker(),
    objectOf: createObjectOfTypeChecker,
    oneOf: createEnumTypeChecker,
    oneOfType: createUnionTypeChecker,
    shape: createShapeTypeChecker,
    exact: createStrictShapeTypeChecker,
  };

  /**
   * inlined Object.is polyfill to avoid requiring consumers ship their own
   * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
   */
  /*eslint-disable no-self-compare*/
  function is(x, y) {
    // SameValue algorithm
    if (x === y) {
      // Steps 1-5, 7-10
      // Steps 6.b-6.e: +0 != -0
      return x !== 0 || 1 / x === 1 / y;
    } else {
      // Step 6.a: NaN == NaN
      return x !== x && y !== y;
    }
  }
  /*eslint-enable no-self-compare*/

  /**
   * We use an Error-like object for backward compatibility as people may call
   * PropTypes directly and inspect their output. However, we don't use real
   * Errors anymore. We don't inspect their stack anyway, and creating them
   * is prohibitively expensive if they are created too often, such as what
   * happens in oneOfType() for any type before the one that matched.
   */
  function PropTypeError(message, data) {
    this.message = message;
    this.data = data && typeof data === 'object' ? data: {};
    this.stack = '';
  }
  // Make `instanceof Error` still work for returned errors.
  PropTypeError.prototype = Error.prototype;

  function createChainableTypeChecker(validate) {
    if (true) {
      var manualPropTypeCallCache = {};
      var manualPropTypeWarningCount = 0;
    }
    function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
      componentName = componentName || ANONYMOUS;
      propFullName = propFullName || propName;

      if (secret !== ReactPropTypesSecret) {
        if (throwOnDirectAccess) {
          // New behavior only for users of `prop-types` package
          var err = new Error(
            'Calling PropTypes validators directly is not supported by the `prop-types` package. ' +
            'Use `PropTypes.checkPropTypes()` to call them. ' +
            'Read more at http://fb.me/use-check-prop-types'
          );
          err.name = 'Invariant Violation';
          throw err;
        } else if ( true && typeof console !== 'undefined') {
          // Old behavior for people using React.PropTypes
          var cacheKey = componentName + ':' + propName;
          if (
            !manualPropTypeCallCache[cacheKey] &&
            // Avoid spamming the console because they are often not actionable except for lib authors
            manualPropTypeWarningCount < 3
          ) {
            printWarning(
              'You are manually calling a React.PropTypes validation ' +
              'function for the `' + propFullName + '` prop on `' + componentName + '`. This is deprecated ' +
              'and will throw in the standalone `prop-types` package. ' +
              'You may be seeing this warning due to a third-party PropTypes ' +
              'library. See https://fb.me/react-warning-dont-call-proptypes ' + 'for details.'
            );
            manualPropTypeCallCache[cacheKey] = true;
            manualPropTypeWarningCount++;
          }
        }
      }
      if (props[propName] == null) {
        if (isRequired) {
          if (props[propName] === null) {
            return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required ' + ('in `' + componentName + '`, but its value is `null`.'));
          }
          return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required in ' + ('`' + componentName + '`, but its value is `undefined`.'));
        }
        return null;
      } else {
        return validate(props, propName, componentName, location, propFullName);
      }
    }

    var chainedCheckType = checkType.bind(null, false);
    chainedCheckType.isRequired = checkType.bind(null, true);

    return chainedCheckType;
  }

  function createPrimitiveTypeChecker(expectedType) {
    function validate(props, propName, componentName, location, propFullName, secret) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== expectedType) {
        // `propValue` being instance of, say, date/regexp, pass the 'object'
        // check, but we can offer a more precise error message here rather than
        // 'of type `object`'.
        var preciseType = getPreciseType(propValue);

        return new PropTypeError(
          'Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + preciseType + '` supplied to `' + componentName + '`, expected ') + ('`' + expectedType + '`.'),
          {expectedType: expectedType}
        );
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createAnyTypeChecker() {
    return createChainableTypeChecker(emptyFunctionThatReturnsNull);
  }

  function createArrayOfTypeChecker(typeChecker) {
    function validate(props, propName, componentName, location, propFullName) {
      if (typeof typeChecker !== 'function') {
        return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside arrayOf.');
      }
      var propValue = props[propName];
      if (!Array.isArray(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an array.'));
      }
      for (var i = 0; i < propValue.length; i++) {
        var error = typeChecker(propValue, i, componentName, location, propFullName + '[' + i + ']', ReactPropTypesSecret);
        if (error instanceof Error) {
          return error;
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createElementTypeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      if (!isValidElement(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected a single ReactElement.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createElementTypeTypeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      if (!ReactIs.isValidElementType(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected a single ReactElement type.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createInstanceTypeChecker(expectedClass) {
    function validate(props, propName, componentName, location, propFullName) {
      if (!(props[propName] instanceof expectedClass)) {
        var expectedClassName = expectedClass.name || ANONYMOUS;
        var actualClassName = getClassName(props[propName]);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + actualClassName + '` supplied to `' + componentName + '`, expected ') + ('instance of `' + expectedClassName + '`.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createEnumTypeChecker(expectedValues) {
    if (!Array.isArray(expectedValues)) {
      if (true) {
        if (arguments.length > 1) {
          printWarning(
            'Invalid arguments supplied to oneOf, expected an array, got ' + arguments.length + ' arguments. ' +
            'A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z]).'
          );
        } else {
          printWarning('Invalid argument supplied to oneOf, expected an array.');
        }
      }
      return emptyFunctionThatReturnsNull;
    }

    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      for (var i = 0; i < expectedValues.length; i++) {
        if (is(propValue, expectedValues[i])) {
          return null;
        }
      }

      var valuesString = JSON.stringify(expectedValues, function replacer(key, value) {
        var type = getPreciseType(value);
        if (type === 'symbol') {
          return String(value);
        }
        return value;
      });
      return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of value `' + String(propValue) + '` ' + ('supplied to `' + componentName + '`, expected one of ' + valuesString + '.'));
    }
    return createChainableTypeChecker(validate);
  }

  function createObjectOfTypeChecker(typeChecker) {
    function validate(props, propName, componentName, location, propFullName) {
      if (typeof typeChecker !== 'function') {
        return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside objectOf.');
      }
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an object.'));
      }
      for (var key in propValue) {
        if (has(propValue, key)) {
          var error = typeChecker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
          if (error instanceof Error) {
            return error;
          }
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createUnionTypeChecker(arrayOfTypeCheckers) {
    if (!Array.isArray(arrayOfTypeCheckers)) {
       true ? printWarning('Invalid argument supplied to oneOfType, expected an instance of array.') : 0;
      return emptyFunctionThatReturnsNull;
    }

    for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
      var checker = arrayOfTypeCheckers[i];
      if (typeof checker !== 'function') {
        printWarning(
          'Invalid argument supplied to oneOfType. Expected an array of check functions, but ' +
          'received ' + getPostfixForTypeWarning(checker) + ' at index ' + i + '.'
        );
        return emptyFunctionThatReturnsNull;
      }
    }

    function validate(props, propName, componentName, location, propFullName) {
      var expectedTypes = [];
      for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
        var checker = arrayOfTypeCheckers[i];
        var checkerResult = checker(props, propName, componentName, location, propFullName, ReactPropTypesSecret);
        if (checkerResult == null) {
          return null;
        }
        if (checkerResult.data && has(checkerResult.data, 'expectedType')) {
          expectedTypes.push(checkerResult.data.expectedType);
        }
      }
      var expectedTypesMessage = (expectedTypes.length > 0) ? ', expected one of type [' + expectedTypes.join(', ') + ']': '';
      return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`' + expectedTypesMessage + '.'));
    }
    return createChainableTypeChecker(validate);
  }

  function createNodeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      if (!isNode(props[propName])) {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`, expected a ReactNode.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function invalidValidatorError(componentName, location, propFullName, key, type) {
    return new PropTypeError(
      (componentName || 'React class') + ': ' + location + ' type `' + propFullName + '.' + key + '` is invalid; ' +
      'it must be a function, usually from the `prop-types` package, but received `' + type + '`.'
    );
  }

  function createShapeTypeChecker(shapeTypes) {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
      }
      for (var key in shapeTypes) {
        var checker = shapeTypes[key];
        if (typeof checker !== 'function') {
          return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
        }
        var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
        if (error) {
          return error;
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createStrictShapeTypeChecker(shapeTypes) {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
      }
      // We need to check all keys in case some are required but missing from props.
      var allKeys = assign({}, props[propName], shapeTypes);
      for (var key in allKeys) {
        var checker = shapeTypes[key];
        if (has(shapeTypes, key) && typeof checker !== 'function') {
          return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
        }
        if (!checker) {
          return new PropTypeError(
            'Invalid ' + location + ' `' + propFullName + '` key `' + key + '` supplied to `' + componentName + '`.' +
            '\nBad object: ' + JSON.stringify(props[propName], null, '  ') +
            '\nValid keys: ' + JSON.stringify(Object.keys(shapeTypes), null, '  ')
          );
        }
        var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
        if (error) {
          return error;
        }
      }
      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function isNode(propValue) {
    switch (typeof propValue) {
      case 'number':
      case 'string':
      case 'undefined':
        return true;
      case 'boolean':
        return !propValue;
      case 'object':
        if (Array.isArray(propValue)) {
          return propValue.every(isNode);
        }
        if (propValue === null || isValidElement(propValue)) {
          return true;
        }

        var iteratorFn = getIteratorFn(propValue);
        if (iteratorFn) {
          var iterator = iteratorFn.call(propValue);
          var step;
          if (iteratorFn !== propValue.entries) {
            while (!(step = iterator.next()).done) {
              if (!isNode(step.value)) {
                return false;
              }
            }
          } else {
            // Iterator will provide entry [k,v] tuples rather than values.
            while (!(step = iterator.next()).done) {
              var entry = step.value;
              if (entry) {
                if (!isNode(entry[1])) {
                  return false;
                }
              }
            }
          }
        } else {
          return false;
        }

        return true;
      default:
        return false;
    }
  }

  function isSymbol(propType, propValue) {
    // Native Symbol.
    if (propType === 'symbol') {
      return true;
    }

    // falsy value can't be a Symbol
    if (!propValue) {
      return false;
    }

    // 19.4.3.5 Symbol.prototype[@@toStringTag] === 'Symbol'
    if (propValue['@@toStringTag'] === 'Symbol') {
      return true;
    }

    // Fallback for non-spec compliant Symbols which are polyfilled.
    if (typeof Symbol === 'function' && propValue instanceof Symbol) {
      return true;
    }

    return false;
  }

  // Equivalent of `typeof` but with special handling for array and regexp.
  function getPropType(propValue) {
    var propType = typeof propValue;
    if (Array.isArray(propValue)) {
      return 'array';
    }
    if (propValue instanceof RegExp) {
      // Old webkits (at least until Android 4.0) return 'function' rather than
      // 'object' for typeof a RegExp. We'll normalize this here so that /bla/
      // passes PropTypes.object.
      return 'object';
    }
    if (isSymbol(propType, propValue)) {
      return 'symbol';
    }
    return propType;
  }

  // This handles more types than `getPropType`. Only used for error messages.
  // See `createPrimitiveTypeChecker`.
  function getPreciseType(propValue) {
    if (typeof propValue === 'undefined' || propValue === null) {
      return '' + propValue;
    }
    var propType = getPropType(propValue);
    if (propType === 'object') {
      if (propValue instanceof Date) {
        return 'date';
      } else if (propValue instanceof RegExp) {
        return 'regexp';
      }
    }
    return propType;
  }

  // Returns a string that is postfixed to a warning about an invalid type.
  // For example, "undefined" or "of type array"
  function getPostfixForTypeWarning(value) {
    var type = getPreciseType(value);
    switch (type) {
      case 'array':
      case 'object':
        return 'an ' + type;
      case 'boolean':
      case 'date':
      case 'regexp':
        return 'a ' + type;
      default:
        return type;
    }
  }

  // Returns class name of the object, if any.
  function getClassName(propValue) {
    if (!propValue.constructor || !propValue.constructor.name) {
      return ANONYMOUS;
    }
    return propValue.constructor.name;
  }

  ReactPropTypes.checkPropTypes = checkPropTypes;
  ReactPropTypes.resetWarningCache = checkPropTypes.resetWarningCache;
  ReactPropTypes.PropTypes = ReactPropTypes;

  return ReactPropTypes;
};


/***/ }),

/***/ "../plugin-slug/node_modules/prop-types/index.js":
/*!*******************************************************!*\
  !*** ../plugin-slug/node_modules/prop-types/index.js ***!
  \*******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

if (true) {
  var ReactIs = __webpack_require__(/*! react-is */ "../plugin-slug/node_modules/prop-types/node_modules/react-is/index.js");

  // By explicitly using `prop-types` you are opting into new development behavior.
  // http://fb.me/prop-types-in-prod
  var throwOnDirectAccess = true;
  module.exports = __webpack_require__(/*! ./factoryWithTypeCheckers */ "../plugin-slug/node_modules/prop-types/factoryWithTypeCheckers.js")(ReactIs.isElement, throwOnDirectAccess);
} else {}


/***/ }),

/***/ "../plugin-slug/node_modules/prop-types/lib/ReactPropTypesSecret.js":
/*!**************************************************************************!*\
  !*** ../plugin-slug/node_modules/prop-types/lib/ReactPropTypesSecret.js ***!
  \**************************************************************************/
/***/ ((module) => {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var ReactPropTypesSecret = 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED';

module.exports = ReactPropTypesSecret;


/***/ }),

/***/ "../plugin-slug/node_modules/prop-types/lib/has.js":
/*!*********************************************************!*\
  !*** ../plugin-slug/node_modules/prop-types/lib/has.js ***!
  \*********************************************************/
/***/ ((module) => {

module.exports = Function.call.bind(Object.prototype.hasOwnProperty);


/***/ }),

/***/ "../plugin-slug/node_modules/prop-types/node_modules/react-is/cjs/react-is.development.js":
/*!************************************************************************************************!*\
  !*** ../plugin-slug/node_modules/prop-types/node_modules/react-is/cjs/react-is.development.js ***!
  \************************************************************************************************/
/***/ ((__unused_webpack_module, exports) => {

"use strict";
/** @license React v16.13.1
 * react-is.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */





if (true) {
  (function() {
'use strict';

// The Symbol used to tag the ReactElement-like types. If there is no native Symbol
// nor polyfill, then a plain number is used for performance.
var hasSymbol = typeof Symbol === 'function' && Symbol.for;
var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for('react.element') : 0xeac7;
var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for('react.portal') : 0xeaca;
var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for('react.fragment') : 0xeacb;
var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for('react.strict_mode') : 0xeacc;
var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for('react.profiler') : 0xead2;
var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for('react.provider') : 0xeacd;
var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for('react.context') : 0xeace; // TODO: We don't use AsyncMode or ConcurrentMode anymore. They were temporary
// (unstable) APIs that have been removed. Can we remove the symbols?

var REACT_ASYNC_MODE_TYPE = hasSymbol ? Symbol.for('react.async_mode') : 0xeacf;
var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for('react.concurrent_mode') : 0xeacf;
var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for('react.forward_ref') : 0xead0;
var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for('react.suspense') : 0xead1;
var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for('react.suspense_list') : 0xead8;
var REACT_MEMO_TYPE = hasSymbol ? Symbol.for('react.memo') : 0xead3;
var REACT_LAZY_TYPE = hasSymbol ? Symbol.for('react.lazy') : 0xead4;
var REACT_BLOCK_TYPE = hasSymbol ? Symbol.for('react.block') : 0xead9;
var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for('react.fundamental') : 0xead5;
var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for('react.responder') : 0xead6;
var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for('react.scope') : 0xead7;

function isValidElementType(type) {
  return typeof type === 'string' || typeof type === 'function' || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
  type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === 'object' && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);
}

function typeOf(object) {
  if (typeof object === 'object' && object !== null) {
    var $$typeof = object.$$typeof;

    switch ($$typeof) {
      case REACT_ELEMENT_TYPE:
        var type = object.type;

        switch (type) {
          case REACT_ASYNC_MODE_TYPE:
          case REACT_CONCURRENT_MODE_TYPE:
          case REACT_FRAGMENT_TYPE:
          case REACT_PROFILER_TYPE:
          case REACT_STRICT_MODE_TYPE:
          case REACT_SUSPENSE_TYPE:
            return type;

          default:
            var $$typeofType = type && type.$$typeof;

            switch ($$typeofType) {
              case REACT_CONTEXT_TYPE:
              case REACT_FORWARD_REF_TYPE:
              case REACT_LAZY_TYPE:
              case REACT_MEMO_TYPE:
              case REACT_PROVIDER_TYPE:
                return $$typeofType;

              default:
                return $$typeof;
            }

        }

      case REACT_PORTAL_TYPE:
        return $$typeof;
    }
  }

  return undefined;
} // AsyncMode is deprecated along with isAsyncMode

var AsyncMode = REACT_ASYNC_MODE_TYPE;
var ConcurrentMode = REACT_CONCURRENT_MODE_TYPE;
var ContextConsumer = REACT_CONTEXT_TYPE;
var ContextProvider = REACT_PROVIDER_TYPE;
var Element = REACT_ELEMENT_TYPE;
var ForwardRef = REACT_FORWARD_REF_TYPE;
var Fragment = REACT_FRAGMENT_TYPE;
var Lazy = REACT_LAZY_TYPE;
var Memo = REACT_MEMO_TYPE;
var Portal = REACT_PORTAL_TYPE;
var Profiler = REACT_PROFILER_TYPE;
var StrictMode = REACT_STRICT_MODE_TYPE;
var Suspense = REACT_SUSPENSE_TYPE;
var hasWarnedAboutDeprecatedIsAsyncMode = false; // AsyncMode should be deprecated

function isAsyncMode(object) {
  {
    if (!hasWarnedAboutDeprecatedIsAsyncMode) {
      hasWarnedAboutDeprecatedIsAsyncMode = true; // Using console['warn'] to evade Babel and ESLint

      console['warn']('The ReactIs.isAsyncMode() alias has been deprecated, ' + 'and will be removed in React 17+. Update your code to use ' + 'ReactIs.isConcurrentMode() instead. It has the exact same API.');
    }
  }

  return isConcurrentMode(object) || typeOf(object) === REACT_ASYNC_MODE_TYPE;
}
function isConcurrentMode(object) {
  return typeOf(object) === REACT_CONCURRENT_MODE_TYPE;
}
function isContextConsumer(object) {
  return typeOf(object) === REACT_CONTEXT_TYPE;
}
function isContextProvider(object) {
  return typeOf(object) === REACT_PROVIDER_TYPE;
}
function isElement(object) {
  return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
}
function isForwardRef(object) {
  return typeOf(object) === REACT_FORWARD_REF_TYPE;
}
function isFragment(object) {
  return typeOf(object) === REACT_FRAGMENT_TYPE;
}
function isLazy(object) {
  return typeOf(object) === REACT_LAZY_TYPE;
}
function isMemo(object) {
  return typeOf(object) === REACT_MEMO_TYPE;
}
function isPortal(object) {
  return typeOf(object) === REACT_PORTAL_TYPE;
}
function isProfiler(object) {
  return typeOf(object) === REACT_PROFILER_TYPE;
}
function isStrictMode(object) {
  return typeOf(object) === REACT_STRICT_MODE_TYPE;
}
function isSuspense(object) {
  return typeOf(object) === REACT_SUSPENSE_TYPE;
}

exports.AsyncMode = AsyncMode;
exports.ConcurrentMode = ConcurrentMode;
exports.ContextConsumer = ContextConsumer;
exports.ContextProvider = ContextProvider;
exports.Element = Element;
exports.ForwardRef = ForwardRef;
exports.Fragment = Fragment;
exports.Lazy = Lazy;
exports.Memo = Memo;
exports.Portal = Portal;
exports.Profiler = Profiler;
exports.StrictMode = StrictMode;
exports.Suspense = Suspense;
exports.isAsyncMode = isAsyncMode;
exports.isConcurrentMode = isConcurrentMode;
exports.isContextConsumer = isContextConsumer;
exports.isContextProvider = isContextProvider;
exports.isElement = isElement;
exports.isForwardRef = isForwardRef;
exports.isFragment = isFragment;
exports.isLazy = isLazy;
exports.isMemo = isMemo;
exports.isPortal = isPortal;
exports.isProfiler = isProfiler;
exports.isStrictMode = isStrictMode;
exports.isSuspense = isSuspense;
exports.isValidElementType = isValidElementType;
exports.typeOf = typeOf;
  })();
}


/***/ }),

/***/ "../plugin-slug/node_modules/prop-types/node_modules/react-is/index.js":
/*!*****************************************************************************!*\
  !*** ../plugin-slug/node_modules/prop-types/node_modules/react-is/index.js ***!
  \*****************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";


if (false) {} else {
  module.exports = __webpack_require__(/*! ./cjs/react-is.development.js */ "../plugin-slug/node_modules/prop-types/node_modules/react-is/cjs/react-is.development.js");
}


/***/ }),

/***/ "../plugin-slug/node_modules/react-responsive-masonry/es/Masonry/index.js":
/*!********************************************************************************!*\
  !*** ../plugin-slug/node_modules/react-responsive-masonry/es/Masonry/index.js ***!
  \********************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! prop-types */ "../plugin-slug/node_modules/prop-types/index.js");
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(prop_types__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
function _extends() { _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf(o, p) { o.__proto__ = p; return o; }; return _setPrototypeOf(o, p); }




var Masonry = /*#__PURE__*/function (_React$Component) {
  _inheritsLoose(Masonry, _React$Component);

  function Masonry() {
    var _this;

    _this = _React$Component.call(this) || this;
    _this.state = {
      columns: [],
      childRefs: [],
      hasDistributed: false
    };
    return _this;
  }

  var _proto = Masonry.prototype;

  _proto.componentDidUpdate = function componentDidUpdate() {
    if (!this.state.hasDistributed && !this.props.sequential) this.distributeChildren();
  };

  Masonry.getDerivedStateFromProps = function getDerivedStateFromProps(props, state) {
    var children = props.children,
        columnsCount = props.columnsCount;
    var hasColumnsChanged = columnsCount !== state.columns.length;
    if (state && children === state.children && !hasColumnsChanged) return null;
    return _extends({}, Masonry.getEqualCountColumns(children, columnsCount), {
      children: children,
      hasDistributed: false
    });
  };

  _proto.shouldComponentUpdate = function shouldComponentUpdate(nextProps) {
    return nextProps.children !== this.state.children || nextProps.columnsCount !== this.props.columnsCount;
  };

  _proto.distributeChildren = function distributeChildren() {
    var _this2 = this;

    var _this$props = this.props,
        children = _this$props.children,
        columnsCount = _this$props.columnsCount;
    var columnHeights = Array(columnsCount).fill(0);
    var isReady = this.state.childRefs.every(function (ref) {
      return ref.current.getBoundingClientRect().height;
    });
    if (!isReady) return;
    var columns = Array.from({
      length: columnsCount
    }, function () {
      return [];
    });
    var validIndex = 0;
    react__WEBPACK_IMPORTED_MODULE_0___default().Children.forEach(children, function (child) {
      if (child && react__WEBPACK_IMPORTED_MODULE_0___default().isValidElement(child)) {
        // .current is undefined if ref was passed to a functional component without forwardRef
        // now passing ref into a wrapper div so it should always be defined
        var childHeight = _this2.state.childRefs[validIndex].current.getBoundingClientRect().height;

        var minHeightColumnIndex = columnHeights.indexOf(Math.min.apply(Math, columnHeights));
        columnHeights[minHeightColumnIndex] += childHeight;
        columns[minHeightColumnIndex].push(child);
        validIndex++;
      }
    });
    this.setState(function (p) {
      return _extends({}, p, {
        columns: columns,
        hasDistributed: true
      });
    });
  };

  Masonry.getEqualCountColumns = function getEqualCountColumns(children, columnsCount) {
    var columns = Array.from({
      length: columnsCount
    }, function () {
      return [];
    });
    var validIndex = 0;
    var childRefs = [];
    react__WEBPACK_IMPORTED_MODULE_0___default().Children.forEach(children, function (child) {
      if (child && react__WEBPACK_IMPORTED_MODULE_0___default().isValidElement(child)) {
        var ref = react__WEBPACK_IMPORTED_MODULE_0___default().createRef();
        childRefs.push(ref);
        columns[validIndex % columnsCount].push( /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0___default().createElement("div", {
          style: {
            display: "flex",
            justifyContent: "stretch"
          },
          key: validIndex,
          ref: ref
        }, child) // React.cloneElement(child, {ref}) // cannot attach refs to functional components without forwardRef
        );
        validIndex++;
      }
    });
    return {
      columns: columns,
      childRefs: childRefs
    };
  };

  _proto.renderColumns = function renderColumns() {
    var _this$props2 = this.props,
        gutter = _this$props2.gutter,
        itemTag = _this$props2.itemTag,
        itemStyle = _this$props2.itemStyle;
    return this.state.columns.map(function (column, i) {
      return react__WEBPACK_IMPORTED_MODULE_0___default().createElement(itemTag, {
        key: i,
        style: _extends({
          display: "flex",
          flexDirection: "column",
          justifyContent: "flex-start",
          alignContent: "stretch",
          flex: 1,
          width: 0,
          gap: gutter
        }, itemStyle)
      }, column.map(function (item) {
        return item;
      }));
    });
  };

  _proto.render = function render() {
    var _this$props3 = this.props,
        gutter = _this$props3.gutter,
        className = _this$props3.className,
        style = _this$props3.style,
        containerTag = _this$props3.containerTag;
    return react__WEBPACK_IMPORTED_MODULE_0___default().createElement(containerTag, {
      style: _extends({
        display: "flex",
        flexDirection: "row",
        justifyContent: "center",
        alignContent: "stretch",
        boxSizing: "border-box",
        width: "100%",
        gap: gutter
      }, style),
      className: className
    }, this.renderColumns());
  };

  return Masonry;
}((react__WEBPACK_IMPORTED_MODULE_0___default().Component));

Masonry.propTypes =  true ? {
  children: prop_types__WEBPACK_IMPORTED_MODULE_1___default().oneOfType([prop_types__WEBPACK_IMPORTED_MODULE_1___default().arrayOf((prop_types__WEBPACK_IMPORTED_MODULE_1___default().node)), (prop_types__WEBPACK_IMPORTED_MODULE_1___default().node)]).isRequired,
  columnsCount: (prop_types__WEBPACK_IMPORTED_MODULE_1___default().number),
  gutter: (prop_types__WEBPACK_IMPORTED_MODULE_1___default().string),
  className: (prop_types__WEBPACK_IMPORTED_MODULE_1___default().string),
  style: (prop_types__WEBPACK_IMPORTED_MODULE_1___default().object),
  containerTag: (prop_types__WEBPACK_IMPORTED_MODULE_1___default().string),
  itemTag: (prop_types__WEBPACK_IMPORTED_MODULE_1___default().string),
  itemStyle: (prop_types__WEBPACK_IMPORTED_MODULE_1___default().object),
  sequential: (prop_types__WEBPACK_IMPORTED_MODULE_1___default().bool)
} : 0;
Masonry.defaultProps = {
  columnsCount: 3,
  gutter: "0",
  className: null,
  style: {},
  containerTag: "div",
  itemTag: "div",
  itemStyle: {},
  sequential: false
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Masonry);

/***/ }),

/***/ "../plugin-slug/node_modules/react-responsive-masonry/es/ResponsiveMasonry/index.js":
/*!******************************************************************************************!*\
  !*** ../plugin-slug/node_modules/react-responsive-masonry/es/ResponsiveMasonry/index.js ***!
  \******************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! prop-types */ "../plugin-slug/node_modules/prop-types/index.js");
/* harmony import */ var prop_types__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(prop_types__WEBPACK_IMPORTED_MODULE_1__);


var DEFAULT_COLUMNS_COUNT = 1;
var DEFAULT_GUTTER = "10px";
var useIsomorphicLayoutEffect = typeof window !== "undefined" ? react__WEBPACK_IMPORTED_MODULE_0__.useLayoutEffect : react__WEBPACK_IMPORTED_MODULE_0__.useEffect;

var useHasMounted = function useHasMounted() {
  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false),
      hasMounted = _useState[0],
      setHasMounted = _useState[1];

  useIsomorphicLayoutEffect(function () {
    setHasMounted(true);
  }, []);
  return hasMounted;
};

var useWindowWidth = function useWindowWidth() {
  var hasMounted = useHasMounted();

  var _useState2 = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(typeof window !== "undefined" ? window.innerWidth : 0),
      width = _useState2[0],
      setWidth = _useState2[1];

  var handleResize = (0,react__WEBPACK_IMPORTED_MODULE_0__.useCallback)(function () {
    if (!hasMounted) return;
    setWidth(window.innerWidth);
  }, [hasMounted]);
  useIsomorphicLayoutEffect(function () {
    if (hasMounted) {
      window.addEventListener("resize", handleResize);
      handleResize();
      return function () {
        return window.removeEventListener("resize", handleResize);
      };
    }
  }, [hasMounted, handleResize]);
  return width;
};

var MasonryResponsive = function MasonryResponsive(_ref) {
  var _ref$columnsCountBrea = _ref.columnsCountBreakPoints,
      columnsCountBreakPoints = _ref$columnsCountBrea === void 0 ? {
    350: 1,
    750: 2,
    900: 3
  } : _ref$columnsCountBrea,
      _ref$gutterBreakPoint = _ref.gutterBreakPoints,
      gutterBreakPoints = _ref$gutterBreakPoint === void 0 ? {} : _ref$gutterBreakPoint,
      children = _ref.children,
      _ref$className = _ref.className,
      className = _ref$className === void 0 ? null : _ref$className,
      _ref$style = _ref.style,
      style = _ref$style === void 0 ? null : _ref$style;
  var windowWidth = useWindowWidth();
  var getResponsiveValue = (0,react__WEBPACK_IMPORTED_MODULE_0__.useCallback)(function (breakPoints, defaultValue) {
    var sortedBreakPoints = Object.keys(breakPoints).sort(function (a, b) {
      return a - b;
    });
    var value = sortedBreakPoints.length > 0 ? breakPoints[sortedBreakPoints[0]] : defaultValue;
    sortedBreakPoints.forEach(function (breakPoint) {
      if (breakPoint < windowWidth) {
        value = breakPoints[breakPoint];
      }
    });
    return value;
  }, [windowWidth]);
  var columnsCount = (0,react__WEBPACK_IMPORTED_MODULE_0__.useMemo)(function () {
    return getResponsiveValue(columnsCountBreakPoints, DEFAULT_COLUMNS_COUNT);
  }, [getResponsiveValue, columnsCountBreakPoints]);
  var gutter = (0,react__WEBPACK_IMPORTED_MODULE_0__.useMemo)(function () {
    return getResponsiveValue(gutterBreakPoints, DEFAULT_GUTTER);
  }, [getResponsiveValue, gutterBreakPoints]);
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0___default().createElement("div", {
    className: className,
    style: style
  }, react__WEBPACK_IMPORTED_MODULE_0___default().Children.map(children, function (child, index) {
    return react__WEBPACK_IMPORTED_MODULE_0___default().cloneElement(child, {
      key: index,
      columnsCount: columnsCount,
      gutter: gutter
    });
  }));
};

MasonryResponsive.propTypes =  true ? {
  children: prop_types__WEBPACK_IMPORTED_MODULE_1___default().oneOfType([prop_types__WEBPACK_IMPORTED_MODULE_1___default().arrayOf((prop_types__WEBPACK_IMPORTED_MODULE_1___default().node)), (prop_types__WEBPACK_IMPORTED_MODULE_1___default().node)]).isRequired,
  columnsCountBreakPoints: (prop_types__WEBPACK_IMPORTED_MODULE_1___default().object),
  className: (prop_types__WEBPACK_IMPORTED_MODULE_1___default().string),
  style: (prop_types__WEBPACK_IMPORTED_MODULE_1___default().object)
} : 0;
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (MasonryResponsive);

/***/ }),

/***/ "../plugin-slug/node_modules/react-responsive-masonry/es/index.js":
/*!************************************************************************!*\
  !*** ../plugin-slug/node_modules/react-responsive-masonry/es/index.js ***!
  \************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ResponsiveMasonry: () => (/* reexport safe */ _ResponsiveMasonry__WEBPACK_IMPORTED_MODULE_1__["default"]),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _Masonry__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Masonry */ "../plugin-slug/node_modules/react-responsive-masonry/es/Masonry/index.js");
/* harmony import */ var _ResponsiveMasonry__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./ResponsiveMasonry */ "../plugin-slug/node_modules/react-responsive-masonry/es/ResponsiveMasonry/index.js");


/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (_Masonry__WEBPACK_IMPORTED_MODULE_0__["default"]);


/***/ }),

/***/ "react":
/*!************************!*\
  !*** external "React" ***!
  \************************/
/***/ ((module) => {

"use strict";
module.exports = window["React"];

/***/ }),

/***/ "@wordpress/element":
/*!*********************************!*\
  !*** external ["wp","element"] ***!
  \*********************************/
/***/ ((module) => {

"use strict";
module.exports = window["wp"]["element"];

/***/ }),

/***/ "@wordpress/i18n":
/*!******************************!*\
  !*** external ["wp","i18n"] ***!
  \******************************/
/***/ ((module) => {

"use strict";
module.exports = window["wp"]["i18n"];

/***/ }),

/***/ "../bpl-tools/node_modules/immer/dist/immer.mjs":
/*!******************************************************!*\
  !*** ../bpl-tools/node_modules/immer/dist/immer.mjs ***!
  \******************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Immer: () => (/* binding */ Immer2),
/* harmony export */   applyPatches: () => (/* binding */ applyPatches),
/* harmony export */   castDraft: () => (/* binding */ castDraft),
/* harmony export */   castImmutable: () => (/* binding */ castImmutable),
/* harmony export */   createDraft: () => (/* binding */ createDraft),
/* harmony export */   current: () => (/* binding */ current),
/* harmony export */   enableMapSet: () => (/* binding */ enableMapSet),
/* harmony export */   enablePatches: () => (/* binding */ enablePatches),
/* harmony export */   finishDraft: () => (/* binding */ finishDraft),
/* harmony export */   freeze: () => (/* binding */ freeze),
/* harmony export */   immerable: () => (/* binding */ DRAFTABLE),
/* harmony export */   isDraft: () => (/* binding */ isDraft),
/* harmony export */   isDraftable: () => (/* binding */ isDraftable),
/* harmony export */   nothing: () => (/* binding */ NOTHING),
/* harmony export */   original: () => (/* binding */ original),
/* harmony export */   produce: () => (/* binding */ produce),
/* harmony export */   produceWithPatches: () => (/* binding */ produceWithPatches),
/* harmony export */   setAutoFreeze: () => (/* binding */ setAutoFreeze),
/* harmony export */   setUseStrictShallowCopy: () => (/* binding */ setUseStrictShallowCopy)
/* harmony export */ });
// src/utils/env.ts
var NOTHING = Symbol.for("immer-nothing");
var DRAFTABLE = Symbol.for("immer-draftable");
var DRAFT_STATE = Symbol.for("immer-state");

// src/utils/errors.ts
var errors =  true ? [
  // All error codes, starting by 0:
  function(plugin) {
    return `The plugin for '${plugin}' has not been loaded into Immer. To enable the plugin, import and call \`enable${plugin}()\` when initializing your application.`;
  },
  function(thing) {
    return `produce can only be called on things that are draftable: plain objects, arrays, Map, Set or classes that are marked with '[immerable]: true'. Got '${thing}'`;
  },
  "This object has been frozen and should not be mutated",
  function(data) {
    return "Cannot use a proxy that has been revoked. Did you pass an object from inside an immer function to an async process? " + data;
  },
  "An immer producer returned a new value *and* modified its draft. Either return a new value *or* modify the draft.",
  "Immer forbids circular references",
  "The first or second argument to `produce` must be a function",
  "The third argument to `produce` must be a function or undefined",
  "First argument to `createDraft` must be a plain object, an array, or an immerable object",
  "First argument to `finishDraft` must be a draft returned by `createDraft`",
  function(thing) {
    return `'current' expects a draft, got: ${thing}`;
  },
  "Object.defineProperty() cannot be used on an Immer draft",
  "Object.setPrototypeOf() cannot be used on an Immer draft",
  "Immer only supports deleting array indices",
  "Immer only supports setting array indices and the 'length' property",
  function(thing) {
    return `'original' expects a draft, got: ${thing}`;
  }
  // Note: if more errors are added, the errorOffset in Patches.ts should be increased
  // See Patches.ts for additional errors
] : 0;
function die(error, ...args) {
  if (true) {
    const e = errors[error];
    const msg = typeof e === "function" ? e.apply(null, args) : e;
    throw new Error(`[Immer] ${msg}`);
  }
  throw new Error(
    `[Immer] minified error nr: ${error}. Full error at: https://bit.ly/3cXEKWf`
  );
}

// src/utils/common.ts
var getPrototypeOf = Object.getPrototypeOf;
function isDraft(value) {
  return !!value && !!value[DRAFT_STATE];
}
function isDraftable(value) {
  if (!value)
    return false;
  return isPlainObject(value) || Array.isArray(value) || !!value[DRAFTABLE] || !!value.constructor?.[DRAFTABLE] || isMap(value) || isSet(value);
}
var objectCtorString = Object.prototype.constructor.toString();
function isPlainObject(value) {
  if (!value || typeof value !== "object")
    return false;
  const proto = getPrototypeOf(value);
  if (proto === null) {
    return true;
  }
  const Ctor = Object.hasOwnProperty.call(proto, "constructor") && proto.constructor;
  if (Ctor === Object)
    return true;
  return typeof Ctor == "function" && Function.toString.call(Ctor) === objectCtorString;
}
function original(value) {
  if (!isDraft(value))
    die(15, value);
  return value[DRAFT_STATE].base_;
}
function each(obj, iter) {
  if (getArchtype(obj) === 0 /* Object */) {
    Reflect.ownKeys(obj).forEach((key) => {
      iter(key, obj[key], obj);
    });
  } else {
    obj.forEach((entry, index) => iter(index, entry, obj));
  }
}
function getArchtype(thing) {
  const state = thing[DRAFT_STATE];
  return state ? state.type_ : Array.isArray(thing) ? 1 /* Array */ : isMap(thing) ? 2 /* Map */ : isSet(thing) ? 3 /* Set */ : 0 /* Object */;
}
function has(thing, prop) {
  return getArchtype(thing) === 2 /* Map */ ? thing.has(prop) : Object.prototype.hasOwnProperty.call(thing, prop);
}
function get(thing, prop) {
  return getArchtype(thing) === 2 /* Map */ ? thing.get(prop) : thing[prop];
}
function set(thing, propOrOldValue, value) {
  const t = getArchtype(thing);
  if (t === 2 /* Map */)
    thing.set(propOrOldValue, value);
  else if (t === 3 /* Set */) {
    thing.add(value);
  } else
    thing[propOrOldValue] = value;
}
function is(x, y) {
  if (x === y) {
    return x !== 0 || 1 / x === 1 / y;
  } else {
    return x !== x && y !== y;
  }
}
function isMap(target) {
  return target instanceof Map;
}
function isSet(target) {
  return target instanceof Set;
}
function latest(state) {
  return state.copy_ || state.base_;
}
function shallowCopy(base, strict) {
  if (isMap(base)) {
    return new Map(base);
  }
  if (isSet(base)) {
    return new Set(base);
  }
  if (Array.isArray(base))
    return Array.prototype.slice.call(base);
  const isPlain = isPlainObject(base);
  if (strict === true || strict === "class_only" && !isPlain) {
    const descriptors = Object.getOwnPropertyDescriptors(base);
    delete descriptors[DRAFT_STATE];
    let keys = Reflect.ownKeys(descriptors);
    for (let i = 0; i < keys.length; i++) {
      const key = keys[i];
      const desc = descriptors[key];
      if (desc.writable === false) {
        desc.writable = true;
        desc.configurable = true;
      }
      if (desc.get || desc.set)
        descriptors[key] = {
          configurable: true,
          writable: true,
          // could live with !!desc.set as well here...
          enumerable: desc.enumerable,
          value: base[key]
        };
    }
    return Object.create(getPrototypeOf(base), descriptors);
  } else {
    const proto = getPrototypeOf(base);
    if (proto !== null && isPlain) {
      return { ...base };
    }
    const obj = Object.create(proto);
    return Object.assign(obj, base);
  }
}
function freeze(obj, deep = false) {
  if (isFrozen(obj) || isDraft(obj) || !isDraftable(obj))
    return obj;
  if (getArchtype(obj) > 1) {
    obj.set = obj.add = obj.clear = obj.delete = dontMutateFrozenCollections;
  }
  Object.freeze(obj);
  if (deep)
    Object.entries(obj).forEach(([key, value]) => freeze(value, true));
  return obj;
}
function dontMutateFrozenCollections() {
  die(2);
}
function isFrozen(obj) {
  return Object.isFrozen(obj);
}

// src/utils/plugins.ts
var plugins = {};
function getPlugin(pluginKey) {
  const plugin = plugins[pluginKey];
  if (!plugin) {
    die(0, pluginKey);
  }
  return plugin;
}
function loadPlugin(pluginKey, implementation) {
  if (!plugins[pluginKey])
    plugins[pluginKey] = implementation;
}

// src/core/scope.ts
var currentScope;
function getCurrentScope() {
  return currentScope;
}
function createScope(parent_, immer_) {
  return {
    drafts_: [],
    parent_,
    immer_,
    // Whenever the modified draft contains a draft from another scope, we
    // need to prevent auto-freezing so the unowned draft can be finalized.
    canAutoFreeze_: true,
    unfinalizedDrafts_: 0
  };
}
function usePatchesInScope(scope, patchListener) {
  if (patchListener) {
    getPlugin("Patches");
    scope.patches_ = [];
    scope.inversePatches_ = [];
    scope.patchListener_ = patchListener;
  }
}
function revokeScope(scope) {
  leaveScope(scope);
  scope.drafts_.forEach(revokeDraft);
  scope.drafts_ = null;
}
function leaveScope(scope) {
  if (scope === currentScope) {
    currentScope = scope.parent_;
  }
}
function enterScope(immer2) {
  return currentScope = createScope(currentScope, immer2);
}
function revokeDraft(draft) {
  const state = draft[DRAFT_STATE];
  if (state.type_ === 0 /* Object */ || state.type_ === 1 /* Array */)
    state.revoke_();
  else
    state.revoked_ = true;
}

// src/core/finalize.ts
function processResult(result, scope) {
  scope.unfinalizedDrafts_ = scope.drafts_.length;
  const baseDraft = scope.drafts_[0];
  const isReplaced = result !== void 0 && result !== baseDraft;
  if (isReplaced) {
    if (baseDraft[DRAFT_STATE].modified_) {
      revokeScope(scope);
      die(4);
    }
    if (isDraftable(result)) {
      result = finalize(scope, result);
      if (!scope.parent_)
        maybeFreeze(scope, result);
    }
    if (scope.patches_) {
      getPlugin("Patches").generateReplacementPatches_(
        baseDraft[DRAFT_STATE].base_,
        result,
        scope.patches_,
        scope.inversePatches_
      );
    }
  } else {
    result = finalize(scope, baseDraft, []);
  }
  revokeScope(scope);
  if (scope.patches_) {
    scope.patchListener_(scope.patches_, scope.inversePatches_);
  }
  return result !== NOTHING ? result : void 0;
}
function finalize(rootScope, value, path) {
  if (isFrozen(value))
    return value;
  const state = value[DRAFT_STATE];
  if (!state) {
    each(
      value,
      (key, childValue) => finalizeProperty(rootScope, state, value, key, childValue, path)
    );
    return value;
  }
  if (state.scope_ !== rootScope)
    return value;
  if (!state.modified_) {
    maybeFreeze(rootScope, state.base_, true);
    return state.base_;
  }
  if (!state.finalized_) {
    state.finalized_ = true;
    state.scope_.unfinalizedDrafts_--;
    const result = state.copy_;
    let resultEach = result;
    let isSet2 = false;
    if (state.type_ === 3 /* Set */) {
      resultEach = new Set(result);
      result.clear();
      isSet2 = true;
    }
    each(
      resultEach,
      (key, childValue) => finalizeProperty(rootScope, state, result, key, childValue, path, isSet2)
    );
    maybeFreeze(rootScope, result, false);
    if (path && rootScope.patches_) {
      getPlugin("Patches").generatePatches_(
        state,
        path,
        rootScope.patches_,
        rootScope.inversePatches_
      );
    }
  }
  return state.copy_;
}
function finalizeProperty(rootScope, parentState, targetObject, prop, childValue, rootPath, targetIsSet) {
  if ( true && childValue === targetObject)
    die(5);
  if (isDraft(childValue)) {
    const path = rootPath && parentState && parentState.type_ !== 3 /* Set */ && // Set objects are atomic since they have no keys.
    !has(parentState.assigned_, prop) ? rootPath.concat(prop) : void 0;
    const res = finalize(rootScope, childValue, path);
    set(targetObject, prop, res);
    if (isDraft(res)) {
      rootScope.canAutoFreeze_ = false;
    } else
      return;
  } else if (targetIsSet) {
    targetObject.add(childValue);
  }
  if (isDraftable(childValue) && !isFrozen(childValue)) {
    if (!rootScope.immer_.autoFreeze_ && rootScope.unfinalizedDrafts_ < 1) {
      return;
    }
    finalize(rootScope, childValue);
    if ((!parentState || !parentState.scope_.parent_) && typeof prop !== "symbol" && Object.prototype.propertyIsEnumerable.call(targetObject, prop))
      maybeFreeze(rootScope, childValue);
  }
}
function maybeFreeze(scope, value, deep = false) {
  if (!scope.parent_ && scope.immer_.autoFreeze_ && scope.canAutoFreeze_) {
    freeze(value, deep);
  }
}

// src/core/proxy.ts
function createProxyProxy(base, parent) {
  const isArray = Array.isArray(base);
  const state = {
    type_: isArray ? 1 /* Array */ : 0 /* Object */,
    // Track which produce call this is associated with.
    scope_: parent ? parent.scope_ : getCurrentScope(),
    // True for both shallow and deep changes.
    modified_: false,
    // Used during finalization.
    finalized_: false,
    // Track which properties have been assigned (true) or deleted (false).
    assigned_: {},
    // The parent draft state.
    parent_: parent,
    // The base state.
    base_: base,
    // The base proxy.
    draft_: null,
    // set below
    // The base copy with any updated values.
    copy_: null,
    // Called by the `produce` function.
    revoke_: null,
    isManual_: false
  };
  let target = state;
  let traps = objectTraps;
  if (isArray) {
    target = [state];
    traps = arrayTraps;
  }
  const { revoke, proxy } = Proxy.revocable(target, traps);
  state.draft_ = proxy;
  state.revoke_ = revoke;
  return proxy;
}
var objectTraps = {
  get(state, prop) {
    if (prop === DRAFT_STATE)
      return state;
    const source = latest(state);
    if (!has(source, prop)) {
      return readPropFromProto(state, source, prop);
    }
    const value = source[prop];
    if (state.finalized_ || !isDraftable(value)) {
      return value;
    }
    if (value === peek(state.base_, prop)) {
      prepareCopy(state);
      return state.copy_[prop] = createProxy(value, state);
    }
    return value;
  },
  has(state, prop) {
    return prop in latest(state);
  },
  ownKeys(state) {
    return Reflect.ownKeys(latest(state));
  },
  set(state, prop, value) {
    const desc = getDescriptorFromProto(latest(state), prop);
    if (desc?.set) {
      desc.set.call(state.draft_, value);
      return true;
    }
    if (!state.modified_) {
      const current2 = peek(latest(state), prop);
      const currentState = current2?.[DRAFT_STATE];
      if (currentState && currentState.base_ === value) {
        state.copy_[prop] = value;
        state.assigned_[prop] = false;
        return true;
      }
      if (is(value, current2) && (value !== void 0 || has(state.base_, prop)))
        return true;
      prepareCopy(state);
      markChanged(state);
    }
    if (state.copy_[prop] === value && // special case: handle new props with value 'undefined'
    (value !== void 0 || prop in state.copy_) || // special case: NaN
    Number.isNaN(value) && Number.isNaN(state.copy_[prop]))
      return true;
    state.copy_[prop] = value;
    state.assigned_[prop] = true;
    return true;
  },
  deleteProperty(state, prop) {
    if (peek(state.base_, prop) !== void 0 || prop in state.base_) {
      state.assigned_[prop] = false;
      prepareCopy(state);
      markChanged(state);
    } else {
      delete state.assigned_[prop];
    }
    if (state.copy_) {
      delete state.copy_[prop];
    }
    return true;
  },
  // Note: We never coerce `desc.value` into an Immer draft, because we can't make
  // the same guarantee in ES5 mode.
  getOwnPropertyDescriptor(state, prop) {
    const owner = latest(state);
    const desc = Reflect.getOwnPropertyDescriptor(owner, prop);
    if (!desc)
      return desc;
    return {
      writable: true,
      configurable: state.type_ !== 1 /* Array */ || prop !== "length",
      enumerable: desc.enumerable,
      value: owner[prop]
    };
  },
  defineProperty() {
    die(11);
  },
  getPrototypeOf(state) {
    return getPrototypeOf(state.base_);
  },
  setPrototypeOf() {
    die(12);
  }
};
var arrayTraps = {};
each(objectTraps, (key, fn) => {
  arrayTraps[key] = function() {
    arguments[0] = arguments[0][0];
    return fn.apply(this, arguments);
  };
});
arrayTraps.deleteProperty = function(state, prop) {
  if ( true && isNaN(parseInt(prop)))
    die(13);
  return arrayTraps.set.call(this, state, prop, void 0);
};
arrayTraps.set = function(state, prop, value) {
  if ( true && prop !== "length" && isNaN(parseInt(prop)))
    die(14);
  return objectTraps.set.call(this, state[0], prop, value, state[0]);
};
function peek(draft, prop) {
  const state = draft[DRAFT_STATE];
  const source = state ? latest(state) : draft;
  return source[prop];
}
function readPropFromProto(state, source, prop) {
  const desc = getDescriptorFromProto(source, prop);
  return desc ? `value` in desc ? desc.value : (
    // This is a very special case, if the prop is a getter defined by the
    // prototype, we should invoke it with the draft as context!
    desc.get?.call(state.draft_)
  ) : void 0;
}
function getDescriptorFromProto(source, prop) {
  if (!(prop in source))
    return void 0;
  let proto = getPrototypeOf(source);
  while (proto) {
    const desc = Object.getOwnPropertyDescriptor(proto, prop);
    if (desc)
      return desc;
    proto = getPrototypeOf(proto);
  }
  return void 0;
}
function markChanged(state) {
  if (!state.modified_) {
    state.modified_ = true;
    if (state.parent_) {
      markChanged(state.parent_);
    }
  }
}
function prepareCopy(state) {
  if (!state.copy_) {
    state.copy_ = shallowCopy(
      state.base_,
      state.scope_.immer_.useStrictShallowCopy_
    );
  }
}

// src/core/immerClass.ts
var Immer2 = class {
  constructor(config) {
    this.autoFreeze_ = true;
    this.useStrictShallowCopy_ = false;
    /**
     * The `produce` function takes a value and a "recipe function" (whose
     * return value often depends on the base state). The recipe function is
     * free to mutate its first argument however it wants. All mutations are
     * only ever applied to a __copy__ of the base state.
     *
     * Pass only a function to create a "curried producer" which relieves you
     * from passing the recipe function every time.
     *
     * Only plain objects and arrays are made mutable. All other objects are
     * considered uncopyable.
     *
     * Note: This function is __bound__ to its `Immer` instance.
     *
     * @param {any} base - the initial state
     * @param {Function} recipe - function that receives a proxy of the base state as first argument and which can be freely modified
     * @param {Function} patchListener - optional function that will be called with all the patches produced here
     * @returns {any} a new state, or the initial state if nothing was modified
     */
    this.produce = (base, recipe, patchListener) => {
      if (typeof base === "function" && typeof recipe !== "function") {
        const defaultBase = recipe;
        recipe = base;
        const self = this;
        return function curriedProduce(base2 = defaultBase, ...args) {
          return self.produce(base2, (draft) => recipe.call(this, draft, ...args));
        };
      }
      if (typeof recipe !== "function")
        die(6);
      if (patchListener !== void 0 && typeof patchListener !== "function")
        die(7);
      let result;
      if (isDraftable(base)) {
        const scope = enterScope(this);
        const proxy = createProxy(base, void 0);
        let hasError = true;
        try {
          result = recipe(proxy);
          hasError = false;
        } finally {
          if (hasError)
            revokeScope(scope);
          else
            leaveScope(scope);
        }
        usePatchesInScope(scope, patchListener);
        return processResult(result, scope);
      } else if (!base || typeof base !== "object") {
        result = recipe(base);
        if (result === void 0)
          result = base;
        if (result === NOTHING)
          result = void 0;
        if (this.autoFreeze_)
          freeze(result, true);
        if (patchListener) {
          const p = [];
          const ip = [];
          getPlugin("Patches").generateReplacementPatches_(base, result, p, ip);
          patchListener(p, ip);
        }
        return result;
      } else
        die(1, base);
    };
    this.produceWithPatches = (base, recipe) => {
      if (typeof base === "function") {
        return (state, ...args) => this.produceWithPatches(state, (draft) => base(draft, ...args));
      }
      let patches, inversePatches;
      const result = this.produce(base, recipe, (p, ip) => {
        patches = p;
        inversePatches = ip;
      });
      return [result, patches, inversePatches];
    };
    if (typeof config?.autoFreeze === "boolean")
      this.setAutoFreeze(config.autoFreeze);
    if (typeof config?.useStrictShallowCopy === "boolean")
      this.setUseStrictShallowCopy(config.useStrictShallowCopy);
  }
  createDraft(base) {
    if (!isDraftable(base))
      die(8);
    if (isDraft(base))
      base = current(base);
    const scope = enterScope(this);
    const proxy = createProxy(base, void 0);
    proxy[DRAFT_STATE].isManual_ = true;
    leaveScope(scope);
    return proxy;
  }
  finishDraft(draft, patchListener) {
    const state = draft && draft[DRAFT_STATE];
    if (!state || !state.isManual_)
      die(9);
    const { scope_: scope } = state;
    usePatchesInScope(scope, patchListener);
    return processResult(void 0, scope);
  }
  /**
   * Pass true to automatically freeze all copies created by Immer.
   *
   * By default, auto-freezing is enabled.
   */
  setAutoFreeze(value) {
    this.autoFreeze_ = value;
  }
  /**
   * Pass true to enable strict shallow copy.
   *
   * By default, immer does not copy the object descriptors such as getter, setter and non-enumrable properties.
   */
  setUseStrictShallowCopy(value) {
    this.useStrictShallowCopy_ = value;
  }
  applyPatches(base, patches) {
    let i;
    for (i = patches.length - 1; i >= 0; i--) {
      const patch = patches[i];
      if (patch.path.length === 0 && patch.op === "replace") {
        base = patch.value;
        break;
      }
    }
    if (i > -1) {
      patches = patches.slice(i + 1);
    }
    const applyPatchesImpl = getPlugin("Patches").applyPatches_;
    if (isDraft(base)) {
      return applyPatchesImpl(base, patches);
    }
    return this.produce(
      base,
      (draft) => applyPatchesImpl(draft, patches)
    );
  }
};
function createProxy(value, parent) {
  const draft = isMap(value) ? getPlugin("MapSet").proxyMap_(value, parent) : isSet(value) ? getPlugin("MapSet").proxySet_(value, parent) : createProxyProxy(value, parent);
  const scope = parent ? parent.scope_ : getCurrentScope();
  scope.drafts_.push(draft);
  return draft;
}

// src/core/current.ts
function current(value) {
  if (!isDraft(value))
    die(10, value);
  return currentImpl(value);
}
function currentImpl(value) {
  if (!isDraftable(value) || isFrozen(value))
    return value;
  const state = value[DRAFT_STATE];
  let copy;
  if (state) {
    if (!state.modified_)
      return state.base_;
    state.finalized_ = true;
    copy = shallowCopy(value, state.scope_.immer_.useStrictShallowCopy_);
  } else {
    copy = shallowCopy(value, true);
  }
  each(copy, (key, childValue) => {
    set(copy, key, currentImpl(childValue));
  });
  if (state) {
    state.finalized_ = false;
  }
  return copy;
}

// src/plugins/patches.ts
function enablePatches() {
  const errorOffset = 16;
  if (true) {
    errors.push(
      'Sets cannot have "replace" patches.',
      function(op) {
        return "Unsupported patch operation: " + op;
      },
      function(path) {
        return "Cannot apply patch, path doesn't resolve: " + path;
      },
      "Patching reserved attributes like __proto__, prototype and constructor is not allowed"
    );
  }
  const REPLACE = "replace";
  const ADD = "add";
  const REMOVE = "remove";
  function generatePatches_(state, basePath, patches, inversePatches) {
    switch (state.type_) {
      case 0 /* Object */:
      case 2 /* Map */:
        return generatePatchesFromAssigned(
          state,
          basePath,
          patches,
          inversePatches
        );
      case 1 /* Array */:
        return generateArrayPatches(state, basePath, patches, inversePatches);
      case 3 /* Set */:
        return generateSetPatches(
          state,
          basePath,
          patches,
          inversePatches
        );
    }
  }
  function generateArrayPatches(state, basePath, patches, inversePatches) {
    let { base_, assigned_ } = state;
    let copy_ = state.copy_;
    if (copy_.length < base_.length) {
      ;
      [base_, copy_] = [copy_, base_];
      [patches, inversePatches] = [inversePatches, patches];
    }
    for (let i = 0; i < base_.length; i++) {
      if (assigned_[i] && copy_[i] !== base_[i]) {
        const path = basePath.concat([i]);
        patches.push({
          op: REPLACE,
          path,
          // Need to maybe clone it, as it can in fact be the original value
          // due to the base/copy inversion at the start of this function
          value: clonePatchValueIfNeeded(copy_[i])
        });
        inversePatches.push({
          op: REPLACE,
          path,
          value: clonePatchValueIfNeeded(base_[i])
        });
      }
    }
    for (let i = base_.length; i < copy_.length; i++) {
      const path = basePath.concat([i]);
      patches.push({
        op: ADD,
        path,
        // Need to maybe clone it, as it can in fact be the original value
        // due to the base/copy inversion at the start of this function
        value: clonePatchValueIfNeeded(copy_[i])
      });
    }
    for (let i = copy_.length - 1; base_.length <= i; --i) {
      const path = basePath.concat([i]);
      inversePatches.push({
        op: REMOVE,
        path
      });
    }
  }
  function generatePatchesFromAssigned(state, basePath, patches, inversePatches) {
    const { base_, copy_ } = state;
    each(state.assigned_, (key, assignedValue) => {
      const origValue = get(base_, key);
      const value = get(copy_, key);
      const op = !assignedValue ? REMOVE : has(base_, key) ? REPLACE : ADD;
      if (origValue === value && op === REPLACE)
        return;
      const path = basePath.concat(key);
      patches.push(op === REMOVE ? { op, path } : { op, path, value });
      inversePatches.push(
        op === ADD ? { op: REMOVE, path } : op === REMOVE ? { op: ADD, path, value: clonePatchValueIfNeeded(origValue) } : { op: REPLACE, path, value: clonePatchValueIfNeeded(origValue) }
      );
    });
  }
  function generateSetPatches(state, basePath, patches, inversePatches) {
    let { base_, copy_ } = state;
    let i = 0;
    base_.forEach((value) => {
      if (!copy_.has(value)) {
        const path = basePath.concat([i]);
        patches.push({
          op: REMOVE,
          path,
          value
        });
        inversePatches.unshift({
          op: ADD,
          path,
          value
        });
      }
      i++;
    });
    i = 0;
    copy_.forEach((value) => {
      if (!base_.has(value)) {
        const path = basePath.concat([i]);
        patches.push({
          op: ADD,
          path,
          value
        });
        inversePatches.unshift({
          op: REMOVE,
          path,
          value
        });
      }
      i++;
    });
  }
  function generateReplacementPatches_(baseValue, replacement, patches, inversePatches) {
    patches.push({
      op: REPLACE,
      path: [],
      value: replacement === NOTHING ? void 0 : replacement
    });
    inversePatches.push({
      op: REPLACE,
      path: [],
      value: baseValue
    });
  }
  function applyPatches_(draft, patches) {
    patches.forEach((patch) => {
      const { path, op } = patch;
      let base = draft;
      for (let i = 0; i < path.length - 1; i++) {
        const parentType = getArchtype(base);
        let p = path[i];
        if (typeof p !== "string" && typeof p !== "number") {
          p = "" + p;
        }
        if ((parentType === 0 /* Object */ || parentType === 1 /* Array */) && (p === "__proto__" || p === "constructor"))
          die(errorOffset + 3);
        if (typeof base === "function" && p === "prototype")
          die(errorOffset + 3);
        base = get(base, p);
        if (typeof base !== "object")
          die(errorOffset + 2, path.join("/"));
      }
      const type = getArchtype(base);
      const value = deepClonePatchValue(patch.value);
      const key = path[path.length - 1];
      switch (op) {
        case REPLACE:
          switch (type) {
            case 2 /* Map */:
              return base.set(key, value);
            case 3 /* Set */:
              die(errorOffset);
            default:
              return base[key] = value;
          }
        case ADD:
          switch (type) {
            case 1 /* Array */:
              return key === "-" ? base.push(value) : base.splice(key, 0, value);
            case 2 /* Map */:
              return base.set(key, value);
            case 3 /* Set */:
              return base.add(value);
            default:
              return base[key] = value;
          }
        case REMOVE:
          switch (type) {
            case 1 /* Array */:
              return base.splice(key, 1);
            case 2 /* Map */:
              return base.delete(key);
            case 3 /* Set */:
              return base.delete(patch.value);
            default:
              return delete base[key];
          }
        default:
          die(errorOffset + 1, op);
      }
    });
    return draft;
  }
  function deepClonePatchValue(obj) {
    if (!isDraftable(obj))
      return obj;
    if (Array.isArray(obj))
      return obj.map(deepClonePatchValue);
    if (isMap(obj))
      return new Map(
        Array.from(obj.entries()).map(([k, v]) => [k, deepClonePatchValue(v)])
      );
    if (isSet(obj))
      return new Set(Array.from(obj).map(deepClonePatchValue));
    const cloned = Object.create(getPrototypeOf(obj));
    for (const key in obj)
      cloned[key] = deepClonePatchValue(obj[key]);
    if (has(obj, DRAFTABLE))
      cloned[DRAFTABLE] = obj[DRAFTABLE];
    return cloned;
  }
  function clonePatchValueIfNeeded(obj) {
    if (isDraft(obj)) {
      return deepClonePatchValue(obj);
    } else
      return obj;
  }
  loadPlugin("Patches", {
    applyPatches_,
    generatePatches_,
    generateReplacementPatches_
  });
}

// src/plugins/mapset.ts
function enableMapSet() {
  class DraftMap extends Map {
    constructor(target, parent) {
      super();
      this[DRAFT_STATE] = {
        type_: 2 /* Map */,
        parent_: parent,
        scope_: parent ? parent.scope_ : getCurrentScope(),
        modified_: false,
        finalized_: false,
        copy_: void 0,
        assigned_: void 0,
        base_: target,
        draft_: this,
        isManual_: false,
        revoked_: false
      };
    }
    get size() {
      return latest(this[DRAFT_STATE]).size;
    }
    has(key) {
      return latest(this[DRAFT_STATE]).has(key);
    }
    set(key, value) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (!latest(state).has(key) || latest(state).get(key) !== value) {
        prepareMapCopy(state);
        markChanged(state);
        state.assigned_.set(key, true);
        state.copy_.set(key, value);
        state.assigned_.set(key, true);
      }
      return this;
    }
    delete(key) {
      if (!this.has(key)) {
        return false;
      }
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareMapCopy(state);
      markChanged(state);
      if (state.base_.has(key)) {
        state.assigned_.set(key, false);
      } else {
        state.assigned_.delete(key);
      }
      state.copy_.delete(key);
      return true;
    }
    clear() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (latest(state).size) {
        prepareMapCopy(state);
        markChanged(state);
        state.assigned_ = /* @__PURE__ */ new Map();
        each(state.base_, (key) => {
          state.assigned_.set(key, false);
        });
        state.copy_.clear();
      }
    }
    forEach(cb, thisArg) {
      const state = this[DRAFT_STATE];
      latest(state).forEach((_value, key, _map) => {
        cb.call(thisArg, this.get(key), key, this);
      });
    }
    get(key) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      const value = latest(state).get(key);
      if (state.finalized_ || !isDraftable(value)) {
        return value;
      }
      if (value !== state.base_.get(key)) {
        return value;
      }
      const draft = createProxy(value, state);
      prepareMapCopy(state);
      state.copy_.set(key, draft);
      return draft;
    }
    keys() {
      return latest(this[DRAFT_STATE]).keys();
    }
    values() {
      const iterator = this.keys();
      return {
        [Symbol.iterator]: () => this.values(),
        next: () => {
          const r = iterator.next();
          if (r.done)
            return r;
          const value = this.get(r.value);
          return {
            done: false,
            value
          };
        }
      };
    }
    entries() {
      const iterator = this.keys();
      return {
        [Symbol.iterator]: () => this.entries(),
        next: () => {
          const r = iterator.next();
          if (r.done)
            return r;
          const value = this.get(r.value);
          return {
            done: false,
            value: [r.value, value]
          };
        }
      };
    }
    [(DRAFT_STATE, Symbol.iterator)]() {
      return this.entries();
    }
  }
  function proxyMap_(target, parent) {
    return new DraftMap(target, parent);
  }
  function prepareMapCopy(state) {
    if (!state.copy_) {
      state.assigned_ = /* @__PURE__ */ new Map();
      state.copy_ = new Map(state.base_);
    }
  }
  class DraftSet extends Set {
    constructor(target, parent) {
      super();
      this[DRAFT_STATE] = {
        type_: 3 /* Set */,
        parent_: parent,
        scope_: parent ? parent.scope_ : getCurrentScope(),
        modified_: false,
        finalized_: false,
        copy_: void 0,
        base_: target,
        draft_: this,
        drafts_: /* @__PURE__ */ new Map(),
        revoked_: false,
        isManual_: false
      };
    }
    get size() {
      return latest(this[DRAFT_STATE]).size;
    }
    has(value) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (!state.copy_) {
        return state.base_.has(value);
      }
      if (state.copy_.has(value))
        return true;
      if (state.drafts_.has(value) && state.copy_.has(state.drafts_.get(value)))
        return true;
      return false;
    }
    add(value) {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (!this.has(value)) {
        prepareSetCopy(state);
        markChanged(state);
        state.copy_.add(value);
      }
      return this;
    }
    delete(value) {
      if (!this.has(value)) {
        return false;
      }
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      markChanged(state);
      return state.copy_.delete(value) || (state.drafts_.has(value) ? state.copy_.delete(state.drafts_.get(value)) : (
        /* istanbul ignore next */
        false
      ));
    }
    clear() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      if (latest(state).size) {
        prepareSetCopy(state);
        markChanged(state);
        state.copy_.clear();
      }
    }
    values() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      return state.copy_.values();
    }
    entries() {
      const state = this[DRAFT_STATE];
      assertUnrevoked(state);
      prepareSetCopy(state);
      return state.copy_.entries();
    }
    keys() {
      return this.values();
    }
    [(DRAFT_STATE, Symbol.iterator)]() {
      return this.values();
    }
    forEach(cb, thisArg) {
      const iterator = this.values();
      let result = iterator.next();
      while (!result.done) {
        cb.call(thisArg, result.value, result.value, this);
        result = iterator.next();
      }
    }
  }
  function proxySet_(target, parent) {
    return new DraftSet(target, parent);
  }
  function prepareSetCopy(state) {
    if (!state.copy_) {
      state.copy_ = /* @__PURE__ */ new Set();
      state.base_.forEach((value) => {
        if (isDraftable(value)) {
          const draft = createProxy(value, state);
          state.drafts_.set(value, draft);
          state.copy_.add(draft);
        } else {
          state.copy_.add(value);
        }
      });
    }
  }
  function assertUnrevoked(state) {
    if (state.revoked_)
      die(3, JSON.stringify(latest(state)));
  }
  loadPlugin("MapSet", { proxyMap_, proxySet_ });
}

// src/immer.ts
var immer = new Immer2();
var produce = immer.produce;
var produceWithPatches = immer.produceWithPatches.bind(
  immer
);
var setAutoFreeze = immer.setAutoFreeze.bind(immer);
var setUseStrictShallowCopy = immer.setUseStrictShallowCopy.bind(immer);
var applyPatches = immer.applyPatches.bind(immer);
var createDraft = immer.createDraft.bind(immer);
var finishDraft = immer.finishDraft.bind(immer);
function castDraft(value) {
  return value;
}
function castImmutable(value) {
  return value;
}

//# sourceMappingURL=immer.mjs.map

/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/a11y.mjs":
/*!***********************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/a11y.mjs ***!
  \***********************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ A11y)
/* harmony export */ });
/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ "../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs");
/* harmony import */ var _shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/classes-to-selector.mjs */ "../plugin-slug/node_modules/swiper/shared/classes-to-selector.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");




function A11y(_ref) {
  let {
    swiper,
    extendParams,
    on
  } = _ref;
  extendParams({
    a11y: {
      enabled: true,
      notificationClass: 'swiper-notification',
      prevSlideMessage: 'Previous slide',
      nextSlideMessage: 'Next slide',
      firstSlideMessage: 'This is the first slide',
      lastSlideMessage: 'This is the last slide',
      paginationBulletMessage: 'Go to slide {{index}}',
      slideLabelMessage: '{{index}} / {{slidesLength}}',
      containerMessage: null,
      containerRoleDescriptionMessage: null,
      containerRole: null,
      itemRoleDescriptionMessage: null,
      slideRole: 'group',
      id: null,
      scrollOnFocus: true
    }
  });
  swiper.a11y = {
    clicked: false
  };
  let liveRegion = null;
  let preventFocusHandler;
  let focusTargetSlideEl;
  let visibilityChangedTimestamp = new Date().getTime();
  function notify(message) {
    const notification = liveRegion;
    if (notification.length === 0) return;
    notification.innerHTML = '';
    notification.innerHTML = message;
  }
  function getRandomNumber(size) {
    if (size === void 0) {
      size = 16;
    }
    const randomChar = () => Math.round(16 * Math.random()).toString(16);
    return 'x'.repeat(size).replace(/x/g, randomChar);
  }
  function makeElFocusable(el) {
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    el.forEach(subEl => {
      subEl.setAttribute('tabIndex', '0');
    });
  }
  function makeElNotFocusable(el) {
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    el.forEach(subEl => {
      subEl.setAttribute('tabIndex', '-1');
    });
  }
  function addElRole(el, role) {
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    el.forEach(subEl => {
      subEl.setAttribute('role', role);
    });
  }
  function addElRoleDescription(el, description) {
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    el.forEach(subEl => {
      subEl.setAttribute('aria-roledescription', description);
    });
  }
  function addElControls(el, controls) {
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    el.forEach(subEl => {
      subEl.setAttribute('aria-controls', controls);
    });
  }
  function addElLabel(el, label) {
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    el.forEach(subEl => {
      subEl.setAttribute('aria-label', label);
    });
  }
  function addElId(el, id) {
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    el.forEach(subEl => {
      subEl.setAttribute('id', id);
    });
  }
  function addElLive(el, live) {
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    el.forEach(subEl => {
      subEl.setAttribute('aria-live', live);
    });
  }
  function disableEl(el) {
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    el.forEach(subEl => {
      subEl.setAttribute('aria-disabled', true);
    });
  }
  function enableEl(el) {
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    el.forEach(subEl => {
      subEl.setAttribute('aria-disabled', false);
    });
  }
  function onEnterOrSpaceKey(e) {
    if (e.keyCode !== 13 && e.keyCode !== 32) return;
    const params = swiper.params.a11y;
    const targetEl = e.target;
    if (swiper.pagination && swiper.pagination.el && (targetEl === swiper.pagination.el || swiper.pagination.el.contains(e.target))) {
      if (!e.target.matches((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_1__.c)(swiper.params.pagination.bulletClass))) return;
    }
    if (swiper.navigation && swiper.navigation.prevEl && swiper.navigation.nextEl) {
      const prevEls = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(swiper.navigation.prevEl);
      const nextEls = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(swiper.navigation.nextEl);
      if (nextEls.includes(targetEl)) {
        if (!(swiper.isEnd && !swiper.params.loop)) {
          swiper.slideNext();
        }
        if (swiper.isEnd) {
          notify(params.lastSlideMessage);
        } else {
          notify(params.nextSlideMessage);
        }
      }
      if (prevEls.includes(targetEl)) {
        if (!(swiper.isBeginning && !swiper.params.loop)) {
          swiper.slidePrev();
        }
        if (swiper.isBeginning) {
          notify(params.firstSlideMessage);
        } else {
          notify(params.prevSlideMessage);
        }
      }
    }
    if (swiper.pagination && targetEl.matches((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_1__.c)(swiper.params.pagination.bulletClass))) {
      targetEl.click();
    }
  }
  function updateNavigation() {
    if (swiper.params.loop || swiper.params.rewind || !swiper.navigation) return;
    const {
      nextEl,
      prevEl
    } = swiper.navigation;
    if (prevEl) {
      if (swiper.isBeginning) {
        disableEl(prevEl);
        makeElNotFocusable(prevEl);
      } else {
        enableEl(prevEl);
        makeElFocusable(prevEl);
      }
    }
    if (nextEl) {
      if (swiper.isEnd) {
        disableEl(nextEl);
        makeElNotFocusable(nextEl);
      } else {
        enableEl(nextEl);
        makeElFocusable(nextEl);
      }
    }
  }
  function hasPagination() {
    return swiper.pagination && swiper.pagination.bullets && swiper.pagination.bullets.length;
  }
  function hasClickablePagination() {
    return hasPagination() && swiper.params.pagination.clickable;
  }
  function updatePagination() {
    const params = swiper.params.a11y;
    if (!hasPagination()) return;
    swiper.pagination.bullets.forEach(bulletEl => {
      if (swiper.params.pagination.clickable) {
        makeElFocusable(bulletEl);
        if (!swiper.params.pagination.renderBullet) {
          addElRole(bulletEl, 'button');
          addElLabel(bulletEl, params.paginationBulletMessage.replace(/\{\{index\}\}/, (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.h)(bulletEl) + 1));
        }
      }
      if (bulletEl.matches((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_1__.c)(swiper.params.pagination.bulletActiveClass))) {
        bulletEl.setAttribute('aria-current', 'true');
      } else {
        bulletEl.removeAttribute('aria-current');
      }
    });
  }
  const initNavEl = (el, wrapperId, message) => {
    makeElFocusable(el);
    if (el.tagName !== 'BUTTON') {
      addElRole(el, 'button');
      el.addEventListener('keydown', onEnterOrSpaceKey);
    }
    addElLabel(el, message);
    addElControls(el, wrapperId);
  };
  const handlePointerDown = e => {
    if (focusTargetSlideEl && focusTargetSlideEl !== e.target && !focusTargetSlideEl.contains(e.target)) {
      preventFocusHandler = true;
    }
    swiper.a11y.clicked = true;
  };
  const handlePointerUp = () => {
    preventFocusHandler = false;
    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        if (!swiper.destroyed) {
          swiper.a11y.clicked = false;
        }
      });
    });
  };
  const onVisibilityChange = e => {
    visibilityChangedTimestamp = new Date().getTime();
  };
  const handleFocus = e => {
    if (swiper.a11y.clicked || !swiper.params.a11y.scrollOnFocus) return;
    if (new Date().getTime() - visibilityChangedTimestamp < 100) return;
    const slideEl = e.target.closest(`.${swiper.params.slideClass}, swiper-slide`);
    if (!slideEl || !swiper.slides.includes(slideEl)) return;
    focusTargetSlideEl = slideEl;
    const isActive = swiper.slides.indexOf(slideEl) === swiper.activeIndex;
    const isVisible = swiper.params.watchSlidesProgress && swiper.visibleSlides && swiper.visibleSlides.includes(slideEl);
    if (isActive || isVisible) return;
    if (e.sourceCapabilities && e.sourceCapabilities.firesTouchEvents) return;
    if (swiper.isHorizontal()) {
      swiper.el.scrollLeft = 0;
    } else {
      swiper.el.scrollTop = 0;
    }
    requestAnimationFrame(() => {
      if (preventFocusHandler) return;
      if (swiper.params.loop) {
        swiper.slideToLoop(parseInt(slideEl.getAttribute('data-swiper-slide-index')), 0);
      } else {
        swiper.slideTo(swiper.slides.indexOf(slideEl), 0);
      }
      preventFocusHandler = false;
    });
  };
  const initSlides = () => {
    const params = swiper.params.a11y;
    if (params.itemRoleDescriptionMessage) {
      addElRoleDescription(swiper.slides, params.itemRoleDescriptionMessage);
    }
    if (params.slideRole) {
      addElRole(swiper.slides, params.slideRole);
    }
    const slidesLength = swiper.slides.length;
    if (params.slideLabelMessage) {
      swiper.slides.forEach((slideEl, index) => {
        const slideIndex = swiper.params.loop ? parseInt(slideEl.getAttribute('data-swiper-slide-index'), 10) : index;
        const ariaLabelMessage = params.slideLabelMessage.replace(/\{\{index\}\}/, slideIndex + 1).replace(/\{\{slidesLength\}\}/, slidesLength);
        addElLabel(slideEl, ariaLabelMessage);
      });
    }
  };
  const init = () => {
    const params = swiper.params.a11y;
    swiper.el.append(liveRegion);

    // Container
    const containerEl = swiper.el;
    if (params.containerRoleDescriptionMessage) {
      addElRoleDescription(containerEl, params.containerRoleDescriptionMessage);
    }
    if (params.containerMessage) {
      addElLabel(containerEl, params.containerMessage);
    }
    if (params.containerRole) {
      addElRole(containerEl, params.containerRole);
    }

    // Wrapper
    const wrapperEl = swiper.wrapperEl;
    const wrapperId = params.id || wrapperEl.getAttribute('id') || `swiper-wrapper-${getRandomNumber(16)}`;
    const live = swiper.params.autoplay && swiper.params.autoplay.enabled ? 'off' : 'polite';
    addElId(wrapperEl, wrapperId);
    addElLive(wrapperEl, live);

    // Slide
    initSlides();

    // Navigation
    let {
      nextEl,
      prevEl
    } = swiper.navigation ? swiper.navigation : {};
    nextEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(nextEl);
    prevEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(prevEl);
    if (nextEl) {
      nextEl.forEach(el => initNavEl(el, wrapperId, params.nextSlideMessage));
    }
    if (prevEl) {
      prevEl.forEach(el => initNavEl(el, wrapperId, params.prevSlideMessage));
    }

    // Pagination
    if (hasClickablePagination()) {
      const paginationEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(swiper.pagination.el);
      paginationEl.forEach(el => {
        el.addEventListener('keydown', onEnterOrSpaceKey);
      });
    }

    // Tab focus
    const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
    document.addEventListener('visibilitychange', onVisibilityChange);
    swiper.el.addEventListener('focus', handleFocus, true);
    swiper.el.addEventListener('focus', handleFocus, true);
    swiper.el.addEventListener('pointerdown', handlePointerDown, true);
    swiper.el.addEventListener('pointerup', handlePointerUp, true);
  };
  function destroy() {
    if (liveRegion) liveRegion.remove();
    let {
      nextEl,
      prevEl
    } = swiper.navigation ? swiper.navigation : {};
    nextEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(nextEl);
    prevEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(prevEl);
    if (nextEl) {
      nextEl.forEach(el => el.removeEventListener('keydown', onEnterOrSpaceKey));
    }
    if (prevEl) {
      prevEl.forEach(el => el.removeEventListener('keydown', onEnterOrSpaceKey));
    }

    // Pagination
    if (hasClickablePagination()) {
      const paginationEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(swiper.pagination.el);
      paginationEl.forEach(el => {
        el.removeEventListener('keydown', onEnterOrSpaceKey);
      });
    }
    const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
    document.removeEventListener('visibilitychange', onVisibilityChange);
    // Tab focus
    if (swiper.el && typeof swiper.el !== 'string') {
      swiper.el.removeEventListener('focus', handleFocus, true);
      swiper.el.removeEventListener('pointerdown', handlePointerDown, true);
      swiper.el.removeEventListener('pointerup', handlePointerUp, true);
    }
  }
  on('beforeInit', () => {
    liveRegion = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.c)('span', swiper.params.a11y.notificationClass);
    liveRegion.setAttribute('aria-live', 'assertive');
    liveRegion.setAttribute('aria-atomic', 'true');
  });
  on('afterInit', () => {
    if (!swiper.params.a11y.enabled) return;
    init();
  });
  on('slidesLengthChange snapGridLengthChange slidesGridLengthChange', () => {
    if (!swiper.params.a11y.enabled) return;
    initSlides();
  });
  on('fromEdge toEdge afterInit lock unlock', () => {
    if (!swiper.params.a11y.enabled) return;
    updateNavigation();
  });
  on('paginationUpdate', () => {
    if (!swiper.params.a11y.enabled) return;
    updatePagination();
  });
  on('destroy', () => {
    if (!swiper.params.a11y.enabled) return;
    destroy();
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/autoplay.mjs":
/*!***************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/autoplay.mjs ***!
  \***************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ Autoplay)
/* harmony export */ });
/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ "../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs");


/* eslint no-underscore-dangle: "off" */
/* eslint no-use-before-define: "off" */
function Autoplay(_ref) {
  let {
    swiper,
    extendParams,
    on,
    emit,
    params
  } = _ref;
  swiper.autoplay = {
    running: false,
    paused: false,
    timeLeft: 0
  };
  extendParams({
    autoplay: {
      enabled: false,
      delay: 3000,
      waitForTransition: true,
      disableOnInteraction: false,
      stopOnLastSlide: false,
      reverseDirection: false,
      pauseOnMouseEnter: false
    }
  });
  let timeout;
  let raf;
  let autoplayDelayTotal = params && params.autoplay ? params.autoplay.delay : 3000;
  let autoplayDelayCurrent = params && params.autoplay ? params.autoplay.delay : 3000;
  let autoplayTimeLeft;
  let autoplayStartTime = new Date().getTime();
  let wasPaused;
  let isTouched;
  let pausedByTouch;
  let touchStartTimeout;
  let slideChanged;
  let pausedByInteraction;
  let pausedByPointerEnter;
  function onTransitionEnd(e) {
    if (!swiper || swiper.destroyed || !swiper.wrapperEl) return;
    if (e.target !== swiper.wrapperEl) return;
    swiper.wrapperEl.removeEventListener('transitionend', onTransitionEnd);
    if (pausedByPointerEnter || e.detail && e.detail.bySwiperTouchMove) {
      return;
    }
    resume();
  }
  const calcTimeLeft = () => {
    if (swiper.destroyed || !swiper.autoplay.running) return;
    if (swiper.autoplay.paused) {
      wasPaused = true;
    } else if (wasPaused) {
      autoplayDelayCurrent = autoplayTimeLeft;
      wasPaused = false;
    }
    const timeLeft = swiper.autoplay.paused ? autoplayTimeLeft : autoplayStartTime + autoplayDelayCurrent - new Date().getTime();
    swiper.autoplay.timeLeft = timeLeft;
    emit('autoplayTimeLeft', timeLeft, timeLeft / autoplayDelayTotal);
    raf = requestAnimationFrame(() => {
      calcTimeLeft();
    });
  };
  const getSlideDelay = () => {
    let activeSlideEl;
    if (swiper.virtual && swiper.params.virtual.enabled) {
      activeSlideEl = swiper.slides.find(slideEl => slideEl.classList.contains('swiper-slide-active'));
    } else {
      activeSlideEl = swiper.slides[swiper.activeIndex];
    }
    if (!activeSlideEl) return undefined;
    const currentSlideDelay = parseInt(activeSlideEl.getAttribute('data-swiper-autoplay'), 10);
    return currentSlideDelay;
  };
  const run = delayForce => {
    if (swiper.destroyed || !swiper.autoplay.running) return;
    cancelAnimationFrame(raf);
    calcTimeLeft();
    let delay = typeof delayForce === 'undefined' ? swiper.params.autoplay.delay : delayForce;
    autoplayDelayTotal = swiper.params.autoplay.delay;
    autoplayDelayCurrent = swiper.params.autoplay.delay;
    const currentSlideDelay = getSlideDelay();
    if (!Number.isNaN(currentSlideDelay) && currentSlideDelay > 0 && typeof delayForce === 'undefined') {
      delay = currentSlideDelay;
      autoplayDelayTotal = currentSlideDelay;
      autoplayDelayCurrent = currentSlideDelay;
    }
    autoplayTimeLeft = delay;
    const speed = swiper.params.speed;
    const proceed = () => {
      if (!swiper || swiper.destroyed) return;
      if (swiper.params.autoplay.reverseDirection) {
        if (!swiper.isBeginning || swiper.params.loop || swiper.params.rewind) {
          swiper.slidePrev(speed, true, true);
          emit('autoplay');
        } else if (!swiper.params.autoplay.stopOnLastSlide) {
          swiper.slideTo(swiper.slides.length - 1, speed, true, true);
          emit('autoplay');
        }
      } else {
        if (!swiper.isEnd || swiper.params.loop || swiper.params.rewind) {
          swiper.slideNext(speed, true, true);
          emit('autoplay');
        } else if (!swiper.params.autoplay.stopOnLastSlide) {
          swiper.slideTo(0, speed, true, true);
          emit('autoplay');
        }
      }
      if (swiper.params.cssMode) {
        autoplayStartTime = new Date().getTime();
        requestAnimationFrame(() => {
          run();
        });
      }
    };
    if (delay > 0) {
      clearTimeout(timeout);
      timeout = setTimeout(() => {
        proceed();
      }, delay);
    } else {
      requestAnimationFrame(() => {
        proceed();
      });
    }

    // eslint-disable-next-line
    return delay;
  };
  const start = () => {
    autoplayStartTime = new Date().getTime();
    swiper.autoplay.running = true;
    run();
    emit('autoplayStart');
  };
  const stop = () => {
    swiper.autoplay.running = false;
    clearTimeout(timeout);
    cancelAnimationFrame(raf);
    emit('autoplayStop');
  };
  const pause = (internal, reset) => {
    if (swiper.destroyed || !swiper.autoplay.running) return;
    clearTimeout(timeout);
    if (!internal) {
      pausedByInteraction = true;
    }
    const proceed = () => {
      emit('autoplayPause');
      if (swiper.params.autoplay.waitForTransition) {
        swiper.wrapperEl.addEventListener('transitionend', onTransitionEnd);
      } else {
        resume();
      }
    };
    swiper.autoplay.paused = true;
    if (reset) {
      if (slideChanged) {
        autoplayTimeLeft = swiper.params.autoplay.delay;
      }
      slideChanged = false;
      proceed();
      return;
    }
    const delay = autoplayTimeLeft || swiper.params.autoplay.delay;
    autoplayTimeLeft = delay - (new Date().getTime() - autoplayStartTime);
    if (swiper.isEnd && autoplayTimeLeft < 0 && !swiper.params.loop) return;
    if (autoplayTimeLeft < 0) autoplayTimeLeft = 0;
    proceed();
  };
  const resume = () => {
    if (swiper.isEnd && autoplayTimeLeft < 0 && !swiper.params.loop || swiper.destroyed || !swiper.autoplay.running) return;
    autoplayStartTime = new Date().getTime();
    if (pausedByInteraction) {
      pausedByInteraction = false;
      run(autoplayTimeLeft);
    } else {
      run();
    }
    swiper.autoplay.paused = false;
    emit('autoplayResume');
  };
  const onVisibilityChange = () => {
    if (swiper.destroyed || !swiper.autoplay.running) return;
    const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
    if (document.visibilityState === 'hidden') {
      pausedByInteraction = true;
      pause(true);
    }
    if (document.visibilityState === 'visible') {
      resume();
    }
  };
  const onPointerEnter = e => {
    if (e.pointerType !== 'mouse') return;
    pausedByInteraction = true;
    pausedByPointerEnter = true;
    if (swiper.animating || swiper.autoplay.paused) return;
    pause(true);
  };
  const onPointerLeave = e => {
    if (e.pointerType !== 'mouse') return;
    pausedByPointerEnter = false;
    if (swiper.autoplay.paused) {
      resume();
    }
  };
  const attachMouseEvents = () => {
    if (swiper.params.autoplay.pauseOnMouseEnter) {
      swiper.el.addEventListener('pointerenter', onPointerEnter);
      swiper.el.addEventListener('pointerleave', onPointerLeave);
    }
  };
  const detachMouseEvents = () => {
    if (swiper.el && typeof swiper.el !== 'string') {
      swiper.el.removeEventListener('pointerenter', onPointerEnter);
      swiper.el.removeEventListener('pointerleave', onPointerLeave);
    }
  };
  const attachDocumentEvents = () => {
    const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
    document.addEventListener('visibilitychange', onVisibilityChange);
  };
  const detachDocumentEvents = () => {
    const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
    document.removeEventListener('visibilitychange', onVisibilityChange);
  };
  on('init', () => {
    if (swiper.params.autoplay.enabled) {
      attachMouseEvents();
      attachDocumentEvents();
      start();
    }
  });
  on('destroy', () => {
    detachMouseEvents();
    detachDocumentEvents();
    if (swiper.autoplay.running) {
      stop();
    }
  });
  on('_freeModeStaticRelease', () => {
    if (pausedByTouch || pausedByInteraction) {
      resume();
    }
  });
  on('_freeModeNoMomentumRelease', () => {
    if (!swiper.params.autoplay.disableOnInteraction) {
      pause(true, true);
    } else {
      stop();
    }
  });
  on('beforeTransitionStart', (_s, speed, internal) => {
    if (swiper.destroyed || !swiper.autoplay.running) return;
    if (internal || !swiper.params.autoplay.disableOnInteraction) {
      pause(true, true);
    } else {
      stop();
    }
  });
  on('sliderFirstMove', () => {
    if (swiper.destroyed || !swiper.autoplay.running) return;
    if (swiper.params.autoplay.disableOnInteraction) {
      stop();
      return;
    }
    isTouched = true;
    pausedByTouch = false;
    pausedByInteraction = false;
    touchStartTimeout = setTimeout(() => {
      pausedByInteraction = true;
      pausedByTouch = true;
      pause(true);
    }, 200);
  });
  on('touchEnd', () => {
    if (swiper.destroyed || !swiper.autoplay.running || !isTouched) return;
    clearTimeout(touchStartTimeout);
    clearTimeout(timeout);
    if (swiper.params.autoplay.disableOnInteraction) {
      pausedByTouch = false;
      isTouched = false;
      return;
    }
    if (pausedByTouch && swiper.params.cssMode) resume();
    pausedByTouch = false;
    isTouched = false;
  });
  on('slideChange', () => {
    if (swiper.destroyed || !swiper.autoplay.running) return;
    slideChanged = true;
  });
  Object.assign(swiper.autoplay, {
    start,
    stop,
    pause,
    resume
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/controller.mjs":
/*!*****************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/controller.mjs ***!
  \*****************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ Controller)
/* harmony export */ });
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");


/* eslint no-bitwise: ["error", { "allow": [">>"] }] */
function Controller(_ref) {
  let {
    swiper,
    extendParams,
    on
  } = _ref;
  extendParams({
    controller: {
      control: undefined,
      inverse: false,
      by: 'slide' // or 'container'
    }
  });

  swiper.controller = {
    control: undefined
  };
  function LinearSpline(x, y) {
    const binarySearch = function search() {
      let maxIndex;
      let minIndex;
      let guess;
      return (array, val) => {
        minIndex = -1;
        maxIndex = array.length;
        while (maxIndex - minIndex > 1) {
          guess = maxIndex + minIndex >> 1;
          if (array[guess] <= val) {
            minIndex = guess;
          } else {
            maxIndex = guess;
          }
        }
        return maxIndex;
      };
    }();
    this.x = x;
    this.y = y;
    this.lastIndex = x.length - 1;
    // Given an x value (x2), return the expected y2 value:
    // (x1,y1) is the known point before given value,
    // (x3,y3) is the known point after given value.
    let i1;
    let i3;
    this.interpolate = function interpolate(x2) {
      if (!x2) return 0;

      // Get the indexes of x1 and x3 (the array indexes before and after given x2):
      i3 = binarySearch(this.x, x2);
      i1 = i3 - 1;

      // We have our indexes i1 & i3, so we can calculate already:
      // y2 := ((x2−x1) × (y3−y1)) ÷ (x3−x1) + y1
      return (x2 - this.x[i1]) * (this.y[i3] - this.y[i1]) / (this.x[i3] - this.x[i1]) + this.y[i1];
    };
    return this;
  }
  function getInterpolateFunction(c) {
    swiper.controller.spline = swiper.params.loop ? new LinearSpline(swiper.slidesGrid, c.slidesGrid) : new LinearSpline(swiper.snapGrid, c.snapGrid);
  }
  function setTranslate(_t, byController) {
    const controlled = swiper.controller.control;
    let multiplier;
    let controlledTranslate;
    const Swiper = swiper.constructor;
    function setControlledTranslate(c) {
      if (c.destroyed) return;

      // this will create an Interpolate function based on the snapGrids
      // x is the Grid of the scrolled scroller and y will be the controlled scroller
      // it makes sense to create this only once and recall it for the interpolation
      // the function does a lot of value caching for performance
      const translate = swiper.rtlTranslate ? -swiper.translate : swiper.translate;
      if (swiper.params.controller.by === 'slide') {
        getInterpolateFunction(c);
        // i am not sure why the values have to be multiplicated this way, tried to invert the snapGrid
        // but it did not work out
        controlledTranslate = -swiper.controller.spline.interpolate(-translate);
      }
      if (!controlledTranslate || swiper.params.controller.by === 'container') {
        multiplier = (c.maxTranslate() - c.minTranslate()) / (swiper.maxTranslate() - swiper.minTranslate());
        if (Number.isNaN(multiplier) || !Number.isFinite(multiplier)) {
          multiplier = 1;
        }
        controlledTranslate = (translate - swiper.minTranslate()) * multiplier + c.minTranslate();
      }
      if (swiper.params.controller.inverse) {
        controlledTranslate = c.maxTranslate() - controlledTranslate;
      }
      c.updateProgress(controlledTranslate);
      c.setTranslate(controlledTranslate, swiper);
      c.updateActiveIndex();
      c.updateSlidesClasses();
    }
    if (Array.isArray(controlled)) {
      for (let i = 0; i < controlled.length; i += 1) {
        if (controlled[i] !== byController && controlled[i] instanceof Swiper) {
          setControlledTranslate(controlled[i]);
        }
      }
    } else if (controlled instanceof Swiper && byController !== controlled) {
      setControlledTranslate(controlled);
    }
  }
  function setTransition(duration, byController) {
    const Swiper = swiper.constructor;
    const controlled = swiper.controller.control;
    let i;
    function setControlledTransition(c) {
      if (c.destroyed) return;
      c.setTransition(duration, swiper);
      if (duration !== 0) {
        c.transitionStart();
        if (c.params.autoHeight) {
          (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.n)(() => {
            c.updateAutoHeight();
          });
        }
        (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.k)(c.wrapperEl, () => {
          if (!controlled) return;
          c.transitionEnd();
        });
      }
    }
    if (Array.isArray(controlled)) {
      for (i = 0; i < controlled.length; i += 1) {
        if (controlled[i] !== byController && controlled[i] instanceof Swiper) {
          setControlledTransition(controlled[i]);
        }
      }
    } else if (controlled instanceof Swiper && byController !== controlled) {
      setControlledTransition(controlled);
    }
  }
  function removeSpline() {
    if (!swiper.controller.control) return;
    if (swiper.controller.spline) {
      swiper.controller.spline = undefined;
      delete swiper.controller.spline;
    }
  }
  on('beforeInit', () => {
    if (typeof window !== 'undefined' && (
    // eslint-disable-line
    typeof swiper.params.controller.control === 'string' || swiper.params.controller.control instanceof HTMLElement)) {
      const controlElements = typeof swiper.params.controller.control === 'string' ? [...document.querySelectorAll(swiper.params.controller.control)] : [swiper.params.controller.control];
      controlElements.forEach(controlElement => {
        if (!swiper.controller.control) swiper.controller.control = [];
        if (controlElement && controlElement.swiper) {
          swiper.controller.control.push(controlElement.swiper);
        } else if (controlElement) {
          const eventName = `${swiper.params.eventsPrefix}init`;
          const onControllerSwiper = e => {
            swiper.controller.control.push(e.detail[0]);
            swiper.update();
            controlElement.removeEventListener(eventName, onControllerSwiper);
          };
          controlElement.addEventListener(eventName, onControllerSwiper);
        }
      });
      return;
    }
    swiper.controller.control = swiper.params.controller.control;
  });
  on('update', () => {
    removeSpline();
  });
  on('resize', () => {
    removeSpline();
  });
  on('observerUpdate', () => {
    removeSpline();
  });
  on('setTranslate', (_s, translate, byController) => {
    if (!swiper.controller.control || swiper.controller.control.destroyed) return;
    swiper.controller.setTranslate(translate, byController);
  });
  on('setTransition', (_s, duration, byController) => {
    if (!swiper.controller.control || swiper.controller.control.destroyed) return;
    swiper.controller.setTransition(duration, byController);
  });
  Object.assign(swiper.controller, {
    setTranslate,
    setTransition
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/effect-cards.mjs":
/*!*******************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/effect-cards.mjs ***!
  \*******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ EffectCards)
/* harmony export */ });
/* harmony import */ var _shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/create-shadow.mjs */ "../plugin-slug/node_modules/swiper/shared/create-shadow.mjs");
/* harmony import */ var _shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/effect-init.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-init.mjs");
/* harmony import */ var _shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/effect-target.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-target.mjs");
/* harmony import */ var _shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../shared/effect-virtual-transition-end.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-virtual-transition-end.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");






function EffectCards(_ref) {
  let {
    swiper,
    extendParams,
    on
  } = _ref;
  extendParams({
    cardsEffect: {
      slideShadows: true,
      rotate: true,
      perSlideRotate: 2,
      perSlideOffset: 8
    }
  });
  const setTranslate = () => {
    const {
      slides,
      activeIndex,
      rtlTranslate: rtl
    } = swiper;
    const params = swiper.params.cardsEffect;
    const {
      startTranslate,
      isTouched
    } = swiper.touchEventsData;
    const currentTranslate = rtl ? -swiper.translate : swiper.translate;
    for (let i = 0; i < slides.length; i += 1) {
      const slideEl = slides[i];
      const slideProgress = slideEl.progress;
      const progress = Math.min(Math.max(slideProgress, -4), 4);
      let offset = slideEl.swiperSlideOffset;
      if (swiper.params.centeredSlides && !swiper.params.cssMode) {
        swiper.wrapperEl.style.transform = `translateX(${swiper.minTranslate()}px)`;
      }
      if (swiper.params.centeredSlides && swiper.params.cssMode) {
        offset -= slides[0].swiperSlideOffset;
      }
      let tX = swiper.params.cssMode ? -offset - swiper.translate : -offset;
      let tY = 0;
      const tZ = -100 * Math.abs(progress);
      let scale = 1;
      let rotate = -params.perSlideRotate * progress;
      let tXAdd = params.perSlideOffset - Math.abs(progress) * 0.75;
      const slideIndex = swiper.virtual && swiper.params.virtual.enabled ? swiper.virtual.from + i : i;
      const isSwipeToNext = (slideIndex === activeIndex || slideIndex === activeIndex - 1) && progress > 0 && progress < 1 && (isTouched || swiper.params.cssMode) && currentTranslate < startTranslate;
      const isSwipeToPrev = (slideIndex === activeIndex || slideIndex === activeIndex + 1) && progress < 0 && progress > -1 && (isTouched || swiper.params.cssMode) && currentTranslate > startTranslate;
      if (isSwipeToNext || isSwipeToPrev) {
        const subProgress = (1 - Math.abs((Math.abs(progress) - 0.5) / 0.5)) ** 0.5;
        rotate += -28 * progress * subProgress;
        scale += -0.5 * subProgress;
        tXAdd += 96 * subProgress;
        tY = `${-25 * subProgress * Math.abs(progress)}%`;
      }
      if (progress < 0) {
        // next
        tX = `calc(${tX}px ${rtl ? '-' : '+'} (${tXAdd * Math.abs(progress)}%))`;
      } else if (progress > 0) {
        // prev
        tX = `calc(${tX}px ${rtl ? '-' : '+'} (-${tXAdd * Math.abs(progress)}%))`;
      } else {
        tX = `${tX}px`;
      }
      if (!swiper.isHorizontal()) {
        const prevY = tY;
        tY = tX;
        tX = prevY;
      }
      const scaleString = progress < 0 ? `${1 + (1 - scale) * progress}` : `${1 - (1 - scale) * progress}`;

      /* eslint-disable */
      const transform = `
        translate3d(${tX}, ${tY}, ${tZ}px)
        rotateZ(${params.rotate ? rtl ? -rotate : rotate : 0}deg)
        scale(${scaleString})
      `;
      /* eslint-enable */

      if (params.slideShadows) {
        // Set shadows
        let shadowEl = slideEl.querySelector('.swiper-slide-shadow');
        if (!shadowEl) {
          shadowEl = (0,_shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('cards', slideEl);
        }
        if (shadowEl) shadowEl.style.opacity = Math.min(Math.max((Math.abs(progress) - 0.5) / 0.5, 0), 1);
      }
      slideEl.style.zIndex = -Math.abs(Math.round(slideProgress)) + slides.length;
      const targetEl = (0,_shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__.e)(params, slideEl);
      targetEl.style.transform = transform;
    }
  };
  const setTransition = duration => {
    const transformElements = swiper.slides.map(slideEl => (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__.g)(slideEl));
    transformElements.forEach(el => {
      el.style.transitionDuration = `${duration}ms`;
      el.querySelectorAll('.swiper-slide-shadow').forEach(shadowEl => {
        shadowEl.style.transitionDuration = `${duration}ms`;
      });
    });
    (0,_shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_3__.e)({
      swiper,
      duration,
      transformElements
    });
  };
  (0,_shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__.e)({
    effect: 'cards',
    swiper,
    on,
    setTranslate,
    setTransition,
    perspective: () => true,
    overwriteParams: () => ({
      watchSlidesProgress: true,
      virtualTranslate: !swiper.params.cssMode
    })
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/effect-coverflow.mjs":
/*!***********************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/effect-coverflow.mjs ***!
  \***********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ EffectCoverflow)
/* harmony export */ });
/* harmony import */ var _shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/create-shadow.mjs */ "../plugin-slug/node_modules/swiper/shared/create-shadow.mjs");
/* harmony import */ var _shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/effect-init.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-init.mjs");
/* harmony import */ var _shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/effect-target.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-target.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");





function EffectCoverflow(_ref) {
  let {
    swiper,
    extendParams,
    on
  } = _ref;
  extendParams({
    coverflowEffect: {
      rotate: 50,
      stretch: 0,
      depth: 100,
      scale: 1,
      modifier: 1,
      slideShadows: true
    }
  });
  const setTranslate = () => {
    const {
      width: swiperWidth,
      height: swiperHeight,
      slides,
      slidesSizesGrid
    } = swiper;
    const params = swiper.params.coverflowEffect;
    const isHorizontal = swiper.isHorizontal();
    const transform = swiper.translate;
    const center = isHorizontal ? -transform + swiperWidth / 2 : -transform + swiperHeight / 2;
    const rotate = isHorizontal ? params.rotate : -params.rotate;
    const translate = params.depth;
    const r = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_3__.o)(swiper);
    // Each slide offset from center
    for (let i = 0, length = slides.length; i < length; i += 1) {
      const slideEl = slides[i];
      const slideSize = slidesSizesGrid[i];
      const slideOffset = slideEl.swiperSlideOffset;
      const centerOffset = (center - slideOffset - slideSize / 2) / slideSize;
      const offsetMultiplier = typeof params.modifier === 'function' ? params.modifier(centerOffset) : centerOffset * params.modifier;
      let rotateY = isHorizontal ? rotate * offsetMultiplier : 0;
      let rotateX = isHorizontal ? 0 : rotate * offsetMultiplier;
      // var rotateZ = 0
      let translateZ = -translate * Math.abs(offsetMultiplier);
      let stretch = params.stretch;
      // Allow percentage to make a relative stretch for responsive sliders
      if (typeof stretch === 'string' && stretch.indexOf('%') !== -1) {
        stretch = parseFloat(params.stretch) / 100 * slideSize;
      }
      let translateY = isHorizontal ? 0 : stretch * offsetMultiplier;
      let translateX = isHorizontal ? stretch * offsetMultiplier : 0;
      let scale = 1 - (1 - params.scale) * Math.abs(offsetMultiplier);

      // Fix for ultra small values
      if (Math.abs(translateX) < 0.001) translateX = 0;
      if (Math.abs(translateY) < 0.001) translateY = 0;
      if (Math.abs(translateZ) < 0.001) translateZ = 0;
      if (Math.abs(rotateY) < 0.001) rotateY = 0;
      if (Math.abs(rotateX) < 0.001) rotateX = 0;
      if (Math.abs(scale) < 0.001) scale = 0;
      const slideTransform = `translate3d(${translateX}px,${translateY}px,${translateZ}px)  rotateX(${r(rotateX)}deg) rotateY(${r(rotateY)}deg) scale(${scale})`;
      const targetEl = (0,_shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__.e)(params, slideEl);
      targetEl.style.transform = slideTransform;
      slideEl.style.zIndex = -Math.abs(Math.round(offsetMultiplier)) + 1;
      if (params.slideShadows) {
        // Set shadows
        let shadowBeforeEl = isHorizontal ? slideEl.querySelector('.swiper-slide-shadow-left') : slideEl.querySelector('.swiper-slide-shadow-top');
        let shadowAfterEl = isHorizontal ? slideEl.querySelector('.swiper-slide-shadow-right') : slideEl.querySelector('.swiper-slide-shadow-bottom');
        if (!shadowBeforeEl) {
          shadowBeforeEl = (0,_shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('coverflow', slideEl, isHorizontal ? 'left' : 'top');
        }
        if (!shadowAfterEl) {
          shadowAfterEl = (0,_shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('coverflow', slideEl, isHorizontal ? 'right' : 'bottom');
        }
        if (shadowBeforeEl) shadowBeforeEl.style.opacity = offsetMultiplier > 0 ? offsetMultiplier : 0;
        if (shadowAfterEl) shadowAfterEl.style.opacity = -offsetMultiplier > 0 ? -offsetMultiplier : 0;
      }
    }
  };
  const setTransition = duration => {
    const transformElements = swiper.slides.map(slideEl => (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_3__.g)(slideEl));
    transformElements.forEach(el => {
      el.style.transitionDuration = `${duration}ms`;
      el.querySelectorAll('.swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left').forEach(shadowEl => {
        shadowEl.style.transitionDuration = `${duration}ms`;
      });
    });
  };
  (0,_shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__.e)({
    effect: 'coverflow',
    swiper,
    on,
    setTranslate,
    setTransition,
    perspective: () => true,
    overwriteParams: () => ({
      watchSlidesProgress: true
    })
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/effect-creative.mjs":
/*!**********************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/effect-creative.mjs ***!
  \**********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ EffectCreative)
/* harmony export */ });
/* harmony import */ var _shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/create-shadow.mjs */ "../plugin-slug/node_modules/swiper/shared/create-shadow.mjs");
/* harmony import */ var _shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/effect-init.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-init.mjs");
/* harmony import */ var _shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/effect-target.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-target.mjs");
/* harmony import */ var _shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../shared/effect-virtual-transition-end.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-virtual-transition-end.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");






function EffectCreative(_ref) {
  let {
    swiper,
    extendParams,
    on
  } = _ref;
  extendParams({
    creativeEffect: {
      limitProgress: 1,
      shadowPerProgress: false,
      progressMultiplier: 1,
      perspective: true,
      prev: {
        translate: [0, 0, 0],
        rotate: [0, 0, 0],
        opacity: 1,
        scale: 1
      },
      next: {
        translate: [0, 0, 0],
        rotate: [0, 0, 0],
        opacity: 1,
        scale: 1
      }
    }
  });
  const getTranslateValue = value => {
    if (typeof value === 'string') return value;
    return `${value}px`;
  };
  const setTranslate = () => {
    const {
      slides,
      wrapperEl,
      slidesSizesGrid
    } = swiper;
    const params = swiper.params.creativeEffect;
    const {
      progressMultiplier: multiplier
    } = params;
    const isCenteredSlides = swiper.params.centeredSlides;
    const rotateFix = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__.o)(swiper);
    if (isCenteredSlides) {
      const margin = slidesSizesGrid[0] / 2 - swiper.params.slidesOffsetBefore || 0;
      wrapperEl.style.transform = `translateX(calc(50% - ${margin}px))`;
    }
    for (let i = 0; i < slides.length; i += 1) {
      const slideEl = slides[i];
      const slideProgress = slideEl.progress;
      const progress = Math.min(Math.max(slideEl.progress, -params.limitProgress), params.limitProgress);
      let originalProgress = progress;
      if (!isCenteredSlides) {
        originalProgress = Math.min(Math.max(slideEl.originalProgress, -params.limitProgress), params.limitProgress);
      }
      const offset = slideEl.swiperSlideOffset;
      const t = [swiper.params.cssMode ? -offset - swiper.translate : -offset, 0, 0];
      const r = [0, 0, 0];
      let custom = false;
      if (!swiper.isHorizontal()) {
        t[1] = t[0];
        t[0] = 0;
      }
      let data = {
        translate: [0, 0, 0],
        rotate: [0, 0, 0],
        scale: 1,
        opacity: 1
      };
      if (progress < 0) {
        data = params.next;
        custom = true;
      } else if (progress > 0) {
        data = params.prev;
        custom = true;
      }
      // set translate
      t.forEach((value, index) => {
        t[index] = `calc(${value}px + (${getTranslateValue(data.translate[index])} * ${Math.abs(progress * multiplier)}))`;
      });
      // set rotates
      r.forEach((value, index) => {
        let val = data.rotate[index] * Math.abs(progress * multiplier);
        r[index] = val;
      });
      slideEl.style.zIndex = -Math.abs(Math.round(slideProgress)) + slides.length;
      const translateString = t.join(', ');
      const rotateString = `rotateX(${rotateFix(r[0])}deg) rotateY(${rotateFix(r[1])}deg) rotateZ(${rotateFix(r[2])}deg)`;
      const scaleString = originalProgress < 0 ? `scale(${1 + (1 - data.scale) * originalProgress * multiplier})` : `scale(${1 - (1 - data.scale) * originalProgress * multiplier})`;
      const opacityString = originalProgress < 0 ? 1 + (1 - data.opacity) * originalProgress * multiplier : 1 - (1 - data.opacity) * originalProgress * multiplier;
      const transform = `translate3d(${translateString}) ${rotateString} ${scaleString}`;

      // Set shadows
      if (custom && data.shadow || !custom) {
        let shadowEl = slideEl.querySelector('.swiper-slide-shadow');
        if (!shadowEl && data.shadow) {
          shadowEl = (0,_shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('creative', slideEl);
        }
        if (shadowEl) {
          const shadowOpacity = params.shadowPerProgress ? progress * (1 / params.limitProgress) : progress;
          shadowEl.style.opacity = Math.min(Math.max(Math.abs(shadowOpacity), 0), 1);
        }
      }
      const targetEl = (0,_shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__.e)(params, slideEl);
      targetEl.style.transform = transform;
      targetEl.style.opacity = opacityString;
      if (data.origin) {
        targetEl.style.transformOrigin = data.origin;
      }
    }
  };
  const setTransition = duration => {
    const transformElements = swiper.slides.map(slideEl => (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__.g)(slideEl));
    transformElements.forEach(el => {
      el.style.transitionDuration = `${duration}ms`;
      el.querySelectorAll('.swiper-slide-shadow').forEach(shadowEl => {
        shadowEl.style.transitionDuration = `${duration}ms`;
      });
    });
    (0,_shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_3__.e)({
      swiper,
      duration,
      transformElements,
      allSlides: true
    });
  };
  (0,_shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__.e)({
    effect: 'creative',
    swiper,
    on,
    setTranslate,
    setTransition,
    perspective: () => swiper.params.creativeEffect.perspective,
    overwriteParams: () => ({
      watchSlidesProgress: true,
      virtualTranslate: !swiper.params.cssMode
    })
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/effect-cube.mjs":
/*!******************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/effect-cube.mjs ***!
  \******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ EffectCube)
/* harmony export */ });
/* harmony import */ var _shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/effect-init.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-init.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");



function EffectCube(_ref) {
  let {
    swiper,
    extendParams,
    on
  } = _ref;
  extendParams({
    cubeEffect: {
      slideShadows: true,
      shadow: true,
      shadowOffset: 20,
      shadowScale: 0.94
    }
  });
  const createSlideShadows = (slideEl, progress, isHorizontal) => {
    let shadowBefore = isHorizontal ? slideEl.querySelector('.swiper-slide-shadow-left') : slideEl.querySelector('.swiper-slide-shadow-top');
    let shadowAfter = isHorizontal ? slideEl.querySelector('.swiper-slide-shadow-right') : slideEl.querySelector('.swiper-slide-shadow-bottom');
    if (!shadowBefore) {
      shadowBefore = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', `swiper-slide-shadow-cube swiper-slide-shadow-${isHorizontal ? 'left' : 'top'}`.split(' '));
      slideEl.append(shadowBefore);
    }
    if (!shadowAfter) {
      shadowAfter = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', `swiper-slide-shadow-cube swiper-slide-shadow-${isHorizontal ? 'right' : 'bottom'}`.split(' '));
      slideEl.append(shadowAfter);
    }
    if (shadowBefore) shadowBefore.style.opacity = Math.max(-progress, 0);
    if (shadowAfter) shadowAfter.style.opacity = Math.max(progress, 0);
  };
  const recreateShadows = () => {
    // create new ones
    const isHorizontal = swiper.isHorizontal();
    swiper.slides.forEach(slideEl => {
      const progress = Math.max(Math.min(slideEl.progress, 1), -1);
      createSlideShadows(slideEl, progress, isHorizontal);
    });
  };
  const setTranslate = () => {
    const {
      el,
      wrapperEl,
      slides,
      width: swiperWidth,
      height: swiperHeight,
      rtlTranslate: rtl,
      size: swiperSize,
      browser
    } = swiper;
    const r = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.o)(swiper);
    const params = swiper.params.cubeEffect;
    const isHorizontal = swiper.isHorizontal();
    const isVirtual = swiper.virtual && swiper.params.virtual.enabled;
    let wrapperRotate = 0;
    let cubeShadowEl;
    if (params.shadow) {
      if (isHorizontal) {
        cubeShadowEl = swiper.wrapperEl.querySelector('.swiper-cube-shadow');
        if (!cubeShadowEl) {
          cubeShadowEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', 'swiper-cube-shadow');
          swiper.wrapperEl.append(cubeShadowEl);
        }
        cubeShadowEl.style.height = `${swiperWidth}px`;
      } else {
        cubeShadowEl = el.querySelector('.swiper-cube-shadow');
        if (!cubeShadowEl) {
          cubeShadowEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', 'swiper-cube-shadow');
          el.append(cubeShadowEl);
        }
      }
    }
    for (let i = 0; i < slides.length; i += 1) {
      const slideEl = slides[i];
      let slideIndex = i;
      if (isVirtual) {
        slideIndex = parseInt(slideEl.getAttribute('data-swiper-slide-index'), 10);
      }
      let slideAngle = slideIndex * 90;
      let round = Math.floor(slideAngle / 360);
      if (rtl) {
        slideAngle = -slideAngle;
        round = Math.floor(-slideAngle / 360);
      }
      const progress = Math.max(Math.min(slideEl.progress, 1), -1);
      let tx = 0;
      let ty = 0;
      let tz = 0;
      if (slideIndex % 4 === 0) {
        tx = -round * 4 * swiperSize;
        tz = 0;
      } else if ((slideIndex - 1) % 4 === 0) {
        tx = 0;
        tz = -round * 4 * swiperSize;
      } else if ((slideIndex - 2) % 4 === 0) {
        tx = swiperSize + round * 4 * swiperSize;
        tz = swiperSize;
      } else if ((slideIndex - 3) % 4 === 0) {
        tx = -swiperSize;
        tz = 3 * swiperSize + swiperSize * 4 * round;
      }
      if (rtl) {
        tx = -tx;
      }
      if (!isHorizontal) {
        ty = tx;
        tx = 0;
      }
      const transform = `rotateX(${r(isHorizontal ? 0 : -slideAngle)}deg) rotateY(${r(isHorizontal ? slideAngle : 0)}deg) translate3d(${tx}px, ${ty}px, ${tz}px)`;
      if (progress <= 1 && progress > -1) {
        wrapperRotate = slideIndex * 90 + progress * 90;
        if (rtl) wrapperRotate = -slideIndex * 90 - progress * 90;
      }
      slideEl.style.transform = transform;
      if (params.slideShadows) {
        createSlideShadows(slideEl, progress, isHorizontal);
      }
    }
    wrapperEl.style.transformOrigin = `50% 50% -${swiperSize / 2}px`;
    wrapperEl.style['-webkit-transform-origin'] = `50% 50% -${swiperSize / 2}px`;
    if (params.shadow) {
      if (isHorizontal) {
        cubeShadowEl.style.transform = `translate3d(0px, ${swiperWidth / 2 + params.shadowOffset}px, ${-swiperWidth / 2}px) rotateX(89.99deg) rotateZ(0deg) scale(${params.shadowScale})`;
      } else {
        const shadowAngle = Math.abs(wrapperRotate) - Math.floor(Math.abs(wrapperRotate) / 90) * 90;
        const multiplier = 1.5 - (Math.sin(shadowAngle * 2 * Math.PI / 360) / 2 + Math.cos(shadowAngle * 2 * Math.PI / 360) / 2);
        const scale1 = params.shadowScale;
        const scale2 = params.shadowScale / multiplier;
        const offset = params.shadowOffset;
        cubeShadowEl.style.transform = `scale3d(${scale1}, 1, ${scale2}) translate3d(0px, ${swiperHeight / 2 + offset}px, ${-swiperHeight / 2 / scale2}px) rotateX(-89.99deg)`;
      }
    }
    const zFactor = (browser.isSafari || browser.isWebView) && browser.needPerspectiveFix ? -swiperSize / 2 : 0;
    wrapperEl.style.transform = `translate3d(0px,0,${zFactor}px) rotateX(${r(swiper.isHorizontal() ? 0 : wrapperRotate)}deg) rotateY(${r(swiper.isHorizontal() ? -wrapperRotate : 0)}deg)`;
    wrapperEl.style.setProperty('--swiper-cube-translate-z', `${zFactor}px`);
  };
  const setTransition = duration => {
    const {
      el,
      slides
    } = swiper;
    slides.forEach(slideEl => {
      slideEl.style.transitionDuration = `${duration}ms`;
      slideEl.querySelectorAll('.swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left').forEach(subEl => {
        subEl.style.transitionDuration = `${duration}ms`;
      });
    });
    if (swiper.params.cubeEffect.shadow && !swiper.isHorizontal()) {
      const shadowEl = el.querySelector('.swiper-cube-shadow');
      if (shadowEl) shadowEl.style.transitionDuration = `${duration}ms`;
    }
  };
  (0,_shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_0__.e)({
    effect: 'cube',
    swiper,
    on,
    setTranslate,
    setTransition,
    recreateShadows,
    getEffectParams: () => swiper.params.cubeEffect,
    perspective: () => true,
    overwriteParams: () => ({
      slidesPerView: 1,
      slidesPerGroup: 1,
      watchSlidesProgress: true,
      resistanceRatio: 0,
      spaceBetween: 0,
      centeredSlides: false,
      virtualTranslate: true
    })
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/effect-fade.mjs":
/*!******************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/effect-fade.mjs ***!
  \******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ EffectFade)
/* harmony export */ });
/* harmony import */ var _shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/effect-init.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-init.mjs");
/* harmony import */ var _shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/effect-target.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-target.mjs");
/* harmony import */ var _shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/effect-virtual-transition-end.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-virtual-transition-end.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");





function EffectFade(_ref) {
  let {
    swiper,
    extendParams,
    on
  } = _ref;
  extendParams({
    fadeEffect: {
      crossFade: false
    }
  });
  const setTranslate = () => {
    const {
      slides
    } = swiper;
    const params = swiper.params.fadeEffect;
    for (let i = 0; i < slides.length; i += 1) {
      const slideEl = swiper.slides[i];
      const offset = slideEl.swiperSlideOffset;
      let tx = -offset;
      if (!swiper.params.virtualTranslate) tx -= swiper.translate;
      let ty = 0;
      if (!swiper.isHorizontal()) {
        ty = tx;
        tx = 0;
      }
      const slideOpacity = swiper.params.fadeEffect.crossFade ? Math.max(1 - Math.abs(slideEl.progress), 0) : 1 + Math.min(Math.max(slideEl.progress, -1), 0);
      const targetEl = (0,_shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(params, slideEl);
      targetEl.style.opacity = slideOpacity;
      targetEl.style.transform = `translate3d(${tx}px, ${ty}px, 0px)`;
    }
  };
  const setTransition = duration => {
    const transformElements = swiper.slides.map(slideEl => (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_3__.g)(slideEl));
    transformElements.forEach(el => {
      el.style.transitionDuration = `${duration}ms`;
    });
    (0,_shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_2__.e)({
      swiper,
      duration,
      transformElements,
      allSlides: true
    });
  };
  (0,_shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_0__.e)({
    effect: 'fade',
    swiper,
    on,
    setTranslate,
    setTransition,
    overwriteParams: () => ({
      slidesPerView: 1,
      slidesPerGroup: 1,
      watchSlidesProgress: true,
      spaceBetween: 0,
      virtualTranslate: !swiper.params.cssMode
    })
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/effect-flip.mjs":
/*!******************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/effect-flip.mjs ***!
  \******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ EffectFlip)
/* harmony export */ });
/* harmony import */ var _shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/create-shadow.mjs */ "../plugin-slug/node_modules/swiper/shared/create-shadow.mjs");
/* harmony import */ var _shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/effect-init.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-init.mjs");
/* harmony import */ var _shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/effect-target.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-target.mjs");
/* harmony import */ var _shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../shared/effect-virtual-transition-end.mjs */ "../plugin-slug/node_modules/swiper/shared/effect-virtual-transition-end.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");






function EffectFlip(_ref) {
  let {
    swiper,
    extendParams,
    on
  } = _ref;
  extendParams({
    flipEffect: {
      slideShadows: true,
      limitRotation: true
    }
  });
  const createSlideShadows = (slideEl, progress) => {
    let shadowBefore = swiper.isHorizontal() ? slideEl.querySelector('.swiper-slide-shadow-left') : slideEl.querySelector('.swiper-slide-shadow-top');
    let shadowAfter = swiper.isHorizontal() ? slideEl.querySelector('.swiper-slide-shadow-right') : slideEl.querySelector('.swiper-slide-shadow-bottom');
    if (!shadowBefore) {
      shadowBefore = (0,_shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('flip', slideEl, swiper.isHorizontal() ? 'left' : 'top');
    }
    if (!shadowAfter) {
      shadowAfter = (0,_shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('flip', slideEl, swiper.isHorizontal() ? 'right' : 'bottom');
    }
    if (shadowBefore) shadowBefore.style.opacity = Math.max(-progress, 0);
    if (shadowAfter) shadowAfter.style.opacity = Math.max(progress, 0);
  };
  const recreateShadows = () => {
    // Set shadows
    swiper.params.flipEffect;
    swiper.slides.forEach(slideEl => {
      let progress = slideEl.progress;
      if (swiper.params.flipEffect.limitRotation) {
        progress = Math.max(Math.min(slideEl.progress, 1), -1);
      }
      createSlideShadows(slideEl, progress);
    });
  };
  const setTranslate = () => {
    const {
      slides,
      rtlTranslate: rtl
    } = swiper;
    const params = swiper.params.flipEffect;
    const rotateFix = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__.o)(swiper);
    for (let i = 0; i < slides.length; i += 1) {
      const slideEl = slides[i];
      let progress = slideEl.progress;
      if (swiper.params.flipEffect.limitRotation) {
        progress = Math.max(Math.min(slideEl.progress, 1), -1);
      }
      const offset = slideEl.swiperSlideOffset;
      const rotate = -180 * progress;
      let rotateY = rotate;
      let rotateX = 0;
      let tx = swiper.params.cssMode ? -offset - swiper.translate : -offset;
      let ty = 0;
      if (!swiper.isHorizontal()) {
        ty = tx;
        tx = 0;
        rotateX = -rotateY;
        rotateY = 0;
      } else if (rtl) {
        rotateY = -rotateY;
      }
      slideEl.style.zIndex = -Math.abs(Math.round(progress)) + slides.length;
      if (params.slideShadows) {
        createSlideShadows(slideEl, progress);
      }
      const transform = `translate3d(${tx}px, ${ty}px, 0px) rotateX(${rotateFix(rotateX)}deg) rotateY(${rotateFix(rotateY)}deg)`;
      const targetEl = (0,_shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__.e)(params, slideEl);
      targetEl.style.transform = transform;
    }
  };
  const setTransition = duration => {
    const transformElements = swiper.slides.map(slideEl => (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__.g)(slideEl));
    transformElements.forEach(el => {
      el.style.transitionDuration = `${duration}ms`;
      el.querySelectorAll('.swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left').forEach(shadowEl => {
        shadowEl.style.transitionDuration = `${duration}ms`;
      });
    });
    (0,_shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_3__.e)({
      swiper,
      duration,
      transformElements
    });
  };
  (0,_shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__.e)({
    effect: 'flip',
    swiper,
    on,
    setTranslate,
    setTransition,
    recreateShadows,
    getEffectParams: () => swiper.params.flipEffect,
    perspective: () => true,
    overwriteParams: () => ({
      slidesPerView: 1,
      slidesPerGroup: 1,
      watchSlidesProgress: true,
      spaceBetween: 0,
      virtualTranslate: !swiper.params.cssMode
    })
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/free-mode.mjs":
/*!****************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/free-mode.mjs ***!
  \****************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ freeMode)
/* harmony export */ });
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");


function freeMode(_ref) {
  let {
    swiper,
    extendParams,
    emit,
    once
  } = _ref;
  extendParams({
    freeMode: {
      enabled: false,
      momentum: true,
      momentumRatio: 1,
      momentumBounce: true,
      momentumBounceRatio: 1,
      momentumVelocityRatio: 1,
      sticky: false,
      minimumVelocity: 0.02
    }
  });
  function onTouchStart() {
    if (swiper.params.cssMode) return;
    const translate = swiper.getTranslate();
    swiper.setTranslate(translate);
    swiper.setTransition(0);
    swiper.touchEventsData.velocities.length = 0;
    swiper.freeMode.onTouchEnd({
      currentPos: swiper.rtl ? swiper.translate : -swiper.translate
    });
  }
  function onTouchMove() {
    if (swiper.params.cssMode) return;
    const {
      touchEventsData: data,
      touches
    } = swiper;
    // Velocity
    if (data.velocities.length === 0) {
      data.velocities.push({
        position: touches[swiper.isHorizontal() ? 'startX' : 'startY'],
        time: data.touchStartTime
      });
    }
    data.velocities.push({
      position: touches[swiper.isHorizontal() ? 'currentX' : 'currentY'],
      time: (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.d)()
    });
  }
  function onTouchEnd(_ref2) {
    let {
      currentPos
    } = _ref2;
    if (swiper.params.cssMode) return;
    const {
      params,
      wrapperEl,
      rtlTranslate: rtl,
      snapGrid,
      touchEventsData: data
    } = swiper;
    // Time diff
    const touchEndTime = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.d)();
    const timeDiff = touchEndTime - data.touchStartTime;
    if (currentPos < -swiper.minTranslate()) {
      swiper.slideTo(swiper.activeIndex);
      return;
    }
    if (currentPos > -swiper.maxTranslate()) {
      if (swiper.slides.length < snapGrid.length) {
        swiper.slideTo(snapGrid.length - 1);
      } else {
        swiper.slideTo(swiper.slides.length - 1);
      }
      return;
    }
    if (params.freeMode.momentum) {
      if (data.velocities.length > 1) {
        const lastMoveEvent = data.velocities.pop();
        const velocityEvent = data.velocities.pop();
        const distance = lastMoveEvent.position - velocityEvent.position;
        const time = lastMoveEvent.time - velocityEvent.time;
        swiper.velocity = distance / time;
        swiper.velocity /= 2;
        if (Math.abs(swiper.velocity) < params.freeMode.minimumVelocity) {
          swiper.velocity = 0;
        }
        // this implies that the user stopped moving a finger then released.
        // There would be no events with distance zero, so the last event is stale.
        if (time > 150 || (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.d)() - lastMoveEvent.time > 300) {
          swiper.velocity = 0;
        }
      } else {
        swiper.velocity = 0;
      }
      swiper.velocity *= params.freeMode.momentumVelocityRatio;
      data.velocities.length = 0;
      let momentumDuration = 1000 * params.freeMode.momentumRatio;
      const momentumDistance = swiper.velocity * momentumDuration;
      let newPosition = swiper.translate + momentumDistance;
      if (rtl) newPosition = -newPosition;
      let doBounce = false;
      let afterBouncePosition;
      const bounceAmount = Math.abs(swiper.velocity) * 20 * params.freeMode.momentumBounceRatio;
      let needsLoopFix;
      if (newPosition < swiper.maxTranslate()) {
        if (params.freeMode.momentumBounce) {
          if (newPosition + swiper.maxTranslate() < -bounceAmount) {
            newPosition = swiper.maxTranslate() - bounceAmount;
          }
          afterBouncePosition = swiper.maxTranslate();
          doBounce = true;
          data.allowMomentumBounce = true;
        } else {
          newPosition = swiper.maxTranslate();
        }
        if (params.loop && params.centeredSlides) needsLoopFix = true;
      } else if (newPosition > swiper.minTranslate()) {
        if (params.freeMode.momentumBounce) {
          if (newPosition - swiper.minTranslate() > bounceAmount) {
            newPosition = swiper.minTranslate() + bounceAmount;
          }
          afterBouncePosition = swiper.minTranslate();
          doBounce = true;
          data.allowMomentumBounce = true;
        } else {
          newPosition = swiper.minTranslate();
        }
        if (params.loop && params.centeredSlides) needsLoopFix = true;
      } else if (params.freeMode.sticky) {
        let nextSlide;
        for (let j = 0; j < snapGrid.length; j += 1) {
          if (snapGrid[j] > -newPosition) {
            nextSlide = j;
            break;
          }
        }
        if (Math.abs(snapGrid[nextSlide] - newPosition) < Math.abs(snapGrid[nextSlide - 1] - newPosition) || swiper.swipeDirection === 'next') {
          newPosition = snapGrid[nextSlide];
        } else {
          newPosition = snapGrid[nextSlide - 1];
        }
        newPosition = -newPosition;
      }
      if (needsLoopFix) {
        once('transitionEnd', () => {
          swiper.loopFix();
        });
      }
      // Fix duration
      if (swiper.velocity !== 0) {
        if (rtl) {
          momentumDuration = Math.abs((-newPosition - swiper.translate) / swiper.velocity);
        } else {
          momentumDuration = Math.abs((newPosition - swiper.translate) / swiper.velocity);
        }
        if (params.freeMode.sticky) {
          // If freeMode.sticky is active and the user ends a swipe with a slow-velocity
          // event, then durations can be 20+ seconds to slide one (or zero!) slides.
          // It's easy to see this when simulating touch with mouse events. To fix this,
          // limit single-slide swipes to the default slide duration. This also has the
          // nice side effect of matching slide speed if the user stopped moving before
          // lifting finger or mouse vs. moving slowly before lifting the finger/mouse.
          // For faster swipes, also apply limits (albeit higher ones).
          const moveDistance = Math.abs((rtl ? -newPosition : newPosition) - swiper.translate);
          const currentSlideSize = swiper.slidesSizesGrid[swiper.activeIndex];
          if (moveDistance < currentSlideSize) {
            momentumDuration = params.speed;
          } else if (moveDistance < 2 * currentSlideSize) {
            momentumDuration = params.speed * 1.5;
          } else {
            momentumDuration = params.speed * 2.5;
          }
        }
      } else if (params.freeMode.sticky) {
        swiper.slideToClosest();
        return;
      }
      if (params.freeMode.momentumBounce && doBounce) {
        swiper.updateProgress(afterBouncePosition);
        swiper.setTransition(momentumDuration);
        swiper.setTranslate(newPosition);
        swiper.transitionStart(true, swiper.swipeDirection);
        swiper.animating = true;
        (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.k)(wrapperEl, () => {
          if (!swiper || swiper.destroyed || !data.allowMomentumBounce) return;
          emit('momentumBounce');
          swiper.setTransition(params.speed);
          setTimeout(() => {
            swiper.setTranslate(afterBouncePosition);
            (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.k)(wrapperEl, () => {
              if (!swiper || swiper.destroyed) return;
              swiper.transitionEnd();
            });
          }, 0);
        });
      } else if (swiper.velocity) {
        emit('_freeModeNoMomentumRelease');
        swiper.updateProgress(newPosition);
        swiper.setTransition(momentumDuration);
        swiper.setTranslate(newPosition);
        swiper.transitionStart(true, swiper.swipeDirection);
        if (!swiper.animating) {
          swiper.animating = true;
          (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.k)(wrapperEl, () => {
            if (!swiper || swiper.destroyed) return;
            swiper.transitionEnd();
          });
        }
      } else {
        swiper.updateProgress(newPosition);
      }
      swiper.updateActiveIndex();
      swiper.updateSlidesClasses();
    } else if (params.freeMode.sticky) {
      swiper.slideToClosest();
      return;
    } else if (params.freeMode) {
      emit('_freeModeNoMomentumRelease');
    }
    if (!params.freeMode.momentum || timeDiff >= params.longSwipesMs) {
      emit('_freeModeStaticRelease');
      swiper.updateProgress();
      swiper.updateActiveIndex();
      swiper.updateSlidesClasses();
    }
  }
  Object.assign(swiper, {
    freeMode: {
      onTouchStart,
      onTouchMove,
      onTouchEnd
    }
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/grid.mjs":
/*!***********************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/grid.mjs ***!
  \***********************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ Grid)
/* harmony export */ });
function Grid(_ref) {
  let {
    swiper,
    extendParams,
    on
  } = _ref;
  extendParams({
    grid: {
      rows: 1,
      fill: 'column'
    }
  });
  let slidesNumberEvenToRows;
  let slidesPerRow;
  let numFullColumns;
  let wasMultiRow;
  const getSpaceBetween = () => {
    let spaceBetween = swiper.params.spaceBetween;
    if (typeof spaceBetween === 'string' && spaceBetween.indexOf('%') >= 0) {
      spaceBetween = parseFloat(spaceBetween.replace('%', '')) / 100 * swiper.size;
    } else if (typeof spaceBetween === 'string') {
      spaceBetween = parseFloat(spaceBetween);
    }
    return spaceBetween;
  };
  const initSlides = slides => {
    const {
      slidesPerView
    } = swiper.params;
    const {
      rows,
      fill
    } = swiper.params.grid;
    const slidesLength = swiper.virtual && swiper.params.virtual.enabled ? swiper.virtual.slides.length : slides.length;
    numFullColumns = Math.floor(slidesLength / rows);
    if (Math.floor(slidesLength / rows) === slidesLength / rows) {
      slidesNumberEvenToRows = slidesLength;
    } else {
      slidesNumberEvenToRows = Math.ceil(slidesLength / rows) * rows;
    }
    if (slidesPerView !== 'auto' && fill === 'row') {
      slidesNumberEvenToRows = Math.max(slidesNumberEvenToRows, slidesPerView * rows);
    }
    slidesPerRow = slidesNumberEvenToRows / rows;
  };
  const unsetSlides = () => {
    if (swiper.slides) {
      swiper.slides.forEach(slide => {
        if (slide.swiperSlideGridSet) {
          slide.style.height = '';
          slide.style[swiper.getDirectionLabel('margin-top')] = '';
        }
      });
    }
  };
  const updateSlide = (i, slide, slides) => {
    const {
      slidesPerGroup
    } = swiper.params;
    const spaceBetween = getSpaceBetween();
    const {
      rows,
      fill
    } = swiper.params.grid;
    const slidesLength = swiper.virtual && swiper.params.virtual.enabled ? swiper.virtual.slides.length : slides.length;
    // Set slides order
    let newSlideOrderIndex;
    let column;
    let row;
    if (fill === 'row' && slidesPerGroup > 1) {
      const groupIndex = Math.floor(i / (slidesPerGroup * rows));
      const slideIndexInGroup = i - rows * slidesPerGroup * groupIndex;
      const columnsInGroup = groupIndex === 0 ? slidesPerGroup : Math.min(Math.ceil((slidesLength - groupIndex * rows * slidesPerGroup) / rows), slidesPerGroup);
      row = Math.floor(slideIndexInGroup / columnsInGroup);
      column = slideIndexInGroup - row * columnsInGroup + groupIndex * slidesPerGroup;
      newSlideOrderIndex = column + row * slidesNumberEvenToRows / rows;
      slide.style.order = newSlideOrderIndex;
    } else if (fill === 'column') {
      column = Math.floor(i / rows);
      row = i - column * rows;
      if (column > numFullColumns || column === numFullColumns && row === rows - 1) {
        row += 1;
        if (row >= rows) {
          row = 0;
          column += 1;
        }
      }
    } else {
      row = Math.floor(i / slidesPerRow);
      column = i - row * slidesPerRow;
    }
    slide.row = row;
    slide.column = column;
    slide.style.height = `calc((100% - ${(rows - 1) * spaceBetween}px) / ${rows})`;
    slide.style[swiper.getDirectionLabel('margin-top')] = row !== 0 ? spaceBetween && `${spaceBetween}px` : '';
    slide.swiperSlideGridSet = true;
  };
  const updateWrapperSize = (slideSize, snapGrid) => {
    const {
      centeredSlides,
      roundLengths
    } = swiper.params;
    const spaceBetween = getSpaceBetween();
    const {
      rows
    } = swiper.params.grid;
    swiper.virtualSize = (slideSize + spaceBetween) * slidesNumberEvenToRows;
    swiper.virtualSize = Math.ceil(swiper.virtualSize / rows) - spaceBetween;
    if (!swiper.params.cssMode) {
      swiper.wrapperEl.style[swiper.getDirectionLabel('width')] = `${swiper.virtualSize + spaceBetween}px`;
    }
    if (centeredSlides) {
      const newSlidesGrid = [];
      for (let i = 0; i < snapGrid.length; i += 1) {
        let slidesGridItem = snapGrid[i];
        if (roundLengths) slidesGridItem = Math.floor(slidesGridItem);
        if (snapGrid[i] < swiper.virtualSize + snapGrid[0]) newSlidesGrid.push(slidesGridItem);
      }
      snapGrid.splice(0, snapGrid.length);
      snapGrid.push(...newSlidesGrid);
    }
  };
  const onInit = () => {
    wasMultiRow = swiper.params.grid && swiper.params.grid.rows > 1;
  };
  const onUpdate = () => {
    const {
      params,
      el
    } = swiper;
    const isMultiRow = params.grid && params.grid.rows > 1;
    if (wasMultiRow && !isMultiRow) {
      el.classList.remove(`${params.containerModifierClass}grid`, `${params.containerModifierClass}grid-column`);
      numFullColumns = 1;
      swiper.emitContainerClasses();
    } else if (!wasMultiRow && isMultiRow) {
      el.classList.add(`${params.containerModifierClass}grid`);
      if (params.grid.fill === 'column') {
        el.classList.add(`${params.containerModifierClass}grid-column`);
      }
      swiper.emitContainerClasses();
    }
    wasMultiRow = isMultiRow;
  };
  on('init', onInit);
  on('update', onUpdate);
  swiper.grid = {
    initSlides,
    unsetSlides,
    updateSlide,
    updateWrapperSize
  };
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/hash-navigation.mjs":
/*!**********************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/hash-navigation.mjs ***!
  \**********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ HashNavigation)
/* harmony export */ });
/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ "../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");



function HashNavigation(_ref) {
  let {
    swiper,
    extendParams,
    emit,
    on
  } = _ref;
  let initialized = false;
  const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
  const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  extendParams({
    hashNavigation: {
      enabled: false,
      replaceState: false,
      watchState: false,
      getSlideIndex(_s, hash) {
        if (swiper.virtual && swiper.params.virtual.enabled) {
          const slideWithHash = swiper.slides.find(slideEl => slideEl.getAttribute('data-hash') === hash);
          if (!slideWithHash) return 0;
          const index = parseInt(slideWithHash.getAttribute('data-swiper-slide-index'), 10);
          return index;
        }
        return swiper.getSlideIndex((0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(swiper.slidesEl, `.${swiper.params.slideClass}[data-hash="${hash}"], swiper-slide[data-hash="${hash}"]`)[0]);
      }
    }
  });
  const onHashChange = () => {
    emit('hashChange');
    const newHash = document.location.hash.replace('#', '');
    const activeSlideEl = swiper.virtual && swiper.params.virtual.enabled ? swiper.slidesEl.querySelector(`[data-swiper-slide-index="${swiper.activeIndex}"]`) : swiper.slides[swiper.activeIndex];
    const activeSlideHash = activeSlideEl ? activeSlideEl.getAttribute('data-hash') : '';
    if (newHash !== activeSlideHash) {
      const newIndex = swiper.params.hashNavigation.getSlideIndex(swiper, newHash);
      if (typeof newIndex === 'undefined' || Number.isNaN(newIndex)) return;
      swiper.slideTo(newIndex);
    }
  };
  const setHash = () => {
    if (!initialized || !swiper.params.hashNavigation.enabled) return;
    const activeSlideEl = swiper.virtual && swiper.params.virtual.enabled ? swiper.slidesEl.querySelector(`[data-swiper-slide-index="${swiper.activeIndex}"]`) : swiper.slides[swiper.activeIndex];
    const activeSlideHash = activeSlideEl ? activeSlideEl.getAttribute('data-hash') || activeSlideEl.getAttribute('data-history') : '';
    if (swiper.params.hashNavigation.replaceState && window.history && window.history.replaceState) {
      window.history.replaceState(null, null, `#${activeSlideHash}` || '');
      emit('hashSet');
    } else {
      document.location.hash = activeSlideHash || '';
      emit('hashSet');
    }
  };
  const init = () => {
    if (!swiper.params.hashNavigation.enabled || swiper.params.history && swiper.params.history.enabled) return;
    initialized = true;
    const hash = document.location.hash.replace('#', '');
    if (hash) {
      const speed = 0;
      const index = swiper.params.hashNavigation.getSlideIndex(swiper, hash);
      swiper.slideTo(index || 0, speed, swiper.params.runCallbacksOnInit, true);
    }
    if (swiper.params.hashNavigation.watchState) {
      window.addEventListener('hashchange', onHashChange);
    }
  };
  const destroy = () => {
    if (swiper.params.hashNavigation.watchState) {
      window.removeEventListener('hashchange', onHashChange);
    }
  };
  on('init', () => {
    if (swiper.params.hashNavigation.enabled) {
      init();
    }
  });
  on('destroy', () => {
    if (swiper.params.hashNavigation.enabled) {
      destroy();
    }
  });
  on('transitionEnd _freeModeNoMomentumRelease', () => {
    if (initialized) {
      setHash();
    }
  });
  on('slideChange', () => {
    if (initialized && swiper.params.cssMode) {
      setHash();
    }
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/history.mjs":
/*!**************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/history.mjs ***!
  \**************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ History)
/* harmony export */ });
/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ "../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs");


function History(_ref) {
  let {
    swiper,
    extendParams,
    on
  } = _ref;
  extendParams({
    history: {
      enabled: false,
      root: '',
      replaceState: false,
      key: 'slides',
      keepQuery: false
    }
  });
  let initialized = false;
  let paths = {};
  const slugify = text => {
    return text.toString().replace(/\s+/g, '-').replace(/[^\w-]+/g, '').replace(/--+/g, '-').replace(/^-+/, '').replace(/-+$/, '');
  };
  const getPathValues = urlOverride => {
    const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
    let location;
    if (urlOverride) {
      location = new URL(urlOverride);
    } else {
      location = window.location;
    }
    const pathArray = location.pathname.slice(1).split('/').filter(part => part !== '');
    const total = pathArray.length;
    const key = pathArray[total - 2];
    const value = pathArray[total - 1];
    return {
      key,
      value
    };
  };
  const setHistory = (key, index) => {
    const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
    if (!initialized || !swiper.params.history.enabled) return;
    let location;
    if (swiper.params.url) {
      location = new URL(swiper.params.url);
    } else {
      location = window.location;
    }
    const slide = swiper.virtual && swiper.params.virtual.enabled ? swiper.slidesEl.querySelector(`[data-swiper-slide-index="${index}"]`) : swiper.slides[index];
    let value = slugify(slide.getAttribute('data-history'));
    if (swiper.params.history.root.length > 0) {
      let root = swiper.params.history.root;
      if (root[root.length - 1] === '/') root = root.slice(0, root.length - 1);
      value = `${root}/${key ? `${key}/` : ''}${value}`;
    } else if (!location.pathname.includes(key)) {
      value = `${key ? `${key}/` : ''}${value}`;
    }
    if (swiper.params.history.keepQuery) {
      value += location.search;
    }
    const currentState = window.history.state;
    if (currentState && currentState.value === value) {
      return;
    }
    if (swiper.params.history.replaceState) {
      window.history.replaceState({
        value
      }, null, value);
    } else {
      window.history.pushState({
        value
      }, null, value);
    }
  };
  const scrollToSlide = (speed, value, runCallbacks) => {
    if (value) {
      for (let i = 0, length = swiper.slides.length; i < length; i += 1) {
        const slide = swiper.slides[i];
        const slideHistory = slugify(slide.getAttribute('data-history'));
        if (slideHistory === value) {
          const index = swiper.getSlideIndex(slide);
          swiper.slideTo(index, speed, runCallbacks);
        }
      }
    } else {
      swiper.slideTo(0, speed, runCallbacks);
    }
  };
  const setHistoryPopState = () => {
    paths = getPathValues(swiper.params.url);
    scrollToSlide(swiper.params.speed, paths.value, false);
  };
  const init = () => {
    const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
    if (!swiper.params.history) return;
    if (!window.history || !window.history.pushState) {
      swiper.params.history.enabled = false;
      swiper.params.hashNavigation.enabled = true;
      return;
    }
    initialized = true;
    paths = getPathValues(swiper.params.url);
    if (!paths.key && !paths.value) {
      if (!swiper.params.history.replaceState) {
        window.addEventListener('popstate', setHistoryPopState);
      }
      return;
    }
    scrollToSlide(0, paths.value, swiper.params.runCallbacksOnInit);
    if (!swiper.params.history.replaceState) {
      window.addEventListener('popstate', setHistoryPopState);
    }
  };
  const destroy = () => {
    const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
    if (!swiper.params.history.replaceState) {
      window.removeEventListener('popstate', setHistoryPopState);
    }
  };
  on('init', () => {
    if (swiper.params.history.enabled) {
      init();
    }
  });
  on('destroy', () => {
    if (swiper.params.history.enabled) {
      destroy();
    }
  });
  on('transitionEnd _freeModeNoMomentumRelease', () => {
    if (initialized) {
      setHistory(swiper.params.history.key, swiper.activeIndex);
    }
  });
  on('slideChange', () => {
    if (initialized && swiper.params.cssMode) {
      setHistory(swiper.params.history.key, swiper.activeIndex);
    }
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/index.mjs":
/*!************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/index.mjs ***!
  \************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A11y: () => (/* reexport safe */ _a11y_mjs__WEBPACK_IMPORTED_MODULE_9__["default"]),
/* harmony export */   Autoplay: () => (/* reexport safe */ _autoplay_mjs__WEBPACK_IMPORTED_MODULE_12__["default"]),
/* harmony export */   Controller: () => (/* reexport safe */ _controller_mjs__WEBPACK_IMPORTED_MODULE_8__["default"]),
/* harmony export */   EffectCards: () => (/* reexport safe */ _effect_cards_mjs__WEBPACK_IMPORTED_MODULE_22__["default"]),
/* harmony export */   EffectCoverflow: () => (/* reexport safe */ _effect_coverflow_mjs__WEBPACK_IMPORTED_MODULE_20__["default"]),
/* harmony export */   EffectCreative: () => (/* reexport safe */ _effect_creative_mjs__WEBPACK_IMPORTED_MODULE_21__["default"]),
/* harmony export */   EffectCube: () => (/* reexport safe */ _effect_cube_mjs__WEBPACK_IMPORTED_MODULE_18__["default"]),
/* harmony export */   EffectFade: () => (/* reexport safe */ _effect_fade_mjs__WEBPACK_IMPORTED_MODULE_17__["default"]),
/* harmony export */   EffectFlip: () => (/* reexport safe */ _effect_flip_mjs__WEBPACK_IMPORTED_MODULE_19__["default"]),
/* harmony export */   FreeMode: () => (/* reexport safe */ _free_mode_mjs__WEBPACK_IMPORTED_MODULE_14__["default"]),
/* harmony export */   Grid: () => (/* reexport safe */ _grid_mjs__WEBPACK_IMPORTED_MODULE_15__["default"]),
/* harmony export */   HashNavigation: () => (/* reexport safe */ _hash_navigation_mjs__WEBPACK_IMPORTED_MODULE_11__["default"]),
/* harmony export */   History: () => (/* reexport safe */ _history_mjs__WEBPACK_IMPORTED_MODULE_10__["default"]),
/* harmony export */   Keyboard: () => (/* reexport safe */ _keyboard_mjs__WEBPACK_IMPORTED_MODULE_1__["default"]),
/* harmony export */   Manipulation: () => (/* reexport safe */ _manipulation_mjs__WEBPACK_IMPORTED_MODULE_16__["default"]),
/* harmony export */   Mousewheel: () => (/* reexport safe */ _mousewheel_mjs__WEBPACK_IMPORTED_MODULE_2__["default"]),
/* harmony export */   Navigation: () => (/* reexport safe */ _navigation_mjs__WEBPACK_IMPORTED_MODULE_3__["default"]),
/* harmony export */   Pagination: () => (/* reexport safe */ _pagination_mjs__WEBPACK_IMPORTED_MODULE_4__["default"]),
/* harmony export */   Parallax: () => (/* reexport safe */ _parallax_mjs__WEBPACK_IMPORTED_MODULE_6__["default"]),
/* harmony export */   Scrollbar: () => (/* reexport safe */ _scrollbar_mjs__WEBPACK_IMPORTED_MODULE_5__["default"]),
/* harmony export */   Thumbs: () => (/* reexport safe */ _thumbs_mjs__WEBPACK_IMPORTED_MODULE_13__["default"]),
/* harmony export */   Virtual: () => (/* reexport safe */ _virtual_mjs__WEBPACK_IMPORTED_MODULE_0__["default"]),
/* harmony export */   Zoom: () => (/* reexport safe */ _zoom_mjs__WEBPACK_IMPORTED_MODULE_7__["default"])
/* harmony export */ });
/* harmony import */ var _virtual_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./virtual.mjs */ "../plugin-slug/node_modules/swiper/modules/virtual.mjs");
/* harmony import */ var _keyboard_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./keyboard.mjs */ "../plugin-slug/node_modules/swiper/modules/keyboard.mjs");
/* harmony import */ var _mousewheel_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./mousewheel.mjs */ "../plugin-slug/node_modules/swiper/modules/mousewheel.mjs");
/* harmony import */ var _navigation_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./navigation.mjs */ "../plugin-slug/node_modules/swiper/modules/navigation.mjs");
/* harmony import */ var _pagination_mjs__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./pagination.mjs */ "../plugin-slug/node_modules/swiper/modules/pagination.mjs");
/* harmony import */ var _scrollbar_mjs__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./scrollbar.mjs */ "../plugin-slug/node_modules/swiper/modules/scrollbar.mjs");
/* harmony import */ var _parallax_mjs__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./parallax.mjs */ "../plugin-slug/node_modules/swiper/modules/parallax.mjs");
/* harmony import */ var _zoom_mjs__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./zoom.mjs */ "../plugin-slug/node_modules/swiper/modules/zoom.mjs");
/* harmony import */ var _controller_mjs__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./controller.mjs */ "../plugin-slug/node_modules/swiper/modules/controller.mjs");
/* harmony import */ var _a11y_mjs__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./a11y.mjs */ "../plugin-slug/node_modules/swiper/modules/a11y.mjs");
/* harmony import */ var _history_mjs__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./history.mjs */ "../plugin-slug/node_modules/swiper/modules/history.mjs");
/* harmony import */ var _hash_navigation_mjs__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./hash-navigation.mjs */ "../plugin-slug/node_modules/swiper/modules/hash-navigation.mjs");
/* harmony import */ var _autoplay_mjs__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./autoplay.mjs */ "../plugin-slug/node_modules/swiper/modules/autoplay.mjs");
/* harmony import */ var _thumbs_mjs__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./thumbs.mjs */ "../plugin-slug/node_modules/swiper/modules/thumbs.mjs");
/* harmony import */ var _free_mode_mjs__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ./free-mode.mjs */ "../plugin-slug/node_modules/swiper/modules/free-mode.mjs");
/* harmony import */ var _grid_mjs__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ./grid.mjs */ "../plugin-slug/node_modules/swiper/modules/grid.mjs");
/* harmony import */ var _manipulation_mjs__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ./manipulation.mjs */ "../plugin-slug/node_modules/swiper/modules/manipulation.mjs");
/* harmony import */ var _effect_fade_mjs__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ./effect-fade.mjs */ "../plugin-slug/node_modules/swiper/modules/effect-fade.mjs");
/* harmony import */ var _effect_cube_mjs__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ./effect-cube.mjs */ "../plugin-slug/node_modules/swiper/modules/effect-cube.mjs");
/* harmony import */ var _effect_flip_mjs__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! ./effect-flip.mjs */ "../plugin-slug/node_modules/swiper/modules/effect-flip.mjs");
/* harmony import */ var _effect_coverflow_mjs__WEBPACK_IMPORTED_MODULE_20__ = __webpack_require__(/*! ./effect-coverflow.mjs */ "../plugin-slug/node_modules/swiper/modules/effect-coverflow.mjs");
/* harmony import */ var _effect_creative_mjs__WEBPACK_IMPORTED_MODULE_21__ = __webpack_require__(/*! ./effect-creative.mjs */ "../plugin-slug/node_modules/swiper/modules/effect-creative.mjs");
/* harmony import */ var _effect_cards_mjs__WEBPACK_IMPORTED_MODULE_22__ = __webpack_require__(/*! ./effect-cards.mjs */ "../plugin-slug/node_modules/swiper/modules/effect-cards.mjs");
























/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/keyboard.mjs":
/*!***************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/keyboard.mjs ***!
  \***************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ Keyboard)
/* harmony export */ });
/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ "../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");



/* eslint-disable consistent-return */
function Keyboard(_ref) {
  let {
    swiper,
    extendParams,
    on,
    emit
  } = _ref;
  const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
  const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  swiper.keyboard = {
    enabled: false
  };
  extendParams({
    keyboard: {
      enabled: false,
      onlyInViewport: true,
      pageUpDown: true
    }
  });
  function handle(event) {
    if (!swiper.enabled) return;
    const {
      rtlTranslate: rtl
    } = swiper;
    let e = event;
    if (e.originalEvent) e = e.originalEvent; // jquery fix
    const kc = e.keyCode || e.charCode;
    const pageUpDown = swiper.params.keyboard.pageUpDown;
    const isPageUp = pageUpDown && kc === 33;
    const isPageDown = pageUpDown && kc === 34;
    const isArrowLeft = kc === 37;
    const isArrowRight = kc === 39;
    const isArrowUp = kc === 38;
    const isArrowDown = kc === 40;
    // Directions locks
    if (!swiper.allowSlideNext && (swiper.isHorizontal() && isArrowRight || swiper.isVertical() && isArrowDown || isPageDown)) {
      return false;
    }
    if (!swiper.allowSlidePrev && (swiper.isHorizontal() && isArrowLeft || swiper.isVertical() && isArrowUp || isPageUp)) {
      return false;
    }
    if (e.shiftKey || e.altKey || e.ctrlKey || e.metaKey) {
      return undefined;
    }
    if (document.activeElement && document.activeElement.nodeName && (document.activeElement.nodeName.toLowerCase() === 'input' || document.activeElement.nodeName.toLowerCase() === 'textarea')) {
      return undefined;
    }
    if (swiper.params.keyboard.onlyInViewport && (isPageUp || isPageDown || isArrowLeft || isArrowRight || isArrowUp || isArrowDown)) {
      let inView = false;
      // Check that swiper should be inside of visible area of window
      if ((0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.a)(swiper.el, `.${swiper.params.slideClass}, swiper-slide`).length > 0 && (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.a)(swiper.el, `.${swiper.params.slideActiveClass}`).length === 0) {
        return undefined;
      }
      const el = swiper.el;
      const swiperWidth = el.clientWidth;
      const swiperHeight = el.clientHeight;
      const windowWidth = window.innerWidth;
      const windowHeight = window.innerHeight;
      const swiperOffset = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.b)(el);
      if (rtl) swiperOffset.left -= el.scrollLeft;
      const swiperCoord = [[swiperOffset.left, swiperOffset.top], [swiperOffset.left + swiperWidth, swiperOffset.top], [swiperOffset.left, swiperOffset.top + swiperHeight], [swiperOffset.left + swiperWidth, swiperOffset.top + swiperHeight]];
      for (let i = 0; i < swiperCoord.length; i += 1) {
        const point = swiperCoord[i];
        if (point[0] >= 0 && point[0] <= windowWidth && point[1] >= 0 && point[1] <= windowHeight) {
          if (point[0] === 0 && point[1] === 0) continue; // eslint-disable-line
          inView = true;
        }
      }
      if (!inView) return undefined;
    }
    if (swiper.isHorizontal()) {
      if (isPageUp || isPageDown || isArrowLeft || isArrowRight) {
        if (e.preventDefault) e.preventDefault();else e.returnValue = false;
      }
      if ((isPageDown || isArrowRight) && !rtl || (isPageUp || isArrowLeft) && rtl) swiper.slideNext();
      if ((isPageUp || isArrowLeft) && !rtl || (isPageDown || isArrowRight) && rtl) swiper.slidePrev();
    } else {
      if (isPageUp || isPageDown || isArrowUp || isArrowDown) {
        if (e.preventDefault) e.preventDefault();else e.returnValue = false;
      }
      if (isPageDown || isArrowDown) swiper.slideNext();
      if (isPageUp || isArrowUp) swiper.slidePrev();
    }
    emit('keyPress', kc);
    return undefined;
  }
  function enable() {
    if (swiper.keyboard.enabled) return;
    document.addEventListener('keydown', handle);
    swiper.keyboard.enabled = true;
  }
  function disable() {
    if (!swiper.keyboard.enabled) return;
    document.removeEventListener('keydown', handle);
    swiper.keyboard.enabled = false;
  }
  on('init', () => {
    if (swiper.params.keyboard.enabled) {
      enable();
    }
  });
  on('destroy', () => {
    if (swiper.keyboard.enabled) {
      disable();
    }
  });
  Object.assign(swiper.keyboard, {
    enable,
    disable
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/manipulation.mjs":
/*!*******************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/manipulation.mjs ***!
  \*******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ Manipulation)
/* harmony export */ });
function appendSlide(slides) {
  const swiper = this;
  const {
    params,
    slidesEl
  } = swiper;
  if (params.loop) {
    swiper.loopDestroy();
  }
  const appendElement = slideEl => {
    if (typeof slideEl === 'string') {
      const tempDOM = document.createElement('div');
      tempDOM.innerHTML = slideEl;
      slidesEl.append(tempDOM.children[0]);
      tempDOM.innerHTML = '';
    } else {
      slidesEl.append(slideEl);
    }
  };
  if (typeof slides === 'object' && 'length' in slides) {
    for (let i = 0; i < slides.length; i += 1) {
      if (slides[i]) appendElement(slides[i]);
    }
  } else {
    appendElement(slides);
  }
  swiper.recalcSlides();
  if (params.loop) {
    swiper.loopCreate();
  }
  if (!params.observer || swiper.isElement) {
    swiper.update();
  }
}

function prependSlide(slides) {
  const swiper = this;
  const {
    params,
    activeIndex,
    slidesEl
  } = swiper;
  if (params.loop) {
    swiper.loopDestroy();
  }
  let newActiveIndex = activeIndex + 1;
  const prependElement = slideEl => {
    if (typeof slideEl === 'string') {
      const tempDOM = document.createElement('div');
      tempDOM.innerHTML = slideEl;
      slidesEl.prepend(tempDOM.children[0]);
      tempDOM.innerHTML = '';
    } else {
      slidesEl.prepend(slideEl);
    }
  };
  if (typeof slides === 'object' && 'length' in slides) {
    for (let i = 0; i < slides.length; i += 1) {
      if (slides[i]) prependElement(slides[i]);
    }
    newActiveIndex = activeIndex + slides.length;
  } else {
    prependElement(slides);
  }
  swiper.recalcSlides();
  if (params.loop) {
    swiper.loopCreate();
  }
  if (!params.observer || swiper.isElement) {
    swiper.update();
  }
  swiper.slideTo(newActiveIndex, 0, false);
}

function addSlide(index, slides) {
  const swiper = this;
  const {
    params,
    activeIndex,
    slidesEl
  } = swiper;
  let activeIndexBuffer = activeIndex;
  if (params.loop) {
    activeIndexBuffer -= swiper.loopedSlides;
    swiper.loopDestroy();
    swiper.recalcSlides();
  }
  const baseLength = swiper.slides.length;
  if (index <= 0) {
    swiper.prependSlide(slides);
    return;
  }
  if (index >= baseLength) {
    swiper.appendSlide(slides);
    return;
  }
  let newActiveIndex = activeIndexBuffer > index ? activeIndexBuffer + 1 : activeIndexBuffer;
  const slidesBuffer = [];
  for (let i = baseLength - 1; i >= index; i -= 1) {
    const currentSlide = swiper.slides[i];
    currentSlide.remove();
    slidesBuffer.unshift(currentSlide);
  }
  if (typeof slides === 'object' && 'length' in slides) {
    for (let i = 0; i < slides.length; i += 1) {
      if (slides[i]) slidesEl.append(slides[i]);
    }
    newActiveIndex = activeIndexBuffer > index ? activeIndexBuffer + slides.length : activeIndexBuffer;
  } else {
    slidesEl.append(slides);
  }
  for (let i = 0; i < slidesBuffer.length; i += 1) {
    slidesEl.append(slidesBuffer[i]);
  }
  swiper.recalcSlides();
  if (params.loop) {
    swiper.loopCreate();
  }
  if (!params.observer || swiper.isElement) {
    swiper.update();
  }
  if (params.loop) {
    swiper.slideTo(newActiveIndex + swiper.loopedSlides, 0, false);
  } else {
    swiper.slideTo(newActiveIndex, 0, false);
  }
}

function removeSlide(slidesIndexes) {
  const swiper = this;
  const {
    params,
    activeIndex
  } = swiper;
  let activeIndexBuffer = activeIndex;
  if (params.loop) {
    activeIndexBuffer -= swiper.loopedSlides;
    swiper.loopDestroy();
  }
  let newActiveIndex = activeIndexBuffer;
  let indexToRemove;
  if (typeof slidesIndexes === 'object' && 'length' in slidesIndexes) {
    for (let i = 0; i < slidesIndexes.length; i += 1) {
      indexToRemove = slidesIndexes[i];
      if (swiper.slides[indexToRemove]) swiper.slides[indexToRemove].remove();
      if (indexToRemove < newActiveIndex) newActiveIndex -= 1;
    }
    newActiveIndex = Math.max(newActiveIndex, 0);
  } else {
    indexToRemove = slidesIndexes;
    if (swiper.slides[indexToRemove]) swiper.slides[indexToRemove].remove();
    if (indexToRemove < newActiveIndex) newActiveIndex -= 1;
    newActiveIndex = Math.max(newActiveIndex, 0);
  }
  swiper.recalcSlides();
  if (params.loop) {
    swiper.loopCreate();
  }
  if (!params.observer || swiper.isElement) {
    swiper.update();
  }
  if (params.loop) {
    swiper.slideTo(newActiveIndex + swiper.loopedSlides, 0, false);
  } else {
    swiper.slideTo(newActiveIndex, 0, false);
  }
}

function removeAllSlides() {
  const swiper = this;
  const slidesIndexes = [];
  for (let i = 0; i < swiper.slides.length; i += 1) {
    slidesIndexes.push(i);
  }
  swiper.removeSlide(slidesIndexes);
}

function Manipulation(_ref) {
  let {
    swiper
  } = _ref;
  Object.assign(swiper, {
    appendSlide: appendSlide.bind(swiper),
    prependSlide: prependSlide.bind(swiper),
    addSlide: addSlide.bind(swiper),
    removeSlide: removeSlide.bind(swiper),
    removeAllSlides: removeAllSlides.bind(swiper)
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/mousewheel.mjs":
/*!*****************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/mousewheel.mjs ***!
  \*****************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ Mousewheel)
/* harmony export */ });
/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ "../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");



/* eslint-disable consistent-return */
function Mousewheel(_ref) {
  let {
    swiper,
    extendParams,
    on,
    emit
  } = _ref;
  const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  extendParams({
    mousewheel: {
      enabled: false,
      releaseOnEdges: false,
      invert: false,
      forceToAxis: false,
      sensitivity: 1,
      eventsTarget: 'container',
      thresholdDelta: null,
      thresholdTime: null,
      noMousewheelClass: 'swiper-no-mousewheel'
    }
  });
  swiper.mousewheel = {
    enabled: false
  };
  let timeout;
  let lastScrollTime = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)();
  let lastEventBeforeSnap;
  const recentWheelEvents = [];
  function normalize(e) {
    // Reasonable defaults
    const PIXEL_STEP = 10;
    const LINE_HEIGHT = 40;
    const PAGE_HEIGHT = 800;
    let sX = 0;
    let sY = 0; // spinX, spinY
    let pX = 0;
    let pY = 0; // pixelX, pixelY

    // Legacy
    if ('detail' in e) {
      sY = e.detail;
    }
    if ('wheelDelta' in e) {
      sY = -e.wheelDelta / 120;
    }
    if ('wheelDeltaY' in e) {
      sY = -e.wheelDeltaY / 120;
    }
    if ('wheelDeltaX' in e) {
      sX = -e.wheelDeltaX / 120;
    }

    // side scrolling on FF with DOMMouseScroll
    if ('axis' in e && e.axis === e.HORIZONTAL_AXIS) {
      sX = sY;
      sY = 0;
    }
    pX = sX * PIXEL_STEP;
    pY = sY * PIXEL_STEP;
    if ('deltaY' in e) {
      pY = e.deltaY;
    }
    if ('deltaX' in e) {
      pX = e.deltaX;
    }
    if (e.shiftKey && !pX) {
      // if user scrolls with shift he wants horizontal scroll
      pX = pY;
      pY = 0;
    }
    if ((pX || pY) && e.deltaMode) {
      if (e.deltaMode === 1) {
        // delta in LINE units
        pX *= LINE_HEIGHT;
        pY *= LINE_HEIGHT;
      } else {
        // delta in PAGE units
        pX *= PAGE_HEIGHT;
        pY *= PAGE_HEIGHT;
      }
    }

    // Fall-back if spin cannot be determined
    if (pX && !sX) {
      sX = pX < 1 ? -1 : 1;
    }
    if (pY && !sY) {
      sY = pY < 1 ? -1 : 1;
    }
    return {
      spinX: sX,
      spinY: sY,
      pixelX: pX,
      pixelY: pY
    };
  }
  function handleMouseEnter() {
    if (!swiper.enabled) return;
    swiper.mouseEntered = true;
  }
  function handleMouseLeave() {
    if (!swiper.enabled) return;
    swiper.mouseEntered = false;
  }
  function animateSlider(newEvent) {
    if (swiper.params.mousewheel.thresholdDelta && newEvent.delta < swiper.params.mousewheel.thresholdDelta) {
      // Prevent if delta of wheel scroll delta is below configured threshold
      return false;
    }
    if (swiper.params.mousewheel.thresholdTime && (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)() - lastScrollTime < swiper.params.mousewheel.thresholdTime) {
      // Prevent if time between scrolls is below configured threshold
      return false;
    }

    // If the movement is NOT big enough and
    // if the last time the user scrolled was too close to the current one (avoid continuously triggering the slider):
    //   Don't go any further (avoid insignificant scroll movement).
    if (newEvent.delta >= 6 && (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)() - lastScrollTime < 60) {
      // Return false as a default
      return true;
    }
    // If user is scrolling towards the end:
    //   If the slider hasn't hit the latest slide or
    //   if the slider is a loop and
    //   if the slider isn't moving right now:
    //     Go to next slide and
    //     emit a scroll event.
    // Else (the user is scrolling towards the beginning) and
    // if the slider hasn't hit the first slide or
    // if the slider is a loop and
    // if the slider isn't moving right now:
    //   Go to prev slide and
    //   emit a scroll event.
    if (newEvent.direction < 0) {
      if ((!swiper.isEnd || swiper.params.loop) && !swiper.animating) {
        swiper.slideNext();
        emit('scroll', newEvent.raw);
      }
    } else if ((!swiper.isBeginning || swiper.params.loop) && !swiper.animating) {
      swiper.slidePrev();
      emit('scroll', newEvent.raw);
    }
    // If you got here is because an animation has been triggered so store the current time
    lastScrollTime = new window.Date().getTime();
    // Return false as a default
    return false;
  }
  function releaseScroll(newEvent) {
    const params = swiper.params.mousewheel;
    if (newEvent.direction < 0) {
      if (swiper.isEnd && !swiper.params.loop && params.releaseOnEdges) {
        // Return true to animate scroll on edges
        return true;
      }
    } else if (swiper.isBeginning && !swiper.params.loop && params.releaseOnEdges) {
      // Return true to animate scroll on edges
      return true;
    }
    return false;
  }
  function handle(event) {
    let e = event;
    let disableParentSwiper = true;
    if (!swiper.enabled) return;

    // Ignore event if the target or its parents have the swiper-no-mousewheel class
    if (event.target.closest(`.${swiper.params.mousewheel.noMousewheelClass}`)) return;
    const params = swiper.params.mousewheel;
    if (swiper.params.cssMode) {
      e.preventDefault();
    }
    let targetEl = swiper.el;
    if (swiper.params.mousewheel.eventsTarget !== 'container') {
      targetEl = document.querySelector(swiper.params.mousewheel.eventsTarget);
    }
    const targetElContainsTarget = targetEl && targetEl.contains(e.target);
    if (!swiper.mouseEntered && !targetElContainsTarget && !params.releaseOnEdges) return true;
    if (e.originalEvent) e = e.originalEvent; // jquery fix
    let delta = 0;
    const rtlFactor = swiper.rtlTranslate ? -1 : 1;
    const data = normalize(e);
    if (params.forceToAxis) {
      if (swiper.isHorizontal()) {
        if (Math.abs(data.pixelX) > Math.abs(data.pixelY)) delta = -data.pixelX * rtlFactor;else return true;
      } else if (Math.abs(data.pixelY) > Math.abs(data.pixelX)) delta = -data.pixelY;else return true;
    } else {
      delta = Math.abs(data.pixelX) > Math.abs(data.pixelY) ? -data.pixelX * rtlFactor : -data.pixelY;
    }
    if (delta === 0) return true;
    if (params.invert) delta = -delta;

    // Get the scroll positions
    let positions = swiper.getTranslate() + delta * params.sensitivity;
    if (positions >= swiper.minTranslate()) positions = swiper.minTranslate();
    if (positions <= swiper.maxTranslate()) positions = swiper.maxTranslate();

    // When loop is true:
    //     the disableParentSwiper will be true.
    // When loop is false:
    //     if the scroll positions is not on edge,
    //     then the disableParentSwiper will be true.
    //     if the scroll on edge positions,
    //     then the disableParentSwiper will be false.
    disableParentSwiper = swiper.params.loop ? true : !(positions === swiper.minTranslate() || positions === swiper.maxTranslate());
    if (disableParentSwiper && swiper.params.nested) e.stopPropagation();
    if (!swiper.params.freeMode || !swiper.params.freeMode.enabled) {
      // Register the new event in a variable which stores the relevant data
      const newEvent = {
        time: (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)(),
        delta: Math.abs(delta),
        direction: Math.sign(delta),
        raw: event
      };

      // Keep the most recent events
      if (recentWheelEvents.length >= 2) {
        recentWheelEvents.shift(); // only store the last N events
      }

      const prevEvent = recentWheelEvents.length ? recentWheelEvents[recentWheelEvents.length - 1] : undefined;
      recentWheelEvents.push(newEvent);

      // If there is at least one previous recorded event:
      //   If direction has changed or
      //   if the scroll is quicker than the previous one:
      //     Animate the slider.
      // Else (this is the first time the wheel is moved):
      //     Animate the slider.
      if (prevEvent) {
        if (newEvent.direction !== prevEvent.direction || newEvent.delta > prevEvent.delta || newEvent.time > prevEvent.time + 150) {
          animateSlider(newEvent);
        }
      } else {
        animateSlider(newEvent);
      }

      // If it's time to release the scroll:
      //   Return now so you don't hit the preventDefault.
      if (releaseScroll(newEvent)) {
        return true;
      }
    } else {
      // Freemode or scrollContainer:

      // If we recently snapped after a momentum scroll, then ignore wheel events
      // to give time for the deceleration to finish. Stop ignoring after 500 msecs
      // or if it's a new scroll (larger delta or inverse sign as last event before
      // an end-of-momentum snap).
      const newEvent = {
        time: (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)(),
        delta: Math.abs(delta),
        direction: Math.sign(delta)
      };
      const ignoreWheelEvents = lastEventBeforeSnap && newEvent.time < lastEventBeforeSnap.time + 500 && newEvent.delta <= lastEventBeforeSnap.delta && newEvent.direction === lastEventBeforeSnap.direction;
      if (!ignoreWheelEvents) {
        lastEventBeforeSnap = undefined;
        let position = swiper.getTranslate() + delta * params.sensitivity;
        const wasBeginning = swiper.isBeginning;
        const wasEnd = swiper.isEnd;
        if (position >= swiper.minTranslate()) position = swiper.minTranslate();
        if (position <= swiper.maxTranslate()) position = swiper.maxTranslate();
        swiper.setTransition(0);
        swiper.setTranslate(position);
        swiper.updateProgress();
        swiper.updateActiveIndex();
        swiper.updateSlidesClasses();
        if (!wasBeginning && swiper.isBeginning || !wasEnd && swiper.isEnd) {
          swiper.updateSlidesClasses();
        }
        if (swiper.params.loop) {
          swiper.loopFix({
            direction: newEvent.direction < 0 ? 'next' : 'prev',
            byMousewheel: true
          });
        }
        if (swiper.params.freeMode.sticky) {
          // When wheel scrolling starts with sticky (aka snap) enabled, then detect
          // the end of a momentum scroll by storing recent (N=15?) wheel events.
          // 1. do all N events have decreasing or same (absolute value) delta?
          // 2. did all N events arrive in the last M (M=500?) msecs?
          // 3. does the earliest event have an (absolute value) delta that's
          //    at least P (P=1?) larger than the most recent event's delta?
          // 4. does the latest event have a delta that's smaller than Q (Q=6?) pixels?
          // If 1-4 are "yes" then we're near the end of a momentum scroll deceleration.
          // Snap immediately and ignore remaining wheel events in this scroll.
          // See comment above for "remaining wheel events in this scroll" determination.
          // If 1-4 aren't satisfied, then wait to snap until 500ms after the last event.
          clearTimeout(timeout);
          timeout = undefined;
          if (recentWheelEvents.length >= 15) {
            recentWheelEvents.shift(); // only store the last N events
          }

          const prevEvent = recentWheelEvents.length ? recentWheelEvents[recentWheelEvents.length - 1] : undefined;
          const firstEvent = recentWheelEvents[0];
          recentWheelEvents.push(newEvent);
          if (prevEvent && (newEvent.delta > prevEvent.delta || newEvent.direction !== prevEvent.direction)) {
            // Increasing or reverse-sign delta means the user started scrolling again. Clear the wheel event log.
            recentWheelEvents.splice(0);
          } else if (recentWheelEvents.length >= 15 && newEvent.time - firstEvent.time < 500 && firstEvent.delta - newEvent.delta >= 1 && newEvent.delta <= 6) {
            // We're at the end of the deceleration of a momentum scroll, so there's no need
            // to wait for more events. Snap ASAP on the next tick.
            // Also, because there's some remaining momentum we'll bias the snap in the
            // direction of the ongoing scroll because it's better UX for the scroll to snap
            // in the same direction as the scroll instead of reversing to snap.  Therefore,
            // if it's already scrolled more than 20% in the current direction, keep going.
            const snapToThreshold = delta > 0 ? 0.8 : 0.2;
            lastEventBeforeSnap = newEvent;
            recentWheelEvents.splice(0);
            timeout = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.n)(() => {
              if (swiper.destroyed || !swiper.params) return;
              swiper.slideToClosest(swiper.params.speed, true, undefined, snapToThreshold);
            }, 0); // no delay; move on next tick
          }

          if (!timeout) {
            // if we get here, then we haven't detected the end of a momentum scroll, so
            // we'll consider a scroll "complete" when there haven't been any wheel events
            // for 500ms.
            timeout = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.n)(() => {
              if (swiper.destroyed || !swiper.params) return;
              const snapToThreshold = 0.5;
              lastEventBeforeSnap = newEvent;
              recentWheelEvents.splice(0);
              swiper.slideToClosest(swiper.params.speed, true, undefined, snapToThreshold);
            }, 500);
          }
        }

        // Emit event
        if (!ignoreWheelEvents) emit('scroll', e);

        // Stop autoplay
        if (swiper.params.autoplay && swiper.params.autoplay.disableOnInteraction) swiper.autoplay.stop();
        // Return page scroll on edge positions
        if (params.releaseOnEdges && (position === swiper.minTranslate() || position === swiper.maxTranslate())) {
          return true;
        }
      }
    }
    if (e.preventDefault) e.preventDefault();else e.returnValue = false;
    return false;
  }
  function events(method) {
    let targetEl = swiper.el;
    if (swiper.params.mousewheel.eventsTarget !== 'container') {
      targetEl = document.querySelector(swiper.params.mousewheel.eventsTarget);
    }
    targetEl[method]('mouseenter', handleMouseEnter);
    targetEl[method]('mouseleave', handleMouseLeave);
    targetEl[method]('wheel', handle);
  }
  function enable() {
    if (swiper.params.cssMode) {
      swiper.wrapperEl.removeEventListener('wheel', handle);
      return true;
    }
    if (swiper.mousewheel.enabled) return false;
    events('addEventListener');
    swiper.mousewheel.enabled = true;
    return true;
  }
  function disable() {
    if (swiper.params.cssMode) {
      swiper.wrapperEl.addEventListener(event, handle);
      return true;
    }
    if (!swiper.mousewheel.enabled) return false;
    events('removeEventListener');
    swiper.mousewheel.enabled = false;
    return true;
  }
  on('init', () => {
    if (!swiper.params.mousewheel.enabled && swiper.params.cssMode) {
      disable();
    }
    if (swiper.params.mousewheel.enabled) enable();
  });
  on('destroy', () => {
    if (swiper.params.cssMode) {
      enable();
    }
    if (swiper.mousewheel.enabled) disable();
  });
  Object.assign(swiper.mousewheel, {
    enable,
    disable
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/navigation.mjs":
/*!*****************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/navigation.mjs ***!
  \*****************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ Navigation)
/* harmony export */ });
/* harmony import */ var _shared_create_element_if_not_defined_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/create-element-if-not-defined.mjs */ "../plugin-slug/node_modules/swiper/shared/create-element-if-not-defined.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");



function Navigation(_ref) {
  let {
    swiper,
    extendParams,
    on,
    emit
  } = _ref;
  extendParams({
    navigation: {
      nextEl: null,
      prevEl: null,
      hideOnClick: false,
      disabledClass: 'swiper-button-disabled',
      hiddenClass: 'swiper-button-hidden',
      lockClass: 'swiper-button-lock',
      navigationDisabledClass: 'swiper-navigation-disabled'
    }
  });
  swiper.navigation = {
    nextEl: null,
    prevEl: null
  };
  function getEl(el) {
    let res;
    if (el && typeof el === 'string' && swiper.isElement) {
      res = swiper.el.querySelector(el) || swiper.hostEl.querySelector(el);
      if (res) return res;
    }
    if (el) {
      if (typeof el === 'string') res = [...document.querySelectorAll(el)];
      if (swiper.params.uniqueNavElements && typeof el === 'string' && res && res.length > 1 && swiper.el.querySelectorAll(el).length === 1) {
        res = swiper.el.querySelector(el);
      } else if (res && res.length === 1) {
        res = res[0];
      }
    }
    if (el && !res) return el;
    // if (Array.isArray(res) && res.length === 1) res = res[0];
    return res;
  }
  function toggleEl(el, disabled) {
    const params = swiper.params.navigation;
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.m)(el);
    el.forEach(subEl => {
      if (subEl) {
        subEl.classList[disabled ? 'add' : 'remove'](...params.disabledClass.split(' '));
        if (subEl.tagName === 'BUTTON') subEl.disabled = disabled;
        if (swiper.params.watchOverflow && swiper.enabled) {
          subEl.classList[swiper.isLocked ? 'add' : 'remove'](params.lockClass);
        }
      }
    });
  }
  function update() {
    // Update Navigation Buttons
    const {
      nextEl,
      prevEl
    } = swiper.navigation;
    if (swiper.params.loop) {
      toggleEl(prevEl, false);
      toggleEl(nextEl, false);
      return;
    }
    toggleEl(prevEl, swiper.isBeginning && !swiper.params.rewind);
    toggleEl(nextEl, swiper.isEnd && !swiper.params.rewind);
  }
  function onPrevClick(e) {
    e.preventDefault();
    if (swiper.isBeginning && !swiper.params.loop && !swiper.params.rewind) return;
    swiper.slidePrev();
    emit('navigationPrev');
  }
  function onNextClick(e) {
    e.preventDefault();
    if (swiper.isEnd && !swiper.params.loop && !swiper.params.rewind) return;
    swiper.slideNext();
    emit('navigationNext');
  }
  function init() {
    const params = swiper.params.navigation;
    swiper.params.navigation = (0,_shared_create_element_if_not_defined_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(swiper, swiper.originalParams.navigation, swiper.params.navigation, {
      nextEl: 'swiper-button-next',
      prevEl: 'swiper-button-prev'
    });
    if (!(params.nextEl || params.prevEl)) return;
    let nextEl = getEl(params.nextEl);
    let prevEl = getEl(params.prevEl);
    Object.assign(swiper.navigation, {
      nextEl,
      prevEl
    });
    nextEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.m)(nextEl);
    prevEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.m)(prevEl);
    const initButton = (el, dir) => {
      if (el) {
        el.addEventListener('click', dir === 'next' ? onNextClick : onPrevClick);
      }
      if (!swiper.enabled && el) {
        el.classList.add(...params.lockClass.split(' '));
      }
    };
    nextEl.forEach(el => initButton(el, 'next'));
    prevEl.forEach(el => initButton(el, 'prev'));
  }
  function destroy() {
    let {
      nextEl,
      prevEl
    } = swiper.navigation;
    nextEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.m)(nextEl);
    prevEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.m)(prevEl);
    const destroyButton = (el, dir) => {
      el.removeEventListener('click', dir === 'next' ? onNextClick : onPrevClick);
      el.classList.remove(...swiper.params.navigation.disabledClass.split(' '));
    };
    nextEl.forEach(el => destroyButton(el, 'next'));
    prevEl.forEach(el => destroyButton(el, 'prev'));
  }
  on('init', () => {
    if (swiper.params.navigation.enabled === false) {
      // eslint-disable-next-line
      disable();
    } else {
      init();
      update();
    }
  });
  on('toEdge fromEdge lock unlock', () => {
    update();
  });
  on('destroy', () => {
    destroy();
  });
  on('enable disable', () => {
    let {
      nextEl,
      prevEl
    } = swiper.navigation;
    nextEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.m)(nextEl);
    prevEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.m)(prevEl);
    if (swiper.enabled) {
      update();
      return;
    }
    [...nextEl, ...prevEl].filter(el => !!el).forEach(el => el.classList.add(swiper.params.navigation.lockClass));
  });
  on('click', (_s, e) => {
    let {
      nextEl,
      prevEl
    } = swiper.navigation;
    nextEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.m)(nextEl);
    prevEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.m)(prevEl);
    const targetEl = e.target;
    let targetIsButton = prevEl.includes(targetEl) || nextEl.includes(targetEl);
    if (swiper.isElement && !targetIsButton) {
      const path = e.path || e.composedPath && e.composedPath();
      if (path) {
        targetIsButton = path.find(pathEl => nextEl.includes(pathEl) || prevEl.includes(pathEl));
      }
    }
    if (swiper.params.navigation.hideOnClick && !targetIsButton) {
      if (swiper.pagination && swiper.params.pagination && swiper.params.pagination.clickable && (swiper.pagination.el === targetEl || swiper.pagination.el.contains(targetEl))) return;
      let isHidden;
      if (nextEl.length) {
        isHidden = nextEl[0].classList.contains(swiper.params.navigation.hiddenClass);
      } else if (prevEl.length) {
        isHidden = prevEl[0].classList.contains(swiper.params.navigation.hiddenClass);
      }
      if (isHidden === true) {
        emit('navigationShow');
      } else {
        emit('navigationHide');
      }
      [...nextEl, ...prevEl].filter(el => !!el).forEach(el => el.classList.toggle(swiper.params.navigation.hiddenClass));
    }
  });
  const enable = () => {
    swiper.el.classList.remove(...swiper.params.navigation.navigationDisabledClass.split(' '));
    init();
    update();
  };
  const disable = () => {
    swiper.el.classList.add(...swiper.params.navigation.navigationDisabledClass.split(' '));
    destroy();
  };
  Object.assign(swiper.navigation, {
    enable,
    disable,
    update,
    init,
    destroy
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/pagination.mjs":
/*!*****************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/pagination.mjs ***!
  \*****************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ Pagination)
/* harmony export */ });
/* harmony import */ var _shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/classes-to-selector.mjs */ "../plugin-slug/node_modules/swiper/shared/classes-to-selector.mjs");
/* harmony import */ var _shared_create_element_if_not_defined_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/create-element-if-not-defined.mjs */ "../plugin-slug/node_modules/swiper/shared/create-element-if-not-defined.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");




function Pagination(_ref) {
  let {
    swiper,
    extendParams,
    on,
    emit
  } = _ref;
  const pfx = 'swiper-pagination';
  extendParams({
    pagination: {
      el: null,
      bulletElement: 'span',
      clickable: false,
      hideOnClick: false,
      renderBullet: null,
      renderProgressbar: null,
      renderFraction: null,
      renderCustom: null,
      progressbarOpposite: false,
      type: 'bullets',
      // 'bullets' or 'progressbar' or 'fraction' or 'custom'
      dynamicBullets: false,
      dynamicMainBullets: 1,
      formatFractionCurrent: number => number,
      formatFractionTotal: number => number,
      bulletClass: `${pfx}-bullet`,
      bulletActiveClass: `${pfx}-bullet-active`,
      modifierClass: `${pfx}-`,
      currentClass: `${pfx}-current`,
      totalClass: `${pfx}-total`,
      hiddenClass: `${pfx}-hidden`,
      progressbarFillClass: `${pfx}-progressbar-fill`,
      progressbarOppositeClass: `${pfx}-progressbar-opposite`,
      clickableClass: `${pfx}-clickable`,
      lockClass: `${pfx}-lock`,
      horizontalClass: `${pfx}-horizontal`,
      verticalClass: `${pfx}-vertical`,
      paginationDisabledClass: `${pfx}-disabled`
    }
  });
  swiper.pagination = {
    el: null,
    bullets: []
  };
  let bulletSize;
  let dynamicBulletIndex = 0;
  function isPaginationDisabled() {
    return !swiper.params.pagination.el || !swiper.pagination.el || Array.isArray(swiper.pagination.el) && swiper.pagination.el.length === 0;
  }
  function setSideBullets(bulletEl, position) {
    const {
      bulletActiveClass
    } = swiper.params.pagination;
    if (!bulletEl) return;
    bulletEl = bulletEl[`${position === 'prev' ? 'previous' : 'next'}ElementSibling`];
    if (bulletEl) {
      bulletEl.classList.add(`${bulletActiveClass}-${position}`);
      bulletEl = bulletEl[`${position === 'prev' ? 'previous' : 'next'}ElementSibling`];
      if (bulletEl) {
        bulletEl.classList.add(`${bulletActiveClass}-${position}-${position}`);
      }
    }
  }
  function getMoveDirection(prevIndex, nextIndex, length) {
    prevIndex = prevIndex % length;
    nextIndex = nextIndex % length;
    if (nextIndex === prevIndex + 1) {
      return 'next';
    } else if (nextIndex === prevIndex - 1) {
      return 'previous';
    }
    return;
  }
  function onBulletClick(e) {
    const bulletEl = e.target.closest((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(swiper.params.pagination.bulletClass));
    if (!bulletEl) {
      return;
    }
    e.preventDefault();
    const index = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.h)(bulletEl) * swiper.params.slidesPerGroup;
    if (swiper.params.loop) {
      if (swiper.realIndex === index) return;
      const moveDirection = getMoveDirection(swiper.realIndex, index, swiper.slides.length);
      if (moveDirection === 'next') {
        swiper.slideNext();
      } else if (moveDirection === 'previous') {
        swiper.slidePrev();
      } else {
        swiper.slideToLoop(index);
      }
    } else {
      swiper.slideTo(index);
    }
  }
  function update() {
    // Render || Update Pagination bullets/items
    const rtl = swiper.rtl;
    const params = swiper.params.pagination;
    if (isPaginationDisabled()) return;
    let el = swiper.pagination.el;
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    // Current/Total
    let current;
    let previousIndex;
    const slidesLength = swiper.virtual && swiper.params.virtual.enabled ? swiper.virtual.slides.length : swiper.slides.length;
    const total = swiper.params.loop ? Math.ceil(slidesLength / swiper.params.slidesPerGroup) : swiper.snapGrid.length;
    if (swiper.params.loop) {
      previousIndex = swiper.previousRealIndex || 0;
      current = swiper.params.slidesPerGroup > 1 ? Math.floor(swiper.realIndex / swiper.params.slidesPerGroup) : swiper.realIndex;
    } else if (typeof swiper.snapIndex !== 'undefined') {
      current = swiper.snapIndex;
      previousIndex = swiper.previousSnapIndex;
    } else {
      previousIndex = swiper.previousIndex || 0;
      current = swiper.activeIndex || 0;
    }
    // Types
    if (params.type === 'bullets' && swiper.pagination.bullets && swiper.pagination.bullets.length > 0) {
      const bullets = swiper.pagination.bullets;
      let firstIndex;
      let lastIndex;
      let midIndex;
      if (params.dynamicBullets) {
        bulletSize = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.f)(bullets[0], swiper.isHorizontal() ? 'width' : 'height', true);
        el.forEach(subEl => {
          subEl.style[swiper.isHorizontal() ? 'width' : 'height'] = `${bulletSize * (params.dynamicMainBullets + 4)}px`;
        });
        if (params.dynamicMainBullets > 1 && previousIndex !== undefined) {
          dynamicBulletIndex += current - (previousIndex || 0);
          if (dynamicBulletIndex > params.dynamicMainBullets - 1) {
            dynamicBulletIndex = params.dynamicMainBullets - 1;
          } else if (dynamicBulletIndex < 0) {
            dynamicBulletIndex = 0;
          }
        }
        firstIndex = Math.max(current - dynamicBulletIndex, 0);
        lastIndex = firstIndex + (Math.min(bullets.length, params.dynamicMainBullets) - 1);
        midIndex = (lastIndex + firstIndex) / 2;
      }
      bullets.forEach(bulletEl => {
        const classesToRemove = [...['', '-next', '-next-next', '-prev', '-prev-prev', '-main'].map(suffix => `${params.bulletActiveClass}${suffix}`)].map(s => typeof s === 'string' && s.includes(' ') ? s.split(' ') : s).flat();
        bulletEl.classList.remove(...classesToRemove);
      });
      if (el.length > 1) {
        bullets.forEach(bullet => {
          const bulletIndex = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.h)(bullet);
          if (bulletIndex === current) {
            bullet.classList.add(...params.bulletActiveClass.split(' '));
          } else if (swiper.isElement) {
            bullet.setAttribute('part', 'bullet');
          }
          if (params.dynamicBullets) {
            if (bulletIndex >= firstIndex && bulletIndex <= lastIndex) {
              bullet.classList.add(...`${params.bulletActiveClass}-main`.split(' '));
            }
            if (bulletIndex === firstIndex) {
              setSideBullets(bullet, 'prev');
            }
            if (bulletIndex === lastIndex) {
              setSideBullets(bullet, 'next');
            }
          }
        });
      } else {
        const bullet = bullets[current];
        if (bullet) {
          bullet.classList.add(...params.bulletActiveClass.split(' '));
        }
        if (swiper.isElement) {
          bullets.forEach((bulletEl, bulletIndex) => {
            bulletEl.setAttribute('part', bulletIndex === current ? 'bullet-active' : 'bullet');
          });
        }
        if (params.dynamicBullets) {
          const firstDisplayedBullet = bullets[firstIndex];
          const lastDisplayedBullet = bullets[lastIndex];
          for (let i = firstIndex; i <= lastIndex; i += 1) {
            if (bullets[i]) {
              bullets[i].classList.add(...`${params.bulletActiveClass}-main`.split(' '));
            }
          }
          setSideBullets(firstDisplayedBullet, 'prev');
          setSideBullets(lastDisplayedBullet, 'next');
        }
      }
      if (params.dynamicBullets) {
        const dynamicBulletsLength = Math.min(bullets.length, params.dynamicMainBullets + 4);
        const bulletsOffset = (bulletSize * dynamicBulletsLength - bulletSize) / 2 - midIndex * bulletSize;
        const offsetProp = rtl ? 'right' : 'left';
        bullets.forEach(bullet => {
          bullet.style[swiper.isHorizontal() ? offsetProp : 'top'] = `${bulletsOffset}px`;
        });
      }
    }
    el.forEach((subEl, subElIndex) => {
      if (params.type === 'fraction') {
        subEl.querySelectorAll((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(params.currentClass)).forEach(fractionEl => {
          fractionEl.textContent = params.formatFractionCurrent(current + 1);
        });
        subEl.querySelectorAll((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(params.totalClass)).forEach(totalEl => {
          totalEl.textContent = params.formatFractionTotal(total);
        });
      }
      if (params.type === 'progressbar') {
        let progressbarDirection;
        if (params.progressbarOpposite) {
          progressbarDirection = swiper.isHorizontal() ? 'vertical' : 'horizontal';
        } else {
          progressbarDirection = swiper.isHorizontal() ? 'horizontal' : 'vertical';
        }
        const scale = (current + 1) / total;
        let scaleX = 1;
        let scaleY = 1;
        if (progressbarDirection === 'horizontal') {
          scaleX = scale;
        } else {
          scaleY = scale;
        }
        subEl.querySelectorAll((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(params.progressbarFillClass)).forEach(progressEl => {
          progressEl.style.transform = `translate3d(0,0,0) scaleX(${scaleX}) scaleY(${scaleY})`;
          progressEl.style.transitionDuration = `${swiper.params.speed}ms`;
        });
      }
      if (params.type === 'custom' && params.renderCustom) {
        subEl.innerHTML = params.renderCustom(swiper, current + 1, total);
        if (subElIndex === 0) emit('paginationRender', subEl);
      } else {
        if (subElIndex === 0) emit('paginationRender', subEl);
        emit('paginationUpdate', subEl);
      }
      if (swiper.params.watchOverflow && swiper.enabled) {
        subEl.classList[swiper.isLocked ? 'add' : 'remove'](params.lockClass);
      }
    });
  }
  function render() {
    // Render Container
    const params = swiper.params.pagination;
    if (isPaginationDisabled()) return;
    const slidesLength = swiper.virtual && swiper.params.virtual.enabled ? swiper.virtual.slides.length : swiper.grid && swiper.params.grid.rows > 1 ? swiper.slides.length / Math.ceil(swiper.params.grid.rows) : swiper.slides.length;
    let el = swiper.pagination.el;
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    let paginationHTML = '';
    if (params.type === 'bullets') {
      let numberOfBullets = swiper.params.loop ? Math.ceil(slidesLength / swiper.params.slidesPerGroup) : swiper.snapGrid.length;
      if (swiper.params.freeMode && swiper.params.freeMode.enabled && numberOfBullets > slidesLength) {
        numberOfBullets = slidesLength;
      }
      for (let i = 0; i < numberOfBullets; i += 1) {
        if (params.renderBullet) {
          paginationHTML += params.renderBullet.call(swiper, i, params.bulletClass);
        } else {
          // prettier-ignore
          paginationHTML += `<${params.bulletElement} ${swiper.isElement ? 'part="bullet"' : ''} class="${params.bulletClass}"></${params.bulletElement}>`;
        }
      }
    }
    if (params.type === 'fraction') {
      if (params.renderFraction) {
        paginationHTML = params.renderFraction.call(swiper, params.currentClass, params.totalClass);
      } else {
        paginationHTML = `<span class="${params.currentClass}"></span>` + ' / ' + `<span class="${params.totalClass}"></span>`;
      }
    }
    if (params.type === 'progressbar') {
      if (params.renderProgressbar) {
        paginationHTML = params.renderProgressbar.call(swiper, params.progressbarFillClass);
      } else {
        paginationHTML = `<span class="${params.progressbarFillClass}"></span>`;
      }
    }
    swiper.pagination.bullets = [];
    el.forEach(subEl => {
      if (params.type !== 'custom') {
        subEl.innerHTML = paginationHTML || '';
      }
      if (params.type === 'bullets') {
        swiper.pagination.bullets.push(...subEl.querySelectorAll((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(params.bulletClass)));
      }
    });
    if (params.type !== 'custom') {
      emit('paginationRender', el[0]);
    }
  }
  function init() {
    swiper.params.pagination = (0,_shared_create_element_if_not_defined_mjs__WEBPACK_IMPORTED_MODULE_1__.c)(swiper, swiper.originalParams.pagination, swiper.params.pagination, {
      el: 'swiper-pagination'
    });
    const params = swiper.params.pagination;
    if (!params.el) return;
    let el;
    if (typeof params.el === 'string' && swiper.isElement) {
      el = swiper.el.querySelector(params.el);
    }
    if (!el && typeof params.el === 'string') {
      el = [...document.querySelectorAll(params.el)];
    }
    if (!el) {
      el = params.el;
    }
    if (!el || el.length === 0) return;
    if (swiper.params.uniqueNavElements && typeof params.el === 'string' && Array.isArray(el) && el.length > 1) {
      el = [...swiper.el.querySelectorAll(params.el)];
      // check if it belongs to another nested Swiper
      if (el.length > 1) {
        el = el.find(subEl => {
          if ((0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.a)(subEl, '.swiper')[0] !== swiper.el) return false;
          return true;
        });
      }
    }
    if (Array.isArray(el) && el.length === 1) el = el[0];
    Object.assign(swiper.pagination, {
      el
    });
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    el.forEach(subEl => {
      if (params.type === 'bullets' && params.clickable) {
        subEl.classList.add(...(params.clickableClass || '').split(' '));
      }
      subEl.classList.add(params.modifierClass + params.type);
      subEl.classList.add(swiper.isHorizontal() ? params.horizontalClass : params.verticalClass);
      if (params.type === 'bullets' && params.dynamicBullets) {
        subEl.classList.add(`${params.modifierClass}${params.type}-dynamic`);
        dynamicBulletIndex = 0;
        if (params.dynamicMainBullets < 1) {
          params.dynamicMainBullets = 1;
        }
      }
      if (params.type === 'progressbar' && params.progressbarOpposite) {
        subEl.classList.add(params.progressbarOppositeClass);
      }
      if (params.clickable) {
        subEl.addEventListener('click', onBulletClick);
      }
      if (!swiper.enabled) {
        subEl.classList.add(params.lockClass);
      }
    });
  }
  function destroy() {
    const params = swiper.params.pagination;
    if (isPaginationDisabled()) return;
    let el = swiper.pagination.el;
    if (el) {
      el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
      el.forEach(subEl => {
        subEl.classList.remove(params.hiddenClass);
        subEl.classList.remove(params.modifierClass + params.type);
        subEl.classList.remove(swiper.isHorizontal() ? params.horizontalClass : params.verticalClass);
        if (params.clickable) {
          subEl.classList.remove(...(params.clickableClass || '').split(' '));
          subEl.removeEventListener('click', onBulletClick);
        }
      });
    }
    if (swiper.pagination.bullets) swiper.pagination.bullets.forEach(subEl => subEl.classList.remove(...params.bulletActiveClass.split(' ')));
  }
  on('changeDirection', () => {
    if (!swiper.pagination || !swiper.pagination.el) return;
    const params = swiper.params.pagination;
    let {
      el
    } = swiper.pagination;
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
    el.forEach(subEl => {
      subEl.classList.remove(params.horizontalClass, params.verticalClass);
      subEl.classList.add(swiper.isHorizontal() ? params.horizontalClass : params.verticalClass);
    });
  });
  on('init', () => {
    if (swiper.params.pagination.enabled === false) {
      // eslint-disable-next-line
      disable();
    } else {
      init();
      render();
      update();
    }
  });
  on('activeIndexChange', () => {
    if (typeof swiper.snapIndex === 'undefined') {
      update();
    }
  });
  on('snapIndexChange', () => {
    update();
  });
  on('snapGridLengthChange', () => {
    render();
    update();
  });
  on('destroy', () => {
    destroy();
  });
  on('enable disable', () => {
    let {
      el
    } = swiper.pagination;
    if (el) {
      el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
      el.forEach(subEl => subEl.classList[swiper.enabled ? 'remove' : 'add'](swiper.params.pagination.lockClass));
    }
  });
  on('lock unlock', () => {
    update();
  });
  on('click', (_s, e) => {
    const targetEl = e.target;
    const el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(swiper.pagination.el);
    if (swiper.params.pagination.el && swiper.params.pagination.hideOnClick && el && el.length > 0 && !targetEl.classList.contains(swiper.params.pagination.bulletClass)) {
      if (swiper.navigation && (swiper.navigation.nextEl && targetEl === swiper.navigation.nextEl || swiper.navigation.prevEl && targetEl === swiper.navigation.prevEl)) return;
      const isHidden = el[0].classList.contains(swiper.params.pagination.hiddenClass);
      if (isHidden === true) {
        emit('paginationShow');
      } else {
        emit('paginationHide');
      }
      el.forEach(subEl => subEl.classList.toggle(swiper.params.pagination.hiddenClass));
    }
  });
  const enable = () => {
    swiper.el.classList.remove(swiper.params.pagination.paginationDisabledClass);
    let {
      el
    } = swiper.pagination;
    if (el) {
      el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
      el.forEach(subEl => subEl.classList.remove(swiper.params.pagination.paginationDisabledClass));
    }
    init();
    render();
    update();
  };
  const disable = () => {
    swiper.el.classList.add(swiper.params.pagination.paginationDisabledClass);
    let {
      el
    } = swiper.pagination;
    if (el) {
      el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.m)(el);
      el.forEach(subEl => subEl.classList.add(swiper.params.pagination.paginationDisabledClass));
    }
    destroy();
  };
  Object.assign(swiper.pagination, {
    enable,
    disable,
    render,
    update,
    init,
    destroy
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/parallax.mjs":
/*!***************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/parallax.mjs ***!
  \***************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ Parallax)
/* harmony export */ });
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");


function Parallax(_ref) {
  let {
    swiper,
    extendParams,
    on
  } = _ref;
  extendParams({
    parallax: {
      enabled: false
    }
  });
  const elementsSelector = '[data-swiper-parallax], [data-swiper-parallax-x], [data-swiper-parallax-y], [data-swiper-parallax-opacity], [data-swiper-parallax-scale]';
  const setTransform = (el, progress) => {
    const {
      rtl
    } = swiper;
    const rtlFactor = rtl ? -1 : 1;
    const p = el.getAttribute('data-swiper-parallax') || '0';
    let x = el.getAttribute('data-swiper-parallax-x');
    let y = el.getAttribute('data-swiper-parallax-y');
    const scale = el.getAttribute('data-swiper-parallax-scale');
    const opacity = el.getAttribute('data-swiper-parallax-opacity');
    const rotate = el.getAttribute('data-swiper-parallax-rotate');
    if (x || y) {
      x = x || '0';
      y = y || '0';
    } else if (swiper.isHorizontal()) {
      x = p;
      y = '0';
    } else {
      y = p;
      x = '0';
    }
    if (x.indexOf('%') >= 0) {
      x = `${parseInt(x, 10) * progress * rtlFactor}%`;
    } else {
      x = `${x * progress * rtlFactor}px`;
    }
    if (y.indexOf('%') >= 0) {
      y = `${parseInt(y, 10) * progress}%`;
    } else {
      y = `${y * progress}px`;
    }
    if (typeof opacity !== 'undefined' && opacity !== null) {
      const currentOpacity = opacity - (opacity - 1) * (1 - Math.abs(progress));
      el.style.opacity = currentOpacity;
    }
    let transform = `translate3d(${x}, ${y}, 0px)`;
    if (typeof scale !== 'undefined' && scale !== null) {
      const currentScale = scale - (scale - 1) * (1 - Math.abs(progress));
      transform += ` scale(${currentScale})`;
    }
    if (rotate && typeof rotate !== 'undefined' && rotate !== null) {
      const currentRotate = rotate * progress * -1;
      transform += ` rotate(${currentRotate}deg)`;
    }
    el.style.transform = transform;
  };
  const setTranslate = () => {
    const {
      el,
      slides,
      progress,
      snapGrid,
      isElement
    } = swiper;
    const elements = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.e)(el, elementsSelector);
    if (swiper.isElement) {
      elements.push(...(0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.e)(swiper.hostEl, elementsSelector));
    }
    elements.forEach(subEl => {
      setTransform(subEl, progress);
    });
    slides.forEach((slideEl, slideIndex) => {
      let slideProgress = slideEl.progress;
      if (swiper.params.slidesPerGroup > 1 && swiper.params.slidesPerView !== 'auto') {
        slideProgress += Math.ceil(slideIndex / 2) - progress * (snapGrid.length - 1);
      }
      slideProgress = Math.min(Math.max(slideProgress, -1), 1);
      slideEl.querySelectorAll(`${elementsSelector}, [data-swiper-parallax-rotate]`).forEach(subEl => {
        setTransform(subEl, slideProgress);
      });
    });
  };
  const setTransition = function (duration) {
    if (duration === void 0) {
      duration = swiper.params.speed;
    }
    const {
      el,
      hostEl
    } = swiper;
    const elements = [...el.querySelectorAll(elementsSelector)];
    if (swiper.isElement) {
      elements.push(...hostEl.querySelectorAll(elementsSelector));
    }
    elements.forEach(parallaxEl => {
      let parallaxDuration = parseInt(parallaxEl.getAttribute('data-swiper-parallax-duration'), 10) || duration;
      if (duration === 0) parallaxDuration = 0;
      parallaxEl.style.transitionDuration = `${parallaxDuration}ms`;
    });
  };
  on('beforeInit', () => {
    if (!swiper.params.parallax.enabled) return;
    swiper.params.watchSlidesProgress = true;
    swiper.originalParams.watchSlidesProgress = true;
  });
  on('init', () => {
    if (!swiper.params.parallax.enabled) return;
    setTranslate();
  });
  on('setTranslate', () => {
    if (!swiper.params.parallax.enabled) return;
    setTranslate();
  });
  on('setTransition', (_swiper, duration) => {
    if (!swiper.params.parallax.enabled) return;
    setTransition(duration);
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/scrollbar.mjs":
/*!****************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/scrollbar.mjs ***!
  \****************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ Scrollbar)
/* harmony export */ });
/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ "../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");
/* harmony import */ var _shared_create_element_if_not_defined_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/create-element-if-not-defined.mjs */ "../plugin-slug/node_modules/swiper/shared/create-element-if-not-defined.mjs");
/* harmony import */ var _shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../shared/classes-to-selector.mjs */ "../plugin-slug/node_modules/swiper/shared/classes-to-selector.mjs");





function Scrollbar(_ref) {
  let {
    swiper,
    extendParams,
    on,
    emit
  } = _ref;
  const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
  let isTouched = false;
  let timeout = null;
  let dragTimeout = null;
  let dragStartPos;
  let dragSize;
  let trackSize;
  let divider;
  extendParams({
    scrollbar: {
      el: null,
      dragSize: 'auto',
      hide: false,
      draggable: false,
      snapOnRelease: true,
      lockClass: 'swiper-scrollbar-lock',
      dragClass: 'swiper-scrollbar-drag',
      scrollbarDisabledClass: 'swiper-scrollbar-disabled',
      horizontalClass: `swiper-scrollbar-horizontal`,
      verticalClass: `swiper-scrollbar-vertical`
    }
  });
  swiper.scrollbar = {
    el: null,
    dragEl: null
  };
  function setTranslate() {
    if (!swiper.params.scrollbar.el || !swiper.scrollbar.el) return;
    const {
      scrollbar,
      rtlTranslate: rtl
    } = swiper;
    const {
      dragEl,
      el
    } = scrollbar;
    const params = swiper.params.scrollbar;
    const progress = swiper.params.loop ? swiper.progressLoop : swiper.progress;
    let newSize = dragSize;
    let newPos = (trackSize - dragSize) * progress;
    if (rtl) {
      newPos = -newPos;
      if (newPos > 0) {
        newSize = dragSize - newPos;
        newPos = 0;
      } else if (-newPos + dragSize > trackSize) {
        newSize = trackSize + newPos;
      }
    } else if (newPos < 0) {
      newSize = dragSize + newPos;
      newPos = 0;
    } else if (newPos + dragSize > trackSize) {
      newSize = trackSize - newPos;
    }
    if (swiper.isHorizontal()) {
      dragEl.style.transform = `translate3d(${newPos}px, 0, 0)`;
      dragEl.style.width = `${newSize}px`;
    } else {
      dragEl.style.transform = `translate3d(0px, ${newPos}px, 0)`;
      dragEl.style.height = `${newSize}px`;
    }
    if (params.hide) {
      clearTimeout(timeout);
      el.style.opacity = 1;
      timeout = setTimeout(() => {
        el.style.opacity = 0;
        el.style.transitionDuration = '400ms';
      }, 1000);
    }
  }
  function setTransition(duration) {
    if (!swiper.params.scrollbar.el || !swiper.scrollbar.el) return;
    swiper.scrollbar.dragEl.style.transitionDuration = `${duration}ms`;
  }
  function updateSize() {
    if (!swiper.params.scrollbar.el || !swiper.scrollbar.el) return;
    const {
      scrollbar
    } = swiper;
    const {
      dragEl,
      el
    } = scrollbar;
    dragEl.style.width = '';
    dragEl.style.height = '';
    trackSize = swiper.isHorizontal() ? el.offsetWidth : el.offsetHeight;
    divider = swiper.size / (swiper.virtualSize + swiper.params.slidesOffsetBefore - (swiper.params.centeredSlides ? swiper.snapGrid[0] : 0));
    if (swiper.params.scrollbar.dragSize === 'auto') {
      dragSize = trackSize * divider;
    } else {
      dragSize = parseInt(swiper.params.scrollbar.dragSize, 10);
    }
    if (swiper.isHorizontal()) {
      dragEl.style.width = `${dragSize}px`;
    } else {
      dragEl.style.height = `${dragSize}px`;
    }
    if (divider >= 1) {
      el.style.display = 'none';
    } else {
      el.style.display = '';
    }
    if (swiper.params.scrollbar.hide) {
      el.style.opacity = 0;
    }
    if (swiper.params.watchOverflow && swiper.enabled) {
      scrollbar.el.classList[swiper.isLocked ? 'add' : 'remove'](swiper.params.scrollbar.lockClass);
    }
  }
  function getPointerPosition(e) {
    return swiper.isHorizontal() ? e.clientX : e.clientY;
  }
  function setDragPosition(e) {
    const {
      scrollbar,
      rtlTranslate: rtl
    } = swiper;
    const {
      el
    } = scrollbar;
    let positionRatio;
    positionRatio = (getPointerPosition(e) - (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.b)(el)[swiper.isHorizontal() ? 'left' : 'top'] - (dragStartPos !== null ? dragStartPos : dragSize / 2)) / (trackSize - dragSize);
    positionRatio = Math.max(Math.min(positionRatio, 1), 0);
    if (rtl) {
      positionRatio = 1 - positionRatio;
    }
    const position = swiper.minTranslate() + (swiper.maxTranslate() - swiper.minTranslate()) * positionRatio;
    swiper.updateProgress(position);
    swiper.setTranslate(position);
    swiper.updateActiveIndex();
    swiper.updateSlidesClasses();
  }
  function onDragStart(e) {
    const params = swiper.params.scrollbar;
    const {
      scrollbar,
      wrapperEl
    } = swiper;
    const {
      el,
      dragEl
    } = scrollbar;
    isTouched = true;
    dragStartPos = e.target === dragEl ? getPointerPosition(e) - e.target.getBoundingClientRect()[swiper.isHorizontal() ? 'left' : 'top'] : null;
    e.preventDefault();
    e.stopPropagation();
    wrapperEl.style.transitionDuration = '100ms';
    dragEl.style.transitionDuration = '100ms';
    setDragPosition(e);
    clearTimeout(dragTimeout);
    el.style.transitionDuration = '0ms';
    if (params.hide) {
      el.style.opacity = 1;
    }
    if (swiper.params.cssMode) {
      swiper.wrapperEl.style['scroll-snap-type'] = 'none';
    }
    emit('scrollbarDragStart', e);
  }
  function onDragMove(e) {
    const {
      scrollbar,
      wrapperEl
    } = swiper;
    const {
      el,
      dragEl
    } = scrollbar;
    if (!isTouched) return;
    if (e.preventDefault && e.cancelable) e.preventDefault();else e.returnValue = false;
    setDragPosition(e);
    wrapperEl.style.transitionDuration = '0ms';
    el.style.transitionDuration = '0ms';
    dragEl.style.transitionDuration = '0ms';
    emit('scrollbarDragMove', e);
  }
  function onDragEnd(e) {
    const params = swiper.params.scrollbar;
    const {
      scrollbar,
      wrapperEl
    } = swiper;
    const {
      el
    } = scrollbar;
    if (!isTouched) return;
    isTouched = false;
    if (swiper.params.cssMode) {
      swiper.wrapperEl.style['scroll-snap-type'] = '';
      wrapperEl.style.transitionDuration = '';
    }
    if (params.hide) {
      clearTimeout(dragTimeout);
      dragTimeout = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.n)(() => {
        el.style.opacity = 0;
        el.style.transitionDuration = '400ms';
      }, 1000);
    }
    emit('scrollbarDragEnd', e);
    if (params.snapOnRelease) {
      swiper.slideToClosest();
    }
  }
  function events(method) {
    const {
      scrollbar,
      params
    } = swiper;
    const el = scrollbar.el;
    if (!el) return;
    const target = el;
    const activeListener = params.passiveListeners ? {
      passive: false,
      capture: false
    } : false;
    const passiveListener = params.passiveListeners ? {
      passive: true,
      capture: false
    } : false;
    if (!target) return;
    const eventMethod = method === 'on' ? 'addEventListener' : 'removeEventListener';
    target[eventMethod]('pointerdown', onDragStart, activeListener);
    document[eventMethod]('pointermove', onDragMove, activeListener);
    document[eventMethod]('pointerup', onDragEnd, passiveListener);
  }
  function enableDraggable() {
    if (!swiper.params.scrollbar.el || !swiper.scrollbar.el) return;
    events('on');
  }
  function disableDraggable() {
    if (!swiper.params.scrollbar.el || !swiper.scrollbar.el) return;
    events('off');
  }
  function init() {
    const {
      scrollbar,
      el: swiperEl
    } = swiper;
    swiper.params.scrollbar = (0,_shared_create_element_if_not_defined_mjs__WEBPACK_IMPORTED_MODULE_2__.c)(swiper, swiper.originalParams.scrollbar, swiper.params.scrollbar, {
      el: 'swiper-scrollbar'
    });
    const params = swiper.params.scrollbar;
    if (!params.el) return;
    let el;
    if (typeof params.el === 'string' && swiper.isElement) {
      el = swiper.el.querySelector(params.el);
    }
    if (!el && typeof params.el === 'string') {
      el = document.querySelectorAll(params.el);
      if (!el.length) return;
    } else if (!el) {
      el = params.el;
    }
    if (swiper.params.uniqueNavElements && typeof params.el === 'string' && el.length > 1 && swiperEl.querySelectorAll(params.el).length === 1) {
      el = swiperEl.querySelector(params.el);
    }
    if (el.length > 0) el = el[0];
    el.classList.add(swiper.isHorizontal() ? params.horizontalClass : params.verticalClass);
    let dragEl;
    if (el) {
      dragEl = el.querySelector((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_3__.c)(swiper.params.scrollbar.dragClass));
      if (!dragEl) {
        dragEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', swiper.params.scrollbar.dragClass);
        el.append(dragEl);
      }
    }
    Object.assign(scrollbar, {
      el,
      dragEl
    });
    if (params.draggable) {
      enableDraggable();
    }
    if (el) {
      el.classList[swiper.enabled ? 'remove' : 'add'](...(0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.i)(swiper.params.scrollbar.lockClass));
    }
  }
  function destroy() {
    const params = swiper.params.scrollbar;
    const el = swiper.scrollbar.el;
    if (el) {
      el.classList.remove(...(0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.i)(swiper.isHorizontal() ? params.horizontalClass : params.verticalClass));
    }
    disableDraggable();
  }
  on('changeDirection', () => {
    if (!swiper.scrollbar || !swiper.scrollbar.el) return;
    const params = swiper.params.scrollbar;
    let {
      el
    } = swiper.scrollbar;
    el = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.m)(el);
    el.forEach(subEl => {
      subEl.classList.remove(params.horizontalClass, params.verticalClass);
      subEl.classList.add(swiper.isHorizontal() ? params.horizontalClass : params.verticalClass);
    });
  });
  on('init', () => {
    if (swiper.params.scrollbar.enabled === false) {
      // eslint-disable-next-line
      disable();
    } else {
      init();
      updateSize();
      setTranslate();
    }
  });
  on('update resize observerUpdate lock unlock changeDirection', () => {
    updateSize();
  });
  on('setTranslate', () => {
    setTranslate();
  });
  on('setTransition', (_s, duration) => {
    setTransition(duration);
  });
  on('enable disable', () => {
    const {
      el
    } = swiper.scrollbar;
    if (el) {
      el.classList[swiper.enabled ? 'remove' : 'add'](...(0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.i)(swiper.params.scrollbar.lockClass));
    }
  });
  on('destroy', () => {
    destroy();
  });
  const enable = () => {
    swiper.el.classList.remove(...(0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.i)(swiper.params.scrollbar.scrollbarDisabledClass));
    if (swiper.scrollbar.el) {
      swiper.scrollbar.el.classList.remove(...(0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.i)(swiper.params.scrollbar.scrollbarDisabledClass));
    }
    init();
    updateSize();
    setTranslate();
  };
  const disable = () => {
    swiper.el.classList.add(...(0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.i)(swiper.params.scrollbar.scrollbarDisabledClass));
    if (swiper.scrollbar.el) {
      swiper.scrollbar.el.classList.add(...(0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.i)(swiper.params.scrollbar.scrollbarDisabledClass));
    }
    destroy();
  };
  Object.assign(swiper.scrollbar, {
    enable,
    disable,
    updateSize,
    setTranslate,
    init,
    destroy
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/thumbs.mjs":
/*!*************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/thumbs.mjs ***!
  \*************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ Thumb)
/* harmony export */ });
/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ "../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");



function Thumb(_ref) {
  let {
    swiper,
    extendParams,
    on
  } = _ref;
  extendParams({
    thumbs: {
      swiper: null,
      multipleActiveThumbs: true,
      autoScrollOffset: 0,
      slideThumbActiveClass: 'swiper-slide-thumb-active',
      thumbsContainerClass: 'swiper-thumbs'
    }
  });
  let initialized = false;
  let swiperCreated = false;
  swiper.thumbs = {
    swiper: null
  };
  function onThumbClick() {
    const thumbsSwiper = swiper.thumbs.swiper;
    if (!thumbsSwiper || thumbsSwiper.destroyed) return;
    const clickedIndex = thumbsSwiper.clickedIndex;
    const clickedSlide = thumbsSwiper.clickedSlide;
    if (clickedSlide && clickedSlide.classList.contains(swiper.params.thumbs.slideThumbActiveClass)) return;
    if (typeof clickedIndex === 'undefined' || clickedIndex === null) return;
    let slideToIndex;
    if (thumbsSwiper.params.loop) {
      slideToIndex = parseInt(thumbsSwiper.clickedSlide.getAttribute('data-swiper-slide-index'), 10);
    } else {
      slideToIndex = clickedIndex;
    }
    if (swiper.params.loop) {
      swiper.slideToLoop(slideToIndex);
    } else {
      swiper.slideTo(slideToIndex);
    }
  }
  function init() {
    const {
      thumbs: thumbsParams
    } = swiper.params;
    if (initialized) return false;
    initialized = true;
    const SwiperClass = swiper.constructor;
    if (thumbsParams.swiper instanceof SwiperClass) {
      swiper.thumbs.swiper = thumbsParams.swiper;
      Object.assign(swiper.thumbs.swiper.originalParams, {
        watchSlidesProgress: true,
        slideToClickedSlide: false
      });
      Object.assign(swiper.thumbs.swiper.params, {
        watchSlidesProgress: true,
        slideToClickedSlide: false
      });
      swiper.thumbs.swiper.update();
    } else if ((0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.l)(thumbsParams.swiper)) {
      const thumbsSwiperParams = Object.assign({}, thumbsParams.swiper);
      Object.assign(thumbsSwiperParams, {
        watchSlidesProgress: true,
        slideToClickedSlide: false
      });
      swiper.thumbs.swiper = new SwiperClass(thumbsSwiperParams);
      swiperCreated = true;
    }
    swiper.thumbs.swiper.el.classList.add(swiper.params.thumbs.thumbsContainerClass);
    swiper.thumbs.swiper.on('tap', onThumbClick);
    return true;
  }
  function update(initial) {
    const thumbsSwiper = swiper.thumbs.swiper;
    if (!thumbsSwiper || thumbsSwiper.destroyed) return;
    const slidesPerView = thumbsSwiper.params.slidesPerView === 'auto' ? thumbsSwiper.slidesPerViewDynamic() : thumbsSwiper.params.slidesPerView;

    // Activate thumbs
    let thumbsToActivate = 1;
    const thumbActiveClass = swiper.params.thumbs.slideThumbActiveClass;
    if (swiper.params.slidesPerView > 1 && !swiper.params.centeredSlides) {
      thumbsToActivate = swiper.params.slidesPerView;
    }
    if (!swiper.params.thumbs.multipleActiveThumbs) {
      thumbsToActivate = 1;
    }
    thumbsToActivate = Math.floor(thumbsToActivate);
    thumbsSwiper.slides.forEach(slideEl => slideEl.classList.remove(thumbActiveClass));
    if (thumbsSwiper.params.loop || thumbsSwiper.params.virtual && thumbsSwiper.params.virtual.enabled) {
      for (let i = 0; i < thumbsToActivate; i += 1) {
        (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(thumbsSwiper.slidesEl, `[data-swiper-slide-index="${swiper.realIndex + i}"]`).forEach(slideEl => {
          slideEl.classList.add(thumbActiveClass);
        });
      }
    } else {
      for (let i = 0; i < thumbsToActivate; i += 1) {
        if (thumbsSwiper.slides[swiper.realIndex + i]) {
          thumbsSwiper.slides[swiper.realIndex + i].classList.add(thumbActiveClass);
        }
      }
    }
    const autoScrollOffset = swiper.params.thumbs.autoScrollOffset;
    const useOffset = autoScrollOffset && !thumbsSwiper.params.loop;
    if (swiper.realIndex !== thumbsSwiper.realIndex || useOffset) {
      const currentThumbsIndex = thumbsSwiper.activeIndex;
      let newThumbsIndex;
      let direction;
      if (thumbsSwiper.params.loop) {
        const newThumbsSlide = thumbsSwiper.slides.find(slideEl => slideEl.getAttribute('data-swiper-slide-index') === `${swiper.realIndex}`);
        newThumbsIndex = thumbsSwiper.slides.indexOf(newThumbsSlide);
        direction = swiper.activeIndex > swiper.previousIndex ? 'next' : 'prev';
      } else {
        newThumbsIndex = swiper.realIndex;
        direction = newThumbsIndex > swiper.previousIndex ? 'next' : 'prev';
      }
      if (useOffset) {
        newThumbsIndex += direction === 'next' ? autoScrollOffset : -1 * autoScrollOffset;
      }
      if (thumbsSwiper.visibleSlidesIndexes && thumbsSwiper.visibleSlidesIndexes.indexOf(newThumbsIndex) < 0) {
        if (thumbsSwiper.params.centeredSlides) {
          if (newThumbsIndex > currentThumbsIndex) {
            newThumbsIndex = newThumbsIndex - Math.floor(slidesPerView / 2) + 1;
          } else {
            newThumbsIndex = newThumbsIndex + Math.floor(slidesPerView / 2) - 1;
          }
        } else if (newThumbsIndex > currentThumbsIndex && thumbsSwiper.params.slidesPerGroup === 1) ;
        thumbsSwiper.slideTo(newThumbsIndex, initial ? 0 : undefined);
      }
    }
  }
  on('beforeInit', () => {
    const {
      thumbs
    } = swiper.params;
    if (!thumbs || !thumbs.swiper) return;
    if (typeof thumbs.swiper === 'string' || thumbs.swiper instanceof HTMLElement) {
      const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
      const getThumbsElementAndInit = () => {
        const thumbsElement = typeof thumbs.swiper === 'string' ? document.querySelector(thumbs.swiper) : thumbs.swiper;
        if (thumbsElement && thumbsElement.swiper) {
          thumbs.swiper = thumbsElement.swiper;
          init();
          update(true);
        } else if (thumbsElement) {
          const eventName = `${swiper.params.eventsPrefix}init`;
          const onThumbsSwiper = e => {
            thumbs.swiper = e.detail[0];
            thumbsElement.removeEventListener(eventName, onThumbsSwiper);
            init();
            update(true);
            thumbs.swiper.update();
            swiper.update();
          };
          thumbsElement.addEventListener(eventName, onThumbsSwiper);
        }
        return thumbsElement;
      };
      const watchForThumbsToAppear = () => {
        if (swiper.destroyed) return;
        const thumbsElement = getThumbsElementAndInit();
        if (!thumbsElement) {
          requestAnimationFrame(watchForThumbsToAppear);
        }
      };
      requestAnimationFrame(watchForThumbsToAppear);
    } else {
      init();
      update(true);
    }
  });
  on('slideChange update resize observerUpdate', () => {
    update();
  });
  on('setTransition', (_s, duration) => {
    const thumbsSwiper = swiper.thumbs.swiper;
    if (!thumbsSwiper || thumbsSwiper.destroyed) return;
    thumbsSwiper.setTransition(duration);
  });
  on('beforeDestroy', () => {
    const thumbsSwiper = swiper.thumbs.swiper;
    if (!thumbsSwiper || thumbsSwiper.destroyed) return;
    if (swiperCreated) {
      thumbsSwiper.destroy();
    }
  });
  Object.assign(swiper.thumbs, {
    init,
    update
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/virtual.mjs":
/*!**************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/virtual.mjs ***!
  \**************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ Virtual)
/* harmony export */ });
/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ "../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");



function Virtual(_ref) {
  let {
    swiper,
    extendParams,
    on,
    emit
  } = _ref;
  extendParams({
    virtual: {
      enabled: false,
      slides: [],
      cache: true,
      renderSlide: null,
      renderExternal: null,
      renderExternalUpdate: true,
      addSlidesBefore: 0,
      addSlidesAfter: 0
    }
  });
  let cssModeTimeout;
  const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
  swiper.virtual = {
    cache: {},
    from: undefined,
    to: undefined,
    slides: [],
    offset: 0,
    slidesGrid: []
  };
  const tempDOM = document.createElement('div');
  function renderSlide(slide, index) {
    const params = swiper.params.virtual;
    if (params.cache && swiper.virtual.cache[index]) {
      return swiper.virtual.cache[index];
    }
    // eslint-disable-next-line
    let slideEl;
    if (params.renderSlide) {
      slideEl = params.renderSlide.call(swiper, slide, index);
      if (typeof slideEl === 'string') {
        tempDOM.innerHTML = slideEl;
        slideEl = tempDOM.children[0];
      }
    } else if (swiper.isElement) {
      slideEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('swiper-slide');
    } else {
      slideEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', swiper.params.slideClass);
    }
    slideEl.setAttribute('data-swiper-slide-index', index);
    if (!params.renderSlide) {
      slideEl.innerHTML = slide;
    }
    if (params.cache) {
      swiper.virtual.cache[index] = slideEl;
    }
    return slideEl;
  }
  function update(force, beforeInit) {
    const {
      slidesPerView,
      slidesPerGroup,
      centeredSlides,
      loop: isLoop,
      initialSlide
    } = swiper.params;
    if (beforeInit && !isLoop && initialSlide > 0) {
      return;
    }
    const {
      addSlidesBefore,
      addSlidesAfter
    } = swiper.params.virtual;
    const {
      from: previousFrom,
      to: previousTo,
      slides,
      slidesGrid: previousSlidesGrid,
      offset: previousOffset
    } = swiper.virtual;
    if (!swiper.params.cssMode) {
      swiper.updateActiveIndex();
    }
    const activeIndex = swiper.activeIndex || 0;
    let offsetProp;
    if (swiper.rtlTranslate) offsetProp = 'right';else offsetProp = swiper.isHorizontal() ? 'left' : 'top';
    let slidesAfter;
    let slidesBefore;
    if (centeredSlides) {
      slidesAfter = Math.floor(slidesPerView / 2) + slidesPerGroup + addSlidesAfter;
      slidesBefore = Math.floor(slidesPerView / 2) + slidesPerGroup + addSlidesBefore;
    } else {
      slidesAfter = slidesPerView + (slidesPerGroup - 1) + addSlidesAfter;
      slidesBefore = (isLoop ? slidesPerView : slidesPerGroup) + addSlidesBefore;
    }
    let from = activeIndex - slidesBefore;
    let to = activeIndex + slidesAfter;
    if (!isLoop) {
      from = Math.max(from, 0);
      to = Math.min(to, slides.length - 1);
    }
    let offset = (swiper.slidesGrid[from] || 0) - (swiper.slidesGrid[0] || 0);
    if (isLoop && activeIndex >= slidesBefore) {
      from -= slidesBefore;
      if (!centeredSlides) offset += swiper.slidesGrid[0];
    } else if (isLoop && activeIndex < slidesBefore) {
      from = -slidesBefore;
      if (centeredSlides) offset += swiper.slidesGrid[0];
    }
    Object.assign(swiper.virtual, {
      from,
      to,
      offset,
      slidesGrid: swiper.slidesGrid,
      slidesBefore,
      slidesAfter
    });
    function onRendered() {
      swiper.updateSlides();
      swiper.updateProgress();
      swiper.updateSlidesClasses();
      emit('virtualUpdate');
    }
    if (previousFrom === from && previousTo === to && !force) {
      if (swiper.slidesGrid !== previousSlidesGrid && offset !== previousOffset) {
        swiper.slides.forEach(slideEl => {
          slideEl.style[offsetProp] = `${offset - Math.abs(swiper.cssOverflowAdjustment())}px`;
        });
      }
      swiper.updateProgress();
      emit('virtualUpdate');
      return;
    }
    if (swiper.params.virtual.renderExternal) {
      swiper.params.virtual.renderExternal.call(swiper, {
        offset,
        from,
        to,
        slides: function getSlides() {
          const slidesToRender = [];
          for (let i = from; i <= to; i += 1) {
            slidesToRender.push(slides[i]);
          }
          return slidesToRender;
        }()
      });
      if (swiper.params.virtual.renderExternalUpdate) {
        onRendered();
      } else {
        emit('virtualUpdate');
      }
      return;
    }
    const prependIndexes = [];
    const appendIndexes = [];
    const getSlideIndex = index => {
      let slideIndex = index;
      if (index < 0) {
        slideIndex = slides.length + index;
      } else if (slideIndex >= slides.length) {
        // eslint-disable-next-line
        slideIndex = slideIndex - slides.length;
      }
      return slideIndex;
    };
    if (force) {
      swiper.slides.filter(el => el.matches(`.${swiper.params.slideClass}, swiper-slide`)).forEach(slideEl => {
        slideEl.remove();
      });
    } else {
      for (let i = previousFrom; i <= previousTo; i += 1) {
        if (i < from || i > to) {
          const slideIndex = getSlideIndex(i);
          swiper.slides.filter(el => el.matches(`.${swiper.params.slideClass}[data-swiper-slide-index="${slideIndex}"], swiper-slide[data-swiper-slide-index="${slideIndex}"]`)).forEach(slideEl => {
            slideEl.remove();
          });
        }
      }
    }
    const loopFrom = isLoop ? -slides.length : 0;
    const loopTo = isLoop ? slides.length * 2 : slides.length;
    for (let i = loopFrom; i < loopTo; i += 1) {
      if (i >= from && i <= to) {
        const slideIndex = getSlideIndex(i);
        if (typeof previousTo === 'undefined' || force) {
          appendIndexes.push(slideIndex);
        } else {
          if (i > previousTo) appendIndexes.push(slideIndex);
          if (i < previousFrom) prependIndexes.push(slideIndex);
        }
      }
    }
    appendIndexes.forEach(index => {
      swiper.slidesEl.append(renderSlide(slides[index], index));
    });
    if (isLoop) {
      for (let i = prependIndexes.length - 1; i >= 0; i -= 1) {
        const index = prependIndexes[i];
        swiper.slidesEl.prepend(renderSlide(slides[index], index));
      }
    } else {
      prependIndexes.sort((a, b) => b - a);
      prependIndexes.forEach(index => {
        swiper.slidesEl.prepend(renderSlide(slides[index], index));
      });
    }
    (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(swiper.slidesEl, '.swiper-slide, swiper-slide').forEach(slideEl => {
      slideEl.style[offsetProp] = `${offset - Math.abs(swiper.cssOverflowAdjustment())}px`;
    });
    onRendered();
  }
  function appendSlide(slides) {
    if (typeof slides === 'object' && 'length' in slides) {
      for (let i = 0; i < slides.length; i += 1) {
        if (slides[i]) swiper.virtual.slides.push(slides[i]);
      }
    } else {
      swiper.virtual.slides.push(slides);
    }
    update(true);
  }
  function prependSlide(slides) {
    const activeIndex = swiper.activeIndex;
    let newActiveIndex = activeIndex + 1;
    let numberOfNewSlides = 1;
    if (Array.isArray(slides)) {
      for (let i = 0; i < slides.length; i += 1) {
        if (slides[i]) swiper.virtual.slides.unshift(slides[i]);
      }
      newActiveIndex = activeIndex + slides.length;
      numberOfNewSlides = slides.length;
    } else {
      swiper.virtual.slides.unshift(slides);
    }
    if (swiper.params.virtual.cache) {
      const cache = swiper.virtual.cache;
      const newCache = {};
      Object.keys(cache).forEach(cachedIndex => {
        const cachedEl = cache[cachedIndex];
        const cachedElIndex = cachedEl.getAttribute('data-swiper-slide-index');
        if (cachedElIndex) {
          cachedEl.setAttribute('data-swiper-slide-index', parseInt(cachedElIndex, 10) + numberOfNewSlides);
        }
        newCache[parseInt(cachedIndex, 10) + numberOfNewSlides] = cachedEl;
      });
      swiper.virtual.cache = newCache;
    }
    update(true);
    swiper.slideTo(newActiveIndex, 0);
  }
  function removeSlide(slidesIndexes) {
    if (typeof slidesIndexes === 'undefined' || slidesIndexes === null) return;
    let activeIndex = swiper.activeIndex;
    if (Array.isArray(slidesIndexes)) {
      for (let i = slidesIndexes.length - 1; i >= 0; i -= 1) {
        if (swiper.params.virtual.cache) {
          delete swiper.virtual.cache[slidesIndexes[i]];
          // shift cache indexes
          Object.keys(swiper.virtual.cache).forEach(key => {
            if (key > slidesIndexes) {
              swiper.virtual.cache[key - 1] = swiper.virtual.cache[key];
              swiper.virtual.cache[key - 1].setAttribute('data-swiper-slide-index', key - 1);
              delete swiper.virtual.cache[key];
            }
          });
        }
        swiper.virtual.slides.splice(slidesIndexes[i], 1);
        if (slidesIndexes[i] < activeIndex) activeIndex -= 1;
        activeIndex = Math.max(activeIndex, 0);
      }
    } else {
      if (swiper.params.virtual.cache) {
        delete swiper.virtual.cache[slidesIndexes];
        // shift cache indexes
        Object.keys(swiper.virtual.cache).forEach(key => {
          if (key > slidesIndexes) {
            swiper.virtual.cache[key - 1] = swiper.virtual.cache[key];
            swiper.virtual.cache[key - 1].setAttribute('data-swiper-slide-index', key - 1);
            delete swiper.virtual.cache[key];
          }
        });
      }
      swiper.virtual.slides.splice(slidesIndexes, 1);
      if (slidesIndexes < activeIndex) activeIndex -= 1;
      activeIndex = Math.max(activeIndex, 0);
    }
    update(true);
    swiper.slideTo(activeIndex, 0);
  }
  function removeAllSlides() {
    swiper.virtual.slides = [];
    if (swiper.params.virtual.cache) {
      swiper.virtual.cache = {};
    }
    update(true);
    swiper.slideTo(0, 0);
  }
  on('beforeInit', () => {
    if (!swiper.params.virtual.enabled) return;
    let domSlidesAssigned;
    if (typeof swiper.passedParams.virtual.slides === 'undefined') {
      const slides = [...swiper.slidesEl.children].filter(el => el.matches(`.${swiper.params.slideClass}, swiper-slide`));
      if (slides && slides.length) {
        swiper.virtual.slides = [...slides];
        domSlidesAssigned = true;
        slides.forEach((slideEl, slideIndex) => {
          slideEl.setAttribute('data-swiper-slide-index', slideIndex);
          swiper.virtual.cache[slideIndex] = slideEl;
          slideEl.remove();
        });
      }
    }
    if (!domSlidesAssigned) {
      swiper.virtual.slides = swiper.params.virtual.slides;
    }
    swiper.classNames.push(`${swiper.params.containerModifierClass}virtual`);
    swiper.params.watchSlidesProgress = true;
    swiper.originalParams.watchSlidesProgress = true;
    update(false, true);
  });
  on('setTranslate', () => {
    if (!swiper.params.virtual.enabled) return;
    if (swiper.params.cssMode && !swiper._immediateVirtual) {
      clearTimeout(cssModeTimeout);
      cssModeTimeout = setTimeout(() => {
        update();
      }, 100);
    } else {
      update();
    }
  });
  on('init update resize', () => {
    if (!swiper.params.virtual.enabled) return;
    if (swiper.params.cssMode) {
      (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.s)(swiper.wrapperEl, '--swiper-virtual-size', `${swiper.virtualSize}px`);
    }
  });
  Object.assign(swiper.virtual, {
    appendSlide,
    prependSlide,
    removeSlide,
    removeAllSlides,
    update
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/modules/zoom.mjs":
/*!***********************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/modules/zoom.mjs ***!
  \***********************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ Zoom)
/* harmony export */ });
/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ "../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs");
/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");



function Zoom(_ref) {
  let {
    swiper,
    extendParams,
    on,
    emit
  } = _ref;
  const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  extendParams({
    zoom: {
      enabled: false,
      limitToOriginalSize: false,
      maxRatio: 3,
      minRatio: 1,
      panOnMouseMove: false,
      toggle: true,
      containerClass: 'swiper-zoom-container',
      zoomedSlideClass: 'swiper-slide-zoomed'
    }
  });
  swiper.zoom = {
    enabled: false
  };
  let currentScale = 1;
  let isScaling = false;
  let isPanningWithMouse = false;
  let mousePanStart = {
    x: 0,
    y: 0
  };
  const mousePanSensitivity = -3; // Negative to invert pan direction
  let fakeGestureTouched;
  let fakeGestureMoved;
  const evCache = [];
  const gesture = {
    originX: 0,
    originY: 0,
    slideEl: undefined,
    slideWidth: undefined,
    slideHeight: undefined,
    imageEl: undefined,
    imageWrapEl: undefined,
    maxRatio: 3
  };
  const image = {
    isTouched: undefined,
    isMoved: undefined,
    currentX: undefined,
    currentY: undefined,
    minX: undefined,
    minY: undefined,
    maxX: undefined,
    maxY: undefined,
    width: undefined,
    height: undefined,
    startX: undefined,
    startY: undefined,
    touchesStart: {},
    touchesCurrent: {}
  };
  const velocity = {
    x: undefined,
    y: undefined,
    prevPositionX: undefined,
    prevPositionY: undefined,
    prevTime: undefined
  };
  let scale = 1;
  Object.defineProperty(swiper.zoom, 'scale', {
    get() {
      return scale;
    },
    set(value) {
      if (scale !== value) {
        const imageEl = gesture.imageEl;
        const slideEl = gesture.slideEl;
        emit('zoomChange', value, imageEl, slideEl);
      }
      scale = value;
    }
  });
  function getDistanceBetweenTouches() {
    if (evCache.length < 2) return 1;
    const x1 = evCache[0].pageX;
    const y1 = evCache[0].pageY;
    const x2 = evCache[1].pageX;
    const y2 = evCache[1].pageY;
    const distance = Math.sqrt((x2 - x1) ** 2 + (y2 - y1) ** 2);
    return distance;
  }
  function getMaxRatio() {
    const params = swiper.params.zoom;
    const maxRatio = gesture.imageWrapEl.getAttribute('data-swiper-zoom') || params.maxRatio;
    if (params.limitToOriginalSize && gesture.imageEl && gesture.imageEl.naturalWidth) {
      const imageMaxRatio = gesture.imageEl.naturalWidth / gesture.imageEl.offsetWidth;
      return Math.min(imageMaxRatio, maxRatio);
    }
    return maxRatio;
  }
  function getScaleOrigin() {
    if (evCache.length < 2) return {
      x: null,
      y: null
    };
    const box = gesture.imageEl.getBoundingClientRect();
    return [(evCache[0].pageX + (evCache[1].pageX - evCache[0].pageX) / 2 - box.x - window.scrollX) / currentScale, (evCache[0].pageY + (evCache[1].pageY - evCache[0].pageY) / 2 - box.y - window.scrollY) / currentScale];
  }
  function getSlideSelector() {
    return swiper.isElement ? `swiper-slide` : `.${swiper.params.slideClass}`;
  }
  function eventWithinSlide(e) {
    const slideSelector = getSlideSelector();
    if (e.target.matches(slideSelector)) return true;
    if (swiper.slides.filter(slideEl => slideEl.contains(e.target)).length > 0) return true;
    return false;
  }
  function eventWithinZoomContainer(e) {
    const selector = `.${swiper.params.zoom.containerClass}`;
    if (e.target.matches(selector)) return true;
    if ([...swiper.hostEl.querySelectorAll(selector)].filter(containerEl => containerEl.contains(e.target)).length > 0) return true;
    return false;
  }

  // Events
  function onGestureStart(e) {
    if (e.pointerType === 'mouse') {
      evCache.splice(0, evCache.length);
    }
    if (!eventWithinSlide(e)) return;
    const params = swiper.params.zoom;
    fakeGestureTouched = false;
    fakeGestureMoved = false;
    evCache.push(e);
    if (evCache.length < 2) {
      return;
    }
    fakeGestureTouched = true;
    gesture.scaleStart = getDistanceBetweenTouches();
    if (!gesture.slideEl) {
      gesture.slideEl = e.target.closest(`.${swiper.params.slideClass}, swiper-slide`);
      if (!gesture.slideEl) gesture.slideEl = swiper.slides[swiper.activeIndex];
      let imageEl = gesture.slideEl.querySelector(`.${params.containerClass}`);
      if (imageEl) {
        imageEl = imageEl.querySelectorAll('picture, img, svg, canvas, .swiper-zoom-target')[0];
      }
      gesture.imageEl = imageEl;
      if (imageEl) {
        gesture.imageWrapEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.a)(gesture.imageEl, `.${params.containerClass}`)[0];
      } else {
        gesture.imageWrapEl = undefined;
      }
      if (!gesture.imageWrapEl) {
        gesture.imageEl = undefined;
        return;
      }
      gesture.maxRatio = getMaxRatio();
    }
    if (gesture.imageEl) {
      const [originX, originY] = getScaleOrigin();
      gesture.originX = originX;
      gesture.originY = originY;
      gesture.imageEl.style.transitionDuration = '0ms';
    }
    isScaling = true;
  }
  function onGestureChange(e) {
    if (!eventWithinSlide(e)) return;
    const params = swiper.params.zoom;
    const zoom = swiper.zoom;
    const pointerIndex = evCache.findIndex(cachedEv => cachedEv.pointerId === e.pointerId);
    if (pointerIndex >= 0) evCache[pointerIndex] = e;
    if (evCache.length < 2) {
      return;
    }
    fakeGestureMoved = true;
    gesture.scaleMove = getDistanceBetweenTouches();
    if (!gesture.imageEl) {
      return;
    }
    zoom.scale = gesture.scaleMove / gesture.scaleStart * currentScale;
    if (zoom.scale > gesture.maxRatio) {
      zoom.scale = gesture.maxRatio - 1 + (zoom.scale - gesture.maxRatio + 1) ** 0.5;
    }
    if (zoom.scale < params.minRatio) {
      zoom.scale = params.minRatio + 1 - (params.minRatio - zoom.scale + 1) ** 0.5;
    }
    gesture.imageEl.style.transform = `translate3d(0,0,0) scale(${zoom.scale})`;
  }
  function onGestureEnd(e) {
    if (!eventWithinSlide(e)) return;
    if (e.pointerType === 'mouse' && e.type === 'pointerout') return;
    const params = swiper.params.zoom;
    const zoom = swiper.zoom;
    const pointerIndex = evCache.findIndex(cachedEv => cachedEv.pointerId === e.pointerId);
    if (pointerIndex >= 0) evCache.splice(pointerIndex, 1);
    if (!fakeGestureTouched || !fakeGestureMoved) {
      return;
    }
    fakeGestureTouched = false;
    fakeGestureMoved = false;
    if (!gesture.imageEl) return;
    zoom.scale = Math.max(Math.min(zoom.scale, gesture.maxRatio), params.minRatio);
    gesture.imageEl.style.transitionDuration = `${swiper.params.speed}ms`;
    gesture.imageEl.style.transform = `translate3d(0,0,0) scale(${zoom.scale})`;
    currentScale = zoom.scale;
    isScaling = false;
    if (zoom.scale > 1 && gesture.slideEl) {
      gesture.slideEl.classList.add(`${params.zoomedSlideClass}`);
    } else if (zoom.scale <= 1 && gesture.slideEl) {
      gesture.slideEl.classList.remove(`${params.zoomedSlideClass}`);
    }
    if (zoom.scale === 1) {
      gesture.originX = 0;
      gesture.originY = 0;
      gesture.slideEl = undefined;
    }
  }
  let allowTouchMoveTimeout;
  function allowTouchMove() {
    swiper.touchEventsData.preventTouchMoveFromPointerMove = false;
  }
  function preventTouchMove() {
    clearTimeout(allowTouchMoveTimeout);
    swiper.touchEventsData.preventTouchMoveFromPointerMove = true;
    allowTouchMoveTimeout = setTimeout(() => {
      if (swiper.destroyed) return;
      allowTouchMove();
    });
  }
  function onTouchStart(e) {
    const device = swiper.device;
    if (!gesture.imageEl) return;
    if (image.isTouched) return;
    if (device.android && e.cancelable) e.preventDefault();
    image.isTouched = true;
    const event = evCache.length > 0 ? evCache[0] : e;
    image.touchesStart.x = event.pageX;
    image.touchesStart.y = event.pageY;
  }
  function onTouchMove(e) {
    const isMouseEvent = e.pointerType === 'mouse';
    const isMousePan = isMouseEvent && swiper.params.zoom.panOnMouseMove;
    if (!eventWithinSlide(e) || !eventWithinZoomContainer(e)) {
      return;
    }
    const zoom = swiper.zoom;
    if (!gesture.imageEl) {
      return;
    }
    if (!image.isTouched || !gesture.slideEl) {
      if (isMousePan) onMouseMove(e);
      return;
    }
    if (isMousePan) {
      onMouseMove(e);
      return;
    }
    if (!image.isMoved) {
      image.width = gesture.imageEl.offsetWidth || gesture.imageEl.clientWidth;
      image.height = gesture.imageEl.offsetHeight || gesture.imageEl.clientHeight;
      image.startX = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.j)(gesture.imageWrapEl, 'x') || 0;
      image.startY = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.j)(gesture.imageWrapEl, 'y') || 0;
      gesture.slideWidth = gesture.slideEl.offsetWidth;
      gesture.slideHeight = gesture.slideEl.offsetHeight;
      gesture.imageWrapEl.style.transitionDuration = '0ms';
    }
    // Define if we need image drag
    const scaledWidth = image.width * zoom.scale;
    const scaledHeight = image.height * zoom.scale;
    image.minX = Math.min(gesture.slideWidth / 2 - scaledWidth / 2, 0);
    image.maxX = -image.minX;
    image.minY = Math.min(gesture.slideHeight / 2 - scaledHeight / 2, 0);
    image.maxY = -image.minY;
    image.touchesCurrent.x = evCache.length > 0 ? evCache[0].pageX : e.pageX;
    image.touchesCurrent.y = evCache.length > 0 ? evCache[0].pageY : e.pageY;
    const touchesDiff = Math.max(Math.abs(image.touchesCurrent.x - image.touchesStart.x), Math.abs(image.touchesCurrent.y - image.touchesStart.y));
    if (touchesDiff > 5) {
      swiper.allowClick = false;
    }
    if (!image.isMoved && !isScaling) {
      if (swiper.isHorizontal() && (Math.floor(image.minX) === Math.floor(image.startX) && image.touchesCurrent.x < image.touchesStart.x || Math.floor(image.maxX) === Math.floor(image.startX) && image.touchesCurrent.x > image.touchesStart.x)) {
        image.isTouched = false;
        allowTouchMove();
        return;
      }
      if (!swiper.isHorizontal() && (Math.floor(image.minY) === Math.floor(image.startY) && image.touchesCurrent.y < image.touchesStart.y || Math.floor(image.maxY) === Math.floor(image.startY) && image.touchesCurrent.y > image.touchesStart.y)) {
        image.isTouched = false;
        allowTouchMove();
        return;
      }
    }
    if (e.cancelable) {
      e.preventDefault();
    }
    e.stopPropagation();
    preventTouchMove();
    image.isMoved = true;
    const scaleRatio = (zoom.scale - currentScale) / (gesture.maxRatio - swiper.params.zoom.minRatio);
    const {
      originX,
      originY
    } = gesture;
    image.currentX = image.touchesCurrent.x - image.touchesStart.x + image.startX + scaleRatio * (image.width - originX * 2);
    image.currentY = image.touchesCurrent.y - image.touchesStart.y + image.startY + scaleRatio * (image.height - originY * 2);
    if (image.currentX < image.minX) {
      image.currentX = image.minX + 1 - (image.minX - image.currentX + 1) ** 0.8;
    }
    if (image.currentX > image.maxX) {
      image.currentX = image.maxX - 1 + (image.currentX - image.maxX + 1) ** 0.8;
    }
    if (image.currentY < image.minY) {
      image.currentY = image.minY + 1 - (image.minY - image.currentY + 1) ** 0.8;
    }
    if (image.currentY > image.maxY) {
      image.currentY = image.maxY - 1 + (image.currentY - image.maxY + 1) ** 0.8;
    }

    // Velocity
    if (!velocity.prevPositionX) velocity.prevPositionX = image.touchesCurrent.x;
    if (!velocity.prevPositionY) velocity.prevPositionY = image.touchesCurrent.y;
    if (!velocity.prevTime) velocity.prevTime = Date.now();
    velocity.x = (image.touchesCurrent.x - velocity.prevPositionX) / (Date.now() - velocity.prevTime) / 2;
    velocity.y = (image.touchesCurrent.y - velocity.prevPositionY) / (Date.now() - velocity.prevTime) / 2;
    if (Math.abs(image.touchesCurrent.x - velocity.prevPositionX) < 2) velocity.x = 0;
    if (Math.abs(image.touchesCurrent.y - velocity.prevPositionY) < 2) velocity.y = 0;
    velocity.prevPositionX = image.touchesCurrent.x;
    velocity.prevPositionY = image.touchesCurrent.y;
    velocity.prevTime = Date.now();
    gesture.imageWrapEl.style.transform = `translate3d(${image.currentX}px, ${image.currentY}px,0)`;
  }
  function onTouchEnd() {
    const zoom = swiper.zoom;
    evCache.length = 0;
    if (!gesture.imageEl) return;
    if (!image.isTouched || !image.isMoved) {
      image.isTouched = false;
      image.isMoved = false;
      return;
    }
    image.isTouched = false;
    image.isMoved = false;
    let momentumDurationX = 300;
    let momentumDurationY = 300;
    const momentumDistanceX = velocity.x * momentumDurationX;
    const newPositionX = image.currentX + momentumDistanceX;
    const momentumDistanceY = velocity.y * momentumDurationY;
    const newPositionY = image.currentY + momentumDistanceY;

    // Fix duration
    if (velocity.x !== 0) momentumDurationX = Math.abs((newPositionX - image.currentX) / velocity.x);
    if (velocity.y !== 0) momentumDurationY = Math.abs((newPositionY - image.currentY) / velocity.y);
    const momentumDuration = Math.max(momentumDurationX, momentumDurationY);
    image.currentX = newPositionX;
    image.currentY = newPositionY;
    // Define if we need image drag
    const scaledWidth = image.width * zoom.scale;
    const scaledHeight = image.height * zoom.scale;
    image.minX = Math.min(gesture.slideWidth / 2 - scaledWidth / 2, 0);
    image.maxX = -image.minX;
    image.minY = Math.min(gesture.slideHeight / 2 - scaledHeight / 2, 0);
    image.maxY = -image.minY;
    image.currentX = Math.max(Math.min(image.currentX, image.maxX), image.minX);
    image.currentY = Math.max(Math.min(image.currentY, image.maxY), image.minY);
    gesture.imageWrapEl.style.transitionDuration = `${momentumDuration}ms`;
    gesture.imageWrapEl.style.transform = `translate3d(${image.currentX}px, ${image.currentY}px,0)`;
  }
  function onTransitionEnd() {
    const zoom = swiper.zoom;
    if (gesture.slideEl && swiper.activeIndex !== swiper.slides.indexOf(gesture.slideEl)) {
      if (gesture.imageEl) {
        gesture.imageEl.style.transform = 'translate3d(0,0,0) scale(1)';
      }
      if (gesture.imageWrapEl) {
        gesture.imageWrapEl.style.transform = 'translate3d(0,0,0)';
      }
      gesture.slideEl.classList.remove(`${swiper.params.zoom.zoomedSlideClass}`);
      zoom.scale = 1;
      currentScale = 1;
      gesture.slideEl = undefined;
      gesture.imageEl = undefined;
      gesture.imageWrapEl = undefined;
      gesture.originX = 0;
      gesture.originY = 0;
    }
  }
  function onMouseMove(e) {
    // Only pan if zoomed in and mouse panning is enabled
    if (currentScale <= 1 || !gesture.imageWrapEl) return;
    if (!eventWithinSlide(e) || !eventWithinZoomContainer(e)) return;
    const currentTransform = window.getComputedStyle(gesture.imageWrapEl).transform;
    const matrix = new window.DOMMatrix(currentTransform);
    if (!isPanningWithMouse) {
      isPanningWithMouse = true;
      mousePanStart.x = e.clientX;
      mousePanStart.y = e.clientY;
      image.startX = matrix.e;
      image.startY = matrix.f;
      image.width = gesture.imageEl.offsetWidth || gesture.imageEl.clientWidth;
      image.height = gesture.imageEl.offsetHeight || gesture.imageEl.clientHeight;
      gesture.slideWidth = gesture.slideEl.offsetWidth;
      gesture.slideHeight = gesture.slideEl.offsetHeight;
      return;
    }
    const deltaX = (e.clientX - mousePanStart.x) * mousePanSensitivity;
    const deltaY = (e.clientY - mousePanStart.y) * mousePanSensitivity;
    const scaledWidth = image.width * currentScale;
    const scaledHeight = image.height * currentScale;
    const slideWidth = gesture.slideWidth;
    const slideHeight = gesture.slideHeight;
    const minX = Math.min(slideWidth / 2 - scaledWidth / 2, 0);
    const maxX = -minX;
    const minY = Math.min(slideHeight / 2 - scaledHeight / 2, 0);
    const maxY = -minY;
    const newX = Math.max(Math.min(image.startX + deltaX, maxX), minX);
    const newY = Math.max(Math.min(image.startY + deltaY, maxY), minY);
    gesture.imageWrapEl.style.transitionDuration = '0ms';
    gesture.imageWrapEl.style.transform = `translate3d(${newX}px, ${newY}px, 0)`;
    mousePanStart.x = e.clientX;
    mousePanStart.y = e.clientY;
    image.startX = newX;
    image.startY = newY;
  }
  function zoomIn(e) {
    const zoom = swiper.zoom;
    const params = swiper.params.zoom;
    if (!gesture.slideEl) {
      if (e && e.target) {
        gesture.slideEl = e.target.closest(`.${swiper.params.slideClass}, swiper-slide`);
      }
      if (!gesture.slideEl) {
        if (swiper.params.virtual && swiper.params.virtual.enabled && swiper.virtual) {
          gesture.slideEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(swiper.slidesEl, `.${swiper.params.slideActiveClass}`)[0];
        } else {
          gesture.slideEl = swiper.slides[swiper.activeIndex];
        }
      }
      let imageEl = gesture.slideEl.querySelector(`.${params.containerClass}`);
      if (imageEl) {
        imageEl = imageEl.querySelectorAll('picture, img, svg, canvas, .swiper-zoom-target')[0];
      }
      gesture.imageEl = imageEl;
      if (imageEl) {
        gesture.imageWrapEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.a)(gesture.imageEl, `.${params.containerClass}`)[0];
      } else {
        gesture.imageWrapEl = undefined;
      }
    }
    if (!gesture.imageEl || !gesture.imageWrapEl) return;
    if (swiper.params.cssMode) {
      swiper.wrapperEl.style.overflow = 'hidden';
      swiper.wrapperEl.style.touchAction = 'none';
    }
    gesture.slideEl.classList.add(`${params.zoomedSlideClass}`);
    let touchX;
    let touchY;
    let offsetX;
    let offsetY;
    let diffX;
    let diffY;
    let translateX;
    let translateY;
    let imageWidth;
    let imageHeight;
    let scaledWidth;
    let scaledHeight;
    let translateMinX;
    let translateMinY;
    let translateMaxX;
    let translateMaxY;
    let slideWidth;
    let slideHeight;
    if (typeof image.touchesStart.x === 'undefined' && e) {
      touchX = e.pageX;
      touchY = e.pageY;
    } else {
      touchX = image.touchesStart.x;
      touchY = image.touchesStart.y;
    }
    const forceZoomRatio = typeof e === 'number' ? e : null;
    if (currentScale === 1 && forceZoomRatio) {
      touchX = undefined;
      touchY = undefined;
      image.touchesStart.x = undefined;
      image.touchesStart.y = undefined;
    }
    const maxRatio = getMaxRatio();
    zoom.scale = forceZoomRatio || maxRatio;
    currentScale = forceZoomRatio || maxRatio;
    if (e && !(currentScale === 1 && forceZoomRatio)) {
      slideWidth = gesture.slideEl.offsetWidth;
      slideHeight = gesture.slideEl.offsetHeight;
      offsetX = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.b)(gesture.slideEl).left + window.scrollX;
      offsetY = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.b)(gesture.slideEl).top + window.scrollY;
      diffX = offsetX + slideWidth / 2 - touchX;
      diffY = offsetY + slideHeight / 2 - touchY;
      imageWidth = gesture.imageEl.offsetWidth || gesture.imageEl.clientWidth;
      imageHeight = gesture.imageEl.offsetHeight || gesture.imageEl.clientHeight;
      scaledWidth = imageWidth * zoom.scale;
      scaledHeight = imageHeight * zoom.scale;
      translateMinX = Math.min(slideWidth / 2 - scaledWidth / 2, 0);
      translateMinY = Math.min(slideHeight / 2 - scaledHeight / 2, 0);
      translateMaxX = -translateMinX;
      translateMaxY = -translateMinY;
      translateX = diffX * zoom.scale;
      translateY = diffY * zoom.scale;
      if (translateX < translateMinX) {
        translateX = translateMinX;
      }
      if (translateX > translateMaxX) {
        translateX = translateMaxX;
      }
      if (translateY < translateMinY) {
        translateY = translateMinY;
      }
      if (translateY > translateMaxY) {
        translateY = translateMaxY;
      }
    } else {
      translateX = 0;
      translateY = 0;
    }
    if (forceZoomRatio && zoom.scale === 1) {
      gesture.originX = 0;
      gesture.originY = 0;
    }
    gesture.imageWrapEl.style.transitionDuration = '300ms';
    gesture.imageWrapEl.style.transform = `translate3d(${translateX}px, ${translateY}px,0)`;
    gesture.imageEl.style.transitionDuration = '300ms';
    gesture.imageEl.style.transform = `translate3d(0,0,0) scale(${zoom.scale})`;
  }
  function zoomOut() {
    const zoom = swiper.zoom;
    const params = swiper.params.zoom;
    if (!gesture.slideEl) {
      if (swiper.params.virtual && swiper.params.virtual.enabled && swiper.virtual) {
        gesture.slideEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(swiper.slidesEl, `.${swiper.params.slideActiveClass}`)[0];
      } else {
        gesture.slideEl = swiper.slides[swiper.activeIndex];
      }
      let imageEl = gesture.slideEl.querySelector(`.${params.containerClass}`);
      if (imageEl) {
        imageEl = imageEl.querySelectorAll('picture, img, svg, canvas, .swiper-zoom-target')[0];
      }
      gesture.imageEl = imageEl;
      if (imageEl) {
        gesture.imageWrapEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.a)(gesture.imageEl, `.${params.containerClass}`)[0];
      } else {
        gesture.imageWrapEl = undefined;
      }
    }
    if (!gesture.imageEl || !gesture.imageWrapEl) return;
    if (swiper.params.cssMode) {
      swiper.wrapperEl.style.overflow = '';
      swiper.wrapperEl.style.touchAction = '';
    }
    zoom.scale = 1;
    currentScale = 1;
    image.touchesStart.x = undefined;
    image.touchesStart.y = undefined;
    gesture.imageWrapEl.style.transitionDuration = '300ms';
    gesture.imageWrapEl.style.transform = 'translate3d(0,0,0)';
    gesture.imageEl.style.transitionDuration = '300ms';
    gesture.imageEl.style.transform = 'translate3d(0,0,0) scale(1)';
    gesture.slideEl.classList.remove(`${params.zoomedSlideClass}`);
    gesture.slideEl = undefined;
    gesture.originX = 0;
    gesture.originY = 0;
    if (swiper.params.zoom.panOnMouseMove) {
      mousePanStart = {
        x: 0,
        y: 0
      };
      if (isPanningWithMouse) {
        isPanningWithMouse = false;
        image.startX = 0;
        image.startY = 0;
      }
    }
  }

  // Toggle Zoom
  function zoomToggle(e) {
    const zoom = swiper.zoom;
    if (zoom.scale && zoom.scale !== 1) {
      // Zoom Out
      zoomOut();
    } else {
      // Zoom In
      zoomIn(e);
    }
  }
  function getListeners() {
    const passiveListener = swiper.params.passiveListeners ? {
      passive: true,
      capture: false
    } : false;
    const activeListenerWithCapture = swiper.params.passiveListeners ? {
      passive: false,
      capture: true
    } : true;
    return {
      passiveListener,
      activeListenerWithCapture
    };
  }

  // Attach/Detach Events
  function enable() {
    const zoom = swiper.zoom;
    if (zoom.enabled) return;
    zoom.enabled = true;
    const {
      passiveListener,
      activeListenerWithCapture
    } = getListeners();

    // Scale image
    swiper.wrapperEl.addEventListener('pointerdown', onGestureStart, passiveListener);
    swiper.wrapperEl.addEventListener('pointermove', onGestureChange, activeListenerWithCapture);
    ['pointerup', 'pointercancel', 'pointerout'].forEach(eventName => {
      swiper.wrapperEl.addEventListener(eventName, onGestureEnd, passiveListener);
    });

    // Move image
    swiper.wrapperEl.addEventListener('pointermove', onTouchMove, activeListenerWithCapture);
  }
  function disable() {
    const zoom = swiper.zoom;
    if (!zoom.enabled) return;
    zoom.enabled = false;
    const {
      passiveListener,
      activeListenerWithCapture
    } = getListeners();

    // Scale image
    swiper.wrapperEl.removeEventListener('pointerdown', onGestureStart, passiveListener);
    swiper.wrapperEl.removeEventListener('pointermove', onGestureChange, activeListenerWithCapture);
    ['pointerup', 'pointercancel', 'pointerout'].forEach(eventName => {
      swiper.wrapperEl.removeEventListener(eventName, onGestureEnd, passiveListener);
    });

    // Move image
    swiper.wrapperEl.removeEventListener('pointermove', onTouchMove, activeListenerWithCapture);
  }
  on('init', () => {
    if (swiper.params.zoom.enabled) {
      enable();
    }
  });
  on('destroy', () => {
    disable();
  });
  on('touchStart', (_s, e) => {
    if (!swiper.zoom.enabled) return;
    onTouchStart(e);
  });
  on('touchEnd', (_s, e) => {
    if (!swiper.zoom.enabled) return;
    onTouchEnd();
  });
  on('doubleTap', (_s, e) => {
    if (!swiper.animating && swiper.params.zoom.enabled && swiper.zoom.enabled && swiper.params.zoom.toggle) {
      zoomToggle(e);
    }
  });
  on('transitionEnd', () => {
    if (swiper.zoom.enabled && swiper.params.zoom.enabled) {
      onTransitionEnd();
    }
  });
  on('slideChange', () => {
    if (swiper.zoom.enabled && swiper.params.zoom.enabled && swiper.params.cssMode) {
      onTransitionEnd();
    }
  });
  Object.assign(swiper.zoom, {
    enable,
    disable,
    in: zoomIn,
    out: zoomOut,
    toggle: zoomToggle
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/shared/classes-to-selector.mjs":
/*!*************************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/shared/classes-to-selector.mjs ***!
  \*************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   c: () => (/* binding */ classesToSelector)
/* harmony export */ });
function classesToSelector(classes) {
  if (classes === void 0) {
    classes = '';
  }
  return `.${classes.trim().replace(/([\.:!+\/])/g, '\\$1') // eslint-disable-line
  .replace(/ /g, '.')}`;
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/shared/create-element-if-not-defined.mjs":
/*!***********************************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/shared/create-element-if-not-defined.mjs ***!
  \***********************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   c: () => (/* binding */ createElementIfNotDefined)
/* harmony export */ });
/* harmony import */ var _utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");


function createElementIfNotDefined(swiper, originalParams, params, checkProps) {
  if (swiper.params.createElements) {
    Object.keys(checkProps).forEach(key => {
      if (!params[key] && params.auto === true) {
        let element = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.e)(swiper.el, `.${checkProps[key]}`)[0];
        if (!element) {
          element = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('div', checkProps[key]);
          element.className = checkProps[key];
          swiper.el.append(element);
        }
        params[key] = element;
        originalParams[key] = element;
      }
    });
  }
  return params;
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/shared/create-shadow.mjs":
/*!*******************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/shared/create-shadow.mjs ***!
  \*******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   c: () => (/* binding */ createShadow)
/* harmony export */ });
/* harmony import */ var _utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");


function createShadow(suffix, slideEl, side) {
  const shadowClass = `swiper-slide-shadow${side ? `-${side}` : ''}${suffix ? ` swiper-slide-shadow-${suffix}` : ''}`;
  const shadowContainer = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.g)(slideEl);
  let shadowEl = shadowContainer.querySelector(`.${shadowClass.split(' ').join('.')}`);
  if (!shadowEl) {
    shadowEl = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('div', shadowClass.split(' '));
    shadowContainer.append(shadowEl);
  }
  return shadowEl;
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/shared/effect-init.mjs":
/*!*****************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/shared/effect-init.mjs ***!
  \*****************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   e: () => (/* binding */ effectInit)
/* harmony export */ });
function effectInit(params) {
  const {
    effect,
    swiper,
    on,
    setTranslate,
    setTransition,
    overwriteParams,
    perspective,
    recreateShadows,
    getEffectParams
  } = params;
  on('beforeInit', () => {
    if (swiper.params.effect !== effect) return;
    swiper.classNames.push(`${swiper.params.containerModifierClass}${effect}`);
    if (perspective && perspective()) {
      swiper.classNames.push(`${swiper.params.containerModifierClass}3d`);
    }
    const overwriteParamsResult = overwriteParams ? overwriteParams() : {};
    Object.assign(swiper.params, overwriteParamsResult);
    Object.assign(swiper.originalParams, overwriteParamsResult);
  });
  on('setTranslate', () => {
    if (swiper.params.effect !== effect) return;
    setTranslate();
  });
  on('setTransition', (_s, duration) => {
    if (swiper.params.effect !== effect) return;
    setTransition(duration);
  });
  on('transitionEnd', () => {
    if (swiper.params.effect !== effect) return;
    if (recreateShadows) {
      if (!getEffectParams || !getEffectParams().slideShadows) return;
      // remove shadows
      swiper.slides.forEach(slideEl => {
        slideEl.querySelectorAll('.swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left').forEach(shadowEl => shadowEl.remove());
      });
      // create new one
      recreateShadows();
    }
  });
  let requireUpdateOnVirtual;
  on('virtualUpdate', () => {
    if (swiper.params.effect !== effect) return;
    if (!swiper.slides.length) {
      requireUpdateOnVirtual = true;
    }
    requestAnimationFrame(() => {
      if (requireUpdateOnVirtual && swiper.slides && swiper.slides.length) {
        setTranslate();
        requireUpdateOnVirtual = false;
      }
    });
  });
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/shared/effect-target.mjs":
/*!*******************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/shared/effect-target.mjs ***!
  \*******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   e: () => (/* binding */ effectTarget)
/* harmony export */ });
/* harmony import */ var _utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");


function effectTarget(effectParams, slideEl) {
  const transformEl = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.g)(slideEl);
  if (transformEl !== slideEl) {
    transformEl.style.backfaceVisibility = 'hidden';
    transformEl.style['-webkit-backface-visibility'] = 'hidden';
  }
  return transformEl;
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/shared/effect-virtual-transition-end.mjs":
/*!***********************************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/shared/effect-virtual-transition-end.mjs ***!
  \***********************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   e: () => (/* binding */ effectVirtualTransitionEnd)
/* harmony export */ });
/* harmony import */ var _utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");


function effectVirtualTransitionEnd(_ref) {
  let {
    swiper,
    duration,
    transformElements,
    allSlides
  } = _ref;
  const {
    activeIndex
  } = swiper;
  const getSlide = el => {
    if (!el.parentElement) {
      // assume shadow root
      const slide = swiper.slides.find(slideEl => slideEl.shadowRoot && slideEl.shadowRoot === el.parentNode);
      return slide;
    }
    return el.parentElement;
  };
  if (swiper.params.virtualTranslate && duration !== 0) {
    let eventTriggered = false;
    let transitionEndTarget;
    if (allSlides) {
      transitionEndTarget = transformElements;
    } else {
      transitionEndTarget = transformElements.filter(transformEl => {
        const el = transformEl.classList.contains('swiper-slide-transform') ? getSlide(transformEl) : transformEl;
        return swiper.getSlideIndex(el) === activeIndex;
      });
    }
    transitionEndTarget.forEach(el => {
      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.k)(el, () => {
        if (eventTriggered) return;
        if (!swiper || swiper.destroyed) return;
        eventTriggered = true;
        swiper.animating = false;
        const evt = new window.CustomEvent('transitionend', {
          bubbles: true,
          cancelable: true
        });
        swiper.wrapperEl.dispatchEvent(evt);
      });
    });
  }
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs":
/*!********************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs ***!
  \********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   a: () => (/* binding */ getWindow),
/* harmony export */   g: () => (/* binding */ getDocument)
/* harmony export */ });
/**
 * SSR Window 4.0.2
 * Better handling for window object in SSR environment
 * https://github.com/nolimits4web/ssr-window
 *
 * Copyright 2021, Vladimir Kharlampidi
 *
 * Licensed under MIT
 *
 * Released on: December 13, 2021
 */
/* eslint-disable no-param-reassign */
function isObject(obj) {
  return obj !== null && typeof obj === 'object' && 'constructor' in obj && obj.constructor === Object;
}
function extend(target, src) {
  if (target === void 0) {
    target = {};
  }
  if (src === void 0) {
    src = {};
  }
  Object.keys(src).forEach(key => {
    if (typeof target[key] === 'undefined') target[key] = src[key];else if (isObject(src[key]) && isObject(target[key]) && Object.keys(src[key]).length > 0) {
      extend(target[key], src[key]);
    }
  });
}
const ssrDocument = {
  body: {},
  addEventListener() {},
  removeEventListener() {},
  activeElement: {
    blur() {},
    nodeName: ''
  },
  querySelector() {
    return null;
  },
  querySelectorAll() {
    return [];
  },
  getElementById() {
    return null;
  },
  createEvent() {
    return {
      initEvent() {}
    };
  },
  createElement() {
    return {
      children: [],
      childNodes: [],
      style: {},
      setAttribute() {},
      getElementsByTagName() {
        return [];
      }
    };
  },
  createElementNS() {
    return {};
  },
  importNode() {
    return null;
  },
  location: {
    hash: '',
    host: '',
    hostname: '',
    href: '',
    origin: '',
    pathname: '',
    protocol: '',
    search: ''
  }
};
function getDocument() {
  const doc = typeof document !== 'undefined' ? document : {};
  extend(doc, ssrDocument);
  return doc;
}
const ssrWindow = {
  document: ssrDocument,
  navigator: {
    userAgent: ''
  },
  location: {
    hash: '',
    host: '',
    hostname: '',
    href: '',
    origin: '',
    pathname: '',
    protocol: '',
    search: ''
  },
  history: {
    replaceState() {},
    pushState() {},
    go() {},
    back() {}
  },
  CustomEvent: function CustomEvent() {
    return this;
  },
  addEventListener() {},
  removeEventListener() {},
  getComputedStyle() {
    return {
      getPropertyValue() {
        return '';
      }
    };
  },
  Image() {},
  Date() {},
  screen: {},
  setTimeout() {},
  clearTimeout() {},
  matchMedia() {
    return {};
  },
  requestAnimationFrame(callback) {
    if (typeof setTimeout === 'undefined') {
      callback();
      return null;
    }
    return setTimeout(callback, 0);
  },
  cancelAnimationFrame(id) {
    if (typeof setTimeout === 'undefined') {
      return;
    }
    clearTimeout(id);
  }
};
function getWindow() {
  const win = typeof window !== 'undefined' ? window : {};
  extend(win, ssrWindow);
  return win;
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/shared/swiper-core.mjs":
/*!*****************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/shared/swiper-core.mjs ***!
  \*****************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   S: () => (/* binding */ Swiper),
/* harmony export */   d: () => (/* binding */ defaults)
/* harmony export */ });
/* harmony import */ var _ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./ssr-window.esm.mjs */ "../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs");
/* harmony import */ var _utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./utils.mjs */ "../plugin-slug/node_modules/swiper/shared/utils.mjs");



let support;
function calcSupport() {
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
  return {
    smoothScroll: document.documentElement && document.documentElement.style && 'scrollBehavior' in document.documentElement.style,
    touch: !!('ontouchstart' in window || window.DocumentTouch && document instanceof window.DocumentTouch)
  };
}
function getSupport() {
  if (!support) {
    support = calcSupport();
  }
  return support;
}

let deviceCached;
function calcDevice(_temp) {
  let {
    userAgent
  } = _temp === void 0 ? {} : _temp;
  const support = getSupport();
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  const platform = window.navigator.platform;
  const ua = userAgent || window.navigator.userAgent;
  const device = {
    ios: false,
    android: false
  };
  const screenWidth = window.screen.width;
  const screenHeight = window.screen.height;
  const android = ua.match(/(Android);?[\s\/]+([\d.]+)?/); // eslint-disable-line
  let ipad = ua.match(/(iPad).*OS\s([\d_]+)/);
  const ipod = ua.match(/(iPod)(.*OS\s([\d_]+))?/);
  const iphone = !ipad && ua.match(/(iPhone\sOS|iOS)\s([\d_]+)/);
  const windows = platform === 'Win32';
  let macos = platform === 'MacIntel';

  // iPadOs 13 fix
  const iPadScreens = ['1024x1366', '1366x1024', '834x1194', '1194x834', '834x1112', '1112x834', '768x1024', '1024x768', '820x1180', '1180x820', '810x1080', '1080x810'];
  if (!ipad && macos && support.touch && iPadScreens.indexOf(`${screenWidth}x${screenHeight}`) >= 0) {
    ipad = ua.match(/(Version)\/([\d.]+)/);
    if (!ipad) ipad = [0, 1, '13_0_0'];
    macos = false;
  }

  // Android
  if (android && !windows) {
    device.os = 'android';
    device.android = true;
  }
  if (ipad || iphone || ipod) {
    device.os = 'ios';
    device.ios = true;
  }

  // Export object
  return device;
}
function getDevice(overrides) {
  if (overrides === void 0) {
    overrides = {};
  }
  if (!deviceCached) {
    deviceCached = calcDevice(overrides);
  }
  return deviceCached;
}

let browser;
function calcBrowser() {
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  const device = getDevice();
  let needPerspectiveFix = false;
  function isSafari() {
    const ua = window.navigator.userAgent.toLowerCase();
    return ua.indexOf('safari') >= 0 && ua.indexOf('chrome') < 0 && ua.indexOf('android') < 0;
  }
  if (isSafari()) {
    const ua = String(window.navigator.userAgent);
    if (ua.includes('Version/')) {
      const [major, minor] = ua.split('Version/')[1].split(' ')[0].split('.').map(num => Number(num));
      needPerspectiveFix = major < 16 || major === 16 && minor < 2;
    }
  }
  const isWebView = /(iPhone|iPod|iPad).*AppleWebKit(?!.*Safari)/i.test(window.navigator.userAgent);
  const isSafariBrowser = isSafari();
  const need3dFix = isSafariBrowser || isWebView && device.ios;
  return {
    isSafari: needPerspectiveFix || isSafariBrowser,
    needPerspectiveFix,
    need3dFix,
    isWebView
  };
}
function getBrowser() {
  if (!browser) {
    browser = calcBrowser();
  }
  return browser;
}

function Resize(_ref) {
  let {
    swiper,
    on,
    emit
  } = _ref;
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  let observer = null;
  let animationFrame = null;
  const resizeHandler = () => {
    if (!swiper || swiper.destroyed || !swiper.initialized) return;
    emit('beforeResize');
    emit('resize');
  };
  const createObserver = () => {
    if (!swiper || swiper.destroyed || !swiper.initialized) return;
    observer = new ResizeObserver(entries => {
      animationFrame = window.requestAnimationFrame(() => {
        const {
          width,
          height
        } = swiper;
        let newWidth = width;
        let newHeight = height;
        entries.forEach(_ref2 => {
          let {
            contentBoxSize,
            contentRect,
            target
          } = _ref2;
          if (target && target !== swiper.el) return;
          newWidth = contentRect ? contentRect.width : (contentBoxSize[0] || contentBoxSize).inlineSize;
          newHeight = contentRect ? contentRect.height : (contentBoxSize[0] || contentBoxSize).blockSize;
        });
        if (newWidth !== width || newHeight !== height) {
          resizeHandler();
        }
      });
    });
    observer.observe(swiper.el);
  };
  const removeObserver = () => {
    if (animationFrame) {
      window.cancelAnimationFrame(animationFrame);
    }
    if (observer && observer.unobserve && swiper.el) {
      observer.unobserve(swiper.el);
      observer = null;
    }
  };
  const orientationChangeHandler = () => {
    if (!swiper || swiper.destroyed || !swiper.initialized) return;
    emit('orientationchange');
  };
  on('init', () => {
    if (swiper.params.resizeObserver && typeof window.ResizeObserver !== 'undefined') {
      createObserver();
      return;
    }
    window.addEventListener('resize', resizeHandler);
    window.addEventListener('orientationchange', orientationChangeHandler);
  });
  on('destroy', () => {
    removeObserver();
    window.removeEventListener('resize', resizeHandler);
    window.removeEventListener('orientationchange', orientationChangeHandler);
  });
}

function Observer(_ref) {
  let {
    swiper,
    extendParams,
    on,
    emit
  } = _ref;
  const observers = [];
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  const attach = function (target, options) {
    if (options === void 0) {
      options = {};
    }
    const ObserverFunc = window.MutationObserver || window.WebkitMutationObserver;
    const observer = new ObserverFunc(mutations => {
      // The observerUpdate event should only be triggered
      // once despite the number of mutations.  Additional
      // triggers are redundant and are very costly
      if (swiper.__preventObserver__) return;
      if (mutations.length === 1) {
        emit('observerUpdate', mutations[0]);
        return;
      }
      const observerUpdate = function observerUpdate() {
        emit('observerUpdate', mutations[0]);
      };
      if (window.requestAnimationFrame) {
        window.requestAnimationFrame(observerUpdate);
      } else {
        window.setTimeout(observerUpdate, 0);
      }
    });
    observer.observe(target, {
      attributes: typeof options.attributes === 'undefined' ? true : options.attributes,
      childList: swiper.isElement || (typeof options.childList === 'undefined' ? true : options).childList,
      characterData: typeof options.characterData === 'undefined' ? true : options.characterData
    });
    observers.push(observer);
  };
  const init = () => {
    if (!swiper.params.observer) return;
    if (swiper.params.observeParents) {
      const containerParents = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.a)(swiper.hostEl);
      for (let i = 0; i < containerParents.length; i += 1) {
        attach(containerParents[i]);
      }
    }
    // Observe container
    attach(swiper.hostEl, {
      childList: swiper.params.observeSlideChildren
    });

    // Observe wrapper
    attach(swiper.wrapperEl, {
      attributes: false
    });
  };
  const destroy = () => {
    observers.forEach(observer => {
      observer.disconnect();
    });
    observers.splice(0, observers.length);
  };
  extendParams({
    observer: false,
    observeParents: false,
    observeSlideChildren: false
  });
  on('init', init);
  on('destroy', destroy);
}

/* eslint-disable no-underscore-dangle */

var eventsEmitter = {
  on(events, handler, priority) {
    const self = this;
    if (!self.eventsListeners || self.destroyed) return self;
    if (typeof handler !== 'function') return self;
    const method = priority ? 'unshift' : 'push';
    events.split(' ').forEach(event => {
      if (!self.eventsListeners[event]) self.eventsListeners[event] = [];
      self.eventsListeners[event][method](handler);
    });
    return self;
  },
  once(events, handler, priority) {
    const self = this;
    if (!self.eventsListeners || self.destroyed) return self;
    if (typeof handler !== 'function') return self;
    function onceHandler() {
      self.off(events, onceHandler);
      if (onceHandler.__emitterProxy) {
        delete onceHandler.__emitterProxy;
      }
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      handler.apply(self, args);
    }
    onceHandler.__emitterProxy = handler;
    return self.on(events, onceHandler, priority);
  },
  onAny(handler, priority) {
    const self = this;
    if (!self.eventsListeners || self.destroyed) return self;
    if (typeof handler !== 'function') return self;
    const method = priority ? 'unshift' : 'push';
    if (self.eventsAnyListeners.indexOf(handler) < 0) {
      self.eventsAnyListeners[method](handler);
    }
    return self;
  },
  offAny(handler) {
    const self = this;
    if (!self.eventsListeners || self.destroyed) return self;
    if (!self.eventsAnyListeners) return self;
    const index = self.eventsAnyListeners.indexOf(handler);
    if (index >= 0) {
      self.eventsAnyListeners.splice(index, 1);
    }
    return self;
  },
  off(events, handler) {
    const self = this;
    if (!self.eventsListeners || self.destroyed) return self;
    if (!self.eventsListeners) return self;
    events.split(' ').forEach(event => {
      if (typeof handler === 'undefined') {
        self.eventsListeners[event] = [];
      } else if (self.eventsListeners[event]) {
        self.eventsListeners[event].forEach((eventHandler, index) => {
          if (eventHandler === handler || eventHandler.__emitterProxy && eventHandler.__emitterProxy === handler) {
            self.eventsListeners[event].splice(index, 1);
          }
        });
      }
    });
    return self;
  },
  emit() {
    const self = this;
    if (!self.eventsListeners || self.destroyed) return self;
    if (!self.eventsListeners) return self;
    let events;
    let data;
    let context;
    for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }
    if (typeof args[0] === 'string' || Array.isArray(args[0])) {
      events = args[0];
      data = args.slice(1, args.length);
      context = self;
    } else {
      events = args[0].events;
      data = args[0].data;
      context = args[0].context || self;
    }
    data.unshift(context);
    const eventsArray = Array.isArray(events) ? events : events.split(' ');
    eventsArray.forEach(event => {
      if (self.eventsAnyListeners && self.eventsAnyListeners.length) {
        self.eventsAnyListeners.forEach(eventHandler => {
          eventHandler.apply(context, [event, ...data]);
        });
      }
      if (self.eventsListeners && self.eventsListeners[event]) {
        self.eventsListeners[event].forEach(eventHandler => {
          eventHandler.apply(context, data);
        });
      }
    });
    return self;
  }
};

function updateSize() {
  const swiper = this;
  let width;
  let height;
  const el = swiper.el;
  if (typeof swiper.params.width !== 'undefined' && swiper.params.width !== null) {
    width = swiper.params.width;
  } else {
    width = el.clientWidth;
  }
  if (typeof swiper.params.height !== 'undefined' && swiper.params.height !== null) {
    height = swiper.params.height;
  } else {
    height = el.clientHeight;
  }
  if (width === 0 && swiper.isHorizontal() || height === 0 && swiper.isVertical()) {
    return;
  }

  // Subtract paddings
  width = width - parseInt((0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.p)(el, 'padding-left') || 0, 10) - parseInt((0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.p)(el, 'padding-right') || 0, 10);
  height = height - parseInt((0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.p)(el, 'padding-top') || 0, 10) - parseInt((0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.p)(el, 'padding-bottom') || 0, 10);
  if (Number.isNaN(width)) width = 0;
  if (Number.isNaN(height)) height = 0;
  Object.assign(swiper, {
    width,
    height,
    size: swiper.isHorizontal() ? width : height
  });
}

function updateSlides() {
  const swiper = this;
  function getDirectionPropertyValue(node, label) {
    return parseFloat(node.getPropertyValue(swiper.getDirectionLabel(label)) || 0);
  }
  const params = swiper.params;
  const {
    wrapperEl,
    slidesEl,
    size: swiperSize,
    rtlTranslate: rtl,
    wrongRTL
  } = swiper;
  const isVirtual = swiper.virtual && params.virtual.enabled;
  const previousSlidesLength = isVirtual ? swiper.virtual.slides.length : swiper.slides.length;
  const slides = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `.${swiper.params.slideClass}, swiper-slide`);
  const slidesLength = isVirtual ? swiper.virtual.slides.length : slides.length;
  let snapGrid = [];
  const slidesGrid = [];
  const slidesSizesGrid = [];
  let offsetBefore = params.slidesOffsetBefore;
  if (typeof offsetBefore === 'function') {
    offsetBefore = params.slidesOffsetBefore.call(swiper);
  }
  let offsetAfter = params.slidesOffsetAfter;
  if (typeof offsetAfter === 'function') {
    offsetAfter = params.slidesOffsetAfter.call(swiper);
  }
  const previousSnapGridLength = swiper.snapGrid.length;
  const previousSlidesGridLength = swiper.slidesGrid.length;
  let spaceBetween = params.spaceBetween;
  let slidePosition = -offsetBefore;
  let prevSlideSize = 0;
  let index = 0;
  if (typeof swiperSize === 'undefined') {
    return;
  }
  if (typeof spaceBetween === 'string' && spaceBetween.indexOf('%') >= 0) {
    spaceBetween = parseFloat(spaceBetween.replace('%', '')) / 100 * swiperSize;
  } else if (typeof spaceBetween === 'string') {
    spaceBetween = parseFloat(spaceBetween);
  }
  swiper.virtualSize = -spaceBetween;

  // reset margins
  slides.forEach(slideEl => {
    if (rtl) {
      slideEl.style.marginLeft = '';
    } else {
      slideEl.style.marginRight = '';
    }
    slideEl.style.marginBottom = '';
    slideEl.style.marginTop = '';
  });

  // reset cssMode offsets
  if (params.centeredSlides && params.cssMode) {
    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.s)(wrapperEl, '--swiper-centered-offset-before', '');
    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.s)(wrapperEl, '--swiper-centered-offset-after', '');
  }
  const gridEnabled = params.grid && params.grid.rows > 1 && swiper.grid;
  if (gridEnabled) {
    swiper.grid.initSlides(slides);
  } else if (swiper.grid) {
    swiper.grid.unsetSlides();
  }

  // Calc slides
  let slideSize;
  const shouldResetSlideSize = params.slidesPerView === 'auto' && params.breakpoints && Object.keys(params.breakpoints).filter(key => {
    return typeof params.breakpoints[key].slidesPerView !== 'undefined';
  }).length > 0;
  for (let i = 0; i < slidesLength; i += 1) {
    slideSize = 0;
    let slide;
    if (slides[i]) slide = slides[i];
    if (gridEnabled) {
      swiper.grid.updateSlide(i, slide, slides);
    }
    if (slides[i] && (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.p)(slide, 'display') === 'none') continue; // eslint-disable-line

    if (params.slidesPerView === 'auto') {
      if (shouldResetSlideSize) {
        slides[i].style[swiper.getDirectionLabel('width')] = ``;
      }
      const slideStyles = getComputedStyle(slide);
      const currentTransform = slide.style.transform;
      const currentWebKitTransform = slide.style.webkitTransform;
      if (currentTransform) {
        slide.style.transform = 'none';
      }
      if (currentWebKitTransform) {
        slide.style.webkitTransform = 'none';
      }
      if (params.roundLengths) {
        slideSize = swiper.isHorizontal() ? (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.f)(slide, 'width', true) : (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.f)(slide, 'height', true);
      } else {
        // eslint-disable-next-line
        const width = getDirectionPropertyValue(slideStyles, 'width');
        const paddingLeft = getDirectionPropertyValue(slideStyles, 'padding-left');
        const paddingRight = getDirectionPropertyValue(slideStyles, 'padding-right');
        const marginLeft = getDirectionPropertyValue(slideStyles, 'margin-left');
        const marginRight = getDirectionPropertyValue(slideStyles, 'margin-right');
        const boxSizing = slideStyles.getPropertyValue('box-sizing');
        if (boxSizing && boxSizing === 'border-box') {
          slideSize = width + marginLeft + marginRight;
        } else {
          const {
            clientWidth,
            offsetWidth
          } = slide;
          slideSize = width + paddingLeft + paddingRight + marginLeft + marginRight + (offsetWidth - clientWidth);
        }
      }
      if (currentTransform) {
        slide.style.transform = currentTransform;
      }
      if (currentWebKitTransform) {
        slide.style.webkitTransform = currentWebKitTransform;
      }
      if (params.roundLengths) slideSize = Math.floor(slideSize);
    } else {
      slideSize = (swiperSize - (params.slidesPerView - 1) * spaceBetween) / params.slidesPerView;
      if (params.roundLengths) slideSize = Math.floor(slideSize);
      if (slides[i]) {
        slides[i].style[swiper.getDirectionLabel('width')] = `${slideSize}px`;
      }
    }
    if (slides[i]) {
      slides[i].swiperSlideSize = slideSize;
    }
    slidesSizesGrid.push(slideSize);
    if (params.centeredSlides) {
      slidePosition = slidePosition + slideSize / 2 + prevSlideSize / 2 + spaceBetween;
      if (prevSlideSize === 0 && i !== 0) slidePosition = slidePosition - swiperSize / 2 - spaceBetween;
      if (i === 0) slidePosition = slidePosition - swiperSize / 2 - spaceBetween;
      if (Math.abs(slidePosition) < 1 / 1000) slidePosition = 0;
      if (params.roundLengths) slidePosition = Math.floor(slidePosition);
      if (index % params.slidesPerGroup === 0) snapGrid.push(slidePosition);
      slidesGrid.push(slidePosition);
    } else {
      if (params.roundLengths) slidePosition = Math.floor(slidePosition);
      if ((index - Math.min(swiper.params.slidesPerGroupSkip, index)) % swiper.params.slidesPerGroup === 0) snapGrid.push(slidePosition);
      slidesGrid.push(slidePosition);
      slidePosition = slidePosition + slideSize + spaceBetween;
    }
    swiper.virtualSize += slideSize + spaceBetween;
    prevSlideSize = slideSize;
    index += 1;
  }
  swiper.virtualSize = Math.max(swiper.virtualSize, swiperSize) + offsetAfter;
  if (rtl && wrongRTL && (params.effect === 'slide' || params.effect === 'coverflow')) {
    wrapperEl.style.width = `${swiper.virtualSize + spaceBetween}px`;
  }
  if (params.setWrapperSize) {
    wrapperEl.style[swiper.getDirectionLabel('width')] = `${swiper.virtualSize + spaceBetween}px`;
  }
  if (gridEnabled) {
    swiper.grid.updateWrapperSize(slideSize, snapGrid);
  }

  // Remove last grid elements depending on width
  if (!params.centeredSlides) {
    const newSlidesGrid = [];
    for (let i = 0; i < snapGrid.length; i += 1) {
      let slidesGridItem = snapGrid[i];
      if (params.roundLengths) slidesGridItem = Math.floor(slidesGridItem);
      if (snapGrid[i] <= swiper.virtualSize - swiperSize) {
        newSlidesGrid.push(slidesGridItem);
      }
    }
    snapGrid = newSlidesGrid;
    if (Math.floor(swiper.virtualSize - swiperSize) - Math.floor(snapGrid[snapGrid.length - 1]) > 1) {
      snapGrid.push(swiper.virtualSize - swiperSize);
    }
  }
  if (isVirtual && params.loop) {
    const size = slidesSizesGrid[0] + spaceBetween;
    if (params.slidesPerGroup > 1) {
      const groups = Math.ceil((swiper.virtual.slidesBefore + swiper.virtual.slidesAfter) / params.slidesPerGroup);
      const groupSize = size * params.slidesPerGroup;
      for (let i = 0; i < groups; i += 1) {
        snapGrid.push(snapGrid[snapGrid.length - 1] + groupSize);
      }
    }
    for (let i = 0; i < swiper.virtual.slidesBefore + swiper.virtual.slidesAfter; i += 1) {
      if (params.slidesPerGroup === 1) {
        snapGrid.push(snapGrid[snapGrid.length - 1] + size);
      }
      slidesGrid.push(slidesGrid[slidesGrid.length - 1] + size);
      swiper.virtualSize += size;
    }
  }
  if (snapGrid.length === 0) snapGrid = [0];
  if (spaceBetween !== 0) {
    const key = swiper.isHorizontal() && rtl ? 'marginLeft' : swiper.getDirectionLabel('marginRight');
    slides.filter((_, slideIndex) => {
      if (!params.cssMode || params.loop) return true;
      if (slideIndex === slides.length - 1) {
        return false;
      }
      return true;
    }).forEach(slideEl => {
      slideEl.style[key] = `${spaceBetween}px`;
    });
  }
  if (params.centeredSlides && params.centeredSlidesBounds) {
    let allSlidesSize = 0;
    slidesSizesGrid.forEach(slideSizeValue => {
      allSlidesSize += slideSizeValue + (spaceBetween || 0);
    });
    allSlidesSize -= spaceBetween;
    const maxSnap = allSlidesSize > swiperSize ? allSlidesSize - swiperSize : 0;
    snapGrid = snapGrid.map(snap => {
      if (snap <= 0) return -offsetBefore;
      if (snap > maxSnap) return maxSnap + offsetAfter;
      return snap;
    });
  }
  if (params.centerInsufficientSlides) {
    let allSlidesSize = 0;
    slidesSizesGrid.forEach(slideSizeValue => {
      allSlidesSize += slideSizeValue + (spaceBetween || 0);
    });
    allSlidesSize -= spaceBetween;
    const offsetSize = (params.slidesOffsetBefore || 0) + (params.slidesOffsetAfter || 0);
    if (allSlidesSize + offsetSize < swiperSize) {
      const allSlidesOffset = (swiperSize - allSlidesSize - offsetSize) / 2;
      snapGrid.forEach((snap, snapIndex) => {
        snapGrid[snapIndex] = snap - allSlidesOffset;
      });
      slidesGrid.forEach((snap, snapIndex) => {
        slidesGrid[snapIndex] = snap + allSlidesOffset;
      });
    }
  }
  Object.assign(swiper, {
    slides,
    snapGrid,
    slidesGrid,
    slidesSizesGrid
  });
  if (params.centeredSlides && params.cssMode && !params.centeredSlidesBounds) {
    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.s)(wrapperEl, '--swiper-centered-offset-before', `${-snapGrid[0]}px`);
    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.s)(wrapperEl, '--swiper-centered-offset-after', `${swiper.size / 2 - slidesSizesGrid[slidesSizesGrid.length - 1] / 2}px`);
    const addToSnapGrid = -swiper.snapGrid[0];
    const addToSlidesGrid = -swiper.slidesGrid[0];
    swiper.snapGrid = swiper.snapGrid.map(v => v + addToSnapGrid);
    swiper.slidesGrid = swiper.slidesGrid.map(v => v + addToSlidesGrid);
  }
  if (slidesLength !== previousSlidesLength) {
    swiper.emit('slidesLengthChange');
  }
  if (snapGrid.length !== previousSnapGridLength) {
    if (swiper.params.watchOverflow) swiper.checkOverflow();
    swiper.emit('snapGridLengthChange');
  }
  if (slidesGrid.length !== previousSlidesGridLength) {
    swiper.emit('slidesGridLengthChange');
  }
  if (params.watchSlidesProgress) {
    swiper.updateSlidesOffset();
  }
  swiper.emit('slidesUpdated');
  if (!isVirtual && !params.cssMode && (params.effect === 'slide' || params.effect === 'fade')) {
    const backFaceHiddenClass = `${params.containerModifierClass}backface-hidden`;
    const hasClassBackfaceClassAdded = swiper.el.classList.contains(backFaceHiddenClass);
    if (slidesLength <= params.maxBackfaceHiddenSlides) {
      if (!hasClassBackfaceClassAdded) swiper.el.classList.add(backFaceHiddenClass);
    } else if (hasClassBackfaceClassAdded) {
      swiper.el.classList.remove(backFaceHiddenClass);
    }
  }
}

function updateAutoHeight(speed) {
  const swiper = this;
  const activeSlides = [];
  const isVirtual = swiper.virtual && swiper.params.virtual.enabled;
  let newHeight = 0;
  let i;
  if (typeof speed === 'number') {
    swiper.setTransition(speed);
  } else if (speed === true) {
    swiper.setTransition(swiper.params.speed);
  }
  const getSlideByIndex = index => {
    if (isVirtual) {
      return swiper.slides[swiper.getSlideIndexByData(index)];
    }
    return swiper.slides[index];
  };
  // Find slides currently in view
  if (swiper.params.slidesPerView !== 'auto' && swiper.params.slidesPerView > 1) {
    if (swiper.params.centeredSlides) {
      (swiper.visibleSlides || []).forEach(slide => {
        activeSlides.push(slide);
      });
    } else {
      for (i = 0; i < Math.ceil(swiper.params.slidesPerView); i += 1) {
        const index = swiper.activeIndex + i;
        if (index > swiper.slides.length && !isVirtual) break;
        activeSlides.push(getSlideByIndex(index));
      }
    }
  } else {
    activeSlides.push(getSlideByIndex(swiper.activeIndex));
  }

  // Find new height from highest slide in view
  for (i = 0; i < activeSlides.length; i += 1) {
    if (typeof activeSlides[i] !== 'undefined') {
      const height = activeSlides[i].offsetHeight;
      newHeight = height > newHeight ? height : newHeight;
    }
  }

  // Update Height
  if (newHeight || newHeight === 0) swiper.wrapperEl.style.height = `${newHeight}px`;
}

function updateSlidesOffset() {
  const swiper = this;
  const slides = swiper.slides;
  // eslint-disable-next-line
  const minusOffset = swiper.isElement ? swiper.isHorizontal() ? swiper.wrapperEl.offsetLeft : swiper.wrapperEl.offsetTop : 0;
  for (let i = 0; i < slides.length; i += 1) {
    slides[i].swiperSlideOffset = (swiper.isHorizontal() ? slides[i].offsetLeft : slides[i].offsetTop) - minusOffset - swiper.cssOverflowAdjustment();
  }
}

const toggleSlideClasses$1 = (slideEl, condition, className) => {
  if (condition && !slideEl.classList.contains(className)) {
    slideEl.classList.add(className);
  } else if (!condition && slideEl.classList.contains(className)) {
    slideEl.classList.remove(className);
  }
};
function updateSlidesProgress(translate) {
  if (translate === void 0) {
    translate = this && this.translate || 0;
  }
  const swiper = this;
  const params = swiper.params;
  const {
    slides,
    rtlTranslate: rtl,
    snapGrid
  } = swiper;
  if (slides.length === 0) return;
  if (typeof slides[0].swiperSlideOffset === 'undefined') swiper.updateSlidesOffset();
  let offsetCenter = -translate;
  if (rtl) offsetCenter = translate;
  swiper.visibleSlidesIndexes = [];
  swiper.visibleSlides = [];
  let spaceBetween = params.spaceBetween;
  if (typeof spaceBetween === 'string' && spaceBetween.indexOf('%') >= 0) {
    spaceBetween = parseFloat(spaceBetween.replace('%', '')) / 100 * swiper.size;
  } else if (typeof spaceBetween === 'string') {
    spaceBetween = parseFloat(spaceBetween);
  }
  for (let i = 0; i < slides.length; i += 1) {
    const slide = slides[i];
    let slideOffset = slide.swiperSlideOffset;
    if (params.cssMode && params.centeredSlides) {
      slideOffset -= slides[0].swiperSlideOffset;
    }
    const slideProgress = (offsetCenter + (params.centeredSlides ? swiper.minTranslate() : 0) - slideOffset) / (slide.swiperSlideSize + spaceBetween);
    const originalSlideProgress = (offsetCenter - snapGrid[0] + (params.centeredSlides ? swiper.minTranslate() : 0) - slideOffset) / (slide.swiperSlideSize + spaceBetween);
    const slideBefore = -(offsetCenter - slideOffset);
    const slideAfter = slideBefore + swiper.slidesSizesGrid[i];
    const isFullyVisible = slideBefore >= 0 && slideBefore <= swiper.size - swiper.slidesSizesGrid[i];
    const isVisible = slideBefore >= 0 && slideBefore < swiper.size - 1 || slideAfter > 1 && slideAfter <= swiper.size || slideBefore <= 0 && slideAfter >= swiper.size;
    if (isVisible) {
      swiper.visibleSlides.push(slide);
      swiper.visibleSlidesIndexes.push(i);
    }
    toggleSlideClasses$1(slide, isVisible, params.slideVisibleClass);
    toggleSlideClasses$1(slide, isFullyVisible, params.slideFullyVisibleClass);
    slide.progress = rtl ? -slideProgress : slideProgress;
    slide.originalProgress = rtl ? -originalSlideProgress : originalSlideProgress;
  }
}

function updateProgress(translate) {
  const swiper = this;
  if (typeof translate === 'undefined') {
    const multiplier = swiper.rtlTranslate ? -1 : 1;
    // eslint-disable-next-line
    translate = swiper && swiper.translate && swiper.translate * multiplier || 0;
  }
  const params = swiper.params;
  const translatesDiff = swiper.maxTranslate() - swiper.minTranslate();
  let {
    progress,
    isBeginning,
    isEnd,
    progressLoop
  } = swiper;
  const wasBeginning = isBeginning;
  const wasEnd = isEnd;
  if (translatesDiff === 0) {
    progress = 0;
    isBeginning = true;
    isEnd = true;
  } else {
    progress = (translate - swiper.minTranslate()) / translatesDiff;
    const isBeginningRounded = Math.abs(translate - swiper.minTranslate()) < 1;
    const isEndRounded = Math.abs(translate - swiper.maxTranslate()) < 1;
    isBeginning = isBeginningRounded || progress <= 0;
    isEnd = isEndRounded || progress >= 1;
    if (isBeginningRounded) progress = 0;
    if (isEndRounded) progress = 1;
  }
  if (params.loop) {
    const firstSlideIndex = swiper.getSlideIndexByData(0);
    const lastSlideIndex = swiper.getSlideIndexByData(swiper.slides.length - 1);
    const firstSlideTranslate = swiper.slidesGrid[firstSlideIndex];
    const lastSlideTranslate = swiper.slidesGrid[lastSlideIndex];
    const translateMax = swiper.slidesGrid[swiper.slidesGrid.length - 1];
    const translateAbs = Math.abs(translate);
    if (translateAbs >= firstSlideTranslate) {
      progressLoop = (translateAbs - firstSlideTranslate) / translateMax;
    } else {
      progressLoop = (translateAbs + translateMax - lastSlideTranslate) / translateMax;
    }
    if (progressLoop > 1) progressLoop -= 1;
  }
  Object.assign(swiper, {
    progress,
    progressLoop,
    isBeginning,
    isEnd
  });
  if (params.watchSlidesProgress || params.centeredSlides && params.autoHeight) swiper.updateSlidesProgress(translate);
  if (isBeginning && !wasBeginning) {
    swiper.emit('reachBeginning toEdge');
  }
  if (isEnd && !wasEnd) {
    swiper.emit('reachEnd toEdge');
  }
  if (wasBeginning && !isBeginning || wasEnd && !isEnd) {
    swiper.emit('fromEdge');
  }
  swiper.emit('progress', progress);
}

const toggleSlideClasses = (slideEl, condition, className) => {
  if (condition && !slideEl.classList.contains(className)) {
    slideEl.classList.add(className);
  } else if (!condition && slideEl.classList.contains(className)) {
    slideEl.classList.remove(className);
  }
};
function updateSlidesClasses() {
  const swiper = this;
  const {
    slides,
    params,
    slidesEl,
    activeIndex
  } = swiper;
  const isVirtual = swiper.virtual && params.virtual.enabled;
  const gridEnabled = swiper.grid && params.grid && params.grid.rows > 1;
  const getFilteredSlide = selector => {
    return (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `.${params.slideClass}${selector}, swiper-slide${selector}`)[0];
  };
  let activeSlide;
  let prevSlide;
  let nextSlide;
  if (isVirtual) {
    if (params.loop) {
      let slideIndex = activeIndex - swiper.virtual.slidesBefore;
      if (slideIndex < 0) slideIndex = swiper.virtual.slides.length + slideIndex;
      if (slideIndex >= swiper.virtual.slides.length) slideIndex -= swiper.virtual.slides.length;
      activeSlide = getFilteredSlide(`[data-swiper-slide-index="${slideIndex}"]`);
    } else {
      activeSlide = getFilteredSlide(`[data-swiper-slide-index="${activeIndex}"]`);
    }
  } else {
    if (gridEnabled) {
      activeSlide = slides.find(slideEl => slideEl.column === activeIndex);
      nextSlide = slides.find(slideEl => slideEl.column === activeIndex + 1);
      prevSlide = slides.find(slideEl => slideEl.column === activeIndex - 1);
    } else {
      activeSlide = slides[activeIndex];
    }
  }
  if (activeSlide) {
    if (!gridEnabled) {
      // Next Slide
      nextSlide = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.q)(activeSlide, `.${params.slideClass}, swiper-slide`)[0];
      if (params.loop && !nextSlide) {
        nextSlide = slides[0];
      }

      // Prev Slide
      prevSlide = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.r)(activeSlide, `.${params.slideClass}, swiper-slide`)[0];
      if (params.loop && !prevSlide === 0) {
        prevSlide = slides[slides.length - 1];
      }
    }
  }
  slides.forEach(slideEl => {
    toggleSlideClasses(slideEl, slideEl === activeSlide, params.slideActiveClass);
    toggleSlideClasses(slideEl, slideEl === nextSlide, params.slideNextClass);
    toggleSlideClasses(slideEl, slideEl === prevSlide, params.slidePrevClass);
  });
  swiper.emitSlidesClasses();
}

const processLazyPreloader = (swiper, imageEl) => {
  if (!swiper || swiper.destroyed || !swiper.params) return;
  const slideSelector = () => swiper.isElement ? `swiper-slide` : `.${swiper.params.slideClass}`;
  const slideEl = imageEl.closest(slideSelector());
  if (slideEl) {
    let lazyEl = slideEl.querySelector(`.${swiper.params.lazyPreloaderClass}`);
    if (!lazyEl && swiper.isElement) {
      if (slideEl.shadowRoot) {
        lazyEl = slideEl.shadowRoot.querySelector(`.${swiper.params.lazyPreloaderClass}`);
      } else {
        // init later
        requestAnimationFrame(() => {
          if (slideEl.shadowRoot) {
            lazyEl = slideEl.shadowRoot.querySelector(`.${swiper.params.lazyPreloaderClass}`);
            if (lazyEl) lazyEl.remove();
          }
        });
      }
    }
    if (lazyEl) lazyEl.remove();
  }
};
const unlazy = (swiper, index) => {
  if (!swiper.slides[index]) return;
  const imageEl = swiper.slides[index].querySelector('[loading="lazy"]');
  if (imageEl) imageEl.removeAttribute('loading');
};
const preload = swiper => {
  if (!swiper || swiper.destroyed || !swiper.params) return;
  let amount = swiper.params.lazyPreloadPrevNext;
  const len = swiper.slides.length;
  if (!len || !amount || amount < 0) return;
  amount = Math.min(amount, len);
  const slidesPerView = swiper.params.slidesPerView === 'auto' ? swiper.slidesPerViewDynamic() : Math.ceil(swiper.params.slidesPerView);
  const activeIndex = swiper.activeIndex;
  if (swiper.params.grid && swiper.params.grid.rows > 1) {
    const activeColumn = activeIndex;
    const preloadColumns = [activeColumn - amount];
    preloadColumns.push(...Array.from({
      length: amount
    }).map((_, i) => {
      return activeColumn + slidesPerView + i;
    }));
    swiper.slides.forEach((slideEl, i) => {
      if (preloadColumns.includes(slideEl.column)) unlazy(swiper, i);
    });
    return;
  }
  const slideIndexLastInView = activeIndex + slidesPerView - 1;
  if (swiper.params.rewind || swiper.params.loop) {
    for (let i = activeIndex - amount; i <= slideIndexLastInView + amount; i += 1) {
      const realIndex = (i % len + len) % len;
      if (realIndex < activeIndex || realIndex > slideIndexLastInView) unlazy(swiper, realIndex);
    }
  } else {
    for (let i = Math.max(activeIndex - amount, 0); i <= Math.min(slideIndexLastInView + amount, len - 1); i += 1) {
      if (i !== activeIndex && (i > slideIndexLastInView || i < activeIndex)) {
        unlazy(swiper, i);
      }
    }
  }
};

function getActiveIndexByTranslate(swiper) {
  const {
    slidesGrid,
    params
  } = swiper;
  const translate = swiper.rtlTranslate ? swiper.translate : -swiper.translate;
  let activeIndex;
  for (let i = 0; i < slidesGrid.length; i += 1) {
    if (typeof slidesGrid[i + 1] !== 'undefined') {
      if (translate >= slidesGrid[i] && translate < slidesGrid[i + 1] - (slidesGrid[i + 1] - slidesGrid[i]) / 2) {
        activeIndex = i;
      } else if (translate >= slidesGrid[i] && translate < slidesGrid[i + 1]) {
        activeIndex = i + 1;
      }
    } else if (translate >= slidesGrid[i]) {
      activeIndex = i;
    }
  }
  // Normalize slideIndex
  if (params.normalizeSlideIndex) {
    if (activeIndex < 0 || typeof activeIndex === 'undefined') activeIndex = 0;
  }
  return activeIndex;
}
function updateActiveIndex(newActiveIndex) {
  const swiper = this;
  const translate = swiper.rtlTranslate ? swiper.translate : -swiper.translate;
  const {
    snapGrid,
    params,
    activeIndex: previousIndex,
    realIndex: previousRealIndex,
    snapIndex: previousSnapIndex
  } = swiper;
  let activeIndex = newActiveIndex;
  let snapIndex;
  const getVirtualRealIndex = aIndex => {
    let realIndex = aIndex - swiper.virtual.slidesBefore;
    if (realIndex < 0) {
      realIndex = swiper.virtual.slides.length + realIndex;
    }
    if (realIndex >= swiper.virtual.slides.length) {
      realIndex -= swiper.virtual.slides.length;
    }
    return realIndex;
  };
  if (typeof activeIndex === 'undefined') {
    activeIndex = getActiveIndexByTranslate(swiper);
  }
  if (snapGrid.indexOf(translate) >= 0) {
    snapIndex = snapGrid.indexOf(translate);
  } else {
    const skip = Math.min(params.slidesPerGroupSkip, activeIndex);
    snapIndex = skip + Math.floor((activeIndex - skip) / params.slidesPerGroup);
  }
  if (snapIndex >= snapGrid.length) snapIndex = snapGrid.length - 1;
  if (activeIndex === previousIndex && !swiper.params.loop) {
    if (snapIndex !== previousSnapIndex) {
      swiper.snapIndex = snapIndex;
      swiper.emit('snapIndexChange');
    }
    return;
  }
  if (activeIndex === previousIndex && swiper.params.loop && swiper.virtual && swiper.params.virtual.enabled) {
    swiper.realIndex = getVirtualRealIndex(activeIndex);
    return;
  }
  const gridEnabled = swiper.grid && params.grid && params.grid.rows > 1;

  // Get real index
  let realIndex;
  if (swiper.virtual && params.virtual.enabled && params.loop) {
    realIndex = getVirtualRealIndex(activeIndex);
  } else if (gridEnabled) {
    const firstSlideInColumn = swiper.slides.find(slideEl => slideEl.column === activeIndex);
    let activeSlideIndex = parseInt(firstSlideInColumn.getAttribute('data-swiper-slide-index'), 10);
    if (Number.isNaN(activeSlideIndex)) {
      activeSlideIndex = Math.max(swiper.slides.indexOf(firstSlideInColumn), 0);
    }
    realIndex = Math.floor(activeSlideIndex / params.grid.rows);
  } else if (swiper.slides[activeIndex]) {
    const slideIndex = swiper.slides[activeIndex].getAttribute('data-swiper-slide-index');
    if (slideIndex) {
      realIndex = parseInt(slideIndex, 10);
    } else {
      realIndex = activeIndex;
    }
  } else {
    realIndex = activeIndex;
  }
  Object.assign(swiper, {
    previousSnapIndex,
    snapIndex,
    previousRealIndex,
    realIndex,
    previousIndex,
    activeIndex
  });
  if (swiper.initialized) {
    preload(swiper);
  }
  swiper.emit('activeIndexChange');
  swiper.emit('snapIndexChange');
  if (swiper.initialized || swiper.params.runCallbacksOnInit) {
    if (previousRealIndex !== realIndex) {
      swiper.emit('realIndexChange');
    }
    swiper.emit('slideChange');
  }
}

function updateClickedSlide(el, path) {
  const swiper = this;
  const params = swiper.params;
  let slide = el.closest(`.${params.slideClass}, swiper-slide`);
  if (!slide && swiper.isElement && path && path.length > 1 && path.includes(el)) {
    [...path.slice(path.indexOf(el) + 1, path.length)].forEach(pathEl => {
      if (!slide && pathEl.matches && pathEl.matches(`.${params.slideClass}, swiper-slide`)) {
        slide = pathEl;
      }
    });
  }
  let slideFound = false;
  let slideIndex;
  if (slide) {
    for (let i = 0; i < swiper.slides.length; i += 1) {
      if (swiper.slides[i] === slide) {
        slideFound = true;
        slideIndex = i;
        break;
      }
    }
  }
  if (slide && slideFound) {
    swiper.clickedSlide = slide;
    if (swiper.virtual && swiper.params.virtual.enabled) {
      swiper.clickedIndex = parseInt(slide.getAttribute('data-swiper-slide-index'), 10);
    } else {
      swiper.clickedIndex = slideIndex;
    }
  } else {
    swiper.clickedSlide = undefined;
    swiper.clickedIndex = undefined;
    return;
  }
  if (params.slideToClickedSlide && swiper.clickedIndex !== undefined && swiper.clickedIndex !== swiper.activeIndex) {
    swiper.slideToClickedSlide();
  }
}

var update = {
  updateSize,
  updateSlides,
  updateAutoHeight,
  updateSlidesOffset,
  updateSlidesProgress,
  updateProgress,
  updateSlidesClasses,
  updateActiveIndex,
  updateClickedSlide
};

function getSwiperTranslate(axis) {
  if (axis === void 0) {
    axis = this.isHorizontal() ? 'x' : 'y';
  }
  const swiper = this;
  const {
    params,
    rtlTranslate: rtl,
    translate,
    wrapperEl
  } = swiper;
  if (params.virtualTranslate) {
    return rtl ? -translate : translate;
  }
  if (params.cssMode) {
    return translate;
  }
  let currentTranslate = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.j)(wrapperEl, axis);
  currentTranslate += swiper.cssOverflowAdjustment();
  if (rtl) currentTranslate = -currentTranslate;
  return currentTranslate || 0;
}

function setTranslate(translate, byController) {
  const swiper = this;
  const {
    rtlTranslate: rtl,
    params,
    wrapperEl,
    progress
  } = swiper;
  let x = 0;
  let y = 0;
  const z = 0;
  if (swiper.isHorizontal()) {
    x = rtl ? -translate : translate;
  } else {
    y = translate;
  }
  if (params.roundLengths) {
    x = Math.floor(x);
    y = Math.floor(y);
  }
  swiper.previousTranslate = swiper.translate;
  swiper.translate = swiper.isHorizontal() ? x : y;
  if (params.cssMode) {
    wrapperEl[swiper.isHorizontal() ? 'scrollLeft' : 'scrollTop'] = swiper.isHorizontal() ? -x : -y;
  } else if (!params.virtualTranslate) {
    if (swiper.isHorizontal()) {
      x -= swiper.cssOverflowAdjustment();
    } else {
      y -= swiper.cssOverflowAdjustment();
    }
    wrapperEl.style.transform = `translate3d(${x}px, ${y}px, ${z}px)`;
  }

  // Check if we need to update progress
  let newProgress;
  const translatesDiff = swiper.maxTranslate() - swiper.minTranslate();
  if (translatesDiff === 0) {
    newProgress = 0;
  } else {
    newProgress = (translate - swiper.minTranslate()) / translatesDiff;
  }
  if (newProgress !== progress) {
    swiper.updateProgress(translate);
  }
  swiper.emit('setTranslate', swiper.translate, byController);
}

function minTranslate() {
  return -this.snapGrid[0];
}

function maxTranslate() {
  return -this.snapGrid[this.snapGrid.length - 1];
}

function translateTo(translate, speed, runCallbacks, translateBounds, internal) {
  if (translate === void 0) {
    translate = 0;
  }
  if (speed === void 0) {
    speed = this.params.speed;
  }
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  if (translateBounds === void 0) {
    translateBounds = true;
  }
  const swiper = this;
  const {
    params,
    wrapperEl
  } = swiper;
  if (swiper.animating && params.preventInteractionOnTransition) {
    return false;
  }
  const minTranslate = swiper.minTranslate();
  const maxTranslate = swiper.maxTranslate();
  let newTranslate;
  if (translateBounds && translate > minTranslate) newTranslate = minTranslate;else if (translateBounds && translate < maxTranslate) newTranslate = maxTranslate;else newTranslate = translate;

  // Update progress
  swiper.updateProgress(newTranslate);
  if (params.cssMode) {
    const isH = swiper.isHorizontal();
    if (speed === 0) {
      wrapperEl[isH ? 'scrollLeft' : 'scrollTop'] = -newTranslate;
    } else {
      if (!swiper.support.smoothScroll) {
        (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.t)({
          swiper,
          targetPosition: -newTranslate,
          side: isH ? 'left' : 'top'
        });
        return true;
      }
      wrapperEl.scrollTo({
        [isH ? 'left' : 'top']: -newTranslate,
        behavior: 'smooth'
      });
    }
    return true;
  }
  if (speed === 0) {
    swiper.setTransition(0);
    swiper.setTranslate(newTranslate);
    if (runCallbacks) {
      swiper.emit('beforeTransitionStart', speed, internal);
      swiper.emit('transitionEnd');
    }
  } else {
    swiper.setTransition(speed);
    swiper.setTranslate(newTranslate);
    if (runCallbacks) {
      swiper.emit('beforeTransitionStart', speed, internal);
      swiper.emit('transitionStart');
    }
    if (!swiper.animating) {
      swiper.animating = true;
      if (!swiper.onTranslateToWrapperTransitionEnd) {
        swiper.onTranslateToWrapperTransitionEnd = function transitionEnd(e) {
          if (!swiper || swiper.destroyed) return;
          if (e.target !== this) return;
          swiper.wrapperEl.removeEventListener('transitionend', swiper.onTranslateToWrapperTransitionEnd);
          swiper.onTranslateToWrapperTransitionEnd = null;
          delete swiper.onTranslateToWrapperTransitionEnd;
          swiper.animating = false;
          if (runCallbacks) {
            swiper.emit('transitionEnd');
          }
        };
      }
      swiper.wrapperEl.addEventListener('transitionend', swiper.onTranslateToWrapperTransitionEnd);
    }
  }
  return true;
}

var translate = {
  getTranslate: getSwiperTranslate,
  setTranslate,
  minTranslate,
  maxTranslate,
  translateTo
};

function setTransition(duration, byController) {
  const swiper = this;
  if (!swiper.params.cssMode) {
    swiper.wrapperEl.style.transitionDuration = `${duration}ms`;
    swiper.wrapperEl.style.transitionDelay = duration === 0 ? `0ms` : '';
  }
  swiper.emit('setTransition', duration, byController);
}

function transitionEmit(_ref) {
  let {
    swiper,
    runCallbacks,
    direction,
    step
  } = _ref;
  const {
    activeIndex,
    previousIndex
  } = swiper;
  let dir = direction;
  if (!dir) {
    if (activeIndex > previousIndex) dir = 'next';else if (activeIndex < previousIndex) dir = 'prev';else dir = 'reset';
  }
  swiper.emit(`transition${step}`);
  if (runCallbacks && activeIndex !== previousIndex) {
    if (dir === 'reset') {
      swiper.emit(`slideResetTransition${step}`);
      return;
    }
    swiper.emit(`slideChangeTransition${step}`);
    if (dir === 'next') {
      swiper.emit(`slideNextTransition${step}`);
    } else {
      swiper.emit(`slidePrevTransition${step}`);
    }
  }
}

function transitionStart(runCallbacks, direction) {
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  const swiper = this;
  const {
    params
  } = swiper;
  if (params.cssMode) return;
  if (params.autoHeight) {
    swiper.updateAutoHeight();
  }
  transitionEmit({
    swiper,
    runCallbacks,
    direction,
    step: 'Start'
  });
}

function transitionEnd(runCallbacks, direction) {
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  const swiper = this;
  const {
    params
  } = swiper;
  swiper.animating = false;
  if (params.cssMode) return;
  swiper.setTransition(0);
  transitionEmit({
    swiper,
    runCallbacks,
    direction,
    step: 'End'
  });
}

var transition = {
  setTransition,
  transitionStart,
  transitionEnd
};

function slideTo(index, speed, runCallbacks, internal, initial) {
  if (index === void 0) {
    index = 0;
  }
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  if (typeof index === 'string') {
    index = parseInt(index, 10);
  }
  const swiper = this;
  let slideIndex = index;
  if (slideIndex < 0) slideIndex = 0;
  const {
    params,
    snapGrid,
    slidesGrid,
    previousIndex,
    activeIndex,
    rtlTranslate: rtl,
    wrapperEl,
    enabled
  } = swiper;
  if (!enabled && !internal && !initial || swiper.destroyed || swiper.animating && params.preventInteractionOnTransition) {
    return false;
  }
  if (typeof speed === 'undefined') {
    speed = swiper.params.speed;
  }
  const skip = Math.min(swiper.params.slidesPerGroupSkip, slideIndex);
  let snapIndex = skip + Math.floor((slideIndex - skip) / swiper.params.slidesPerGroup);
  if (snapIndex >= snapGrid.length) snapIndex = snapGrid.length - 1;
  const translate = -snapGrid[snapIndex];
  // Normalize slideIndex
  if (params.normalizeSlideIndex) {
    for (let i = 0; i < slidesGrid.length; i += 1) {
      const normalizedTranslate = -Math.floor(translate * 100);
      const normalizedGrid = Math.floor(slidesGrid[i] * 100);
      const normalizedGridNext = Math.floor(slidesGrid[i + 1] * 100);
      if (typeof slidesGrid[i + 1] !== 'undefined') {
        if (normalizedTranslate >= normalizedGrid && normalizedTranslate < normalizedGridNext - (normalizedGridNext - normalizedGrid) / 2) {
          slideIndex = i;
        } else if (normalizedTranslate >= normalizedGrid && normalizedTranslate < normalizedGridNext) {
          slideIndex = i + 1;
        }
      } else if (normalizedTranslate >= normalizedGrid) {
        slideIndex = i;
      }
    }
  }
  // Directions locks
  if (swiper.initialized && slideIndex !== activeIndex) {
    if (!swiper.allowSlideNext && (rtl ? translate > swiper.translate && translate > swiper.minTranslate() : translate < swiper.translate && translate < swiper.minTranslate())) {
      return false;
    }
    if (!swiper.allowSlidePrev && translate > swiper.translate && translate > swiper.maxTranslate()) {
      if ((activeIndex || 0) !== slideIndex) {
        return false;
      }
    }
  }
  if (slideIndex !== (previousIndex || 0) && runCallbacks) {
    swiper.emit('beforeSlideChangeStart');
  }

  // Update progress
  swiper.updateProgress(translate);
  let direction;
  if (slideIndex > activeIndex) direction = 'next';else if (slideIndex < activeIndex) direction = 'prev';else direction = 'reset';

  // initial virtual
  const isVirtual = swiper.virtual && swiper.params.virtual.enabled;
  const isInitialVirtual = isVirtual && initial;
  // Update Index
  if (!isInitialVirtual && (rtl && -translate === swiper.translate || !rtl && translate === swiper.translate)) {
    swiper.updateActiveIndex(slideIndex);
    // Update Height
    if (params.autoHeight) {
      swiper.updateAutoHeight();
    }
    swiper.updateSlidesClasses();
    if (params.effect !== 'slide') {
      swiper.setTranslate(translate);
    }
    if (direction !== 'reset') {
      swiper.transitionStart(runCallbacks, direction);
      swiper.transitionEnd(runCallbacks, direction);
    }
    return false;
  }
  if (params.cssMode) {
    const isH = swiper.isHorizontal();
    const t = rtl ? translate : -translate;
    if (speed === 0) {
      if (isVirtual) {
        swiper.wrapperEl.style.scrollSnapType = 'none';
        swiper._immediateVirtual = true;
      }
      if (isVirtual && !swiper._cssModeVirtualInitialSet && swiper.params.initialSlide > 0) {
        swiper._cssModeVirtualInitialSet = true;
        requestAnimationFrame(() => {
          wrapperEl[isH ? 'scrollLeft' : 'scrollTop'] = t;
        });
      } else {
        wrapperEl[isH ? 'scrollLeft' : 'scrollTop'] = t;
      }
      if (isVirtual) {
        requestAnimationFrame(() => {
          swiper.wrapperEl.style.scrollSnapType = '';
          swiper._immediateVirtual = false;
        });
      }
    } else {
      if (!swiper.support.smoothScroll) {
        (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.t)({
          swiper,
          targetPosition: t,
          side: isH ? 'left' : 'top'
        });
        return true;
      }
      wrapperEl.scrollTo({
        [isH ? 'left' : 'top']: t,
        behavior: 'smooth'
      });
    }
    return true;
  }
  swiper.setTransition(speed);
  swiper.setTranslate(translate);
  swiper.updateActiveIndex(slideIndex);
  swiper.updateSlidesClasses();
  swiper.emit('beforeTransitionStart', speed, internal);
  swiper.transitionStart(runCallbacks, direction);
  if (speed === 0) {
    swiper.transitionEnd(runCallbacks, direction);
  } else if (!swiper.animating) {
    swiper.animating = true;
    if (!swiper.onSlideToWrapperTransitionEnd) {
      swiper.onSlideToWrapperTransitionEnd = function transitionEnd(e) {
        if (!swiper || swiper.destroyed) return;
        if (e.target !== this) return;
        swiper.wrapperEl.removeEventListener('transitionend', swiper.onSlideToWrapperTransitionEnd);
        swiper.onSlideToWrapperTransitionEnd = null;
        delete swiper.onSlideToWrapperTransitionEnd;
        swiper.transitionEnd(runCallbacks, direction);
      };
    }
    swiper.wrapperEl.addEventListener('transitionend', swiper.onSlideToWrapperTransitionEnd);
  }
  return true;
}

function slideToLoop(index, speed, runCallbacks, internal) {
  if (index === void 0) {
    index = 0;
  }
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  if (typeof index === 'string') {
    const indexAsNumber = parseInt(index, 10);
    index = indexAsNumber;
  }
  const swiper = this;
  if (swiper.destroyed) return;
  if (typeof speed === 'undefined') {
    speed = swiper.params.speed;
  }
  const gridEnabled = swiper.grid && swiper.params.grid && swiper.params.grid.rows > 1;
  let newIndex = index;
  if (swiper.params.loop) {
    if (swiper.virtual && swiper.params.virtual.enabled) {
      // eslint-disable-next-line
      newIndex = newIndex + swiper.virtual.slidesBefore;
    } else {
      let targetSlideIndex;
      if (gridEnabled) {
        const slideIndex = newIndex * swiper.params.grid.rows;
        targetSlideIndex = swiper.slides.find(slideEl => slideEl.getAttribute('data-swiper-slide-index') * 1 === slideIndex).column;
      } else {
        targetSlideIndex = swiper.getSlideIndexByData(newIndex);
      }
      const cols = gridEnabled ? Math.ceil(swiper.slides.length / swiper.params.grid.rows) : swiper.slides.length;
      const {
        centeredSlides
      } = swiper.params;
      let slidesPerView = swiper.params.slidesPerView;
      if (slidesPerView === 'auto') {
        slidesPerView = swiper.slidesPerViewDynamic();
      } else {
        slidesPerView = Math.ceil(parseFloat(swiper.params.slidesPerView, 10));
        if (centeredSlides && slidesPerView % 2 === 0) {
          slidesPerView = slidesPerView + 1;
        }
      }
      let needLoopFix = cols - targetSlideIndex < slidesPerView;
      if (centeredSlides) {
        needLoopFix = needLoopFix || targetSlideIndex < Math.ceil(slidesPerView / 2);
      }
      if (internal && centeredSlides && swiper.params.slidesPerView !== 'auto' && !gridEnabled) {
        needLoopFix = false;
      }
      if (needLoopFix) {
        const direction = centeredSlides ? targetSlideIndex < swiper.activeIndex ? 'prev' : 'next' : targetSlideIndex - swiper.activeIndex - 1 < swiper.params.slidesPerView ? 'next' : 'prev';
        swiper.loopFix({
          direction,
          slideTo: true,
          activeSlideIndex: direction === 'next' ? targetSlideIndex + 1 : targetSlideIndex - cols + 1,
          slideRealIndex: direction === 'next' ? swiper.realIndex : undefined
        });
      }
      if (gridEnabled) {
        const slideIndex = newIndex * swiper.params.grid.rows;
        newIndex = swiper.slides.find(slideEl => slideEl.getAttribute('data-swiper-slide-index') * 1 === slideIndex).column;
      } else {
        newIndex = swiper.getSlideIndexByData(newIndex);
      }
    }
  }
  requestAnimationFrame(() => {
    swiper.slideTo(newIndex, speed, runCallbacks, internal);
  });
  return swiper;
}

/* eslint no-unused-vars: "off" */
function slideNext(speed, runCallbacks, internal) {
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  const swiper = this;
  const {
    enabled,
    params,
    animating
  } = swiper;
  if (!enabled || swiper.destroyed) return swiper;
  if (typeof speed === 'undefined') {
    speed = swiper.params.speed;
  }
  let perGroup = params.slidesPerGroup;
  if (params.slidesPerView === 'auto' && params.slidesPerGroup === 1 && params.slidesPerGroupAuto) {
    perGroup = Math.max(swiper.slidesPerViewDynamic('current', true), 1);
  }
  const increment = swiper.activeIndex < params.slidesPerGroupSkip ? 1 : perGroup;
  const isVirtual = swiper.virtual && params.virtual.enabled;
  if (params.loop) {
    if (animating && !isVirtual && params.loopPreventsSliding) return false;
    swiper.loopFix({
      direction: 'next'
    });
    // eslint-disable-next-line
    swiper._clientLeft = swiper.wrapperEl.clientLeft;
    if (swiper.activeIndex === swiper.slides.length - 1 && params.cssMode) {
      requestAnimationFrame(() => {
        swiper.slideTo(swiper.activeIndex + increment, speed, runCallbacks, internal);
      });
      return true;
    }
  }
  if (params.rewind && swiper.isEnd) {
    return swiper.slideTo(0, speed, runCallbacks, internal);
  }
  return swiper.slideTo(swiper.activeIndex + increment, speed, runCallbacks, internal);
}

/* eslint no-unused-vars: "off" */
function slidePrev(speed, runCallbacks, internal) {
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  const swiper = this;
  const {
    params,
    snapGrid,
    slidesGrid,
    rtlTranslate,
    enabled,
    animating
  } = swiper;
  if (!enabled || swiper.destroyed) return swiper;
  if (typeof speed === 'undefined') {
    speed = swiper.params.speed;
  }
  const isVirtual = swiper.virtual && params.virtual.enabled;
  if (params.loop) {
    if (animating && !isVirtual && params.loopPreventsSliding) return false;
    swiper.loopFix({
      direction: 'prev'
    });
    // eslint-disable-next-line
    swiper._clientLeft = swiper.wrapperEl.clientLeft;
  }
  const translate = rtlTranslate ? swiper.translate : -swiper.translate;
  function normalize(val) {
    if (val < 0) return -Math.floor(Math.abs(val));
    return Math.floor(val);
  }
  const normalizedTranslate = normalize(translate);
  const normalizedSnapGrid = snapGrid.map(val => normalize(val));
  let prevSnap = snapGrid[normalizedSnapGrid.indexOf(normalizedTranslate) - 1];
  if (typeof prevSnap === 'undefined' && params.cssMode) {
    let prevSnapIndex;
    snapGrid.forEach((snap, snapIndex) => {
      if (normalizedTranslate >= snap) {
        // prevSnap = snap;
        prevSnapIndex = snapIndex;
      }
    });
    if (typeof prevSnapIndex !== 'undefined') {
      prevSnap = snapGrid[prevSnapIndex > 0 ? prevSnapIndex - 1 : prevSnapIndex];
    }
  }
  let prevIndex = 0;
  if (typeof prevSnap !== 'undefined') {
    prevIndex = slidesGrid.indexOf(prevSnap);
    if (prevIndex < 0) prevIndex = swiper.activeIndex - 1;
    if (params.slidesPerView === 'auto' && params.slidesPerGroup === 1 && params.slidesPerGroupAuto) {
      prevIndex = prevIndex - swiper.slidesPerViewDynamic('previous', true) + 1;
      prevIndex = Math.max(prevIndex, 0);
    }
  }
  if (params.rewind && swiper.isBeginning) {
    const lastIndex = swiper.params.virtual && swiper.params.virtual.enabled && swiper.virtual ? swiper.virtual.slides.length - 1 : swiper.slides.length - 1;
    return swiper.slideTo(lastIndex, speed, runCallbacks, internal);
  } else if (params.loop && swiper.activeIndex === 0 && params.cssMode) {
    requestAnimationFrame(() => {
      swiper.slideTo(prevIndex, speed, runCallbacks, internal);
    });
    return true;
  }
  return swiper.slideTo(prevIndex, speed, runCallbacks, internal);
}

/* eslint no-unused-vars: "off" */
function slideReset(speed, runCallbacks, internal) {
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  const swiper = this;
  if (swiper.destroyed) return;
  if (typeof speed === 'undefined') {
    speed = swiper.params.speed;
  }
  return swiper.slideTo(swiper.activeIndex, speed, runCallbacks, internal);
}

/* eslint no-unused-vars: "off" */
function slideToClosest(speed, runCallbacks, internal, threshold) {
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  if (threshold === void 0) {
    threshold = 0.5;
  }
  const swiper = this;
  if (swiper.destroyed) return;
  if (typeof speed === 'undefined') {
    speed = swiper.params.speed;
  }
  let index = swiper.activeIndex;
  const skip = Math.min(swiper.params.slidesPerGroupSkip, index);
  const snapIndex = skip + Math.floor((index - skip) / swiper.params.slidesPerGroup);
  const translate = swiper.rtlTranslate ? swiper.translate : -swiper.translate;
  if (translate >= swiper.snapGrid[snapIndex]) {
    // The current translate is on or after the current snap index, so the choice
    // is between the current index and the one after it.
    const currentSnap = swiper.snapGrid[snapIndex];
    const nextSnap = swiper.snapGrid[snapIndex + 1];
    if (translate - currentSnap > (nextSnap - currentSnap) * threshold) {
      index += swiper.params.slidesPerGroup;
    }
  } else {
    // The current translate is before the current snap index, so the choice
    // is between the current index and the one before it.
    const prevSnap = swiper.snapGrid[snapIndex - 1];
    const currentSnap = swiper.snapGrid[snapIndex];
    if (translate - prevSnap <= (currentSnap - prevSnap) * threshold) {
      index -= swiper.params.slidesPerGroup;
    }
  }
  index = Math.max(index, 0);
  index = Math.min(index, swiper.slidesGrid.length - 1);
  return swiper.slideTo(index, speed, runCallbacks, internal);
}

function slideToClickedSlide() {
  const swiper = this;
  if (swiper.destroyed) return;
  const {
    params,
    slidesEl
  } = swiper;
  const slidesPerView = params.slidesPerView === 'auto' ? swiper.slidesPerViewDynamic() : params.slidesPerView;
  let slideToIndex = swiper.clickedIndex;
  let realIndex;
  const slideSelector = swiper.isElement ? `swiper-slide` : `.${params.slideClass}`;
  if (params.loop) {
    if (swiper.animating) return;
    realIndex = parseInt(swiper.clickedSlide.getAttribute('data-swiper-slide-index'), 10);
    if (params.centeredSlides) {
      if (slideToIndex < swiper.loopedSlides - slidesPerView / 2 || slideToIndex > swiper.slides.length - swiper.loopedSlides + slidesPerView / 2) {
        swiper.loopFix();
        slideToIndex = swiper.getSlideIndex((0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `${slideSelector}[data-swiper-slide-index="${realIndex}"]`)[0]);
        (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.n)(() => {
          swiper.slideTo(slideToIndex);
        });
      } else {
        swiper.slideTo(slideToIndex);
      }
    } else if (slideToIndex > swiper.slides.length - slidesPerView) {
      swiper.loopFix();
      slideToIndex = swiper.getSlideIndex((0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `${slideSelector}[data-swiper-slide-index="${realIndex}"]`)[0]);
      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.n)(() => {
        swiper.slideTo(slideToIndex);
      });
    } else {
      swiper.slideTo(slideToIndex);
    }
  } else {
    swiper.slideTo(slideToIndex);
  }
}

var slide = {
  slideTo,
  slideToLoop,
  slideNext,
  slidePrev,
  slideReset,
  slideToClosest,
  slideToClickedSlide
};

function loopCreate(slideRealIndex) {
  const swiper = this;
  const {
    params,
    slidesEl
  } = swiper;
  if (!params.loop || swiper.virtual && swiper.params.virtual.enabled) return;
  const initSlides = () => {
    const slides = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `.${params.slideClass}, swiper-slide`);
    slides.forEach((el, index) => {
      el.setAttribute('data-swiper-slide-index', index);
    });
  };
  const gridEnabled = swiper.grid && params.grid && params.grid.rows > 1;
  const slidesPerGroup = params.slidesPerGroup * (gridEnabled ? params.grid.rows : 1);
  const shouldFillGroup = swiper.slides.length % slidesPerGroup !== 0;
  const shouldFillGrid = gridEnabled && swiper.slides.length % params.grid.rows !== 0;
  const addBlankSlides = amountOfSlides => {
    for (let i = 0; i < amountOfSlides; i += 1) {
      const slideEl = swiper.isElement ? (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('swiper-slide', [params.slideBlankClass]) : (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', [params.slideClass, params.slideBlankClass]);
      swiper.slidesEl.append(slideEl);
    }
  };
  if (shouldFillGroup) {
    if (params.loopAddBlankSlides) {
      const slidesToAdd = slidesPerGroup - swiper.slides.length % slidesPerGroup;
      addBlankSlides(slidesToAdd);
      swiper.recalcSlides();
      swiper.updateSlides();
    } else {
      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.u)('Swiper Loop Warning: The number of slides is not even to slidesPerGroup, loop mode may not function properly. You need to add more slides (or make duplicates, or empty slides)');
    }
    initSlides();
  } else if (shouldFillGrid) {
    if (params.loopAddBlankSlides) {
      const slidesToAdd = params.grid.rows - swiper.slides.length % params.grid.rows;
      addBlankSlides(slidesToAdd);
      swiper.recalcSlides();
      swiper.updateSlides();
    } else {
      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.u)('Swiper Loop Warning: The number of slides is not even to grid.rows, loop mode may not function properly. You need to add more slides (or make duplicates, or empty slides)');
    }
    initSlides();
  } else {
    initSlides();
  }
  swiper.loopFix({
    slideRealIndex,
    direction: params.centeredSlides ? undefined : 'next'
  });
}

function loopFix(_temp) {
  let {
    slideRealIndex,
    slideTo = true,
    direction,
    setTranslate,
    activeSlideIndex,
    byController,
    byMousewheel
  } = _temp === void 0 ? {} : _temp;
  const swiper = this;
  if (!swiper.params.loop) return;
  swiper.emit('beforeLoopFix');
  const {
    slides,
    allowSlidePrev,
    allowSlideNext,
    slidesEl,
    params
  } = swiper;
  const {
    centeredSlides
  } = params;
  swiper.allowSlidePrev = true;
  swiper.allowSlideNext = true;
  if (swiper.virtual && params.virtual.enabled) {
    if (slideTo) {
      if (!params.centeredSlides && swiper.snapIndex === 0) {
        swiper.slideTo(swiper.virtual.slides.length, 0, false, true);
      } else if (params.centeredSlides && swiper.snapIndex < params.slidesPerView) {
        swiper.slideTo(swiper.virtual.slides.length + swiper.snapIndex, 0, false, true);
      } else if (swiper.snapIndex === swiper.snapGrid.length - 1) {
        swiper.slideTo(swiper.virtual.slidesBefore, 0, false, true);
      }
    }
    swiper.allowSlidePrev = allowSlidePrev;
    swiper.allowSlideNext = allowSlideNext;
    swiper.emit('loopFix');
    return;
  }
  let slidesPerView = params.slidesPerView;
  if (slidesPerView === 'auto') {
    slidesPerView = swiper.slidesPerViewDynamic();
  } else {
    slidesPerView = Math.ceil(parseFloat(params.slidesPerView, 10));
    if (centeredSlides && slidesPerView % 2 === 0) {
      slidesPerView = slidesPerView + 1;
    }
  }
  const slidesPerGroup = params.slidesPerGroupAuto ? slidesPerView : params.slidesPerGroup;
  let loopedSlides = slidesPerGroup;
  if (loopedSlides % slidesPerGroup !== 0) {
    loopedSlides += slidesPerGroup - loopedSlides % slidesPerGroup;
  }
  loopedSlides += params.loopAdditionalSlides;
  swiper.loopedSlides = loopedSlides;
  const gridEnabled = swiper.grid && params.grid && params.grid.rows > 1;
  if (slides.length < slidesPerView + loopedSlides) {
    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.u)('Swiper Loop Warning: The number of slides is not enough for loop mode, it will be disabled and not function properly. You need to add more slides (or make duplicates) or lower the values of slidesPerView and slidesPerGroup parameters');
  } else if (gridEnabled && params.grid.fill === 'row') {
    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.u)('Swiper Loop Warning: Loop mode is not compatible with grid.fill = `row`');
  }
  const prependSlidesIndexes = [];
  const appendSlidesIndexes = [];
  let activeIndex = swiper.activeIndex;
  if (typeof activeSlideIndex === 'undefined') {
    activeSlideIndex = swiper.getSlideIndex(slides.find(el => el.classList.contains(params.slideActiveClass)));
  } else {
    activeIndex = activeSlideIndex;
  }
  const isNext = direction === 'next' || !direction;
  const isPrev = direction === 'prev' || !direction;
  let slidesPrepended = 0;
  let slidesAppended = 0;
  const cols = gridEnabled ? Math.ceil(slides.length / params.grid.rows) : slides.length;
  const activeColIndex = gridEnabled ? slides[activeSlideIndex].column : activeSlideIndex;
  const activeColIndexWithShift = activeColIndex + (centeredSlides && typeof setTranslate === 'undefined' ? -slidesPerView / 2 + 0.5 : 0);
  // prepend last slides before start
  if (activeColIndexWithShift < loopedSlides) {
    slidesPrepended = Math.max(loopedSlides - activeColIndexWithShift, slidesPerGroup);
    for (let i = 0; i < loopedSlides - activeColIndexWithShift; i += 1) {
      const index = i - Math.floor(i / cols) * cols;
      if (gridEnabled) {
        const colIndexToPrepend = cols - index - 1;
        for (let i = slides.length - 1; i >= 0; i -= 1) {
          if (slides[i].column === colIndexToPrepend) prependSlidesIndexes.push(i);
        }
        // slides.forEach((slide, slideIndex) => {
        //   if (slide.column === colIndexToPrepend) prependSlidesIndexes.push(slideIndex);
        // });
      } else {
        prependSlidesIndexes.push(cols - index - 1);
      }
    }
  } else if (activeColIndexWithShift + slidesPerView > cols - loopedSlides) {
    slidesAppended = Math.max(activeColIndexWithShift - (cols - loopedSlides * 2), slidesPerGroup);
    for (let i = 0; i < slidesAppended; i += 1) {
      const index = i - Math.floor(i / cols) * cols;
      if (gridEnabled) {
        slides.forEach((slide, slideIndex) => {
          if (slide.column === index) appendSlidesIndexes.push(slideIndex);
        });
      } else {
        appendSlidesIndexes.push(index);
      }
    }
  }
  swiper.__preventObserver__ = true;
  requestAnimationFrame(() => {
    swiper.__preventObserver__ = false;
  });
  if (isPrev) {
    prependSlidesIndexes.forEach(index => {
      slides[index].swiperLoopMoveDOM = true;
      slidesEl.prepend(slides[index]);
      slides[index].swiperLoopMoveDOM = false;
    });
  }
  if (isNext) {
    appendSlidesIndexes.forEach(index => {
      slides[index].swiperLoopMoveDOM = true;
      slidesEl.append(slides[index]);
      slides[index].swiperLoopMoveDOM = false;
    });
  }
  swiper.recalcSlides();
  if (params.slidesPerView === 'auto') {
    swiper.updateSlides();
  } else if (gridEnabled && (prependSlidesIndexes.length > 0 && isPrev || appendSlidesIndexes.length > 0 && isNext)) {
    swiper.slides.forEach((slide, slideIndex) => {
      swiper.grid.updateSlide(slideIndex, slide, swiper.slides);
    });
  }
  if (params.watchSlidesProgress) {
    swiper.updateSlidesOffset();
  }
  if (slideTo) {
    if (prependSlidesIndexes.length > 0 && isPrev) {
      if (typeof slideRealIndex === 'undefined') {
        const currentSlideTranslate = swiper.slidesGrid[activeIndex];
        const newSlideTranslate = swiper.slidesGrid[activeIndex + slidesPrepended];
        const diff = newSlideTranslate - currentSlideTranslate;
        if (byMousewheel) {
          swiper.setTranslate(swiper.translate - diff);
        } else {
          swiper.slideTo(activeIndex + Math.ceil(slidesPrepended), 0, false, true);
          if (setTranslate) {
            swiper.touchEventsData.startTranslate = swiper.touchEventsData.startTranslate - diff;
            swiper.touchEventsData.currentTranslate = swiper.touchEventsData.currentTranslate - diff;
          }
        }
      } else {
        if (setTranslate) {
          const shift = gridEnabled ? prependSlidesIndexes.length / params.grid.rows : prependSlidesIndexes.length;
          swiper.slideTo(swiper.activeIndex + shift, 0, false, true);
          swiper.touchEventsData.currentTranslate = swiper.translate;
        }
      }
    } else if (appendSlidesIndexes.length > 0 && isNext) {
      if (typeof slideRealIndex === 'undefined') {
        const currentSlideTranslate = swiper.slidesGrid[activeIndex];
        const newSlideTranslate = swiper.slidesGrid[activeIndex - slidesAppended];
        const diff = newSlideTranslate - currentSlideTranslate;
        if (byMousewheel) {
          swiper.setTranslate(swiper.translate - diff);
        } else {
          swiper.slideTo(activeIndex - slidesAppended, 0, false, true);
          if (setTranslate) {
            swiper.touchEventsData.startTranslate = swiper.touchEventsData.startTranslate - diff;
            swiper.touchEventsData.currentTranslate = swiper.touchEventsData.currentTranslate - diff;
          }
        }
      } else {
        const shift = gridEnabled ? appendSlidesIndexes.length / params.grid.rows : appendSlidesIndexes.length;
        swiper.slideTo(swiper.activeIndex - shift, 0, false, true);
      }
    }
  }
  swiper.allowSlidePrev = allowSlidePrev;
  swiper.allowSlideNext = allowSlideNext;
  if (swiper.controller && swiper.controller.control && !byController) {
    const loopParams = {
      slideRealIndex,
      direction,
      setTranslate,
      activeSlideIndex,
      byController: true
    };
    if (Array.isArray(swiper.controller.control)) {
      swiper.controller.control.forEach(c => {
        if (!c.destroyed && c.params.loop) c.loopFix({
          ...loopParams,
          slideTo: c.params.slidesPerView === params.slidesPerView ? slideTo : false
        });
      });
    } else if (swiper.controller.control instanceof swiper.constructor && swiper.controller.control.params.loop) {
      swiper.controller.control.loopFix({
        ...loopParams,
        slideTo: swiper.controller.control.params.slidesPerView === params.slidesPerView ? slideTo : false
      });
    }
  }
  swiper.emit('loopFix');
}

function loopDestroy() {
  const swiper = this;
  const {
    params,
    slidesEl
  } = swiper;
  if (!params.loop || swiper.virtual && swiper.params.virtual.enabled) return;
  swiper.recalcSlides();
  const newSlidesOrder = [];
  swiper.slides.forEach(slideEl => {
    const index = typeof slideEl.swiperSlideIndex === 'undefined' ? slideEl.getAttribute('data-swiper-slide-index') * 1 : slideEl.swiperSlideIndex;
    newSlidesOrder[index] = slideEl;
  });
  swiper.slides.forEach(slideEl => {
    slideEl.removeAttribute('data-swiper-slide-index');
  });
  newSlidesOrder.forEach(slideEl => {
    slidesEl.append(slideEl);
  });
  swiper.recalcSlides();
  swiper.slideTo(swiper.realIndex, 0);
}

var loop = {
  loopCreate,
  loopFix,
  loopDestroy
};

function setGrabCursor(moving) {
  const swiper = this;
  if (!swiper.params.simulateTouch || swiper.params.watchOverflow && swiper.isLocked || swiper.params.cssMode) return;
  const el = swiper.params.touchEventsTarget === 'container' ? swiper.el : swiper.wrapperEl;
  if (swiper.isElement) {
    swiper.__preventObserver__ = true;
  }
  el.style.cursor = 'move';
  el.style.cursor = moving ? 'grabbing' : 'grab';
  if (swiper.isElement) {
    requestAnimationFrame(() => {
      swiper.__preventObserver__ = false;
    });
  }
}

function unsetGrabCursor() {
  const swiper = this;
  if (swiper.params.watchOverflow && swiper.isLocked || swiper.params.cssMode) {
    return;
  }
  if (swiper.isElement) {
    swiper.__preventObserver__ = true;
  }
  swiper[swiper.params.touchEventsTarget === 'container' ? 'el' : 'wrapperEl'].style.cursor = '';
  if (swiper.isElement) {
    requestAnimationFrame(() => {
      swiper.__preventObserver__ = false;
    });
  }
}

var grabCursor = {
  setGrabCursor,
  unsetGrabCursor
};

// Modified from https://stackoverflow.com/questions/54520554/custom-element-getrootnode-closest-function-crossing-multiple-parent-shadowd
function closestElement(selector, base) {
  if (base === void 0) {
    base = this;
  }
  function __closestFrom(el) {
    if (!el || el === (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)() || el === (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)()) return null;
    if (el.assignedSlot) el = el.assignedSlot;
    const found = el.closest(selector);
    if (!found && !el.getRootNode) {
      return null;
    }
    return found || __closestFrom(el.getRootNode().host);
  }
  return __closestFrom(base);
}
function preventEdgeSwipe(swiper, event, startX) {
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  const {
    params
  } = swiper;
  const edgeSwipeDetection = params.edgeSwipeDetection;
  const edgeSwipeThreshold = params.edgeSwipeThreshold;
  if (edgeSwipeDetection && (startX <= edgeSwipeThreshold || startX >= window.innerWidth - edgeSwipeThreshold)) {
    if (edgeSwipeDetection === 'prevent') {
      event.preventDefault();
      return true;
    }
    return false;
  }
  return true;
}
function onTouchStart(event) {
  const swiper = this;
  const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
  let e = event;
  if (e.originalEvent) e = e.originalEvent;
  const data = swiper.touchEventsData;
  if (e.type === 'pointerdown') {
    if (data.pointerId !== null && data.pointerId !== e.pointerId) {
      return;
    }
    data.pointerId = e.pointerId;
  } else if (e.type === 'touchstart' && e.targetTouches.length === 1) {
    data.touchId = e.targetTouches[0].identifier;
  }
  if (e.type === 'touchstart') {
    // don't proceed touch event
    preventEdgeSwipe(swiper, e, e.targetTouches[0].pageX);
    return;
  }
  const {
    params,
    touches,
    enabled
  } = swiper;
  if (!enabled) return;
  if (!params.simulateTouch && e.pointerType === 'mouse') return;
  if (swiper.animating && params.preventInteractionOnTransition) {
    return;
  }
  if (!swiper.animating && params.cssMode && params.loop) {
    swiper.loopFix();
  }
  let targetEl = e.target;
  if (params.touchEventsTarget === 'wrapper') {
    if (!(0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.v)(targetEl, swiper.wrapperEl)) return;
  }
  if ('which' in e && e.which === 3) return;
  if ('button' in e && e.button > 0) return;
  if (data.isTouched && data.isMoved) return;

  // change target el for shadow root component
  const swipingClassHasValue = !!params.noSwipingClass && params.noSwipingClass !== '';
  // eslint-disable-next-line
  const eventPath = e.composedPath ? e.composedPath() : e.path;
  if (swipingClassHasValue && e.target && e.target.shadowRoot && eventPath) {
    targetEl = eventPath[0];
  }
  const noSwipingSelector = params.noSwipingSelector ? params.noSwipingSelector : `.${params.noSwipingClass}`;
  const isTargetShadow = !!(e.target && e.target.shadowRoot);

  // use closestElement for shadow root element to get the actual closest for nested shadow root element
  if (params.noSwiping && (isTargetShadow ? closestElement(noSwipingSelector, targetEl) : targetEl.closest(noSwipingSelector))) {
    swiper.allowClick = true;
    return;
  }
  if (params.swipeHandler) {
    if (!targetEl.closest(params.swipeHandler)) return;
  }
  touches.currentX = e.pageX;
  touches.currentY = e.pageY;
  const startX = touches.currentX;
  const startY = touches.currentY;

  // Do NOT start if iOS edge swipe is detected. Otherwise iOS app cannot swipe-to-go-back anymore

  if (!preventEdgeSwipe(swiper, e, startX)) {
    return;
  }
  Object.assign(data, {
    isTouched: true,
    isMoved: false,
    allowTouchCallbacks: true,
    isScrolling: undefined,
    startMoving: undefined
  });
  touches.startX = startX;
  touches.startY = startY;
  data.touchStartTime = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)();
  swiper.allowClick = true;
  swiper.updateSize();
  swiper.swipeDirection = undefined;
  if (params.threshold > 0) data.allowThresholdMove = false;
  let preventDefault = true;
  if (targetEl.matches(data.focusableElements)) {
    preventDefault = false;
    if (targetEl.nodeName === 'SELECT') {
      data.isTouched = false;
    }
  }
  if (document.activeElement && document.activeElement.matches(data.focusableElements) && document.activeElement !== targetEl && (e.pointerType === 'mouse' || e.pointerType !== 'mouse' && !targetEl.matches(data.focusableElements))) {
    document.activeElement.blur();
  }
  const shouldPreventDefault = preventDefault && swiper.allowTouchMove && params.touchStartPreventDefault;
  if ((params.touchStartForcePreventDefault || shouldPreventDefault) && !targetEl.isContentEditable) {
    e.preventDefault();
  }
  if (params.freeMode && params.freeMode.enabled && swiper.freeMode && swiper.animating && !params.cssMode) {
    swiper.freeMode.onTouchStart();
  }
  swiper.emit('touchStart', e);
}

function onTouchMove(event) {
  const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
  const swiper = this;
  const data = swiper.touchEventsData;
  const {
    params,
    touches,
    rtlTranslate: rtl,
    enabled
  } = swiper;
  if (!enabled) return;
  if (!params.simulateTouch && event.pointerType === 'mouse') return;
  let e = event;
  if (e.originalEvent) e = e.originalEvent;
  if (e.type === 'pointermove') {
    if (data.touchId !== null) return; // return from pointer if we use touch
    const id = e.pointerId;
    if (id !== data.pointerId) return;
  }
  let targetTouch;
  if (e.type === 'touchmove') {
    targetTouch = [...e.changedTouches].find(t => t.identifier === data.touchId);
    if (!targetTouch || targetTouch.identifier !== data.touchId) return;
  } else {
    targetTouch = e;
  }
  if (!data.isTouched) {
    if (data.startMoving && data.isScrolling) {
      swiper.emit('touchMoveOpposite', e);
    }
    return;
  }
  const pageX = targetTouch.pageX;
  const pageY = targetTouch.pageY;
  if (e.preventedByNestedSwiper) {
    touches.startX = pageX;
    touches.startY = pageY;
    return;
  }
  if (!swiper.allowTouchMove) {
    if (!e.target.matches(data.focusableElements)) {
      swiper.allowClick = false;
    }
    if (data.isTouched) {
      Object.assign(touches, {
        startX: pageX,
        startY: pageY,
        currentX: pageX,
        currentY: pageY
      });
      data.touchStartTime = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)();
    }
    return;
  }
  if (params.touchReleaseOnEdges && !params.loop) {
    if (swiper.isVertical()) {
      // Vertical
      if (pageY < touches.startY && swiper.translate <= swiper.maxTranslate() || pageY > touches.startY && swiper.translate >= swiper.minTranslate()) {
        data.isTouched = false;
        data.isMoved = false;
        return;
      }
    } else if (pageX < touches.startX && swiper.translate <= swiper.maxTranslate() || pageX > touches.startX && swiper.translate >= swiper.minTranslate()) {
      return;
    }
  }
  if (document.activeElement && document.activeElement.matches(data.focusableElements) && document.activeElement !== e.target && e.pointerType !== 'mouse') {
    document.activeElement.blur();
  }
  if (document.activeElement) {
    if (e.target === document.activeElement && e.target.matches(data.focusableElements)) {
      data.isMoved = true;
      swiper.allowClick = false;
      return;
    }
  }
  if (data.allowTouchCallbacks) {
    swiper.emit('touchMove', e);
  }
  touches.previousX = touches.currentX;
  touches.previousY = touches.currentY;
  touches.currentX = pageX;
  touches.currentY = pageY;
  const diffX = touches.currentX - touches.startX;
  const diffY = touches.currentY - touches.startY;
  if (swiper.params.threshold && Math.sqrt(diffX ** 2 + diffY ** 2) < swiper.params.threshold) return;
  if (typeof data.isScrolling === 'undefined') {
    let touchAngle;
    if (swiper.isHorizontal() && touches.currentY === touches.startY || swiper.isVertical() && touches.currentX === touches.startX) {
      data.isScrolling = false;
    } else {
      // eslint-disable-next-line
      if (diffX * diffX + diffY * diffY >= 25) {
        touchAngle = Math.atan2(Math.abs(diffY), Math.abs(diffX)) * 180 / Math.PI;
        data.isScrolling = swiper.isHorizontal() ? touchAngle > params.touchAngle : 90 - touchAngle > params.touchAngle;
      }
    }
  }
  if (data.isScrolling) {
    swiper.emit('touchMoveOpposite', e);
  }
  if (typeof data.startMoving === 'undefined') {
    if (touches.currentX !== touches.startX || touches.currentY !== touches.startY) {
      data.startMoving = true;
    }
  }
  if (data.isScrolling || e.type === 'touchmove' && data.preventTouchMoveFromPointerMove) {
    data.isTouched = false;
    return;
  }
  if (!data.startMoving) {
    return;
  }
  swiper.allowClick = false;
  if (!params.cssMode && e.cancelable) {
    e.preventDefault();
  }
  if (params.touchMoveStopPropagation && !params.nested) {
    e.stopPropagation();
  }
  let diff = swiper.isHorizontal() ? diffX : diffY;
  let touchesDiff = swiper.isHorizontal() ? touches.currentX - touches.previousX : touches.currentY - touches.previousY;
  if (params.oneWayMovement) {
    diff = Math.abs(diff) * (rtl ? 1 : -1);
    touchesDiff = Math.abs(touchesDiff) * (rtl ? 1 : -1);
  }
  touches.diff = diff;
  diff *= params.touchRatio;
  if (rtl) {
    diff = -diff;
    touchesDiff = -touchesDiff;
  }
  const prevTouchesDirection = swiper.touchesDirection;
  swiper.swipeDirection = diff > 0 ? 'prev' : 'next';
  swiper.touchesDirection = touchesDiff > 0 ? 'prev' : 'next';
  const isLoop = swiper.params.loop && !params.cssMode;
  const allowLoopFix = swiper.touchesDirection === 'next' && swiper.allowSlideNext || swiper.touchesDirection === 'prev' && swiper.allowSlidePrev;
  if (!data.isMoved) {
    if (isLoop && allowLoopFix) {
      swiper.loopFix({
        direction: swiper.swipeDirection
      });
    }
    data.startTranslate = swiper.getTranslate();
    swiper.setTransition(0);
    if (swiper.animating) {
      const evt = new window.CustomEvent('transitionend', {
        bubbles: true,
        cancelable: true,
        detail: {
          bySwiperTouchMove: true
        }
      });
      swiper.wrapperEl.dispatchEvent(evt);
    }
    data.allowMomentumBounce = false;
    // Grab Cursor
    if (params.grabCursor && (swiper.allowSlideNext === true || swiper.allowSlidePrev === true)) {
      swiper.setGrabCursor(true);
    }
    swiper.emit('sliderFirstMove', e);
  }
  let loopFixed;
  new Date().getTime();
  if (data.isMoved && data.allowThresholdMove && prevTouchesDirection !== swiper.touchesDirection && isLoop && allowLoopFix && Math.abs(diff) >= 1) {
    Object.assign(touches, {
      startX: pageX,
      startY: pageY,
      currentX: pageX,
      currentY: pageY,
      startTranslate: data.currentTranslate
    });
    data.loopSwapReset = true;
    data.startTranslate = data.currentTranslate;
    return;
  }
  swiper.emit('sliderMove', e);
  data.isMoved = true;
  data.currentTranslate = diff + data.startTranslate;
  let disableParentSwiper = true;
  let resistanceRatio = params.resistanceRatio;
  if (params.touchReleaseOnEdges) {
    resistanceRatio = 0;
  }
  if (diff > 0) {
    if (isLoop && allowLoopFix && !loopFixed && data.allowThresholdMove && data.currentTranslate > (params.centeredSlides ? swiper.minTranslate() - swiper.slidesSizesGrid[swiper.activeIndex + 1] - (params.slidesPerView !== 'auto' && swiper.slides.length - params.slidesPerView >= 2 ? swiper.slidesSizesGrid[swiper.activeIndex + 1] + swiper.params.spaceBetween : 0) - swiper.params.spaceBetween : swiper.minTranslate())) {
      swiper.loopFix({
        direction: 'prev',
        setTranslate: true,
        activeSlideIndex: 0
      });
    }
    if (data.currentTranslate > swiper.minTranslate()) {
      disableParentSwiper = false;
      if (params.resistance) {
        data.currentTranslate = swiper.minTranslate() - 1 + (-swiper.minTranslate() + data.startTranslate + diff) ** resistanceRatio;
      }
    }
  } else if (diff < 0) {
    if (isLoop && allowLoopFix && !loopFixed && data.allowThresholdMove && data.currentTranslate < (params.centeredSlides ? swiper.maxTranslate() + swiper.slidesSizesGrid[swiper.slidesSizesGrid.length - 1] + swiper.params.spaceBetween + (params.slidesPerView !== 'auto' && swiper.slides.length - params.slidesPerView >= 2 ? swiper.slidesSizesGrid[swiper.slidesSizesGrid.length - 1] + swiper.params.spaceBetween : 0) : swiper.maxTranslate())) {
      swiper.loopFix({
        direction: 'next',
        setTranslate: true,
        activeSlideIndex: swiper.slides.length - (params.slidesPerView === 'auto' ? swiper.slidesPerViewDynamic() : Math.ceil(parseFloat(params.slidesPerView, 10)))
      });
    }
    if (data.currentTranslate < swiper.maxTranslate()) {
      disableParentSwiper = false;
      if (params.resistance) {
        data.currentTranslate = swiper.maxTranslate() + 1 - (swiper.maxTranslate() - data.startTranslate - diff) ** resistanceRatio;
      }
    }
  }
  if (disableParentSwiper) {
    e.preventedByNestedSwiper = true;
  }

  // Directions locks
  if (!swiper.allowSlideNext && swiper.swipeDirection === 'next' && data.currentTranslate < data.startTranslate) {
    data.currentTranslate = data.startTranslate;
  }
  if (!swiper.allowSlidePrev && swiper.swipeDirection === 'prev' && data.currentTranslate > data.startTranslate) {
    data.currentTranslate = data.startTranslate;
  }
  if (!swiper.allowSlidePrev && !swiper.allowSlideNext) {
    data.currentTranslate = data.startTranslate;
  }

  // Threshold
  if (params.threshold > 0) {
    if (Math.abs(diff) > params.threshold || data.allowThresholdMove) {
      if (!data.allowThresholdMove) {
        data.allowThresholdMove = true;
        touches.startX = touches.currentX;
        touches.startY = touches.currentY;
        data.currentTranslate = data.startTranslate;
        touches.diff = swiper.isHorizontal() ? touches.currentX - touches.startX : touches.currentY - touches.startY;
        return;
      }
    } else {
      data.currentTranslate = data.startTranslate;
      return;
    }
  }
  if (!params.followFinger || params.cssMode) return;

  // Update active index in free mode
  if (params.freeMode && params.freeMode.enabled && swiper.freeMode || params.watchSlidesProgress) {
    swiper.updateActiveIndex();
    swiper.updateSlidesClasses();
  }
  if (params.freeMode && params.freeMode.enabled && swiper.freeMode) {
    swiper.freeMode.onTouchMove();
  }
  // Update progress
  swiper.updateProgress(data.currentTranslate);
  // Update translate
  swiper.setTranslate(data.currentTranslate);
}

function onTouchEnd(event) {
  const swiper = this;
  const data = swiper.touchEventsData;
  let e = event;
  if (e.originalEvent) e = e.originalEvent;
  let targetTouch;
  const isTouchEvent = e.type === 'touchend' || e.type === 'touchcancel';
  if (!isTouchEvent) {
    if (data.touchId !== null) return; // return from pointer if we use touch
    if (e.pointerId !== data.pointerId) return;
    targetTouch = e;
  } else {
    targetTouch = [...e.changedTouches].find(t => t.identifier === data.touchId);
    if (!targetTouch || targetTouch.identifier !== data.touchId) return;
  }
  if (['pointercancel', 'pointerout', 'pointerleave', 'contextmenu'].includes(e.type)) {
    const proceed = ['pointercancel', 'contextmenu'].includes(e.type) && (swiper.browser.isSafari || swiper.browser.isWebView);
    if (!proceed) {
      return;
    }
  }
  data.pointerId = null;
  data.touchId = null;
  const {
    params,
    touches,
    rtlTranslate: rtl,
    slidesGrid,
    enabled
  } = swiper;
  if (!enabled) return;
  if (!params.simulateTouch && e.pointerType === 'mouse') return;
  if (data.allowTouchCallbacks) {
    swiper.emit('touchEnd', e);
  }
  data.allowTouchCallbacks = false;
  if (!data.isTouched) {
    if (data.isMoved && params.grabCursor) {
      swiper.setGrabCursor(false);
    }
    data.isMoved = false;
    data.startMoving = false;
    return;
  }

  // Return Grab Cursor
  if (params.grabCursor && data.isMoved && data.isTouched && (swiper.allowSlideNext === true || swiper.allowSlidePrev === true)) {
    swiper.setGrabCursor(false);
  }

  // Time diff
  const touchEndTime = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)();
  const timeDiff = touchEndTime - data.touchStartTime;

  // Tap, doubleTap, Click
  if (swiper.allowClick) {
    const pathTree = e.path || e.composedPath && e.composedPath();
    swiper.updateClickedSlide(pathTree && pathTree[0] || e.target, pathTree);
    swiper.emit('tap click', e);
    if (timeDiff < 300 && touchEndTime - data.lastClickTime < 300) {
      swiper.emit('doubleTap doubleClick', e);
    }
  }
  data.lastClickTime = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)();
  (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.n)(() => {
    if (!swiper.destroyed) swiper.allowClick = true;
  });
  if (!data.isTouched || !data.isMoved || !swiper.swipeDirection || touches.diff === 0 && !data.loopSwapReset || data.currentTranslate === data.startTranslate && !data.loopSwapReset) {
    data.isTouched = false;
    data.isMoved = false;
    data.startMoving = false;
    return;
  }
  data.isTouched = false;
  data.isMoved = false;
  data.startMoving = false;
  let currentPos;
  if (params.followFinger) {
    currentPos = rtl ? swiper.translate : -swiper.translate;
  } else {
    currentPos = -data.currentTranslate;
  }
  if (params.cssMode) {
    return;
  }
  if (params.freeMode && params.freeMode.enabled) {
    swiper.freeMode.onTouchEnd({
      currentPos
    });
    return;
  }

  // Find current slide
  const swipeToLast = currentPos >= -swiper.maxTranslate() && !swiper.params.loop;
  let stopIndex = 0;
  let groupSize = swiper.slidesSizesGrid[0];
  for (let i = 0; i < slidesGrid.length; i += i < params.slidesPerGroupSkip ? 1 : params.slidesPerGroup) {
    const increment = i < params.slidesPerGroupSkip - 1 ? 1 : params.slidesPerGroup;
    if (typeof slidesGrid[i + increment] !== 'undefined') {
      if (swipeToLast || currentPos >= slidesGrid[i] && currentPos < slidesGrid[i + increment]) {
        stopIndex = i;
        groupSize = slidesGrid[i + increment] - slidesGrid[i];
      }
    } else if (swipeToLast || currentPos >= slidesGrid[i]) {
      stopIndex = i;
      groupSize = slidesGrid[slidesGrid.length - 1] - slidesGrid[slidesGrid.length - 2];
    }
  }
  let rewindFirstIndex = null;
  let rewindLastIndex = null;
  if (params.rewind) {
    if (swiper.isBeginning) {
      rewindLastIndex = params.virtual && params.virtual.enabled && swiper.virtual ? swiper.virtual.slides.length - 1 : swiper.slides.length - 1;
    } else if (swiper.isEnd) {
      rewindFirstIndex = 0;
    }
  }
  // Find current slide size
  const ratio = (currentPos - slidesGrid[stopIndex]) / groupSize;
  const increment = stopIndex < params.slidesPerGroupSkip - 1 ? 1 : params.slidesPerGroup;
  if (timeDiff > params.longSwipesMs) {
    // Long touches
    if (!params.longSwipes) {
      swiper.slideTo(swiper.activeIndex);
      return;
    }
    if (swiper.swipeDirection === 'next') {
      if (ratio >= params.longSwipesRatio) swiper.slideTo(params.rewind && swiper.isEnd ? rewindFirstIndex : stopIndex + increment);else swiper.slideTo(stopIndex);
    }
    if (swiper.swipeDirection === 'prev') {
      if (ratio > 1 - params.longSwipesRatio) {
        swiper.slideTo(stopIndex + increment);
      } else if (rewindLastIndex !== null && ratio < 0 && Math.abs(ratio) > params.longSwipesRatio) {
        swiper.slideTo(rewindLastIndex);
      } else {
        swiper.slideTo(stopIndex);
      }
    }
  } else {
    // Short swipes
    if (!params.shortSwipes) {
      swiper.slideTo(swiper.activeIndex);
      return;
    }
    const isNavButtonTarget = swiper.navigation && (e.target === swiper.navigation.nextEl || e.target === swiper.navigation.prevEl);
    if (!isNavButtonTarget) {
      if (swiper.swipeDirection === 'next') {
        swiper.slideTo(rewindFirstIndex !== null ? rewindFirstIndex : stopIndex + increment);
      }
      if (swiper.swipeDirection === 'prev') {
        swiper.slideTo(rewindLastIndex !== null ? rewindLastIndex : stopIndex);
      }
    } else if (e.target === swiper.navigation.nextEl) {
      swiper.slideTo(stopIndex + increment);
    } else {
      swiper.slideTo(stopIndex);
    }
  }
}

function onResize() {
  const swiper = this;
  const {
    params,
    el
  } = swiper;
  if (el && el.offsetWidth === 0) return;

  // Breakpoints
  if (params.breakpoints) {
    swiper.setBreakpoint();
  }

  // Save locks
  const {
    allowSlideNext,
    allowSlidePrev,
    snapGrid
  } = swiper;
  const isVirtual = swiper.virtual && swiper.params.virtual.enabled;

  // Disable locks on resize
  swiper.allowSlideNext = true;
  swiper.allowSlidePrev = true;
  swiper.updateSize();
  swiper.updateSlides();
  swiper.updateSlidesClasses();
  const isVirtualLoop = isVirtual && params.loop;
  if ((params.slidesPerView === 'auto' || params.slidesPerView > 1) && swiper.isEnd && !swiper.isBeginning && !swiper.params.centeredSlides && !isVirtualLoop) {
    swiper.slideTo(swiper.slides.length - 1, 0, false, true);
  } else {
    if (swiper.params.loop && !isVirtual) {
      swiper.slideToLoop(swiper.realIndex, 0, false, true);
    } else {
      swiper.slideTo(swiper.activeIndex, 0, false, true);
    }
  }
  if (swiper.autoplay && swiper.autoplay.running && swiper.autoplay.paused) {
    clearTimeout(swiper.autoplay.resizeTimeout);
    swiper.autoplay.resizeTimeout = setTimeout(() => {
      if (swiper.autoplay && swiper.autoplay.running && swiper.autoplay.paused) {
        swiper.autoplay.resume();
      }
    }, 500);
  }
  // Return locks after resize
  swiper.allowSlidePrev = allowSlidePrev;
  swiper.allowSlideNext = allowSlideNext;
  if (swiper.params.watchOverflow && snapGrid !== swiper.snapGrid) {
    swiper.checkOverflow();
  }
}

function onClick(e) {
  const swiper = this;
  if (!swiper.enabled) return;
  if (!swiper.allowClick) {
    if (swiper.params.preventClicks) e.preventDefault();
    if (swiper.params.preventClicksPropagation && swiper.animating) {
      e.stopPropagation();
      e.stopImmediatePropagation();
    }
  }
}

function onScroll() {
  const swiper = this;
  const {
    wrapperEl,
    rtlTranslate,
    enabled
  } = swiper;
  if (!enabled) return;
  swiper.previousTranslate = swiper.translate;
  if (swiper.isHorizontal()) {
    swiper.translate = -wrapperEl.scrollLeft;
  } else {
    swiper.translate = -wrapperEl.scrollTop;
  }
  // eslint-disable-next-line
  if (swiper.translate === 0) swiper.translate = 0;
  swiper.updateActiveIndex();
  swiper.updateSlidesClasses();
  let newProgress;
  const translatesDiff = swiper.maxTranslate() - swiper.minTranslate();
  if (translatesDiff === 0) {
    newProgress = 0;
  } else {
    newProgress = (swiper.translate - swiper.minTranslate()) / translatesDiff;
  }
  if (newProgress !== swiper.progress) {
    swiper.updateProgress(rtlTranslate ? -swiper.translate : swiper.translate);
  }
  swiper.emit('setTranslate', swiper.translate, false);
}

function onLoad(e) {
  const swiper = this;
  processLazyPreloader(swiper, e.target);
  if (swiper.params.cssMode || swiper.params.slidesPerView !== 'auto' && !swiper.params.autoHeight) {
    return;
  }
  swiper.update();
}

function onDocumentTouchStart() {
  const swiper = this;
  if (swiper.documentTouchHandlerProceeded) return;
  swiper.documentTouchHandlerProceeded = true;
  if (swiper.params.touchReleaseOnEdges) {
    swiper.el.style.touchAction = 'auto';
  }
}

const events = (swiper, method) => {
  const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
  const {
    params,
    el,
    wrapperEl,
    device
  } = swiper;
  const capture = !!params.nested;
  const domMethod = method === 'on' ? 'addEventListener' : 'removeEventListener';
  const swiperMethod = method;
  if (!el || typeof el === 'string') return;

  // Touch Events
  document[domMethod]('touchstart', swiper.onDocumentTouchStart, {
    passive: false,
    capture
  });
  el[domMethod]('touchstart', swiper.onTouchStart, {
    passive: false
  });
  el[domMethod]('pointerdown', swiper.onTouchStart, {
    passive: false
  });
  document[domMethod]('touchmove', swiper.onTouchMove, {
    passive: false,
    capture
  });
  document[domMethod]('pointermove', swiper.onTouchMove, {
    passive: false,
    capture
  });
  document[domMethod]('touchend', swiper.onTouchEnd, {
    passive: true
  });
  document[domMethod]('pointerup', swiper.onTouchEnd, {
    passive: true
  });
  document[domMethod]('pointercancel', swiper.onTouchEnd, {
    passive: true
  });
  document[domMethod]('touchcancel', swiper.onTouchEnd, {
    passive: true
  });
  document[domMethod]('pointerout', swiper.onTouchEnd, {
    passive: true
  });
  document[domMethod]('pointerleave', swiper.onTouchEnd, {
    passive: true
  });
  document[domMethod]('contextmenu', swiper.onTouchEnd, {
    passive: true
  });

  // Prevent Links Clicks
  if (params.preventClicks || params.preventClicksPropagation) {
    el[domMethod]('click', swiper.onClick, true);
  }
  if (params.cssMode) {
    wrapperEl[domMethod]('scroll', swiper.onScroll);
  }

  // Resize handler
  if (params.updateOnWindowResize) {
    swiper[swiperMethod](device.ios || device.android ? 'resize orientationchange observerUpdate' : 'resize observerUpdate', onResize, true);
  } else {
    swiper[swiperMethod]('observerUpdate', onResize, true);
  }

  // Images loader
  el[domMethod]('load', swiper.onLoad, {
    capture: true
  });
};
function attachEvents() {
  const swiper = this;
  const {
    params
  } = swiper;
  swiper.onTouchStart = onTouchStart.bind(swiper);
  swiper.onTouchMove = onTouchMove.bind(swiper);
  swiper.onTouchEnd = onTouchEnd.bind(swiper);
  swiper.onDocumentTouchStart = onDocumentTouchStart.bind(swiper);
  if (params.cssMode) {
    swiper.onScroll = onScroll.bind(swiper);
  }
  swiper.onClick = onClick.bind(swiper);
  swiper.onLoad = onLoad.bind(swiper);
  events(swiper, 'on');
}
function detachEvents() {
  const swiper = this;
  events(swiper, 'off');
}
var events$1 = {
  attachEvents,
  detachEvents
};

const isGridEnabled = (swiper, params) => {
  return swiper.grid && params.grid && params.grid.rows > 1;
};
function setBreakpoint() {
  const swiper = this;
  const {
    realIndex,
    initialized,
    params,
    el
  } = swiper;
  const breakpoints = params.breakpoints;
  if (!breakpoints || breakpoints && Object.keys(breakpoints).length === 0) return;
  const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();

  // Get breakpoint for window/container width and update parameters
  const breakpointsBase = params.breakpointsBase === 'window' || !params.breakpointsBase ? params.breakpointsBase : 'container';
  const breakpointContainer = ['window', 'container'].includes(params.breakpointsBase) || !params.breakpointsBase ? swiper.el : document.querySelector(params.breakpointsBase);
  const breakpoint = swiper.getBreakpoint(breakpoints, breakpointsBase, breakpointContainer);
  if (!breakpoint || swiper.currentBreakpoint === breakpoint) return;
  const breakpointOnlyParams = breakpoint in breakpoints ? breakpoints[breakpoint] : undefined;
  const breakpointParams = breakpointOnlyParams || swiper.originalParams;
  const wasMultiRow = isGridEnabled(swiper, params);
  const isMultiRow = isGridEnabled(swiper, breakpointParams);
  const wasGrabCursor = swiper.params.grabCursor;
  const isGrabCursor = breakpointParams.grabCursor;
  const wasEnabled = params.enabled;
  if (wasMultiRow && !isMultiRow) {
    el.classList.remove(`${params.containerModifierClass}grid`, `${params.containerModifierClass}grid-column`);
    swiper.emitContainerClasses();
  } else if (!wasMultiRow && isMultiRow) {
    el.classList.add(`${params.containerModifierClass}grid`);
    if (breakpointParams.grid.fill && breakpointParams.grid.fill === 'column' || !breakpointParams.grid.fill && params.grid.fill === 'column') {
      el.classList.add(`${params.containerModifierClass}grid-column`);
    }
    swiper.emitContainerClasses();
  }
  if (wasGrabCursor && !isGrabCursor) {
    swiper.unsetGrabCursor();
  } else if (!wasGrabCursor && isGrabCursor) {
    swiper.setGrabCursor();
  }

  // Toggle navigation, pagination, scrollbar
  ['navigation', 'pagination', 'scrollbar'].forEach(prop => {
    if (typeof breakpointParams[prop] === 'undefined') return;
    const wasModuleEnabled = params[prop] && params[prop].enabled;
    const isModuleEnabled = breakpointParams[prop] && breakpointParams[prop].enabled;
    if (wasModuleEnabled && !isModuleEnabled) {
      swiper[prop].disable();
    }
    if (!wasModuleEnabled && isModuleEnabled) {
      swiper[prop].enable();
    }
  });
  const directionChanged = breakpointParams.direction && breakpointParams.direction !== params.direction;
  const needsReLoop = params.loop && (breakpointParams.slidesPerView !== params.slidesPerView || directionChanged);
  const wasLoop = params.loop;
  if (directionChanged && initialized) {
    swiper.changeDirection();
  }
  (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.w)(swiper.params, breakpointParams);
  const isEnabled = swiper.params.enabled;
  const hasLoop = swiper.params.loop;
  Object.assign(swiper, {
    allowTouchMove: swiper.params.allowTouchMove,
    allowSlideNext: swiper.params.allowSlideNext,
    allowSlidePrev: swiper.params.allowSlidePrev
  });
  if (wasEnabled && !isEnabled) {
    swiper.disable();
  } else if (!wasEnabled && isEnabled) {
    swiper.enable();
  }
  swiper.currentBreakpoint = breakpoint;
  swiper.emit('_beforeBreakpoint', breakpointParams);
  if (initialized) {
    if (needsReLoop) {
      swiper.loopDestroy();
      swiper.loopCreate(realIndex);
      swiper.updateSlides();
    } else if (!wasLoop && hasLoop) {
      swiper.loopCreate(realIndex);
      swiper.updateSlides();
    } else if (wasLoop && !hasLoop) {
      swiper.loopDestroy();
    }
  }
  swiper.emit('breakpoint', breakpointParams);
}

function getBreakpoint(breakpoints, base, containerEl) {
  if (base === void 0) {
    base = 'window';
  }
  if (!breakpoints || base === 'container' && !containerEl) return undefined;
  let breakpoint = false;
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  const currentHeight = base === 'window' ? window.innerHeight : containerEl.clientHeight;
  const points = Object.keys(breakpoints).map(point => {
    if (typeof point === 'string' && point.indexOf('@') === 0) {
      const minRatio = parseFloat(point.substr(1));
      const value = currentHeight * minRatio;
      return {
        value,
        point
      };
    }
    return {
      value: point,
      point
    };
  });
  points.sort((a, b) => parseInt(a.value, 10) - parseInt(b.value, 10));
  for (let i = 0; i < points.length; i += 1) {
    const {
      point,
      value
    } = points[i];
    if (base === 'window') {
      if (window.matchMedia(`(min-width: ${value}px)`).matches) {
        breakpoint = point;
      }
    } else if (value <= containerEl.clientWidth) {
      breakpoint = point;
    }
  }
  return breakpoint || 'max';
}

var breakpoints = {
  setBreakpoint,
  getBreakpoint
};

function prepareClasses(entries, prefix) {
  const resultClasses = [];
  entries.forEach(item => {
    if (typeof item === 'object') {
      Object.keys(item).forEach(classNames => {
        if (item[classNames]) {
          resultClasses.push(prefix + classNames);
        }
      });
    } else if (typeof item === 'string') {
      resultClasses.push(prefix + item);
    }
  });
  return resultClasses;
}
function addClasses() {
  const swiper = this;
  const {
    classNames,
    params,
    rtl,
    el,
    device
  } = swiper;
  // prettier-ignore
  const suffixes = prepareClasses(['initialized', params.direction, {
    'free-mode': swiper.params.freeMode && params.freeMode.enabled
  }, {
    'autoheight': params.autoHeight
  }, {
    'rtl': rtl
  }, {
    'grid': params.grid && params.grid.rows > 1
  }, {
    'grid-column': params.grid && params.grid.rows > 1 && params.grid.fill === 'column'
  }, {
    'android': device.android
  }, {
    'ios': device.ios
  }, {
    'css-mode': params.cssMode
  }, {
    'centered': params.cssMode && params.centeredSlides
  }, {
    'watch-progress': params.watchSlidesProgress
  }], params.containerModifierClass);
  classNames.push(...suffixes);
  el.classList.add(...classNames);
  swiper.emitContainerClasses();
}

function removeClasses() {
  const swiper = this;
  const {
    el,
    classNames
  } = swiper;
  if (!el || typeof el === 'string') return;
  el.classList.remove(...classNames);
  swiper.emitContainerClasses();
}

var classes = {
  addClasses,
  removeClasses
};

function checkOverflow() {
  const swiper = this;
  const {
    isLocked: wasLocked,
    params
  } = swiper;
  const {
    slidesOffsetBefore
  } = params;
  if (slidesOffsetBefore) {
    const lastSlideIndex = swiper.slides.length - 1;
    const lastSlideRightEdge = swiper.slidesGrid[lastSlideIndex] + swiper.slidesSizesGrid[lastSlideIndex] + slidesOffsetBefore * 2;
    swiper.isLocked = swiper.size > lastSlideRightEdge;
  } else {
    swiper.isLocked = swiper.snapGrid.length === 1;
  }
  if (params.allowSlideNext === true) {
    swiper.allowSlideNext = !swiper.isLocked;
  }
  if (params.allowSlidePrev === true) {
    swiper.allowSlidePrev = !swiper.isLocked;
  }
  if (wasLocked && wasLocked !== swiper.isLocked) {
    swiper.isEnd = false;
  }
  if (wasLocked !== swiper.isLocked) {
    swiper.emit(swiper.isLocked ? 'lock' : 'unlock');
  }
}
var checkOverflow$1 = {
  checkOverflow
};

var defaults = {
  init: true,
  direction: 'horizontal',
  oneWayMovement: false,
  swiperElementNodeName: 'SWIPER-CONTAINER',
  touchEventsTarget: 'wrapper',
  initialSlide: 0,
  speed: 300,
  cssMode: false,
  updateOnWindowResize: true,
  resizeObserver: true,
  nested: false,
  createElements: false,
  eventsPrefix: 'swiper',
  enabled: true,
  focusableElements: 'input, select, option, textarea, button, video, label',
  // Overrides
  width: null,
  height: null,
  //
  preventInteractionOnTransition: false,
  // ssr
  userAgent: null,
  url: null,
  // To support iOS's swipe-to-go-back gesture (when being used in-app).
  edgeSwipeDetection: false,
  edgeSwipeThreshold: 20,
  // Autoheight
  autoHeight: false,
  // Set wrapper width
  setWrapperSize: false,
  // Virtual Translate
  virtualTranslate: false,
  // Effects
  effect: 'slide',
  // 'slide' or 'fade' or 'cube' or 'coverflow' or 'flip'

  // Breakpoints
  breakpoints: undefined,
  breakpointsBase: 'window',
  // Slides grid
  spaceBetween: 0,
  slidesPerView: 1,
  slidesPerGroup: 1,
  slidesPerGroupSkip: 0,
  slidesPerGroupAuto: false,
  centeredSlides: false,
  centeredSlidesBounds: false,
  slidesOffsetBefore: 0,
  // in px
  slidesOffsetAfter: 0,
  // in px
  normalizeSlideIndex: true,
  centerInsufficientSlides: false,
  // Disable swiper and hide navigation when container not overflow
  watchOverflow: true,
  // Round length
  roundLengths: false,
  // Touches
  touchRatio: 1,
  touchAngle: 45,
  simulateTouch: true,
  shortSwipes: true,
  longSwipes: true,
  longSwipesRatio: 0.5,
  longSwipesMs: 300,
  followFinger: true,
  allowTouchMove: true,
  threshold: 5,
  touchMoveStopPropagation: false,
  touchStartPreventDefault: true,
  touchStartForcePreventDefault: false,
  touchReleaseOnEdges: false,
  // Unique Navigation Elements
  uniqueNavElements: true,
  // Resistance
  resistance: true,
  resistanceRatio: 0.85,
  // Progress
  watchSlidesProgress: false,
  // Cursor
  grabCursor: false,
  // Clicks
  preventClicks: true,
  preventClicksPropagation: true,
  slideToClickedSlide: false,
  // loop
  loop: false,
  loopAddBlankSlides: true,
  loopAdditionalSlides: 0,
  loopPreventsSliding: true,
  // rewind
  rewind: false,
  // Swiping/no swiping
  allowSlidePrev: true,
  allowSlideNext: true,
  swipeHandler: null,
  // '.swipe-handler',
  noSwiping: true,
  noSwipingClass: 'swiper-no-swiping',
  noSwipingSelector: null,
  // Passive Listeners
  passiveListeners: true,
  maxBackfaceHiddenSlides: 10,
  // NS
  containerModifierClass: 'swiper-',
  // NEW
  slideClass: 'swiper-slide',
  slideBlankClass: 'swiper-slide-blank',
  slideActiveClass: 'swiper-slide-active',
  slideVisibleClass: 'swiper-slide-visible',
  slideFullyVisibleClass: 'swiper-slide-fully-visible',
  slideNextClass: 'swiper-slide-next',
  slidePrevClass: 'swiper-slide-prev',
  wrapperClass: 'swiper-wrapper',
  lazyPreloaderClass: 'swiper-lazy-preloader',
  lazyPreloadPrevNext: 0,
  // Callbacks
  runCallbacksOnInit: true,
  // Internals
  _emitClasses: false
};

function moduleExtendParams(params, allModulesParams) {
  return function extendParams(obj) {
    if (obj === void 0) {
      obj = {};
    }
    const moduleParamName = Object.keys(obj)[0];
    const moduleParams = obj[moduleParamName];
    if (typeof moduleParams !== 'object' || moduleParams === null) {
      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.w)(allModulesParams, obj);
      return;
    }
    if (params[moduleParamName] === true) {
      params[moduleParamName] = {
        enabled: true
      };
    }
    if (moduleParamName === 'navigation' && params[moduleParamName] && params[moduleParamName].enabled && !params[moduleParamName].prevEl && !params[moduleParamName].nextEl) {
      params[moduleParamName].auto = true;
    }
    if (['pagination', 'scrollbar'].indexOf(moduleParamName) >= 0 && params[moduleParamName] && params[moduleParamName].enabled && !params[moduleParamName].el) {
      params[moduleParamName].auto = true;
    }
    if (!(moduleParamName in params && 'enabled' in moduleParams)) {
      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.w)(allModulesParams, obj);
      return;
    }
    if (typeof params[moduleParamName] === 'object' && !('enabled' in params[moduleParamName])) {
      params[moduleParamName].enabled = true;
    }
    if (!params[moduleParamName]) params[moduleParamName] = {
      enabled: false
    };
    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.w)(allModulesParams, obj);
  };
}

/* eslint no-param-reassign: "off" */
const prototypes = {
  eventsEmitter,
  update,
  translate,
  transition,
  slide,
  loop,
  grabCursor,
  events: events$1,
  breakpoints,
  checkOverflow: checkOverflow$1,
  classes
};
const extendedDefaults = {};
class Swiper {
  constructor() {
    let el;
    let params;
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    if (args.length === 1 && args[0].constructor && Object.prototype.toString.call(args[0]).slice(8, -1) === 'Object') {
      params = args[0];
    } else {
      [el, params] = args;
    }
    if (!params) params = {};
    params = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.w)({}, params);
    if (el && !params.el) params.el = el;
    const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
    if (params.el && typeof params.el === 'string' && document.querySelectorAll(params.el).length > 1) {
      const swipers = [];
      document.querySelectorAll(params.el).forEach(containerEl => {
        const newParams = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.w)({}, params, {
          el: containerEl
        });
        swipers.push(new Swiper(newParams));
      });
      // eslint-disable-next-line no-constructor-return
      return swipers;
    }

    // Swiper Instance
    const swiper = this;
    swiper.__swiper__ = true;
    swiper.support = getSupport();
    swiper.device = getDevice({
      userAgent: params.userAgent
    });
    swiper.browser = getBrowser();
    swiper.eventsListeners = {};
    swiper.eventsAnyListeners = [];
    swiper.modules = [...swiper.__modules__];
    if (params.modules && Array.isArray(params.modules)) {
      swiper.modules.push(...params.modules);
    }
    const allModulesParams = {};
    swiper.modules.forEach(mod => {
      mod({
        params,
        swiper,
        extendParams: moduleExtendParams(params, allModulesParams),
        on: swiper.on.bind(swiper),
        once: swiper.once.bind(swiper),
        off: swiper.off.bind(swiper),
        emit: swiper.emit.bind(swiper)
      });
    });

    // Extend defaults with modules params
    const swiperParams = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.w)({}, defaults, allModulesParams);

    // Extend defaults with passed params
    swiper.params = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.w)({}, swiperParams, extendedDefaults, params);
    swiper.originalParams = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.w)({}, swiper.params);
    swiper.passedParams = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.w)({}, params);

    // add event listeners
    if (swiper.params && swiper.params.on) {
      Object.keys(swiper.params.on).forEach(eventName => {
        swiper.on(eventName, swiper.params.on[eventName]);
      });
    }
    if (swiper.params && swiper.params.onAny) {
      swiper.onAny(swiper.params.onAny);
    }

    // Extend Swiper
    Object.assign(swiper, {
      enabled: swiper.params.enabled,
      el,
      // Classes
      classNames: [],
      // Slides
      slides: [],
      slidesGrid: [],
      snapGrid: [],
      slidesSizesGrid: [],
      // isDirection
      isHorizontal() {
        return swiper.params.direction === 'horizontal';
      },
      isVertical() {
        return swiper.params.direction === 'vertical';
      },
      // Indexes
      activeIndex: 0,
      realIndex: 0,
      //
      isBeginning: true,
      isEnd: false,
      // Props
      translate: 0,
      previousTranslate: 0,
      progress: 0,
      velocity: 0,
      animating: false,
      cssOverflowAdjustment() {
        // Returns 0 unless `translate` is > 2**23
        // Should be subtracted from css values to prevent overflow
        return Math.trunc(this.translate / 2 ** 23) * 2 ** 23;
      },
      // Locks
      allowSlideNext: swiper.params.allowSlideNext,
      allowSlidePrev: swiper.params.allowSlidePrev,
      // Touch Events
      touchEventsData: {
        isTouched: undefined,
        isMoved: undefined,
        allowTouchCallbacks: undefined,
        touchStartTime: undefined,
        isScrolling: undefined,
        currentTranslate: undefined,
        startTranslate: undefined,
        allowThresholdMove: undefined,
        // Form elements to match
        focusableElements: swiper.params.focusableElements,
        // Last click time
        lastClickTime: 0,
        clickTimeout: undefined,
        // Velocities
        velocities: [],
        allowMomentumBounce: undefined,
        startMoving: undefined,
        pointerId: null,
        touchId: null
      },
      // Clicks
      allowClick: true,
      // Touches
      allowTouchMove: swiper.params.allowTouchMove,
      touches: {
        startX: 0,
        startY: 0,
        currentX: 0,
        currentY: 0,
        diff: 0
      },
      // Images
      imagesToLoad: [],
      imagesLoaded: 0
    });
    swiper.emit('_swiper');

    // Init
    if (swiper.params.init) {
      swiper.init();
    }

    // Return app instance
    // eslint-disable-next-line no-constructor-return
    return swiper;
  }
  getDirectionLabel(property) {
    if (this.isHorizontal()) {
      return property;
    }
    // prettier-ignore
    return {
      'width': 'height',
      'margin-top': 'margin-left',
      'margin-bottom ': 'margin-right',
      'margin-left': 'margin-top',
      'margin-right': 'margin-bottom',
      'padding-left': 'padding-top',
      'padding-right': 'padding-bottom',
      'marginRight': 'marginBottom'
    }[property];
  }
  getSlideIndex(slideEl) {
    const {
      slidesEl,
      params
    } = this;
    const slides = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `.${params.slideClass}, swiper-slide`);
    const firstSlideIndex = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.h)(slides[0]);
    return (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.h)(slideEl) - firstSlideIndex;
  }
  getSlideIndexByData(index) {
    return this.getSlideIndex(this.slides.find(slideEl => slideEl.getAttribute('data-swiper-slide-index') * 1 === index));
  }
  recalcSlides() {
    const swiper = this;
    const {
      slidesEl,
      params
    } = swiper;
    swiper.slides = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `.${params.slideClass}, swiper-slide`);
  }
  enable() {
    const swiper = this;
    if (swiper.enabled) return;
    swiper.enabled = true;
    if (swiper.params.grabCursor) {
      swiper.setGrabCursor();
    }
    swiper.emit('enable');
  }
  disable() {
    const swiper = this;
    if (!swiper.enabled) return;
    swiper.enabled = false;
    if (swiper.params.grabCursor) {
      swiper.unsetGrabCursor();
    }
    swiper.emit('disable');
  }
  setProgress(progress, speed) {
    const swiper = this;
    progress = Math.min(Math.max(progress, 0), 1);
    const min = swiper.minTranslate();
    const max = swiper.maxTranslate();
    const current = (max - min) * progress + min;
    swiper.translateTo(current, typeof speed === 'undefined' ? 0 : speed);
    swiper.updateActiveIndex();
    swiper.updateSlidesClasses();
  }
  emitContainerClasses() {
    const swiper = this;
    if (!swiper.params._emitClasses || !swiper.el) return;
    const cls = swiper.el.className.split(' ').filter(className => {
      return className.indexOf('swiper') === 0 || className.indexOf(swiper.params.containerModifierClass) === 0;
    });
    swiper.emit('_containerClasses', cls.join(' '));
  }
  getSlideClasses(slideEl) {
    const swiper = this;
    if (swiper.destroyed) return '';
    return slideEl.className.split(' ').filter(className => {
      return className.indexOf('swiper-slide') === 0 || className.indexOf(swiper.params.slideClass) === 0;
    }).join(' ');
  }
  emitSlidesClasses() {
    const swiper = this;
    if (!swiper.params._emitClasses || !swiper.el) return;
    const updates = [];
    swiper.slides.forEach(slideEl => {
      const classNames = swiper.getSlideClasses(slideEl);
      updates.push({
        slideEl,
        classNames
      });
      swiper.emit('_slideClass', slideEl, classNames);
    });
    swiper.emit('_slideClasses', updates);
  }
  slidesPerViewDynamic(view, exact) {
    if (view === void 0) {
      view = 'current';
    }
    if (exact === void 0) {
      exact = false;
    }
    const swiper = this;
    const {
      params,
      slides,
      slidesGrid,
      slidesSizesGrid,
      size: swiperSize,
      activeIndex
    } = swiper;
    let spv = 1;
    if (typeof params.slidesPerView === 'number') return params.slidesPerView;
    if (params.centeredSlides) {
      let slideSize = slides[activeIndex] ? Math.ceil(slides[activeIndex].swiperSlideSize) : 0;
      let breakLoop;
      for (let i = activeIndex + 1; i < slides.length; i += 1) {
        if (slides[i] && !breakLoop) {
          slideSize += Math.ceil(slides[i].swiperSlideSize);
          spv += 1;
          if (slideSize > swiperSize) breakLoop = true;
        }
      }
      for (let i = activeIndex - 1; i >= 0; i -= 1) {
        if (slides[i] && !breakLoop) {
          slideSize += slides[i].swiperSlideSize;
          spv += 1;
          if (slideSize > swiperSize) breakLoop = true;
        }
      }
    } else {
      // eslint-disable-next-line
      if (view === 'current') {
        for (let i = activeIndex + 1; i < slides.length; i += 1) {
          const slideInView = exact ? slidesGrid[i] + slidesSizesGrid[i] - slidesGrid[activeIndex] < swiperSize : slidesGrid[i] - slidesGrid[activeIndex] < swiperSize;
          if (slideInView) {
            spv += 1;
          }
        }
      } else {
        // previous
        for (let i = activeIndex - 1; i >= 0; i -= 1) {
          const slideInView = slidesGrid[activeIndex] - slidesGrid[i] < swiperSize;
          if (slideInView) {
            spv += 1;
          }
        }
      }
    }
    return spv;
  }
  update() {
    const swiper = this;
    if (!swiper || swiper.destroyed) return;
    const {
      snapGrid,
      params
    } = swiper;
    // Breakpoints
    if (params.breakpoints) {
      swiper.setBreakpoint();
    }
    [...swiper.el.querySelectorAll('[loading="lazy"]')].forEach(imageEl => {
      if (imageEl.complete) {
        processLazyPreloader(swiper, imageEl);
      }
    });
    swiper.updateSize();
    swiper.updateSlides();
    swiper.updateProgress();
    swiper.updateSlidesClasses();
    function setTranslate() {
      const translateValue = swiper.rtlTranslate ? swiper.translate * -1 : swiper.translate;
      const newTranslate = Math.min(Math.max(translateValue, swiper.maxTranslate()), swiper.minTranslate());
      swiper.setTranslate(newTranslate);
      swiper.updateActiveIndex();
      swiper.updateSlidesClasses();
    }
    let translated;
    if (params.freeMode && params.freeMode.enabled && !params.cssMode) {
      setTranslate();
      if (params.autoHeight) {
        swiper.updateAutoHeight();
      }
    } else {
      if ((params.slidesPerView === 'auto' || params.slidesPerView > 1) && swiper.isEnd && !params.centeredSlides) {
        const slides = swiper.virtual && params.virtual.enabled ? swiper.virtual.slides : swiper.slides;
        translated = swiper.slideTo(slides.length - 1, 0, false, true);
      } else {
        translated = swiper.slideTo(swiper.activeIndex, 0, false, true);
      }
      if (!translated) {
        setTranslate();
      }
    }
    if (params.watchOverflow && snapGrid !== swiper.snapGrid) {
      swiper.checkOverflow();
    }
    swiper.emit('update');
  }
  changeDirection(newDirection, needUpdate) {
    if (needUpdate === void 0) {
      needUpdate = true;
    }
    const swiper = this;
    const currentDirection = swiper.params.direction;
    if (!newDirection) {
      // eslint-disable-next-line
      newDirection = currentDirection === 'horizontal' ? 'vertical' : 'horizontal';
    }
    if (newDirection === currentDirection || newDirection !== 'horizontal' && newDirection !== 'vertical') {
      return swiper;
    }
    swiper.el.classList.remove(`${swiper.params.containerModifierClass}${currentDirection}`);
    swiper.el.classList.add(`${swiper.params.containerModifierClass}${newDirection}`);
    swiper.emitContainerClasses();
    swiper.params.direction = newDirection;
    swiper.slides.forEach(slideEl => {
      if (newDirection === 'vertical') {
        slideEl.style.width = '';
      } else {
        slideEl.style.height = '';
      }
    });
    swiper.emit('changeDirection');
    if (needUpdate) swiper.update();
    return swiper;
  }
  changeLanguageDirection(direction) {
    const swiper = this;
    if (swiper.rtl && direction === 'rtl' || !swiper.rtl && direction === 'ltr') return;
    swiper.rtl = direction === 'rtl';
    swiper.rtlTranslate = swiper.params.direction === 'horizontal' && swiper.rtl;
    if (swiper.rtl) {
      swiper.el.classList.add(`${swiper.params.containerModifierClass}rtl`);
      swiper.el.dir = 'rtl';
    } else {
      swiper.el.classList.remove(`${swiper.params.containerModifierClass}rtl`);
      swiper.el.dir = 'ltr';
    }
    swiper.update();
  }
  mount(element) {
    const swiper = this;
    if (swiper.mounted) return true;

    // Find el
    let el = element || swiper.params.el;
    if (typeof el === 'string') {
      el = document.querySelector(el);
    }
    if (!el) {
      return false;
    }
    el.swiper = swiper;
    if (el.parentNode && el.parentNode.host && el.parentNode.host.nodeName === swiper.params.swiperElementNodeName.toUpperCase()) {
      swiper.isElement = true;
    }
    const getWrapperSelector = () => {
      return `.${(swiper.params.wrapperClass || '').trim().split(' ').join('.')}`;
    };
    const getWrapper = () => {
      if (el && el.shadowRoot && el.shadowRoot.querySelector) {
        const res = el.shadowRoot.querySelector(getWrapperSelector());
        // Children needs to return slot items
        return res;
      }
      return (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(el, getWrapperSelector())[0];
    };
    // Find Wrapper
    let wrapperEl = getWrapper();
    if (!wrapperEl && swiper.params.createElements) {
      wrapperEl = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', swiper.params.wrapperClass);
      el.append(wrapperEl);
      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(el, `.${swiper.params.slideClass}`).forEach(slideEl => {
        wrapperEl.append(slideEl);
      });
    }
    Object.assign(swiper, {
      el,
      wrapperEl,
      slidesEl: swiper.isElement && !el.parentNode.host.slideSlots ? el.parentNode.host : wrapperEl,
      hostEl: swiper.isElement ? el.parentNode.host : el,
      mounted: true,
      // RTL
      rtl: el.dir.toLowerCase() === 'rtl' || (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.p)(el, 'direction') === 'rtl',
      rtlTranslate: swiper.params.direction === 'horizontal' && (el.dir.toLowerCase() === 'rtl' || (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.p)(el, 'direction') === 'rtl'),
      wrongRTL: (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.p)(wrapperEl, 'display') === '-webkit-box'
    });
    return true;
  }
  init(el) {
    const swiper = this;
    if (swiper.initialized) return swiper;
    const mounted = swiper.mount(el);
    if (mounted === false) return swiper;
    swiper.emit('beforeInit');

    // Set breakpoint
    if (swiper.params.breakpoints) {
      swiper.setBreakpoint();
    }

    // Add Classes
    swiper.addClasses();

    // Update size
    swiper.updateSize();

    // Update slides
    swiper.updateSlides();
    if (swiper.params.watchOverflow) {
      swiper.checkOverflow();
    }

    // Set Grab Cursor
    if (swiper.params.grabCursor && swiper.enabled) {
      swiper.setGrabCursor();
    }

    // Slide To Initial Slide
    if (swiper.params.loop && swiper.virtual && swiper.params.virtual.enabled) {
      swiper.slideTo(swiper.params.initialSlide + swiper.virtual.slidesBefore, 0, swiper.params.runCallbacksOnInit, false, true);
    } else {
      swiper.slideTo(swiper.params.initialSlide, 0, swiper.params.runCallbacksOnInit, false, true);
    }

    // Create loop
    if (swiper.params.loop) {
      swiper.loopCreate();
    }

    // Attach events
    swiper.attachEvents();
    const lazyElements = [...swiper.el.querySelectorAll('[loading="lazy"]')];
    if (swiper.isElement) {
      lazyElements.push(...swiper.hostEl.querySelectorAll('[loading="lazy"]'));
    }
    lazyElements.forEach(imageEl => {
      if (imageEl.complete) {
        processLazyPreloader(swiper, imageEl);
      } else {
        imageEl.addEventListener('load', e => {
          processLazyPreloader(swiper, e.target);
        });
      }
    });
    preload(swiper);

    // Init Flag
    swiper.initialized = true;
    preload(swiper);

    // Emit
    swiper.emit('init');
    swiper.emit('afterInit');
    return swiper;
  }
  destroy(deleteInstance, cleanStyles) {
    if (deleteInstance === void 0) {
      deleteInstance = true;
    }
    if (cleanStyles === void 0) {
      cleanStyles = true;
    }
    const swiper = this;
    const {
      params,
      el,
      wrapperEl,
      slides
    } = swiper;
    if (typeof swiper.params === 'undefined' || swiper.destroyed) {
      return null;
    }
    swiper.emit('beforeDestroy');

    // Init Flag
    swiper.initialized = false;

    // Detach events
    swiper.detachEvents();

    // Destroy loop
    if (params.loop) {
      swiper.loopDestroy();
    }

    // Cleanup styles
    if (cleanStyles) {
      swiper.removeClasses();
      if (el && typeof el !== 'string') {
        el.removeAttribute('style');
      }
      if (wrapperEl) {
        wrapperEl.removeAttribute('style');
      }
      if (slides && slides.length) {
        slides.forEach(slideEl => {
          slideEl.classList.remove(params.slideVisibleClass, params.slideFullyVisibleClass, params.slideActiveClass, params.slideNextClass, params.slidePrevClass);
          slideEl.removeAttribute('style');
          slideEl.removeAttribute('data-swiper-slide-index');
        });
      }
    }
    swiper.emit('destroy');

    // Detach emitter events
    Object.keys(swiper.eventsListeners).forEach(eventName => {
      swiper.off(eventName);
    });
    if (deleteInstance !== false) {
      if (swiper.el && typeof swiper.el !== 'string') {
        swiper.el.swiper = null;
      }
      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.x)(swiper);
    }
    swiper.destroyed = true;
    return null;
  }
  static extendDefaults(newDefaults) {
    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.w)(extendedDefaults, newDefaults);
  }
  static get extendedDefaults() {
    return extendedDefaults;
  }
  static get defaults() {
    return defaults;
  }
  static installModule(mod) {
    if (!Swiper.prototype.__modules__) Swiper.prototype.__modules__ = [];
    const modules = Swiper.prototype.__modules__;
    if (typeof mod === 'function' && modules.indexOf(mod) < 0) {
      modules.push(mod);
    }
  }
  static use(module) {
    if (Array.isArray(module)) {
      module.forEach(m => Swiper.installModule(m));
      return Swiper;
    }
    Swiper.installModule(module);
    return Swiper;
  }
}
Object.keys(prototypes).forEach(prototypeGroup => {
  Object.keys(prototypes[prototypeGroup]).forEach(protoMethod => {
    Swiper.prototype[protoMethod] = prototypes[prototypeGroup][protoMethod];
  });
});
Swiper.use([Resize, Observer]);




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/shared/update-on-virtual-data.mjs":
/*!****************************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/shared/update-on-virtual-data.mjs ***!
  \****************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   a: () => (/* binding */ getChangedParams),
/* harmony export */   g: () => (/* binding */ getParams),
/* harmony export */   m: () => (/* binding */ mountSwiper),
/* harmony export */   u: () => (/* binding */ updateOnVirtualData)
/* harmony export */ });
/* harmony import */ var _update_swiper_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./update-swiper.mjs */ "../plugin-slug/node_modules/swiper/shared/update-swiper.mjs");
/* harmony import */ var _swiper_core_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./swiper-core.mjs */ "../plugin-slug/node_modules/swiper/shared/swiper-core.mjs");



function getParams(obj, splitEvents) {
  if (obj === void 0) {
    obj = {};
  }
  if (splitEvents === void 0) {
    splitEvents = true;
  }
  const params = {
    on: {}
  };
  const events = {};
  const passedParams = {};
  (0,_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_0__.e)(params, _swiper_core_mjs__WEBPACK_IMPORTED_MODULE_1__.d);
  params._emitClasses = true;
  params.init = false;
  const rest = {};
  const allowedParams = _update_swiper_mjs__WEBPACK_IMPORTED_MODULE_0__.p.map(key => key.replace(/_/, ''));
  const plainObj = Object.assign({}, obj);
  Object.keys(plainObj).forEach(key => {
    if (typeof obj[key] === 'undefined') return;
    if (allowedParams.indexOf(key) >= 0) {
      if ((0,_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_0__.i)(obj[key])) {
        params[key] = {};
        passedParams[key] = {};
        (0,_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_0__.e)(params[key], obj[key]);
        (0,_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_0__.e)(passedParams[key], obj[key]);
      } else {
        params[key] = obj[key];
        passedParams[key] = obj[key];
      }
    } else if (key.search(/on[A-Z]/) === 0 && typeof obj[key] === 'function') {
      if (splitEvents) {
        events[`${key[2].toLowerCase()}${key.substr(3)}`] = obj[key];
      } else {
        params.on[`${key[2].toLowerCase()}${key.substr(3)}`] = obj[key];
      }
    } else {
      rest[key] = obj[key];
    }
  });
  ['navigation', 'pagination', 'scrollbar'].forEach(key => {
    if (params[key] === true) params[key] = {};
    if (params[key] === false) delete params[key];
  });
  return {
    params,
    passedParams,
    rest,
    events
  };
}

function mountSwiper(_ref, swiperParams) {
  let {
    el,
    nextEl,
    prevEl,
    paginationEl,
    scrollbarEl,
    swiper
  } = _ref;
  if ((0,_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_0__.n)(swiperParams) && nextEl && prevEl) {
    swiper.params.navigation.nextEl = nextEl;
    swiper.originalParams.navigation.nextEl = nextEl;
    swiper.params.navigation.prevEl = prevEl;
    swiper.originalParams.navigation.prevEl = prevEl;
  }
  if ((0,_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_0__.a)(swiperParams) && paginationEl) {
    swiper.params.pagination.el = paginationEl;
    swiper.originalParams.pagination.el = paginationEl;
  }
  if ((0,_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_0__.b)(swiperParams) && scrollbarEl) {
    swiper.params.scrollbar.el = scrollbarEl;
    swiper.originalParams.scrollbar.el = scrollbarEl;
  }
  swiper.init(el);
}

function getChangedParams(swiperParams, oldParams, children, oldChildren, getKey) {
  const keys = [];
  if (!oldParams) return keys;
  const addKey = key => {
    if (keys.indexOf(key) < 0) keys.push(key);
  };
  if (children && oldChildren) {
    const oldChildrenKeys = oldChildren.map(getKey);
    const childrenKeys = children.map(getKey);
    if (oldChildrenKeys.join('') !== childrenKeys.join('')) addKey('children');
    if (oldChildren.length !== children.length) addKey('children');
  }
  const watchParams = _update_swiper_mjs__WEBPACK_IMPORTED_MODULE_0__.p.filter(key => key[0] === '_').map(key => key.replace(/_/, ''));
  watchParams.forEach(key => {
    if (key in swiperParams && key in oldParams) {
      if ((0,_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_0__.i)(swiperParams[key]) && (0,_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_0__.i)(oldParams[key])) {
        const newKeys = Object.keys(swiperParams[key]);
        const oldKeys = Object.keys(oldParams[key]);
        if (newKeys.length !== oldKeys.length) {
          addKey(key);
        } else {
          newKeys.forEach(newKey => {
            if (swiperParams[key][newKey] !== oldParams[key][newKey]) {
              addKey(key);
            }
          });
          oldKeys.forEach(oldKey => {
            if (swiperParams[key][oldKey] !== oldParams[key][oldKey]) addKey(key);
          });
        }
      } else if (swiperParams[key] !== oldParams[key]) {
        addKey(key);
      }
    }
  });
  return keys;
}

const updateOnVirtualData = swiper => {
  if (!swiper || swiper.destroyed || !swiper.params.virtual || swiper.params.virtual && !swiper.params.virtual.enabled) return;
  swiper.updateSlides();
  swiper.updateProgress();
  swiper.updateSlidesClasses();
  if (swiper.parallax && swiper.params.parallax && swiper.params.parallax.enabled) {
    swiper.parallax.setTranslate();
  }
};




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/shared/update-swiper.mjs":
/*!*******************************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/shared/update-swiper.mjs ***!
  \*******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   a: () => (/* binding */ needsPagination),
/* harmony export */   b: () => (/* binding */ needsScrollbar),
/* harmony export */   c: () => (/* binding */ attrToProp),
/* harmony export */   d: () => (/* binding */ uniqueClasses),
/* harmony export */   e: () => (/* binding */ extend),
/* harmony export */   i: () => (/* binding */ isObject),
/* harmony export */   n: () => (/* binding */ needsNavigation),
/* harmony export */   p: () => (/* binding */ paramsList),
/* harmony export */   u: () => (/* binding */ updateSwiper),
/* harmony export */   w: () => (/* binding */ wrapperClass)
/* harmony export */ });
/* underscore in name -> watch for changes */
const paramsList = ['eventsPrefix', 'injectStyles', 'injectStylesUrls', 'modules', 'init', '_direction', 'oneWayMovement', 'swiperElementNodeName', 'touchEventsTarget', 'initialSlide', '_speed', 'cssMode', 'updateOnWindowResize', 'resizeObserver', 'nested', 'focusableElements', '_enabled', '_width', '_height', 'preventInteractionOnTransition', 'userAgent', 'url', '_edgeSwipeDetection', '_edgeSwipeThreshold', '_freeMode', '_autoHeight', 'setWrapperSize', 'virtualTranslate', '_effect', 'breakpoints', 'breakpointsBase', '_spaceBetween', '_slidesPerView', 'maxBackfaceHiddenSlides', '_grid', '_slidesPerGroup', '_slidesPerGroupSkip', '_slidesPerGroupAuto', '_centeredSlides', '_centeredSlidesBounds', '_slidesOffsetBefore', '_slidesOffsetAfter', 'normalizeSlideIndex', '_centerInsufficientSlides', '_watchOverflow', 'roundLengths', 'touchRatio', 'touchAngle', 'simulateTouch', '_shortSwipes', '_longSwipes', 'longSwipesRatio', 'longSwipesMs', '_followFinger', 'allowTouchMove', '_threshold', 'touchMoveStopPropagation', 'touchStartPreventDefault', 'touchStartForcePreventDefault', 'touchReleaseOnEdges', 'uniqueNavElements', '_resistance', '_resistanceRatio', '_watchSlidesProgress', '_grabCursor', 'preventClicks', 'preventClicksPropagation', '_slideToClickedSlide', '_loop', 'loopAdditionalSlides', 'loopAddBlankSlides', 'loopPreventsSliding', '_rewind', '_allowSlidePrev', '_allowSlideNext', '_swipeHandler', '_noSwiping', 'noSwipingClass', 'noSwipingSelector', 'passiveListeners', 'containerModifierClass', 'slideClass', 'slideActiveClass', 'slideVisibleClass', 'slideFullyVisibleClass', 'slideNextClass', 'slidePrevClass', 'slideBlankClass', 'wrapperClass', 'lazyPreloaderClass', 'lazyPreloadPrevNext', 'runCallbacksOnInit', 'observer', 'observeParents', 'observeSlideChildren',
// modules
'a11y', '_autoplay', '_controller', 'coverflowEffect', 'cubeEffect', 'fadeEffect', 'flipEffect', 'creativeEffect', 'cardsEffect', 'hashNavigation', 'history', 'keyboard', 'mousewheel', '_navigation', '_pagination', 'parallax', '_scrollbar', '_thumbs', 'virtual', 'zoom', 'control'];

function isObject(o) {
  return typeof o === 'object' && o !== null && o.constructor && Object.prototype.toString.call(o).slice(8, -1) === 'Object' && !o.__swiper__;
}
function extend(target, src) {
  const noExtend = ['__proto__', 'constructor', 'prototype'];
  Object.keys(src).filter(key => noExtend.indexOf(key) < 0).forEach(key => {
    if (typeof target[key] === 'undefined') target[key] = src[key];else if (isObject(src[key]) && isObject(target[key]) && Object.keys(src[key]).length > 0) {
      if (src[key].__swiper__) target[key] = src[key];else extend(target[key], src[key]);
    } else {
      target[key] = src[key];
    }
  });
}
function needsNavigation(params) {
  if (params === void 0) {
    params = {};
  }
  return params.navigation && typeof params.navigation.nextEl === 'undefined' && typeof params.navigation.prevEl === 'undefined';
}
function needsPagination(params) {
  if (params === void 0) {
    params = {};
  }
  return params.pagination && typeof params.pagination.el === 'undefined';
}
function needsScrollbar(params) {
  if (params === void 0) {
    params = {};
  }
  return params.scrollbar && typeof params.scrollbar.el === 'undefined';
}
function uniqueClasses(classNames) {
  if (classNames === void 0) {
    classNames = '';
  }
  const classes = classNames.split(' ').map(c => c.trim()).filter(c => !!c);
  const unique = [];
  classes.forEach(c => {
    if (unique.indexOf(c) < 0) unique.push(c);
  });
  return unique.join(' ');
}
function attrToProp(attrName) {
  if (attrName === void 0) {
    attrName = '';
  }
  return attrName.replace(/-[a-z]/g, l => l.toUpperCase().replace('-', ''));
}
function wrapperClass(className) {
  if (className === void 0) {
    className = '';
  }
  if (!className) return 'swiper-wrapper';
  if (!className.includes('swiper-wrapper')) return `swiper-wrapper ${className}`;
  return className;
}

function updateSwiper(_ref) {
  let {
    swiper,
    slides,
    passedParams,
    changedParams,
    nextEl,
    prevEl,
    scrollbarEl,
    paginationEl
  } = _ref;
  const updateParams = changedParams.filter(key => key !== 'children' && key !== 'direction' && key !== 'wrapperClass');
  const {
    params: currentParams,
    pagination,
    navigation,
    scrollbar,
    virtual,
    thumbs
  } = swiper;
  let needThumbsInit;
  let needControllerInit;
  let needPaginationInit;
  let needScrollbarInit;
  let needNavigationInit;
  let loopNeedDestroy;
  let loopNeedEnable;
  let loopNeedReloop;
  if (changedParams.includes('thumbs') && passedParams.thumbs && passedParams.thumbs.swiper && !passedParams.thumbs.swiper.destroyed && currentParams.thumbs && (!currentParams.thumbs.swiper || currentParams.thumbs.swiper.destroyed)) {
    needThumbsInit = true;
  }
  if (changedParams.includes('controller') && passedParams.controller && passedParams.controller.control && currentParams.controller && !currentParams.controller.control) {
    needControllerInit = true;
  }
  if (changedParams.includes('pagination') && passedParams.pagination && (passedParams.pagination.el || paginationEl) && (currentParams.pagination || currentParams.pagination === false) && pagination && !pagination.el) {
    needPaginationInit = true;
  }
  if (changedParams.includes('scrollbar') && passedParams.scrollbar && (passedParams.scrollbar.el || scrollbarEl) && (currentParams.scrollbar || currentParams.scrollbar === false) && scrollbar && !scrollbar.el) {
    needScrollbarInit = true;
  }
  if (changedParams.includes('navigation') && passedParams.navigation && (passedParams.navigation.prevEl || prevEl) && (passedParams.navigation.nextEl || nextEl) && (currentParams.navigation || currentParams.navigation === false) && navigation && !navigation.prevEl && !navigation.nextEl) {
    needNavigationInit = true;
  }
  const destroyModule = mod => {
    if (!swiper[mod]) return;
    swiper[mod].destroy();
    if (mod === 'navigation') {
      if (swiper.isElement) {
        swiper[mod].prevEl.remove();
        swiper[mod].nextEl.remove();
      }
      currentParams[mod].prevEl = undefined;
      currentParams[mod].nextEl = undefined;
      swiper[mod].prevEl = undefined;
      swiper[mod].nextEl = undefined;
    } else {
      if (swiper.isElement) {
        swiper[mod].el.remove();
      }
      currentParams[mod].el = undefined;
      swiper[mod].el = undefined;
    }
  };
  if (changedParams.includes('loop') && swiper.isElement) {
    if (currentParams.loop && !passedParams.loop) {
      loopNeedDestroy = true;
    } else if (!currentParams.loop && passedParams.loop) {
      loopNeedEnable = true;
    } else {
      loopNeedReloop = true;
    }
  }
  updateParams.forEach(key => {
    if (isObject(currentParams[key]) && isObject(passedParams[key])) {
      Object.assign(currentParams[key], passedParams[key]);
      if ((key === 'navigation' || key === 'pagination' || key === 'scrollbar') && 'enabled' in passedParams[key] && !passedParams[key].enabled) {
        destroyModule(key);
      }
    } else {
      const newValue = passedParams[key];
      if ((newValue === true || newValue === false) && (key === 'navigation' || key === 'pagination' || key === 'scrollbar')) {
        if (newValue === false) {
          destroyModule(key);
        }
      } else {
        currentParams[key] = passedParams[key];
      }
    }
  });
  if (updateParams.includes('controller') && !needControllerInit && swiper.controller && swiper.controller.control && currentParams.controller && currentParams.controller.control) {
    swiper.controller.control = currentParams.controller.control;
  }
  if (changedParams.includes('children') && slides && virtual && currentParams.virtual.enabled) {
    virtual.slides = slides;
    virtual.update(true);
  } else if (changedParams.includes('virtual') && virtual && currentParams.virtual.enabled) {
    if (slides) virtual.slides = slides;
    virtual.update(true);
  }
  if (changedParams.includes('children') && slides && currentParams.loop) {
    loopNeedReloop = true;
  }
  if (needThumbsInit) {
    const initialized = thumbs.init();
    if (initialized) thumbs.update(true);
  }
  if (needControllerInit) {
    swiper.controller.control = currentParams.controller.control;
  }
  if (needPaginationInit) {
    if (swiper.isElement && (!paginationEl || typeof paginationEl === 'string')) {
      paginationEl = document.createElement('div');
      paginationEl.classList.add('swiper-pagination');
      paginationEl.part.add('pagination');
      swiper.el.appendChild(paginationEl);
    }
    if (paginationEl) currentParams.pagination.el = paginationEl;
    pagination.init();
    pagination.render();
    pagination.update();
  }
  if (needScrollbarInit) {
    if (swiper.isElement && (!scrollbarEl || typeof scrollbarEl === 'string')) {
      scrollbarEl = document.createElement('div');
      scrollbarEl.classList.add('swiper-scrollbar');
      scrollbarEl.part.add('scrollbar');
      swiper.el.appendChild(scrollbarEl);
    }
    if (scrollbarEl) currentParams.scrollbar.el = scrollbarEl;
    scrollbar.init();
    scrollbar.updateSize();
    scrollbar.setTranslate();
  }
  if (needNavigationInit) {
    if (swiper.isElement) {
      if (!nextEl || typeof nextEl === 'string') {
        nextEl = document.createElement('div');
        nextEl.classList.add('swiper-button-next');
        nextEl.innerHTML = swiper.hostEl.constructor.nextButtonSvg;
        nextEl.part.add('button-next');
        swiper.el.appendChild(nextEl);
      }
      if (!prevEl || typeof prevEl === 'string') {
        prevEl = document.createElement('div');
        prevEl.classList.add('swiper-button-prev');
        prevEl.innerHTML = swiper.hostEl.constructor.prevButtonSvg;
        prevEl.part.add('button-prev');
        swiper.el.appendChild(prevEl);
      }
    }
    if (nextEl) currentParams.navigation.nextEl = nextEl;
    if (prevEl) currentParams.navigation.prevEl = prevEl;
    navigation.init();
    navigation.update();
  }
  if (changedParams.includes('allowSlideNext')) {
    swiper.allowSlideNext = passedParams.allowSlideNext;
  }
  if (changedParams.includes('allowSlidePrev')) {
    swiper.allowSlidePrev = passedParams.allowSlidePrev;
  }
  if (changedParams.includes('direction')) {
    swiper.changeDirection(passedParams.direction, false);
  }
  if (loopNeedDestroy || loopNeedReloop) {
    swiper.loopDestroy();
  }
  if (loopNeedEnable || loopNeedReloop) {
    swiper.loopCreate();
  }
  swiper.update();
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/shared/utils.mjs":
/*!***********************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/shared/utils.mjs ***!
  \***********************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   a: () => (/* binding */ elementParents),
/* harmony export */   b: () => (/* binding */ elementOffset),
/* harmony export */   c: () => (/* binding */ createElement),
/* harmony export */   d: () => (/* binding */ now),
/* harmony export */   e: () => (/* binding */ elementChildren),
/* harmony export */   f: () => (/* binding */ elementOuterSize),
/* harmony export */   g: () => (/* binding */ getSlideTransformEl),
/* harmony export */   h: () => (/* binding */ elementIndex),
/* harmony export */   i: () => (/* binding */ classesToTokens),
/* harmony export */   j: () => (/* binding */ getTranslate),
/* harmony export */   k: () => (/* binding */ elementTransitionEnd),
/* harmony export */   l: () => (/* binding */ isObject),
/* harmony export */   m: () => (/* binding */ makeElementsArray),
/* harmony export */   n: () => (/* binding */ nextTick),
/* harmony export */   o: () => (/* binding */ getRotateFix),
/* harmony export */   p: () => (/* binding */ elementStyle),
/* harmony export */   q: () => (/* binding */ elementNextAll),
/* harmony export */   r: () => (/* binding */ elementPrevAll),
/* harmony export */   s: () => (/* binding */ setCSSProperty),
/* harmony export */   t: () => (/* binding */ animateCSSModeScroll),
/* harmony export */   u: () => (/* binding */ showWarning),
/* harmony export */   v: () => (/* binding */ elementIsChildOf),
/* harmony export */   w: () => (/* binding */ extend),
/* harmony export */   x: () => (/* binding */ deleteProps)
/* harmony export */ });
/* harmony import */ var _ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./ssr-window.esm.mjs */ "../plugin-slug/node_modules/swiper/shared/ssr-window.esm.mjs");


function classesToTokens(classes) {
  if (classes === void 0) {
    classes = '';
  }
  return classes.trim().split(' ').filter(c => !!c.trim());
}

function deleteProps(obj) {
  const object = obj;
  Object.keys(object).forEach(key => {
    try {
      object[key] = null;
    } catch (e) {
      // no getter for object
    }
    try {
      delete object[key];
    } catch (e) {
      // something got wrong
    }
  });
}
function nextTick(callback, delay) {
  if (delay === void 0) {
    delay = 0;
  }
  return setTimeout(callback, delay);
}
function now() {
  return Date.now();
}
function getComputedStyle(el) {
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  let style;
  if (window.getComputedStyle) {
    style = window.getComputedStyle(el, null);
  }
  if (!style && el.currentStyle) {
    style = el.currentStyle;
  }
  if (!style) {
    style = el.style;
  }
  return style;
}
function getTranslate(el, axis) {
  if (axis === void 0) {
    axis = 'x';
  }
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  let matrix;
  let curTransform;
  let transformMatrix;
  const curStyle = getComputedStyle(el);
  if (window.WebKitCSSMatrix) {
    curTransform = curStyle.transform || curStyle.webkitTransform;
    if (curTransform.split(',').length > 6) {
      curTransform = curTransform.split(', ').map(a => a.replace(',', '.')).join(', ');
    }
    // Some old versions of Webkit choke when 'none' is passed; pass
    // empty string instead in this case
    transformMatrix = new window.WebKitCSSMatrix(curTransform === 'none' ? '' : curTransform);
  } else {
    transformMatrix = curStyle.MozTransform || curStyle.OTransform || curStyle.MsTransform || curStyle.msTransform || curStyle.transform || curStyle.getPropertyValue('transform').replace('translate(', 'matrix(1, 0, 0, 1,');
    matrix = transformMatrix.toString().split(',');
  }
  if (axis === 'x') {
    // Latest Chrome and webkits Fix
    if (window.WebKitCSSMatrix) curTransform = transformMatrix.m41;
    // Crazy IE10 Matrix
    else if (matrix.length === 16) curTransform = parseFloat(matrix[12]);
    // Normal Browsers
    else curTransform = parseFloat(matrix[4]);
  }
  if (axis === 'y') {
    // Latest Chrome and webkits Fix
    if (window.WebKitCSSMatrix) curTransform = transformMatrix.m42;
    // Crazy IE10 Matrix
    else if (matrix.length === 16) curTransform = parseFloat(matrix[13]);
    // Normal Browsers
    else curTransform = parseFloat(matrix[5]);
  }
  return curTransform || 0;
}
function isObject(o) {
  return typeof o === 'object' && o !== null && o.constructor && Object.prototype.toString.call(o).slice(8, -1) === 'Object';
}
function isNode(node) {
  // eslint-disable-next-line
  if (typeof window !== 'undefined' && typeof window.HTMLElement !== 'undefined') {
    return node instanceof HTMLElement;
  }
  return node && (node.nodeType === 1 || node.nodeType === 11);
}
function extend() {
  const to = Object(arguments.length <= 0 ? undefined : arguments[0]);
  const noExtend = ['__proto__', 'constructor', 'prototype'];
  for (let i = 1; i < arguments.length; i += 1) {
    const nextSource = i < 0 || arguments.length <= i ? undefined : arguments[i];
    if (nextSource !== undefined && nextSource !== null && !isNode(nextSource)) {
      const keysArray = Object.keys(Object(nextSource)).filter(key => noExtend.indexOf(key) < 0);
      for (let nextIndex = 0, len = keysArray.length; nextIndex < len; nextIndex += 1) {
        const nextKey = keysArray[nextIndex];
        const desc = Object.getOwnPropertyDescriptor(nextSource, nextKey);
        if (desc !== undefined && desc.enumerable) {
          if (isObject(to[nextKey]) && isObject(nextSource[nextKey])) {
            if (nextSource[nextKey].__swiper__) {
              to[nextKey] = nextSource[nextKey];
            } else {
              extend(to[nextKey], nextSource[nextKey]);
            }
          } else if (!isObject(to[nextKey]) && isObject(nextSource[nextKey])) {
            to[nextKey] = {};
            if (nextSource[nextKey].__swiper__) {
              to[nextKey] = nextSource[nextKey];
            } else {
              extend(to[nextKey], nextSource[nextKey]);
            }
          } else {
            to[nextKey] = nextSource[nextKey];
          }
        }
      }
    }
  }
  return to;
}
function setCSSProperty(el, varName, varValue) {
  el.style.setProperty(varName, varValue);
}
function animateCSSModeScroll(_ref) {
  let {
    swiper,
    targetPosition,
    side
  } = _ref;
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  const startPosition = -swiper.translate;
  let startTime = null;
  let time;
  const duration = swiper.params.speed;
  swiper.wrapperEl.style.scrollSnapType = 'none';
  window.cancelAnimationFrame(swiper.cssModeFrameID);
  const dir = targetPosition > startPosition ? 'next' : 'prev';
  const isOutOfBound = (current, target) => {
    return dir === 'next' && current >= target || dir === 'prev' && current <= target;
  };
  const animate = () => {
    time = new Date().getTime();
    if (startTime === null) {
      startTime = time;
    }
    const progress = Math.max(Math.min((time - startTime) / duration, 1), 0);
    const easeProgress = 0.5 - Math.cos(progress * Math.PI) / 2;
    let currentPosition = startPosition + easeProgress * (targetPosition - startPosition);
    if (isOutOfBound(currentPosition, targetPosition)) {
      currentPosition = targetPosition;
    }
    swiper.wrapperEl.scrollTo({
      [side]: currentPosition
    });
    if (isOutOfBound(currentPosition, targetPosition)) {
      swiper.wrapperEl.style.overflow = 'hidden';
      swiper.wrapperEl.style.scrollSnapType = '';
      setTimeout(() => {
        swiper.wrapperEl.style.overflow = '';
        swiper.wrapperEl.scrollTo({
          [side]: currentPosition
        });
      });
      window.cancelAnimationFrame(swiper.cssModeFrameID);
      return;
    }
    swiper.cssModeFrameID = window.requestAnimationFrame(animate);
  };
  animate();
}
function getSlideTransformEl(slideEl) {
  return slideEl.querySelector('.swiper-slide-transform') || slideEl.shadowRoot && slideEl.shadowRoot.querySelector('.swiper-slide-transform') || slideEl;
}
function elementChildren(element, selector) {
  if (selector === void 0) {
    selector = '';
  }
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  const children = [...element.children];
  if (window.HTMLSlotElement && element instanceof HTMLSlotElement) {
    children.push(...element.assignedElements());
  }
  if (!selector) {
    return children;
  }
  return children.filter(el => el.matches(selector));
}
function elementIsChildOfSlot(el, slot) {
  // Breadth-first search through all parent's children and assigned elements
  const elementsQueue = [slot];
  while (elementsQueue.length > 0) {
    const elementToCheck = elementsQueue.shift();
    if (el === elementToCheck) {
      return true;
    }
    elementsQueue.push(...elementToCheck.children, ...(elementToCheck.shadowRoot?.children || []), ...(elementToCheck.assignedElements?.() || []));
  }
}
function elementIsChildOf(el, parent) {
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  let isChild = parent.contains(el);
  if (!isChild && window.HTMLSlotElement && parent instanceof HTMLSlotElement) {
    const children = [...parent.assignedElements()];
    isChild = children.includes(el);
    if (!isChild) {
      isChild = elementIsChildOfSlot(el, parent);
    }
  }
  return isChild;
}
function showWarning(text) {
  try {
    console.warn(text);
    return;
  } catch (err) {
    // err
  }
}
function createElement(tag, classes) {
  if (classes === void 0) {
    classes = [];
  }
  const el = document.createElement(tag);
  el.classList.add(...(Array.isArray(classes) ? classes : classesToTokens(classes)));
  return el;
}
function elementOffset(el) {
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();
  const box = el.getBoundingClientRect();
  const body = document.body;
  const clientTop = el.clientTop || body.clientTop || 0;
  const clientLeft = el.clientLeft || body.clientLeft || 0;
  const scrollTop = el === window ? window.scrollY : el.scrollTop;
  const scrollLeft = el === window ? window.scrollX : el.scrollLeft;
  return {
    top: box.top + scrollTop - clientTop,
    left: box.left + scrollLeft - clientLeft
  };
}
function elementPrevAll(el, selector) {
  const prevEls = [];
  while (el.previousElementSibling) {
    const prev = el.previousElementSibling; // eslint-disable-line
    if (selector) {
      if (prev.matches(selector)) prevEls.push(prev);
    } else prevEls.push(prev);
    el = prev;
  }
  return prevEls;
}
function elementNextAll(el, selector) {
  const nextEls = [];
  while (el.nextElementSibling) {
    const next = el.nextElementSibling; // eslint-disable-line
    if (selector) {
      if (next.matches(selector)) nextEls.push(next);
    } else nextEls.push(next);
    el = next;
  }
  return nextEls;
}
function elementStyle(el, prop) {
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  return window.getComputedStyle(el, null).getPropertyValue(prop);
}
function elementIndex(el) {
  let child = el;
  let i;
  if (child) {
    i = 0;
    // eslint-disable-next-line
    while ((child = child.previousSibling) !== null) {
      if (child.nodeType === 1) i += 1;
    }
    return i;
  }
  return undefined;
}
function elementParents(el, selector) {
  const parents = []; // eslint-disable-line
  let parent = el.parentElement; // eslint-disable-line
  while (parent) {
    if (selector) {
      if (parent.matches(selector)) parents.push(parent);
    } else {
      parents.push(parent);
    }
    parent = parent.parentElement;
  }
  return parents;
}
function elementTransitionEnd(el, callback) {
  function fireCallBack(e) {
    if (e.target !== el) return;
    callback.call(el, e);
    el.removeEventListener('transitionend', fireCallBack);
  }
  if (callback) {
    el.addEventListener('transitionend', fireCallBack);
  }
}
function elementOuterSize(el, size, includeMargins) {
  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();
  if (includeMargins) {
    return el[size === 'width' ? 'offsetWidth' : 'offsetHeight'] + parseFloat(window.getComputedStyle(el, null).getPropertyValue(size === 'width' ? 'margin-right' : 'margin-top')) + parseFloat(window.getComputedStyle(el, null).getPropertyValue(size === 'width' ? 'margin-left' : 'margin-bottom'));
  }
  return el.offsetWidth;
}
function makeElementsArray(el) {
  return (Array.isArray(el) ? el : [el]).filter(e => !!e);
}
function getRotateFix(swiper) {
  return v => {
    if (Math.abs(v) > 0 && swiper.browser && swiper.browser.need3dFix && Math.abs(v) % 90 === 0) {
      return v + 0.001;
    }
    return v;
  };
}




/***/ }),

/***/ "../plugin-slug/node_modules/swiper/swiper-react.mjs":
/*!***********************************************************!*\
  !*** ../plugin-slug/node_modules/swiper/swiper-react.mjs ***!
  \***********************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Swiper: () => (/* binding */ Swiper),
/* harmony export */   SwiperSlide: () => (/* binding */ SwiperSlide),
/* harmony export */   useSwiper: () => (/* binding */ useSwiper),
/* harmony export */   useSwiperSlide: () => (/* binding */ useSwiperSlide)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var _shared_swiper_core_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./shared/swiper-core.mjs */ "../plugin-slug/node_modules/swiper/shared/swiper-core.mjs");
/* harmony import */ var _shared_update_on_virtual_data_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./shared/update-on-virtual-data.mjs */ "../plugin-slug/node_modules/swiper/shared/update-on-virtual-data.mjs");
/* harmony import */ var _shared_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./shared/update-swiper.mjs */ "../plugin-slug/node_modules/swiper/shared/update-swiper.mjs");
/**
 * Swiper React 11.2.1
 * Most modern mobile touch slider and framework with hardware accelerated transitions
 * https://swiperjs.com
 *
 * Copyright 2014-2025 Vladimir Kharlampidi
 *
 * Released under the MIT License
 *
 * Released on: January 10, 2025
 */






function _extends() {
  _extends = Object.assign ? Object.assign.bind() : function (target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends.apply(this, arguments);
}

function isChildSwiperSlide(child) {
  return child.type && child.type.displayName && child.type.displayName.includes('SwiperSlide');
}
function processChildren(c) {
  const slides = [];
  react__WEBPACK_IMPORTED_MODULE_0__.Children.toArray(c).forEach(child => {
    if (isChildSwiperSlide(child)) {
      slides.push(child);
    } else if (child.props && child.props.children) {
      processChildren(child.props.children).forEach(slide => slides.push(slide));
    }
  });
  return slides;
}
function getChildren(c) {
  const slides = [];
  const slots = {
    'container-start': [],
    'container-end': [],
    'wrapper-start': [],
    'wrapper-end': []
  };
  react__WEBPACK_IMPORTED_MODULE_0__.Children.toArray(c).forEach(child => {
    if (isChildSwiperSlide(child)) {
      slides.push(child);
    } else if (child.props && child.props.slot && slots[child.props.slot]) {
      slots[child.props.slot].push(child);
    } else if (child.props && child.props.children) {
      const foundSlides = processChildren(child.props.children);
      if (foundSlides.length > 0) {
        foundSlides.forEach(slide => slides.push(slide));
      } else {
        slots['container-end'].push(child);
      }
    } else {
      slots['container-end'].push(child);
    }
  });
  return {
    slides,
    slots
  };
}

function renderVirtual(swiper, slides, virtualData) {
  if (!virtualData) return null;
  const getSlideIndex = index => {
    let slideIndex = index;
    if (index < 0) {
      slideIndex = slides.length + index;
    } else if (slideIndex >= slides.length) {
      // eslint-disable-next-line
      slideIndex = slideIndex - slides.length;
    }
    return slideIndex;
  };
  const style = swiper.isHorizontal() ? {
    [swiper.rtlTranslate ? 'right' : 'left']: `${virtualData.offset}px`
  } : {
    top: `${virtualData.offset}px`
  };
  const {
    from,
    to
  } = virtualData;
  const loopFrom = swiper.params.loop ? -slides.length : 0;
  const loopTo = swiper.params.loop ? slides.length * 2 : slides.length;
  const slidesToRender = [];
  for (let i = loopFrom; i < loopTo; i += 1) {
    if (i >= from && i <= to) {
      slidesToRender.push(slides[getSlideIndex(i)]);
    }
  }
  return slidesToRender.map((child, index) => {
    return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.cloneElement(child, {
      swiper,
      style,
      key: child.props.virtualIndex || child.key || `slide-${index}`
    });
  });
}

function useIsomorphicLayoutEffect(callback, deps) {
  // eslint-disable-next-line
  if (typeof window === 'undefined') return (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(callback, deps);
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.useLayoutEffect)(callback, deps);
}

const SwiperSlideContext = /*#__PURE__*/(0,react__WEBPACK_IMPORTED_MODULE_0__.createContext)(null);
const useSwiperSlide = () => {
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.useContext)(SwiperSlideContext);
};
const SwiperContext = /*#__PURE__*/(0,react__WEBPACK_IMPORTED_MODULE_0__.createContext)(null);
const useSwiper = () => {
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.useContext)(SwiperContext);
};

const Swiper = /*#__PURE__*/(0,react__WEBPACK_IMPORTED_MODULE_0__.forwardRef)(function (_temp, externalElRef) {
  let {
    className,
    tag: Tag = 'div',
    wrapperTag: WrapperTag = 'div',
    children,
    onSwiper,
    ...rest
  } = _temp === void 0 ? {} : _temp;
  let eventsAssigned = false;
  const [containerClasses, setContainerClasses] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)('swiper');
  const [virtualData, setVirtualData] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(null);
  const [breakpointChanged, setBreakpointChanged] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const initializedRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
  const swiperElRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const swiperRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const oldPassedParamsRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const oldSlides = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const nextElRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const prevElRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const paginationElRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const scrollbarElRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const {
    params: swiperParams,
    passedParams,
    rest: restProps,
    events
  } = (0,_shared_update_on_virtual_data_mjs__WEBPACK_IMPORTED_MODULE_2__.g)(rest);
  const {
    slides,
    slots
  } = getChildren(children);
  const onBeforeBreakpoint = () => {
    setBreakpointChanged(!breakpointChanged);
  };
  Object.assign(swiperParams.on, {
    _containerClasses(swiper, classes) {
      setContainerClasses(classes);
    }
  });
  const initSwiper = () => {
    // init swiper
    Object.assign(swiperParams.on, events);
    eventsAssigned = true;
    const passParams = {
      ...swiperParams
    };
    delete passParams.wrapperClass;
    swiperRef.current = new _shared_swiper_core_mjs__WEBPACK_IMPORTED_MODULE_1__.S(passParams);
    if (swiperRef.current.virtual && swiperRef.current.params.virtual.enabled) {
      swiperRef.current.virtual.slides = slides;
      const extendWith = {
        cache: false,
        slides,
        renderExternal: setVirtualData,
        renderExternalUpdate: false
      };
      (0,_shared_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_3__.e)(swiperRef.current.params.virtual, extendWith);
      (0,_shared_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_3__.e)(swiperRef.current.originalParams.virtual, extendWith);
    }
  };
  if (!swiperElRef.current) {
    initSwiper();
  }

  // Listen for breakpoints change
  if (swiperRef.current) {
    swiperRef.current.on('_beforeBreakpoint', onBeforeBreakpoint);
  }
  const attachEvents = () => {
    if (eventsAssigned || !events || !swiperRef.current) return;
    Object.keys(events).forEach(eventName => {
      swiperRef.current.on(eventName, events[eventName]);
    });
  };
  const detachEvents = () => {
    if (!events || !swiperRef.current) return;
    Object.keys(events).forEach(eventName => {
      swiperRef.current.off(eventName, events[eventName]);
    });
  };
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    return () => {
      if (swiperRef.current) swiperRef.current.off('_beforeBreakpoint', onBeforeBreakpoint);
    };
  });

  // set initialized flag
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (!initializedRef.current && swiperRef.current) {
      swiperRef.current.emitSlidesClasses();
      initializedRef.current = true;
    }
  });

  // mount swiper
  useIsomorphicLayoutEffect(() => {
    if (externalElRef) {
      externalElRef.current = swiperElRef.current;
    }
    if (!swiperElRef.current) return;
    if (swiperRef.current.destroyed) {
      initSwiper();
    }
    (0,_shared_update_on_virtual_data_mjs__WEBPACK_IMPORTED_MODULE_2__.m)({
      el: swiperElRef.current,
      nextEl: nextElRef.current,
      prevEl: prevElRef.current,
      paginationEl: paginationElRef.current,
      scrollbarEl: scrollbarElRef.current,
      swiper: swiperRef.current
    }, swiperParams);
    if (onSwiper && !swiperRef.current.destroyed) onSwiper(swiperRef.current);
    // eslint-disable-next-line
    return () => {
      if (swiperRef.current && !swiperRef.current.destroyed) {
        swiperRef.current.destroy(true, false);
      }
    };
  }, []);

  // watch for params change
  useIsomorphicLayoutEffect(() => {
    attachEvents();
    const changedParams = (0,_shared_update_on_virtual_data_mjs__WEBPACK_IMPORTED_MODULE_2__.a)(passedParams, oldPassedParamsRef.current, slides, oldSlides.current, c => c.key);
    oldPassedParamsRef.current = passedParams;
    oldSlides.current = slides;
    if (changedParams.length && swiperRef.current && !swiperRef.current.destroyed) {
      (0,_shared_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_3__.u)({
        swiper: swiperRef.current,
        slides,
        passedParams,
        changedParams,
        nextEl: nextElRef.current,
        prevEl: prevElRef.current,
        scrollbarEl: scrollbarElRef.current,
        paginationEl: paginationElRef.current
      });
    }
    return () => {
      detachEvents();
    };
  });

  // update on virtual update
  useIsomorphicLayoutEffect(() => {
    (0,_shared_update_on_virtual_data_mjs__WEBPACK_IMPORTED_MODULE_2__.u)(swiperRef.current);
  }, [virtualData]);

  // bypass swiper instance to slides
  function renderSlides() {
    if (swiperParams.virtual) {
      return renderVirtual(swiperRef.current, slides, virtualData);
    }
    return slides.map((child, index) => {
      return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.cloneElement(child, {
        swiper: swiperRef.current,
        swiperSlideIndex: index
      });
    });
  }
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(Tag, _extends({
    ref: swiperElRef,
    className: (0,_shared_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_3__.d)(`${containerClasses}${className ? ` ${className}` : ''}`)
  }, restProps), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(SwiperContext.Provider, {
    value: swiperRef.current
  }, slots['container-start'], /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(WrapperTag, {
    className: (0,_shared_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_3__.w)(swiperParams.wrapperClass)
  }, slots['wrapper-start'], renderSlides(), slots['wrapper-end']), (0,_shared_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_3__.n)(swiperParams) && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    ref: prevElRef,
    className: "swiper-button-prev"
  }), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    ref: nextElRef,
    className: "swiper-button-next"
  })), (0,_shared_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_3__.b)(swiperParams) && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    ref: scrollbarElRef,
    className: "swiper-scrollbar"
  }), (0,_shared_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_3__.a)(swiperParams) && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    ref: paginationElRef,
    className: "swiper-pagination"
  }), slots['container-end']));
});
Swiper.displayName = 'Swiper';

const SwiperSlide = /*#__PURE__*/(0,react__WEBPACK_IMPORTED_MODULE_0__.forwardRef)(function (_temp, externalRef) {
  let {
    tag: Tag = 'div',
    children,
    className = '',
    swiper,
    zoom,
    lazy,
    virtualIndex,
    swiperSlideIndex,
    ...rest
  } = _temp === void 0 ? {} : _temp;
  const slideElRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const [slideClasses, setSlideClasses] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)('swiper-slide');
  const [lazyLoaded, setLazyLoaded] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  function updateClasses(_s, el, classNames) {
    if (el === slideElRef.current) {
      setSlideClasses(classNames);
    }
  }
  useIsomorphicLayoutEffect(() => {
    if (typeof swiperSlideIndex !== 'undefined') {
      slideElRef.current.swiperSlideIndex = swiperSlideIndex;
    }
    if (externalRef) {
      externalRef.current = slideElRef.current;
    }
    if (!slideElRef.current || !swiper) {
      return;
    }
    if (swiper.destroyed) {
      if (slideClasses !== 'swiper-slide') {
        setSlideClasses('swiper-slide');
      }
      return;
    }
    swiper.on('_slideClass', updateClasses);
    // eslint-disable-next-line
    return () => {
      if (!swiper) return;
      swiper.off('_slideClass', updateClasses);
    };
  });
  useIsomorphicLayoutEffect(() => {
    if (swiper && slideElRef.current && !swiper.destroyed) {
      setSlideClasses(swiper.getSlideClasses(slideElRef.current));
    }
  }, [swiper]);
  const slideData = {
    isActive: slideClasses.indexOf('swiper-slide-active') >= 0,
    isVisible: slideClasses.indexOf('swiper-slide-visible') >= 0,
    isPrev: slideClasses.indexOf('swiper-slide-prev') >= 0,
    isNext: slideClasses.indexOf('swiper-slide-next') >= 0
  };
  const renderChildren = () => {
    return typeof children === 'function' ? children(slideData) : children;
  };
  const onLoad = () => {
    setLazyLoaded(true);
  };
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(Tag, _extends({
    ref: slideElRef,
    className: (0,_shared_update_swiper_mjs__WEBPACK_IMPORTED_MODULE_3__.d)(`${slideClasses}${className ? ` ${className}` : ''}`),
    "data-swiper-slide-index": virtualIndex,
    onLoad: onLoad
  }, rest), zoom && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(SwiperSlideContext.Provider, {
    value: slideData
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "swiper-zoom-container",
    "data-swiper-zoom": typeof zoom === 'number' ? zoom : undefined
  }, renderChildren(), lazy && !lazyLoaded && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "swiper-lazy-preloader"
  }))), !zoom && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(SwiperSlideContext.Provider, {
    value: slideData
  }, renderChildren(), lazy && !lazyLoaded && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "swiper-lazy-preloader"
  })));
});
SwiperSlide.displayName = 'SwiperSlide';




/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	(() => {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = (module) => {
/******/ 			var getter = module && module.__esModule ?
/******/ 				() => (module['default']) :
/******/ 				() => (module);
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry needs to be wrapped in an IIFE because it needs to be in strict mode.
(() => {
"use strict";
/*!*********************************************!*\
  !*** ./src/blocks/testimonial-form/view.js ***!
  \*********************************************/
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _shared_styles_frontend_scss__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../shared/styles/frontend.scss */ "./src/shared/styles/frontend.scss");
/* harmony import */ var _shared_view__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../shared/view */ "./src/shared/view.js");
/* harmony import */ var _shared_styles_form_scss__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../shared/styles/form.scss */ "./src/shared/styles/form.scss");



const setMessage = (form, text, type) => {
  const msg = form.querySelector('.btb-tform-msg');
  if (!msg) {
    return;
  }
  msg.textContent = text;
  msg.className = `btb-tform-msg is-${type}`;
};
const submitForm = async form => {
  const submitBtn = form.querySelector('.btb-tform-submit');
  const endpoint = form.dataset.endpoint;

  // Nothing to post to: render.php fills this in, so an empty value means
  // stale cached markup rather than a visitor mistake.
  if (!endpoint) {
    setMessage(form, 'This form is not available right now. Please reload the page.', 'error');
    return;
  }
  const data = new FormData(form);
  if (!data.has('nonce')) {
    data.append('nonce', form.dataset.nonce || '');
  }

  // Cookie-authenticated REST requests must carry the REST nonce, or
  // WordPress drops the current user to 0 for the duration of the
  // request -- which makes the form's own nonce fail to verify for
  // anyone logged in. Anonymous visitors are unaffected either way.
  const restNonce = form.dataset.restNonce || window.wpApiSettings && window.wpApiSettings.nonce || '';
  const headers = restNonce ? {
    'X-WP-Nonce': restNonce
  } : {};
  if (submitBtn) {
    submitBtn.disabled = true;
  }
  setMessage(form, '', '');
  try {
    const res = await fetch(endpoint, {
      method: 'POST',
      body: data,
      headers,
      credentials: 'same-origin'
    });
    const json = await res.json();
    if (res.ok && json.success) {
      setMessage(form, form.dataset.success || json.message, 'success');
      form.reset();
    } else if ('rest_cookie_invalid_nonce' === json.code) {
      // A page cached long enough for the nonce to expire. WordPress's
      // own wording here ("Cookie check failed") means nothing to a
      // visitor, so say what actually helps.
      setMessage(form, 'This page has been open too long. Please reload and try again.', 'error');
    } else {
      setMessage(form, json.message || 'Submission failed.', 'error');
    }
  } catch (err) {
    setMessage(form, 'Submission failed. Please try again.', 'error');
  } finally {
    if (submitBtn) {
      submitBtn.disabled = false;
    }
  }
};

// One delegated listener rather than one per form.
//
// The form is rendered by React (shared/view), so it is not in the DOM when this
// script runs, and binding per element meant racing that mount -- three attempts
// over half a second, which a page carrying every block in the plugin loses. A
// form that lost the race submitted natively, and because it posts to the page
// URL, WordPress read its `name` field as the post-slug query var and served the
// 404 template. Delegation cannot be early or late: the listener is on the
// document before any form exists and catches every one that ever appears.
//
// Capture phase, so nothing that stops propagation in between can let the native
// submit through.
document.addEventListener('submit', e => {
  const form = e.target?.closest?.('.btb-tform');
  if (!form) {
    return;
  }
  e.preventDefault();
  submitForm(form);
}, true);
})();

/******/ })()
;
//# sourceMappingURL=view.js.map